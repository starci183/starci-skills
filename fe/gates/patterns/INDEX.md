# Gate 4 — patterns

This is a linear source materializer. It consumes the exact Gate 3 hash and emits one file,
contract and pure/connected component plan. It cannot alter the accepted design or generate variants.

Goal: [`GOAL.md`](GOAL.md).
