# B14 — proposals are per block

For every requested block, Gate 2 must return its own set of 3–4 candidates. Never turn several
blocks into 3–4 page-wide combinations. A surface with `N` blocks yields 3–4 × N proposals.
