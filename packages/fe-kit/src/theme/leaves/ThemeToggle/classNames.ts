/** The bordered group element the three theme choices sit in; styled in the app's globals.css. */
export const THEME_TOGGLE_GROUP_CLASS_NAME = "theme-toggle"

/** The unframed variant of the same group, for a surface that draws its own boundary. */
export const THEME_TOGGLE_PLAIN_CLASS_NAME = "theme-toggle-plain"

/** One choice button inside the group; the pressed state is styled in the app's globals.css. */
export const THEME_TOGGLE_OPTION_CLASS_NAME = "theme-toggle-option"
