/** The bordered group element the per-locale links sit in; styled in the app's globals.css. */
export const LOCALE_SWITCHER_GROUP_CLASS_NAME = "locale-switcher"

/** The unframed variant of the same group, for a surface that draws its own boundary. */
export const LOCALE_SWITCHER_PLAIN_CLASS_NAME = "locale-switcher-plain"

/** One locale link inside the group; the current locale is styled in the app's globals.css. */
export const LOCALE_SWITCHER_OPTION_CLASS_NAME = "locale-switcher-option"
