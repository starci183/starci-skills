# UI audit retry — concise comparison

Both calibrations are `BLOCKED`. This is a read-only StarCi self-upgrade calibration; product worktrees and existing runtime owners were not changed.

| Case | Direct output verdict | Primary contradiction | Proven consecutive passes | Smallest shared runtime gap |
| --- | --- | --- | ---: | --- |
| Personal Project reconstruct | `BLOCKED` | The surface renders but does not demonstrate Course-page benchmark craft, hierarchy, state depth, responsive measurement, or repeatability. | `0 / 2` | Terminal acceptance is not visibly bound to pixels-first benchmark quality and fresh stability proof. |
| CV new | `BLOCKED` | The named CV output is absent; the primary region is an English error/retry state inside a Vietnamese page. | `0 / 2` | Terminal acceptance does not fail closed when the primary payload is missing, and no loaded/stability proof is required. |

## What the comparison proves

- The two failures are not equivalent. Personal Project is a quality/craft and proof failure; CV is first an outcome-correctness failure because no CV is visible.
- Code changes, tests, route rendering, component coverage, or supported recovery states cannot override a wrong user-visible output.
- The recurring runtime owner is the acceptance boundary spanning visual validation, terminal output contract, and delivery proof. Exact producer prompt/state execution evidence is unavailable, so this calibration does not claim a narrower file-level cause and does not mutate a runtime owner.
- The required repair direction is fail-closed: inspect pixels first, assert the named payload/outcome, compare against frozen benchmark criteria, validate all required UI states and measurements, and require two fresh consecutive passes.

## Preserved unavailable measurements

For both prior producer runs, elapsed time, time to first action, step count, redundant step count, context bytes, exact model/media receipt, transition trace, operator receipts, measured responsive values, and independent review receipts are unavailable. They remain `null` or explicit evidence gaps in the results; no values were inferred.

## Artifacts

- Mission input: `.claude/upgrades/ui-audit-retry/input.json`
- Personal Project evidence/result: `.claude/upgrades/ui-audit-retry/personal-project/evidence.md`, `.claude/upgrades/ui-audit-retry/personal-project/result.json`
- CV evidence/result: `.claude/upgrades/ui-audit-retry/cv/evidence.md`, `.claude/upgrades/ui-audit-retry/cv/result.json`

Final verdict: `BLOCKED` for both. Calibration cannot repair runtime owners, and neither fixture has two fresh correct outputs.
