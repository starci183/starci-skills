---
title: Initialization · bootstrap
---

# Bootstrap

## LOADS

None.

`<Source>/AGENTS.md` and `<Source>/CLAUDE.md` are two runtime entry names carrying the same minimal,
relative link to the trust tree's `INDEX.md`. Before writing, classify each as `create`, `reuse`,
`repoint`, `slim`, or `collision`. Never overwrite unseen owner content. A missing tree entry blocks the
boundary; an absolute link is invalid because it only works on one machine.

Evidence is both complete current files and the resolved tree entry. Action creates, repoints, or slims
only these two files. Proof follows each written link from the Source exactly as an agent would. Rules
belong in the trust tree, never in the bootstrap.
