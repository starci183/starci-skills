# Swarm reference

`stack.yaml` is a single-manager Docker Swarm reference. Build and publish its app image as an immutable reviewed artifact before deployment. `swarm-secret.sh` creates the versioned external secret without retaining plaintext. Never place the age identity here or in Git. Ubuntu operation, backup/restore and production ingress remain unproven until exercised on the target VPS.
