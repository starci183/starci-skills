schema: work/node@2
id: sds-data-receipt
kind: architecture
required: true
description: Synthetic authored specification leaf.
state: done
assertions:
  - contract-reviewed
extensions:
  work3:
    specification:
      schema: starci/sds@4
      op: architecture.decide
      status: pass
      id: DATA-RECEIPT
      nodeType: data
      name: Recipient command receipt
      summary: Synthetic Recipient command receipt; all code units are planned and tests are not executed.
      refs:
        - type: business-data
          nodeId: srs-data-request
          itemId: DATA-CMD-REQUEST
        - type: code-unit
          nodeId: sds-code-recipient
          itemId: CU-RECIPIENT
      content:
        businessDataRefs:
          - DATA-CMD-REQUEST
        storage: Recipient relational database.
        modelCodeRefs:
          - CU-RECIPIENT
        fields:
          - intentId
          - payloadFingerprint
          - status
          - result
        relationships: []
        constraints:
          - Unique recipient installation plus intent ID.
        indexes:
          - Unique intent lookup.
        migration: Planned additive table.
        authority: Recipient module is authoritative.
        writeOwnerRef: CU-RECIPIENT
        readerRefs:
          - CU-RECIPIENT
        repositoryRefs:
          - Named injected EntityManager
        transaction: Receipt and domain effect commit in one applicable transaction.
        consistency: Receipt is authoritative after response loss.
        concurrency: Unique key plus fingerprint comparison serializes same intent.
        isolation: Installation ID participates in every lookup.
        classification: Synthetic command metadata.
        lifecycle: Retention is an undecided policy.
        recovery: Restore receipt with domain state and reconcile external ambiguity.
completion:
  inputDigest: 010ea8267796eff6f35410f4119907cfc1d1f974e133980674932ac113f7c683
  review:
    schema: starci/design-review@1
    reviewer: Synthetic fixture reviewer
    authority: Synthetic runtime regression authority only.
    reviewedAt: 2026-09-10T00:00:00Z
    observations:
      - id: contract-reviewed
        outcome: pass
        observation: The synthetic specification is complete enough to exercise runtime bindings; no product
          or code acceptance is claimed.
    limitations:
      - All code units and verification tests are planned and were not executed.
