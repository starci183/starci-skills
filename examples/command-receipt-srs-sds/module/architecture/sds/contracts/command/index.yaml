schema: work/node@2
id: sds-contract-command
kind: architecture
required: true
description: Synthetic authored specification leaf.
state: done
assertions:
  - contract-reviewed
extensions:
  work3:
    specification:
      schema: starci/sds@4
      op: architecture.decide
      status: pass
      id: CMD-V1
      nodeType: contract
      name: Versioned named command
      summary: Synthetic Versioned named command; all code units are planned and tests are not executed.
      refs:
        - type: code-unit
          nodeId: sds-code-router
          itemId: CU-ROUTER
        - type: code-unit
          nodeId: sds-code-recipient
          itemId: CU-RECIPIENT
      content:
        owner: Recipient module
        callerRef: CU-ROUTER
        receiverRef: CU-RECIPIENT
        interaction: In-process asynchronous result
        endpoint: Named executeCommand interface
        requestSchema: CommandEnvelope@1
        responseSchema: CommandReceipt@1
        validation: Recipient validates versioned capability/schema and fingerprint.
        errors:
          - unavailable
          - forbidden
          - wrong-scope
          - intent-conflict
          - outcome-unknown
        errorMapping: Preserve typed outcomes through UI.
        authentication: Trusted principal context.
        authorization: Recipient checks consent, action and target resource.
        resourceScope: Exact source and target installation.
        correlation: Stable intent ID.
        idempotency: Intent plus payload fingerprint; changed reuse conflicts.
        ordering: Recipient receipt transition order.
        timeout: Caller timeout yields outcome unknown.
        retry: Reconcile same intent before unchanged retry.
        cancellation: Cancellation cannot undo a committed effect.
        outcomeLookup: Recipient receipt by stable intent and installation scope.
        version: Capability and envelope version are explicit.
        compatibility: Unsupported versions reject before effects.
completion:
  inputDigest: d3695fe0d609e7c42548f772aae13a1f59a8b306e4d82092499120fd77fa713c
  review:
    schema: starci/design-review@1
    reviewer: Synthetic fixture reviewer
    authority: Synthetic runtime regression authority only.
    reviewedAt: 2026-09-10T00:00:00Z
    observations:
      - id: contract-reviewed
        outcome: pass
        observation: The synthetic specification is complete enough to exercise runtime bindings; no product
          or code acceptance is claimed.
    limitations:
      - All code units and verification tests are planned and were not executed.
