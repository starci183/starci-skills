Generate a polished flat product UI screenshot, 1536x1024, light theme, straight-on browser content with no browser chrome or device frame. Use case: ui-mockup. This is a proposed visual direction, not an implementation capture.

SUBJECT: populated Cart of "Northwind Supply" at http://localhost:4069/cart.

FIXED ANATOMY (must not change):
- Image 3 is the EDIT TARGET, examples/ecommerce-app-be/.starciwork/features/checkout/ui/cart/assets/cart.anatomy.png. Make ONE targeted colour correction: all primary teal button backgrounds and selected teal accents must be UNIFORM FLAT exact sRGB #0D9488, RGB(13,148,136). The previous image drifted too cyan. Replace that cyan with the specified slightly greener muted teal. NO gradients, noise, mottling, lighting variation or texture on UI button fills; apply an exact single colour. Preserve black button labels. Preserve all other pixels as closely as possible: same text, positions, mascot policy, external collection labels/captions, edge-to-edge dividers and all control shapes. Where the duck has a scarf, recolour its teal to #0D9488 too, but never introduce a duck on a screen that has none. References 1 and 2 remain anatomy sources only. Do not copy their blue. Keep the top-right navigation EMPTY: no avatar, Alex or Sign out. The previous anatomy edit already removed them.
- Anatomy reference images only, not edit targets: Image 1 is examples/todo-app-backend/.starciwork/brand/assets/grammar-reference/tasks-screen.png; Image 2 is examples/todo-app-backend/.starciwork/brand/assets/grammar-reference/primitives.png. Preserve SurfaceListCard external header and footer, SurfaceCard internal label, and pill controls. Reference brands are not ecommerce authority.
- White top bar: "Northwind Supply" left; "Browse", "Cart", "Checkout", "Account" navigation; thin teal underline at "Cart".
- Main heading "Your cart" and subtitle "What you are about to order." are ON THE PAGE BACKGROUND, OUTSIDE all cards.
- Two app-owned composition columns: wide cart collection at left, narrower order summary at right, aligned below the heading.
- THE COLLECTION: this is the part people get wrong. Literal SurfaceListCard anatomy: label and fact on the page background above the card; the card contains only the rows; the caption below it. "Cart items" at left and "3 items" at far right are siblings OUTSIDE and ABOVE one white raised rounded list shell. The shell contains ONLY THREE ROWS, not the section header or footer. No card per row.
- Row 1: small neutral lamp illustration, "Aurelis Desk Lamp", "$89.00 each", a compact neutral secondary quantity field showing "1" with label "Quantity", line total "$89.00", neutral outline "Remove" pill. Row 2: tote illustration, "Meridian Carry Tote", "$145.00 each", "Quantity" field "1", "$145.00", "Remove". Row 3: notebook illustration, "Solstice Notebook", "$24.00 each", "Quantity" field "1", "$24.00", "Remove". Every row and all its controls are bounded INSIDE the single list shell.
- Between the three rows, thin edge-to-edge dividers touch BOTH edges of the list shell. The shell has no side padding; only each row's contents are inset. Do not inset dividers. This is the static-row geometry documented by the B3 anatomy-source observation, not cards per row.
- OUTSIDE BELOW the list on page background, caption "Quantities are checked again at checkout." Below it a simple dark "Continue shopping" text link.
- Separate right SurfaceCard: label "Order summary" INSIDE its top. Within that SAME white card: "Subtotal" and "$258.00", "Shipping and taxes calculated at checkout.", a divider, "Estimated total" and "$258.00", then teal "Continue to checkout" pill. All summary content is INSIDE its card, unlike the collection's external label.
- Bottom-right outside all regions, small neutral review caption "DIRECTION - NOT IMPLEMENTATION". Review-image label only, not product UI.

BRAND + TOKENS (non-negotiable):
- Authority: examples/ecommerce-app-be/.starciwork/brand/index.yaml, rev 1. Exact primary #0D9488; #FFFFFF surfaces; Inter. Neutral page #F7F7F5, 12px card radius and one soft diffuse shadow. Body 16px, labels 14px, heading about 24px, ordinary controls about 36px high.
- Teal only on primary CTA and selected navigation. Primary label BLACK #000000, 5.61:1 on #0D9488; never white or a substituted darker teal. Dark links and text, muted #5E6664.
- Quantity inputs inside the white list surface are the secondary variant, LIGHT GREY FILLED with visible boundary through fill contrast, no white-on-white fields. Remove pills are neutral grey outline with dark labels, following primitives.png.
- ZERO duck, turtle or other mascot anywhere: this populated cart contains removal actions. Neutral product drawings are not mascots.

CREATIVE LICENSE:
You MAY:
- Refine the app-owned two-column proportions, product thumbnails and whitespace while preserving all containment and labels.
- Use one subtle neutral emphasis on the estimated-total line, within its existing card.
You MAY NOT:
- Put "Cart items", "3 items" or the list caption inside the list card; put "Order summary" outside its own card; split the list into cards; remove or inset row dividers; add backgrounds to external labels.
- Change teal, add mascot/gradients/heavy shadows, invent coupons, payment methods, navigation, service promises or additional regions.

TEXT RULE: all quoted labels and amounts exact; no lorem ipsum, garbled text, source paths or ports. Follow knowledge/ui/proof/anatomy-source.yaml ANATOMY-5 and prompt-shape-b2: FIXED ANATOMY -> BRAND + TOKENS -> explicit You MAY / You MAY NOT. This populated cart and its mutation controls are proposed, not implemented.
