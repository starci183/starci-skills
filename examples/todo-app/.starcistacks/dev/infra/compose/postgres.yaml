services:
  postgres:
    image: postgres:16
    env_file:
      - ../../runtime/env/app.env
    ports:
      - "5432:5432"
    volumes:
      - ../../seeds:/docker-entrypoint-initdb.d:ro
      - todo-postgres:/var/lib/postgresql/data
volumes:
  todo-postgres: {}

