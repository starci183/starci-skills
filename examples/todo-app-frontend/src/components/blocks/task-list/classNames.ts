/** The one wrapping card class this block's view renders. */
export const cardClassName = 'grammar-card';

/** The one per-row class each task list item renders. */
export const rowClassName = 'grammar-row';
