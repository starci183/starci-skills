/// <reference types="next/image-types/global" />
/*
 * Next's own global image-module declarations - the same file the generated (gitignored)
 * `next-env.d.ts` references - so `./turtle-master.png` type-checks as `StaticImageData` whether or
 * not a dev server has run in this checkout yet.
 */
