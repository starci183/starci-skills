import { PlanRootPage } from "@/components/pages/PlanRootPage"

/** The plan index's thin shell; the page component owns the redirect to the usage screen. */
const Page = () => <PlanRootPage />

export default Page
