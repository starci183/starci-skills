import { NotifyPreferencesPage } from "@/components/pages/NotifyPreferencesPage"

/** The route adapter that mounts the notification preferences page (ui.notify.preferences) and
 * nothing else. */
const Page = () => <NotifyPreferencesPage />

export default Page
