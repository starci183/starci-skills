import { PlanUsageScreen } from '@/components/plan/usage-screen';

/** The route adapter that mounts the plan usage screen and nothing else. */
const Page = () => {
  return <PlanUsageScreen />;
};

export default Page;
