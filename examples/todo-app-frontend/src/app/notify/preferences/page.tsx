import { NotifyPreferencesBlock } from '@/components/notify/preferences';

/** The route adapter that mounts the notification preferences screen (ui.notify.preferences) and
 * nothing else. */
const Page = () => {
  return <NotifyPreferencesBlock />;
};

export default Page;
