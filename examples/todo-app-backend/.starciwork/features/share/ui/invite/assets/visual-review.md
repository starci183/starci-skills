# Visual review — invitation refused

The direction shows the desktop-1280 refused state with an invitee-email field, a primary invite button, one danger message explaining a duplicate collaborator and an unchanged collaborator section. The collaborator rows are peers under their own heading with visible roles and no collection card.

The empty state derives by removing the collaborator section; inviting derives by disabling and relabelling the invite button; pending-list derives by retaining rows with pending status; accepted derives by changing a row status while preserving the section. Revoke remains a required GrammarButton in those derived row states, although the selected still direction does not visibly establish its placement.

This image is a proposed visual direction generated from the retained prompt. It is not a render or screenshot of the running product, does not prove exact Grammar components or props (including revoke controls), and does not prove invitation behavior, permissions, accessibility announcements, API behavior or implementation conformance.
