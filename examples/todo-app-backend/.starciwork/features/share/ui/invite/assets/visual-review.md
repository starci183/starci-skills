# Visual review

The saved PNG opens correctly at 1402 x 1122. **Share this task** leads into a clearly bounded invite region and one collaborator collection. The invalid email remains visible with local feedback; Viewer and Editor are the only roles; two existing rows preserve email, role, text status, and named Revoke actions. Requested strings are correct.

The direction depicts the intended mix of Common and application-owned anatomy: `PageContainer`, headings, `SurfaceCard`, error-owning `Input`, native labelled radios, primary `Button`, `SurfaceListCard`, `Badge`, and weaker outline actions. At mobile-390 the form can stack and long row content can wrap without reordering regions.

ImageGen introduced conventional red input feedback and muted green/yellow badge fills. Those are not approved brand tokens or exact Badge palette proof; implementation should use installed component states or neutral/approved tokens. This is proposed direction only and does not prove invitation, status, revoke, accessibility, or API behavior.
