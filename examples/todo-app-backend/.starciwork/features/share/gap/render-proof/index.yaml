schema: work/gap
id: gap.share.render-proof
title: The invite-screen captures fail the canonical render checks - impl.share.todo-app-frontend.invite-screen cannot claim done
state: done
statement: "impl.share.todo-app-frontend.invite-screen keeps real captures - all five mapped
  states of /tasks/<id>/share at desktop-1280 and mobile-390, produced by ./assets/capture.mjs
  driving the served build against the live API. The artifacts are real; the proof is not. Run
  through checks/render.mjs, entity-list-in-card fails on running-page-accepted at both
  viewports: 3 `border-(--separator)` items sit in a `ul` inside `starci-core-surface` - a list
  of entities is a page section with a heading; a card is one item. The inviting-state captures
  additionally fail palette-off-brand on #97b5ff (60-63% of saturated pixels, nearest
  --starci-core-border at deltaE 16.42). Both findings are properties of the shipped markup and
  paint in todo-app-frontend, outside the .starciwork record scope, so the done claim is
  unproven and the record is held at todo, blockedBy this gap. Repro: node
  scripts/example-render-proof.mjs --work examples/todo-app-backend/.starciwork --record
  impl.share.todo-app-frontend.invite-screen"
closedBy: impl.share.todo-app-frontend.invite-screen
verificationSource: authored-claim
because: "closedBy names impl.share.todo-app-frontend.invite-screen, itself done again with a
  fresh evidence.yaml whose render-proof assertion reruns the canonical
  scripts/example-render-proof.mjs and now holds for every capture: lane w10 replaced the
  blended pending-tint/soft-foreground ink with declared tokens in globals.css and re-captured
  all ten states with the shared convention; entity-list-in-card and palette-off-brand pass on
  the fresh markup and bytes. The gap's closure is a derived fact about that record's own
  evidence, not a separate command."
change:
  rev: 2
  kind: editorial
  at: 2026-09-19T22:45:00.000Z
  reason: "Closed by lane w10: the #97b5ff inviting-state dominance was the installed theme's
    blended label ink on pending buttons and the accepted-state entity-list failure was
    separator-styled rows - declared-token styling in globals.css plus re-captured markup now
    pass every canonical check. The rev-1 opening narrative below stands verbatim. Opened at the v7-13 gate close: v7-9's honest recapture surfaced the anatomy and
    palette failures above on real markup and bytes. Neither editing checks/*.mjs nor staging
    captures can close this - the rendered page itself violates the design system's laws."
