schema: work/gap
id: gap.share.direction-not-drawn
title: No interface.draw run has produced a direction for share.invite
state: done
statement: ui.share.invite names its representative state (refused) and an exact
  prompt at share/ui/invite/assets/invite-refused.prompt.txt, but no interface.draw run has invoked built-in image_gen.imagegen
  against it yet - the lead performs that run through Orca (sol 5.6). Until it runs, the screen has a
  designed prompt and a coverage map, not a direction image or its generation provenance.
closedBy: ui.share.invite
verificationSource: authored-claim
because: "Closed by ui.share.invite, whose own evidence.yaml (scripts/example-evidence.mjs) now proves
  the refused-state direction PNG, its sibling prompt and a complete ui.coverage.map exist for this
  screen - the absence this gap named no longer holds."
change: {rev: 1, kind: initial, at: 2026-09-18T08:00:00.000Z}
