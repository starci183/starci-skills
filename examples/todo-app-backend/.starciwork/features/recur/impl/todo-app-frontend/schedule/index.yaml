schema: work/implementation
id: impl.recur.todo-app-frontend.schedule
title: The schedule screen
state: todo
repository: todo-app-frontend
owners:
  - {role: module, path: app/recur/schedule-screen.tsx}
  - {role: module, path: app/recur/upcoming-list.tsx}
  - {role: module, path: app/recur/end-rule-dialog.tsx}
proves: [ui.recur.schedule]
# gap.recur.unbuilt-module closed at rev 2 (closedBy: impl.recur.todo-app-backend.engine, the backend
# half); the blockedBy edge that named it here is removed because it is now stale (a resolved blocker
# cited with no rev pin), matching impl.login.todo-app-frontend.sign-in's own precedent for the same
# situation. This is a housekeeping removal only - none of app/recur/{schedule-screen,upcoming-list,
# end-rule-dialog}.tsx exist yet, so this record stays state: todo and this backend-only lane does not
# build them (out of its own owner-ruled scope: "examples only").
change: {rev: 1, kind: editorial, at: 2026-09-18T16:00:00.000Z, reason: "Removed the stale blockedBy edge on gap.recur.unbuilt-module (done at rev 2, cited with no rev pin). No other field changed; the frontend schedule screen remains unbuilt."}
