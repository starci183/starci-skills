# Visual review

The saved PNG opens correctly at 1448 x 1086. **Schedule** leads into one recurrence form with a consistent label/value rhythm. Frequency is Every N days, the retained invalid interval is 0 with its local correction, time is 09:00, time zone is Asia/Bangkok, and Save schedule is the only primary action. All requested strings are legible.

The regenerated direction removed the first attempt's unproven chevrons. It now depicts text-only `PressableField` controls, one error-owning `Input`, and one primary `Button` inside `PageContainer` and `SurfaceCard`. At mobile-390 the same ordered fields can remain a single fluid column.

This is proposed direction only. It does not prove picker interactions, recurrence generation, time-zone computation, persistence, accessibility, or API results.
