# Visual review

The direction shows the refused schedule state at desktop-1280: the entered repeat rule remains visible, the time-of-day picker trigger is retained, an assertive sentence names the invalid weekday condition, and the save action remains in the make-recurring surface. No upcoming-occurrences section or end-rule action is shown.

Real `@starci/grammar/common` anatomy depicted: `SurfaceCard`, `Input`, `PressableField`, `Text`, and `Button`. The representative refused direction derives no-rule by removing the refusal text; active replaces the form with a rule-summary `Text` and outline end-rule `Button`, then adds `SurfaceListCard` with `StaticStateRow` occurrences; ended removes the end-rule action, retains past rows, and adds `EmptyNotice` for nothing upcoming.

This is a visual direction generated from the retained prompt. It is not a render or screenshot of the running product, and it does not prove exact Grammar rendering, component props, semantic DOM, accessibility behavior, recurrence calculation, picker behavior, or API behavior.
