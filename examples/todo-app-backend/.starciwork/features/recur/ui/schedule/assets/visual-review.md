# Visual review — recurring schedule refused

The direction shows the desktop-1280 refused state with retained rule, time and timezone values, a danger message naming the invalid repeat-days field, save and end-rule actions, and an upcoming-occurrences section of equal plain rows without a card wrapper.

No-rule derives by removing the upcoming section and end-rule action; active derives by removing the danger message while keeping the rule summary, upcoming rows and end-rule action; ended removes the future preview while retaining past occurrence rows with their original status. Mobile-390 derives by keeping the form before the occurrence section in one column.

This image is a proposed visual direction generated from the retained prompt. The placeholder dates do not prove occurrence calculation or persistence. It is not a render or screenshot of the running product, does not prove exact Grammar components or props, and does not prove scheduling behavior, timezone handling, accessibility announcements, API behavior or implementation conformance.
