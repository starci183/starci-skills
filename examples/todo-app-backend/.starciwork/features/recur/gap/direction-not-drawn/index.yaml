schema: work/gap
id: gap.recur.direction-not-drawn
title: No interface.draw run has produced a direction for recur.schedule
state: todo
statement: ui.recur.schedule names its representative state (refused) and an exact
  prompt at recur/ui/schedule/assets/schedule-refused.prompt.txt, but no interface.draw run has invoked built-in image_gen.imagegen
  against it yet - the lead performs that run through Orca (sol 5.6). Until it runs, the screen has a
  designed prompt and a coverage map, not a direction image or its generation provenance.
closedBy: ui.recur.schedule
change: {rev: 1, kind: initial, at: 2026-09-18T08:00:00.000Z}
