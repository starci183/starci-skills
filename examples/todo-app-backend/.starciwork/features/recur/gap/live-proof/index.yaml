schema: work/gap
id: gap.recur.live-proof
title: No uat.verify run has produced a live run for recur.make-recurring
state: todo
statement: uat.recur.make-recurring names its entry, actor, ordered steps, fixtures, effects and cleanup
  in the op's own shape, but no uat.verify run has executed it against the running product yet - there is
  no uat/make-recurring/runs/<runId>/ folder with real screenshots, a playable video and a passing result.md,
  only the designed flow this record and its accounts.yaml/fixtures ref describe.
closedBy: uat.recur.make-recurring
change: {rev: 1, kind: initial, at: 2026-09-18T08:00:00.000Z}
