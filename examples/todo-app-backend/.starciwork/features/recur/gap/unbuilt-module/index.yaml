schema: work/gap@1
id: gap.recur.unbuilt-module
title: No recur module exists under the NestJS/Next.js layout yet
state: done
statement: The example's backend target moved to NestJS and the frontend to Next.js. Neither side has
  been built for this feature at this commit - there is no occurrence store, generator, zone-conversion
  module or schedule screen - so no rule, flow, component or screen in this feature has anything to run
  acceptance criteria or a UAT walk against.
closedBy: impl.recur.todo-app-backend.engine
verificationSource: authored-claim
because: "closedBy names impl.recur.todo-app-backend.engine, itself done with a fresh evidence.yaml whose
  assertions verified the real module (unit tests plus scripts/live-proof-recur.sh's live run against the
  dev stack, including a real integration.recur.scheduler tick). The gap's closure is a derived fact about
  that record reaching done, not something with a separate command of its own to run - the same pattern
  gap.recur.direction-not-drawn's own evidence-free done already uses."
change: {rev: 2, kind: editorial, at: 2026-09-18T16:00:00.000Z, reason: "impl.recur.todo-app-backend.engine reached done - the occurrence store, generator and zone-conversion module this gap named now exist under src/modules/bussiness/recur, proven by real jest specs and scripts/live-proof-recur.sh's live run. closedBy names only the backend implementation, so this gap closes on that record's own completion; the frontend schedule screen (impl.recur.todo-app-frontend.schedule) remains unbuilt but is a different record's concern, not named in this gap's closedBy."}
