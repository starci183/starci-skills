schema: work/implementation
id: impl.task.todo-app-backend.list
title: The owned-list query
state: todo
repository: todo-app
owners:
  - {role: module, path: src/modules/bussiness/task}
proves: [nfr.task.list.latency]

# decision.task.list.scope is now decided (owner-only: this product has no organization concept, so a
# list is unconditionally the reader's own tasks), which is exactly br.task.list.owned's own statement.
# This record's own honest-done claim still cannot move ahead of br.task.list.owned - ListTasksHandler is
# real and its ac.task.list.owned.excludes-others test passes today, but nfr.task.list.latency separately
# has no k6 harness in this host to measure against - but br.task.list.owned is an ordinary record with
# no gap or open decision of its own to name as the reason, so no blockedBy edge is authored onto it here
# (a dead-end edge is a warned, not refused, shape, but this lane's own scope is to bring the tree's
# BLOCKER_UNROOTED count down, not add to it for a record no gap has been written against yet).
