schema: work/implementation
id: impl.task.todo-app-backend.list
title: The owned-list query
state: todo
repository: todo-app
owners:
  - {role: module, path: src/modules/bussiness/task}
proves: [nfr.task.list.latency]

blockedBy:
  - {record: decision.task.list.scope, because: "br.task.list.owned (what this query implements and nfr.task.list.latency measures) is itself todo: whether an organization owner sees its members' tasks is undecided, and this record cannot honestly claim done against a rule whose own shape is still open. ListTasksHandler is real and its ac.task.list.owned.excludes-others test passes today, but that proves the current owner-only reading, not the rule this record is filed against once that decision lands. nfr.task.list.latency separately has no k6 harness in this host to measure against."}
