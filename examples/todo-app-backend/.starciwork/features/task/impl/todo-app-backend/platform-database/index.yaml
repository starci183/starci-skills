schema: work/implementation
id: impl.task.todo-app-backend.platform-database
title: The platform database module (shared Postgres integration)
state: done
repository: todo-app
owners:
  - {role: module, path: src/modules/platform/databases/postgresql/primary}
revision: 750bd4b1
# This module is shared: it owns both TaskEntity (task) and SessionEntity (login), and both features'
# repositories depend on it. It is recorded once, here under task, because Part C's brief scopes this
# pass to the login and task feature trees and a shared infrastructure module needs exactly one owner
# record, not one per consuming feature. impl.login.todo-app-backend.auth's session-store proof and this
# record's task-repository proof both rest on the same TaskEntity/SessionEntity shapes this module owns.
# Renamed from modules/integrations/postgres to modules/platform/databases/postgresql/primary on
# ex-nivo-shape, matching nivo's centralised-entities/named-connection convention.
proves: [data.task.task]
