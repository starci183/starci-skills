schema: work/implementation
id: impl.task.todo-app-backend.ownership
title: Ownership binding and its guard
state: todo
repository: todo-app
owners:
  - {role: module, path: src/modules/bussiness/task}
proves: [br.task.single-owner, sds.task.ownership-guard]

# gap.task.collaborator-completion closed (lane be-share): CompletionAuthorityRegistry's seam already
# supported t-collaborator before this lane started (see bussiness/task/completion-authority.spec.ts's
# pre-existing "a registered widened authority" test), and ShareCompletionAuthority now really
# registers into it. Re-proving this record against br.task.single-owner/sds.task.ownership-guard's
# rev 2 is lane login-impl/task's own call to make, not something this lane can verify from outside
# task's module, so it stays todo with no blockedBy of its own rather than citing a resolved gap.
