schema: work/implementation@1
id: impl.task.todo-app-backend.delete-task
title: The delete-task feature
state: done
repository: todo-app-backend
owners:
  - {role: feature, path: src/modules/bussiness/task}
  - {role: module, path: src/features/todo/graphql/mutations/task/delete-task}
revision: bfc3cdabb3c6c5afcd3f6371beaeca184a55a75d
# Done: br.task.delete.final is proven at rev 1 (delete-task.handler.spec.ts's named stays-gone test
# - TASK_NOT_FOUND on read-back and absence from the owner's own list - plus the live scripts/
# live-proof.sh deleteTask + list-absence steps against an API built from this source tree; the
# "after a restart" half rides on the single durable store, which impl.task.todo-app-backend.
# platform-database proves done), and event.task.deleted is published from this handler only after
# the row is gone (named event.task.deleted test). The old "does not outrun the rule it implements"
# hold is resolved: the rule no longer needs outrunning.
proves: [br.task.delete.final, event.task.deleted]
change: {rev: 1, kind: clarifying, at: 2026-09-19T17:00:00.000Z, reason: "Owners expansion (v10-2):
  owners now also claims the shipped delete-task mutation door,
  src/features/todo/graphql/mutations/task/delete-task - its resolver dispatches DeleteTaskCommand
  into the module above. Record metadata only; the revision pin and the code are unchanged."}
