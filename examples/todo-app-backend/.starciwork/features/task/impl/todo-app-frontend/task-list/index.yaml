schema: work/implementation
id: impl.task.todo-app-frontend.task-list
title: The list screen and the create form
state: todo
repository: todo-app-frontend
owners:
  - {role: block, path: src/components/blocks/task-list/**}
  - {role: feature, path: src/features/pages/tasks/**}
  - {role: route, path: src/app/tasks/**}
revision: bfc3cdabb3c6c5afcd3f6371beaeca184a55a75d
proves: [ui.task.list]
verification: [source inspection only; no fresh focused test run]
verificationSource: authored-claim
# ui.task.list itself is state: todo (brand rev 2 changed the primary colour token and the capture set was
# never retaken - see that record's own note). This implementation record does not outrun it: real,
# vitest-tested code exists at the owners above (see src/components/blocks/task-list/component.spec.tsx),
# but it is left todo rather than claimed done against a ui-screen record that is itself still open.
