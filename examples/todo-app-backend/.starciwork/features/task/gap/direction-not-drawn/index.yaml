schema: work/gap
id: gap.task.direction-not-drawn
title: No interface.draw run has produced a direction for task.list
state: todo
statement: ui.task.list names its representative state (many-tasks) and an exact
  prompt at task/ui/list/assets/list-many-tasks.prompt.txt, but no interface.draw run has invoked built-in image_gen.imagegen
  against it yet - the lead performs that run through Orca (sol 5.6). Until it runs, the screen has a
  designed prompt and a coverage map, not a direction image or its generation provenance.
closedBy: ui.task.list
change: {rev: 1, kind: initial, at: 2026-09-18T09:00:00.000Z}
