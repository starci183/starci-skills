schema: work/gap@1
id: gap.task.direction-not-drawn
title: No interface.draw run has produced a direction for task.list
state: done
statement: ui.task.list named its representative state (many-tasks) and an exact
  prompt at task/ui/list/assets/list-many-tasks.prompt.txt; interface.draw has since run and invoked
  built-in image_gen.imagegen against it, producing the many-tasks direction image and its generation
  provenance, and the ui-screen record's own evidence.yaml verified it. The screen now has a direction
  image, not only a designed prompt and a coverage map.
closedBy: ui.task.list
verificationSource: authored-claim
because: "closedBy names ui.task.list, itself done with a fresh evidence.yaml whose assertions verified the drawn direction; the gap's closure is a derived fact about that record reaching done, not something with a separate command of its own to run."
change: {rev: 2, kind: editorial, at: 2026-09-18T10:00:00.000Z}
