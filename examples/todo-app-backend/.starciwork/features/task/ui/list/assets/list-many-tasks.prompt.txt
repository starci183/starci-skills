Screen: The task list and its create form (task.list), route /tasks, actor: owner
State: many-tasks - the owner has multiple tasks in their list
Behavior to depict: the create-task form sits above a list of rows, each with a completion checkbox, its
title, and a delete action; several rows are shown, at least one complete and one incomplete

Business grounding (accepted, do not invent beyond these):
  - fr.task.create

Brand (brand/index.yaml rev 2, read do not restate elsewhere):
  - primary: #2F6BFF
  - surface: #FFFFFF
  - typography: Inter, sans-serif
  - don't: Never place text on the primary colour below a 4.5:1 contrast ratio.

Relevant UI knowledge:
  - knowledge/ui/composition/collection.yaml (COLLECTION-1, COLLECTION-2)
  - knowledge/ui/proof/brand.yaml (BRAND-1, BRAND-2, BRAND-3)

Actual installed Grammar anatomy (@starci/grammar@0.4.13, family common, resolved at
examples/todo-app-frontend/node_modules/@starci/grammar/package.json, package sha256
9fffbd5601e4e120d763b2108a35cf7fca4e4de551ddbfbc465ff69972880824) - depict exactly these components,
verified against the real consuming block at examples/todo-app-frontend/src/components/blocks/task-list/component.tsx,
no invented ones. many-tasks, not refused, is the representative state: the real block's `refused` branch
renders ONLY a Text line and hides the form and the list entirely
(`state === 'refused' ? <Text live="assertive">...</Text> : <>form + list</>`), so refused is the
LOWEST-anatomy state on this screen, not the densest one, and is not chosen here.
  - SurfaceCard {ariaLabel: "Tasks", measure: "formCompact"} - the one bounded surface for the whole
    screen; the task rows are peers inside it, not their own nested card (COLLECTION-2)
  - Input {id: "new-task-title", name: "title", label: "New task", value, onValueChange}
  - Button {type: "submit", variant: "primary", isDisabled} - the create action
  - a plain `<ul>`/`<li>` row per task (native list markup, not a Grammar composite - the block renders
    these by hand rather than through SurfaceListCard/StaticStateRow), each row carrying: a native
    checkbox input bound to the task's `complete` boolean, the task's title text beside it, and
    Button {type: "button", variant: "ghost", onPress} for delete

Nothing outside this exact export list from @starci/grammar/common may appear in the frame - no vendored
GrammarButton/GrammarTextField/GrammarMessage anatomy, no SurfaceListCard/StaticStateRow (the real block
does not use them here), no HeroUI component reached directly, no invented Alert/Toast/Banner.

Layout note: The mounting page wraps the card in GrammarRoot > main > Heading(level: 1, "Your tasks") per
examples/todo-app-frontend/src/features/pages/tasks/index.tsx - draw that heading above the card. The
create form sits above the row list inside the one SurfaceCard; rows are plain list items, never
individually carded (COLLECTION-2).

Forbidden treatments: no card wrapper around the list of tasks; no text on the primary colour below 4.5:1
contrast; no invented component outside the Grammar set named above; no fabricated brand mark beyond
brand/assets/**; no SurfaceListCard/StaticStateRow substitution for the real block's native list markup.

Viewport: desktop-1280, single column, product-shaped placeholder content only (no real person data).
