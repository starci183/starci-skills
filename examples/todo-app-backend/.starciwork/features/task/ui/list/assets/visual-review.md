# Visual review — task list refused

The direction shows the desktop-1280 refused state with a task-title field, a primary create button, one danger message for the required title and an unchanged multi-row task-list section. The rows are visually equal, separated by rules and grouped by the “Your tasks” heading without a card wrapper, matching the intended collection composition.

The empty state derives by removing all rows and adding its next-step explanation; the one-task state derives by retaining one plain row; the many-tasks state derives by repeating equal rows and removing the refusal message. Mobile-390 derives by preserving the create-form-first, list-second single-column order.

This image is a proposed visual direction generated from the retained prompt. It is not a render or screenshot of the running product, does not prove that exact Grammar components, props or CSS tokens were used, and does not prove creation behavior, persistence, ownership, accessibility announcements, API behavior or implementation conformance.
