schema: work/uat-flow@1
id: uat.task.create
title: Create a task, and fail to delete somebody else's
state: done
entry: /tasks
actor: owner
accounts: accounts.yaml
environment: environment.todo-app.dev
fixtures: [fixture.todo-app.seed]
steps:
    - {order: 1, actor: owner, action: "Sign in as the owner.", expected: "the task list loads", checks: [{id: loading, expected: yes}, {id: completion, expected: yes}]}
    - {order: 2, actor: owner, action: "Create a task with a title.", expected: "the task is created and appears in the list", checks: [{id: completion, expected: yes}, {id: validation, expected: no}]}
    - {order: 3, actor: owner, action: "Read the list.", expected: "exactly that one task is shown", checks: []}
    - {order: 4, actor: stranger, action: "Sign in as the stranger and attempt to delete it.", expected: "the delete is refused", checks: [{id: errorFeedback, expected: yes}]}
    - {order: 5, actor: owner, action: "Read the owner's list again.", expected: "the task is still present, unchanged", checks: []}
effects:
  - "insert one owner-owned task row"
cleanup:
  - "delete the run-owned task row by its recorded id"
# The run's stranger step did observe br.task.single-owner's stranger-delete arm (assertion recorded
# yes in runs/20260919T112844Z-5c10a673's result.md/ux-checks.json/manifest.yaml), but the rule stays
# todo - its own evidence never exercises the accepted-editor arm - and the gate refuses a done flow
# whose proves names a todo record. The observation lives in the run artifacts, not on this edge.
proves: [fr.task.create]
change: {rev: 1, kind: initial, at: 2026-09-19T11:30:00.000Z, reason: "state flips to done: run 20260919T112844Z-5c10a673 walked every step against the real stack (screens + video + outcome: pass in result.md; see evidence.yaml's run). The former blockedBy edge on gap.task.live-proof is removed because that gap is now closed by this record, and proves is narrowed to fr.task.create - br.task.single-owner was observed in the run but is still todo, and a done record may not claim to prove it."}
