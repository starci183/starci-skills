# Visual review — notification preferences refused

The direction shows the desktop-1280 refused state with one notification setting kept on, a danger message explaining that the preference was not saved, a primary save action and a separately reachable unsubscribe action. Settings are presented as rows without a card wrapper.

Loading derives by making the setting inert and removing the message; subscribed keeps the on value without the error; unsubscribed uses the off value and subsequent non-danger confirmation; saving disables and relabels the save action. Mobile-390 derives by preserving the same row and action order.

This image is a proposed visual direction generated from the retained prompt. Its switch-like drawing does not prove the required GrammarButton toggle-proxy implementation, and the image is not a render or screenshot of the running product. It does not prove exact Grammar components or props, unauthenticated reachability, persistence, accessibility announcements, API behavior or implementation conformance.
