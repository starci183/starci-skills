schema: work/implementation
id: impl.audit.todo-app-backend.erasure
title: The erasure-request module
state: todo
repository: todo-app
owners:
  - {role: module, path: src/audit/erasure.service.ts}
  - {role: module, path: src/audit/erasure-request.repository.ts}
proves: [br.audit.erasure.right, br.audit.erasure.logged, sds.audit.erasure-request]

blockedBy:
  - {record: gap.audit.unbuilt-module, because: "decision.audit.erasure-method is now decided (crypto-shred), so this module has a settled mechanism to build against; what remains is that no Nest module exists yet under the layout the example is moving to."}
