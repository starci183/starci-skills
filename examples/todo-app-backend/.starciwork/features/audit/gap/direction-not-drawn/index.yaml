schema: work/gap@1
id: gap.audit.direction-not-drawn
title: No interface.draw run has produced a direction for audit.privacy
state: done
statement: ui.audit.privacy named its representative state (erasure-refused) and an exact
  prompt at audit/ui/privacy/assets/privacy-erasure-refused.prompt.txt; interface.draw has since run and
  invoked built-in image_gen.imagegen against it, producing the erasure-refused direction image and its
  generation provenance, and the ui-screen record's own evidence.yaml verified it. The screen now has a
  direction image, not only a designed prompt and a coverage map.
closedBy: ui.audit.privacy
verificationSource: authored-claim
because: "closedBy names ui.audit.privacy, itself done with a fresh evidence.yaml whose assertions verified the drawn direction; the gap's closure is a derived fact about that record reaching done, not something with a separate command of its own to run."
change: {rev: 2, kind: editorial, at: 2026-09-18T10:00:00.000Z}
