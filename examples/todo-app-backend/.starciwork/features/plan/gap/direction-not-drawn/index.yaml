schema: work/gap
id: gap.plan.direction-not-drawn
title: No interface.draw run has produced a direction for plan.usage
state: todo
statement: ui.plan.usage names its representative state (over-cap-frozen) and an exact
  prompt at plan/ui/usage/assets/usage-over-cap-frozen.prompt.txt, but no interface.draw run has invoked built-in image_gen.imagegen
  against it yet - the lead performs that run through Orca (sol 5.6). Until it runs, the screen has a
  designed prompt and a coverage map, not a direction image or its generation provenance.
closedBy: ui.plan.usage
change: {rev: 1, kind: initial, at: 2026-09-18T08:00:00.000Z}
