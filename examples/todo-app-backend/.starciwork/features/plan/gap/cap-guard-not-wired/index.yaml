schema: work/gap
id: gap.plan.cap-guard-not-wired
title: Nothing on the create path calls the cap guard yet
state: todo
statement: sds.plan.cap-guard specifies checkCanCreateTask and contract.plan.create-precondition names the
  shape task must call before writing, but fr.task.create's mainFlow ("The task is created, owned by the
  submitter, not complete.") and its implementation (impl.task.todo-app-backend.ownership) still create
  unconditionally once the title is non-empty - neither the guard's own build nor the call into it exists,
  which is the one fact contract.plan.create-precondition and sds.plan.cap-guard were each citing the other
  over before this record gave it one name.
closedBy: contract.plan.create-precondition
change: {rev: 1, kind: initial, at: 2026-09-18T10:00:00.000Z}
