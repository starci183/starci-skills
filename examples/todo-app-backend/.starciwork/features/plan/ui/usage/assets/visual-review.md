# Visual review

The direction shows the over-cap-frozen usage state at desktop-1280: a prominent 10-of-10 usage metric, full progress bar, cap-and-upgrade refusal sentence, and primary upgrade action share one compact surface.

Real `@starci/grammar/common` anatomy depicted: `SurfaceCard`, metric-lead `Text`, `Progress`, assertive-live `Text`, and `Button`. The representative over-cap-frozen direction derives under-cap by removing the refusal and upgrade action, at-cap by using polite warning copy and a secondary upgrade action, and paid-unlimited by retaining only the metric while removing cap-specific anatomy.

This is a visual direction generated from the retained prompt. It is not a render or screenshot of the running product, and it does not prove exact Grammar rendering, component props, semantic DOM, accessibility behavior, plan enforcement, upgrade behavior, or API behavior.
