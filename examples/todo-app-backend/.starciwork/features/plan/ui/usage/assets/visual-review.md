# Visual review — usage over cap

The direction shows the desktop-1280 over-cap-frozen state with the task cap and usage count, a danger message naming why creation is blocked and a prominent upgrade action beside that message above the fold. Supporting plan details keep the conversion context visible.

Under-cap derives by retaining the usage count while removing the danger and upgrade prompt; at-cap replaces the refusal with a non-danger warning and keeps upgrade optional; paid-unlimited removes the cap and upgrade actions and leaves only usage. Mobile-390 derives by stacking the message and primary action without hiding either.

This image is a proposed visual direction generated from the retained prompt. It is not a render or screenshot of the running product, does not prove exact Grammar components or props, and does not prove the displayed count, cap enforcement, checkout, accessibility announcements, API behavior or implementation conformance.
