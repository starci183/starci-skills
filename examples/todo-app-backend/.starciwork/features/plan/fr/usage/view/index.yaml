schema: work/functional-requirement@1
id: fr.plan.usage.view
title: View usage against the cap
state: todo
actors: [owner]
trigger: The owner opens the usage screen.
mainFlow:
  - The owner's effective plan is read (t-revert-on-read) and their active task count is computed
    (br.plan.active-scope).
  - The screen shows the count against the cap, or "no cap" on the paid plan, and an upgrade action when free.
postconditions:
  - The count shown always matches what the cap guard would compute for the same person at that moment.
refs: [sds.plan.subscription-lifecycle, br.plan.active-scope, ui.plan.usage]

requiresProof:
  uat:
    required: true

change:
  rev: 4
  kind: clarifying
  at: 2026-09-19T14:45:00.000Z
  reason: "v9-3's run walked this surface live and observed the at-cap state correctly; the blockedBy
    because text claiming 'has not run' is corrected. The edge stays - the run settles fail, not the
    pass this record's uat proof needs."
  withdraws:
    - "uat.verify has not run the usage surface against the running product yet."

blockedBy:
  - {record: gap.plan.live-proof, because: "The usage surface has now been walked live - uat.plan.upgrade-after-cap run 20260919T143402Z observed the at-cap state naming the cap with the upgrade action present - but the run it was observed in settles fail (pending SePay gateway leg; no create-refusal feedback on /tasks), so the required live uat proof is exercised yet unsettled and stays open with the gap."}
