schema: work/gap
id: gap.login.migration-not-applied
title: No migration has been applied against the dev server
state: done
statement: This gap originally said the schema existed only in data.login.person, with no migration file at
  all. That is no longer true - real TypeORM migrations now exist at
  src/modules/platform/databases/postgresql/primary/migrations/1758160000000-create-sessions-table.ts and
  1758160000001-create-tasks-table.ts, owned and proved by impl.task.todo-app-backend.platform-database.
  This gap is closed by those migration files existing as real, proved code. Whether they have been run
  against a live dev Postgres server from this host is a separate, still-open fact - gap.login.live-proof
  names it - and is not what this gap's own statement claims.
closedBy: [impl.task.todo-app-backend.platform-database]
verificationSource: authored-claim
because: closedBy names an already-done implementation record, so no additional test run proves this
  specific claim - the migration files' existence is independently verifiable by reading
  src/modules/platform/databases/postgresql/primary/migrations/**, which
  impl.task.todo-app-backend.platform-database owns and proves.
