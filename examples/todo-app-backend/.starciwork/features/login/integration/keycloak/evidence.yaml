# Written by starci-kernel when it settles the operation: the assertions below are the checks it
# re-ran itself, not what the worker reported. It is a file of its own so that the history of a
# record is the history of the record, and every re-run does not rewrite what the record says.
schema: work/evidence
record: integration.login.keycloak
recordDigest: 9e2d7a5c3f8e1b6d4a9c2f5b8e1d4a7c0f3b6e9d2a5c8f1b4e7d0a3c6f9b24c1
stale: true
staleSince: {record: integration.login.keycloak}
staleReason: The record's index.yaml bytes no longer match recordDigest above; something
  edited the record after this run captured it, and no fresh run has re-verified it since.
outcome: pass
proof:
  boundary: live
  fakes: []
assertions:
  - id: token-exchange-returns-a-subject
    outcome: pass
    observation: npm run test:integration -- auth/keycloak exited 0 against localhost:8089
  - id: a-wrong-pair-returns-no-subject
    outcome: pass
    observation: npm run test:integration -- auth/keycloak/refusal exited 0 against localhost:8089
provenance:
  actor: starci-kernel
  tool: starci-kernel
  environment: dev
  servedVersions:
    - repository: todo-app
      commit: 6f1e4b7d0a9c2e5f8b1d4a7c30f3b6e9d2a5c8f1
      artifact: worktree
  capturedAt: 2026-09-18T04:44:00.000Z
