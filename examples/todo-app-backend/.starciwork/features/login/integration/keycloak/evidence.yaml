# Written by hand: a work/integration@1's requiresProof.live asks for proof {boundary: live, fakes: []}
# against the real provider (schemas/work-layout.yaml), which is not an --assert-shaped unit-test
# command scripts/example-evidence.mjs runs, but a replayable shell script this record's own note
# already names (scripts/live-proof.sh).
schema: work/evidence@1
record: integration.login.keycloak
proof:
  boundary: live
  fakes: []
outcome: pass
assertions:
  - id: real-keycloak-token-exchange
    command: bash scripts/live-proof.sh
    exit: 0
    outcome: pass
    observation: >-
      Ran against the real Keycloak container (todo-app-dev-keycloak-1, port 8089, imported realm
      "todo" - reused read-only, this lane started/stopped nothing on it) with the backend's real
      KeycloakClient (no FakeKeycloakClient). Step "signIn demo (correct password) -> sessionToken"
      completes a real direct-access-grant token exchange against
      /realms/todo/protocol/openid-connect/token and returns a session token; steps "signIn wrong
      password" and "signIn unknown email" both get INVALID_CREDENTIALS, identically, from the real
      provider's own refusal. 22/22 live-proof.sh steps passed (run: 2026-09-18, this lane).
provenance:
  actor: example-evidence
  tool: scripts/live-proof.sh (run by hand, not scripts/example-evidence.mjs)
  environment: local
  capturedAt: 2026-09-18T08:52:00.000Z
