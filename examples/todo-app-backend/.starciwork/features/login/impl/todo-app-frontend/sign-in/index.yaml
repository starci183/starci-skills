schema: work/implementation
id: impl.login.todo-app-frontend.sign-in
title: The sign-in route, feature and block
state: todo
repository: todo-app-frontend
owners:
  - {role: route, path: src/app/sign-in}
  - {role: feature, path: src/features/pages/sign-in}
  - {role: block, path: src/components/blocks/sign-in-form}
proves: [ui.login.sign-in]

# gap.login.direction-not-drawn closed once interface.draw ran and ui.login.sign-in reached done
# (see that record's evidence.yaml); the blockedBy edge that named it is removed because the gate
# refuses a blocker whose target has since reached done (a stale citation), not because this impl
# record itself now claims done. The route/feature/block code at the owners above is real and its
# own vitest suite passes, but re-claiming done against the now-done ui node is implementation work
# this record does not perform here; it stays state: todo, simply no longer citing a resolved blocker.
change: {rev: 1, kind: editorial, at: 2026-09-18T10:00:00.000Z, reason: "Removed the blockedBy edge on gap.login.direction-not-drawn: that gap reached done (closed by ui.login.sign-in), so the edge was stale per the layout's own staleness rule. No other field changed."}
