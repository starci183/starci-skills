schema: work/event@1
id: event.login.new-device-signin
title: A person signed in from a device that ended a prior session
state: todo
producer: fr.login.sign-in
payload:
  - {name: personId, type: text}
  - {name: deviceId, type: text, note: "Identifies the device well enough that the same device signing in twice reads as not new."}
  - {name: signedInAt, type: timestamp}
  - {name: sourceEventId, type: text, note: "Stable and unique per sign-in."}
delivery: {guarantee: at-least-once, ordering: per-key}
refs: [fr.login.sign-in, br.login.session.single-device]
blockedBy:
  - {record: gap.login.device-field, because: "decision.login.session.devices is decided (single-device), so the question is settled - what is missing is the field: no deviceId exists to put in this payload until a device field is added to the session/person shape."}
change: {rev: 2, kind: clarifying, at: 2026-09-19T12:00:00.000Z, reason: "because text corrected: decision.login.session.devices has been decided since 2026-09-18, so 'until the decision is settled' described a wait that is already over; the remaining absence is the field itself, which gap.login.device-field names."}
