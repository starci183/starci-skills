# Written by hand (not scripts/example-evidence.mjs, which only proves unit/integration records
# against --assert commands): a uat-flow's evidence is the run itself - schemas/work-layout.yaml's
# uatRun note is what check-example-work.mjs's concept 12 actually checks (a `run` field naming
# runs/<runId>, that run having screens/, videos/ and a result.md whose outcome is pass).
schema: work/evidence
record: uat.login.sign-in
run: runs/20260918T090054Z-240aa95e
outcome: pass
provenance:
  actor: example-evidence
  tool: 'npm run uat -- uat.login.sign-in.spec.ts (uat/playwright.config.ts), against the real
    todo-app-dev compose stack (Postgres 5432, Keycloak 8089, another lane''s stack reused
    read-only) plus this lane''s own backend (ts-node-dev, port 3001) and frontend (next build &&
    next start -p 3000), with UAT_LIVE_LOGIN_AUTHORIZED=true, UAT_DEMO_PASSWORD=<seeded demo
    password>, UAT_PASSWORD_STRANGER=<seeded demo2 password>.'
  environment: local
  capturedAt: 2026-09-18T09:00:54.900Z
