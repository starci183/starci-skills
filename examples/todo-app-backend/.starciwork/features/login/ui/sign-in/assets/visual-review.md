# Visual review — sign-in refused

The direction shows the desktop-1280 refused state as a centred, single-column sign-in form with a plain product wordmark, one danger message above the controls, a retained placeholder email, a cleared password field and a primary submit button. It reflects the specified Grammar anatomy: two labelled text fields, one submit button and one danger message.

The empty, filled and working states derive from this direction by keeping the same order and spacing while changing field values, message visibility and submit availability or label as recorded in `ui.coverage.map`. The mobile-390 layout derives by preserving the single-column order without hiding controls.

This image is a proposed visual direction generated from the retained prompt. It is not a render or screenshot of the running product, does not prove that exact Grammar components or props were used, and does not prove application behavior, accessibility announcements, API behavior or implementation conformance.
