// The CURRENT bytes. The evidence beside the record was captured when this file still read
// `export const behaviour = "v1"` - the code moved, the proof did not.
export const behaviour = "v2 - rewritten after the proof was captured";
