schema: work/functional-requirement
id: fr.lease.holds
title: The lease holds
state: done
verificationSource: authored-claim
because: Fixture carries an authored-claim; the defect lives in the run ledgers, not this record.
actors: [system]
trigger: A run leases a fixture.
mainFlow:
  - The fixture is held exclusively by that run.
postconditions:
  - No other run claims the same fixture as fresh.
