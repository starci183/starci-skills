# DEMO-ONLY age identity for the todo-app-backend example.
#
# This identity exists so that a reader of this example can run `sops -d` on the
# example's `.enc` files and see a genuine, round-tripping SOPS document instead of a
# fake placeholder. It is committed to the repository on purpose, which is exactly why
# it must never be used to protect anything real: any secret encrypted to its public
# key is decryptable by everyone who can read this file, forever.
#
# DO NOT reuse this identity outside this example. DO NOT encrypt a real credential to
# its public key. DO NOT copy this pattern into `.stacks` (the production layout) or
# into any non-example environment. Rotate/replace it (and every `.enc` file encrypted
# to it) if it is ever mistaken for a live key.
#
# created: 2026-09-18T10:50:14+07:00
# public key: age1fpvksnujfqcrf6z0000uxnqn9phkjxm0p5kd8ecwt3tjtmdv5uqsuzfsh6
AGE-SECRET-KEY-1FVY35973E4P27JU5UVNQ8W9PPZT24UAA5032LG08S9YLZJ8RR7MSRE9MW6
