services:
  prometheus:
    image: prom/prometheus:latest
    ports:
      # Host 9090 collides with an already-running starci-prometheus container; host side only.
      - "9091:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro

