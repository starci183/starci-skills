// Synthetic source supplied to the quality.verify forward-test; not product coverage.
import test from 'node:test';
import assert from 'node:assert/strict';

test('synthetic arithmetic contract', () => {
  assert.equal(2 + 3, 5);
});
