# StarCi Lite profile — 2026-09-06

The user requested a separate low-overhead entry for questions, documentation and small local fixes, with full StarCi retained for complex work and formal UAT. The package includes the separate skill. The CLI selects full by default and supports explicit Lite installation; updates retain the recorded profile. Custom host instructions are preserved, and existing full workflows keep their current evidence and gates. No token savings are claimed without measured comparable runs.

The same request required current-only execution instead of old-ledger acceptance. Published replan history remains evidence, but an activeFromStep field cannot bypass current delivery ordering or first-step reachability. Coordinate-preserving re-entry was verified on a fresh current workflow without changing its frozen UAT prerequisite.

Focused profile and runtime regressions, the full suite, archive hashes and packed installation proofs are recorded in the release support manifest. They do not claim completed product UAT.
