# Business head projections — 2026-09-06

Read-only inspection of the existing business authority at `C:/Repositories/ac/starci-academy-backend/.worktrees/starci-academy/businesses` found two independently authored business features with the same useful document boundary. The worktree was clean at `fd91c9c455c2d90c5d4b8415a31a4f0b3a5927bd`; this inspection changed no feature, registry object or worktree registration. The user confirmed this project-partitioned storage is intended: `<Workflow>/.worktrees/<project>/businesses`. The old unpartitioned alias was stale. Correcting that alias preserves the explicitly locked worktree and its registry; it does not move it or initialize a competing empty authority.

- `features/course-learning/` contains exactly ten root files: `CONTEXT.md`, `overview.md`, `actors.md`, `contracts.md`, `rules.md`, `states.md`, `spec.md`, `acceptance.md`, `evidence.json` and `model.json`. Its model is 22,200 bytes, declares schema version 2 and implemented authority, and holds one journey, five surfaces, two business rules and five states. Its 9,308-byte specification also includes entities, operations, acceptance conditions and explicit unknowns. The context navigation links the split documents and optional flow/surface modules.
- `features/identity-access/` independently contains the same ten root files. Its model is 12,194 bytes and declares schema version 1; it holds one sign-in journey, one surface, three business rules and five states. Its 6,588-byte specification includes actors, data contracts, acceptance and unresolved decisions. Its context explicitly identifies `model.json` as machine authority and Markdown as generated projections. No credentials or identity provisioning data were inspected.

Both models contain structured source references, scopes, actors, journeys, business rules, states, entities, operations, surfaces, acceptance and unknowns. These observations support preserving substantive content and machine authority when generating a business head. The generic alias resolves the frozen workflow project's partition. The observed product name, legacy schemas and runtime states remain evidence rather than canonical law.

At baseline runtime commit `1d3c6bfe1b406bac9d94b114bf715c90be41cfde`, `templates/kinds/model.schema.json` was a closed schema whose product content consisted of three promise strings. `scripts/business-registry.mjs` planned and archived model, claims and optional coverage documents; it did not generate the ten-file feature bundle. The business decision gate could therefore accept a machine summary without enforcing the rich split documents requested by the user. Hand-copying legacy Markdown would neither repair that gate nor bind the copied text to the current model's content address.

The additive model documentation contract stores six authored Markdown sections with claim and coverage references. The Source-owned projection manifest maps them into the ten generated files, including an assembled specification and evidence JSON. The model's existing self-fingerprint and immutable object address cover the authored content. The registry remains the discoverable authority, while byte comparison rejects edited or missing projections. Old model bytes are historical evidence and are not silently rewritten into this additive shape.

`scripts/business-head.spec.mjs` exercises seventeen synthetic business topics across the six sections: modules, journeys, lifecycle, entitlements, RAG evidence, open decisions and legacy/target comparison remain actual nested Markdown. It also checks first-head publication, later archived revisions, every missing or changed output, corrupt evidence, invalid references, path safety and stale registry publication. Test results belong to the release verification; these legacy observations alone do not prove a current publication or product outcome.

## Release verification

The complete isolated 2.5.0 tree passed `npm test` on 2026-09-06: all routing, resource, knowledge citation, alias, operator, helper, defaults and template gates; 27 operator self-tests and one helper self-test; 472 script tests with 471 passed, one skipped and zero failures; and all 76 generated documentation files. `git diff --cached --check` also passed.

The bundle and publisher regressions prove project separation, refusal of an unpartitioned or mismatched authority, retention of substantial Markdown blocks, exact ten-file readback, content-addressed historical lineage, stale-plan refusal, and preservation of an existing registered Git worktree lock. Publication candidates need their frozen request, complete operator receipt, running attempt and exclusive feature lease before effects. Missing authority never initializes a competing registry. Hardlink replacements preserve the other owner, while symlink and junction paths are refused.

The full run exposed installation-dependent fixtures in data.seed, identity.provision and uat.verify: early cases used the installation host while later cases owned a temporary declared host. All cases now use the fixture host and retain missing-stack rejection coverage. Migration fixtures likewise bind real temporary routes and measured Git heads. These repairs change test setup only; production runtime and write gates remain enforced.

The branch starts at the requested 2.4.6 baseline, `1d3c6bfe1b406bac9d94b114bf715c90be41cfde`. Remote main independently advanced to 2.5.0-rc.4 during this work; this verification does not claim an integration with that newer line. The original shared runtime checkout retains its original dirty files. No live business head, product source, database, service, active ledger or worktree registration was modified by this implementation.
