# Workflow re-entry evidence — 2026-09-06

## Observations before repair

The published baseline is `8a3e1cdc43298d51b547947fcdbae57b919f855b` (2.5.0-rc.6).

1. Nivo Chatbot session `20260905160512-nivo-ca563924` has accepted architecture 18/1 and a preserved mismatched backend generation 19/1. A resume preview revalidates accepted workspace binding 12 against today's dirty checkout and refuses `CHECKOUT_DIRTY`. That old binding declared the control-plane roots; the prepared backend files occupy roots named in request 19 but missing from the current mission discovery code impact. Direct inspection of the current state contradicted the initial assumption that discovery already covered them. The authored discovery contract describes generalized code ownership and its gate binds repository/role, not an exhaustive path allowlist. The owner traced these technical roots to the user-confirmed architecture 18 and existing module scope. Their omission alone is not proof of scope expansion; new exact checkout binding and mutable/protected ownership checks remain required. The shared primary registry file was restored clean and assigned to the Accounting integration owner. A historical receipt is evidence of its original invocation, while a new binding must still validate the actual checkout and every current write boundary.
2. Nivo Accounting session `20260905-nivo-accounting-01a07247` sealed 34/1 as blocked `INVALID_INPUT`. Its source window contained a commit amendment and correctly failed source acceptance. The blocked response truthfully carries `changes.md`, inconclusive criteria and no source-credit artifact. It must remain immutable. A new technical attempt needs an honest fresh source window and actual verification; neither relabeling the old result nor changing the caller stop into a blanket resume is acceptable.

The existing entry already allows mismatched work to retry at a fresh coordinate with the prior attempt identity, preserved or strengthened expected outcomes and changed method/input. The planner lacks the corresponding bounded edit. Full reforecast preview is possible but repeats unrelated prerequisites; this repair must retain dependencies and unchanged accepted work.

## Required verification

- Retained request identity, original mission confirmation and complete accepted evidence hashes remain checked; tampering must fail.
- Historical binding replay cannot silently become permission for a new checkout or broader roots.
- A fresh rebind remains confined to the current confirmed role, repository, checkout and business/module scope, with exact mutable/protected boundaries and foreign owners excluded. Generalized discovery code references are not silently converted to a new exhaustive glob allowlist.
- Retry preserves the failed cell, same logical operator/goal and expected requirements, with an explicit prior attempt and actual changed method/input; identical retry remains NO_PROGRESS.
- Technical input correction cannot answer an unresolved business choice, expand permissions or bypass an external stop.
- Preview and commit must both obey these constraints, including concurrent state change refusal.
- Native session probes are read-only. Only their owning tasks may open and execute the resulting attempts after publication.

## Status

Candidate implementation and verification are in progress. This note does not assert a passing regression, publication, successful product retry, API proof or browser UAT. Final release evidence must replace this status with measured results and their limits.



## Candidate review findings

The coordinator reviewed the implementation before release and required these corrections:

- Local-input validation returns no errors for non-applicable refs and imported slots. Historical binding authentication must independently verify the full evidence manifest and reject an unexpected import marker/noncanonical output ref; an empty error list alone is not a seal.
- Reflog errors include unavailable or unreadable windows. Only a measured preserved window can establish a technical source-history correction; inability to inspect it cannot grant retry authority.
- Current HEAD must match the retry base at opening, but legitimate subsequent normal commits must not invalidate acceptance or historical replay of that invocation. The lifecycle test must include a new commit and later unrelated progress, not only immediate zero-diff reuse.

These are pre-release review findings. Their resolution still requires implementation and passing adversarial lifecycle tests.

## Measured partial verification

- `related-first.log`: 21 tests passed, 0 failures and 0 skips. Existing workspace checkout, plan history and unit-expansion checks remain green.
- `reentry-third.log`: 2 tests passed, 0 failures and 0 skips. The fixture performs accepted mismatch → reviewed forecast preview/commit → official fresh retry open/accept, rejects unchanged retry, and retains the failed response bytes. Missing seals, route tampering and import-marker substitution are refused.
- `actual-sealed-bind.json`: the candidate verifies the real Chatbot 12/1 historical binding read-only, with state and request bytes unchanged.

These are partial checks. Source-history retry with a new normal commit, optional rebind, full regression, native continuation and publication remain unproved here. The HEAD phase concern is a lifecycle test obligation; existing acceptance phase separation may already satisfy it and must not be described as a confirmed defect without a failing reproduction.

## Independent progress review

A read-only probe against the real Chatbot 19 request found that an otherwise identical retry was refused with NO_PROGRESS, but merely reversing contexts and read-resource order avoided that refusal. Those lists describe the same resources and heads. The retry progress fingerprint must canonicalize semantically unordered collections; permutation alone is not execution progress. No native attempt was opened and no native file was changed. This finding is pending correction and regression evidence.

## Final candidate verification

The frozen RC.7 implementation passed the complete `npm test` command: **447 tests, 447 passed, 0 failed, 0 skipped**, process exit 0. The generated-document check reports **76 files matching**. `full-run.json` binds the actual exit code to the full log digest; `full-final.log` retains the output.

The final focused set reports **6/6**, including mismatched and inconclusive retry, genuine backend done acceptance after a new normal commit, later HEAD/dirt replay, immutable historical binding, optional fresh rebind and protected wildcard paths. Both independent review findings were fixed: semantic ordering no longer grants progress, and protected-path comparison uses the existing backend glob semantics.

The actual read-only Chatbot progress probe now refuses both unchanged and reordered-only requests; its state and request bytes remain unchanged. The native historical binding probe remains valid without changing the peer ledger.

`npm pack --dry-run --json` succeeds for `@starci/skills@2.5.0-rc.7`: 679 entries, 1,692,654 archive bytes and 6,484,761 unpacked bytes. This is a package dry-run, not a registry publication.

There are exactly 12 reviewed candidate paths and no deletions. Guarded adoption checks the baseline and per-file bytes and excludes unrelated live findings. Native full preview must run against the adopted real Source, because its registered repository routes belong to that Source; its result is retained in `native-preview-result.json`. Publication and remote readback are separately recorded in `publication.json` after success. No native attempt, product test or UAT is credited by these runtime fixtures.
