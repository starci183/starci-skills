# Blocked continuation compatibility — 2026-09-06

The business writer's confirmed document mission stopped at its first environment.preflight under the earlier readiness rules. Runtime commit `38d917f3f1c6a100fdd3ea0f7e8f026ef5ecdce5` repaired document applicability, but read-only continuation inspection found three separate admission failures: blocked lifecycle could not preview a plan, a resume edit with no planHistory was silently discarded, and current operator validation rejected nine formerly accepted wall rows. The accepted request, response, invocation and mission bytes were intact. These are enforcement gaps in the existing same-mission resume and immutable-history rules, not new product policy.

The original session is `C:/Repositories/ac/starci-academy-backend/.worktrees/sessions/20260906092033-starci-academy-e06e39de`. Its original state SHA-256 was `381611196d71831bd00d96846cc42ed4d83b4d9efe0ae8c8ff464f0728b44958`. Its owner subsequently changed only status to running, lifecycle.phase to active and removed stoppedAt, yielding `33b1bcbf0f905adc92db98e7e8791a2de19ee636b139a8dc72467b809b5f4806`. Read-only inspection reconstructed the prior three fields in memory and recovered the exact original state hash. Neither form was edited by the runtime repair task. The newer active shape still retained its terminal blocked attempt, original current cell, empty leases and absent plan history, so activation alone did not repair resume mapping or historical revalidation.

The continuation contract now binds an actual request from the native owner and the changed condition to an explicit resume edit. Preview changes nothing. A reviewed plan commit records the actual prior state and stop, retains the previous execution bytes, and emits an audited transition. Both the original blocked shape and the already-active legacy shape can take that route without inventing approval or backdating a lifecycle transition. The latter audit says it started active; it does not pretend the earlier manual activation was performed by this API.

Portable regression fixtures reproduce the observed ledger structure with synthetic scope and readiness content; private user-authored product text is not published as runtime law. Tests exercise immutable blocked history independently of current operator law, exact owner and confirmation checks, absent/malformed request and edit refusals, stale previews, missing or changed seals, preserved request/response/history bytes and lawful successor admission. The historical reader is unavailable for matched success, typed runtime repair or imported evidence reuse. New work must prove current readiness and cannot use an old failure as completion evidence.

This repair changes no live business head, product source, runtime service, database, original session or shared runtime installation. Verification and the refreshed sibling runtime commit are reported by the release run; these observations alone do not assert a successful business publication.

## Release verification

The isolated 2.6.0 run passed all 100 targeted continuation, history, planning, chain, waiting and correction tests. The complete `npm test` then exited zero: every structural gate, 27 operator self-tests, one helper self-test, 512 script tests (511 passed, one skipped, zero failures), and 76 generated documentation files. Independent code review found no release blocker; diff whitespace validation and the changed-file secret scan were clean. The end-to-end continuation fixture opened and leased a new preflight, rejected false success while a real declaration wall remained, then accepted its truthful new blocked receipt without changing the original sealed evidence.
