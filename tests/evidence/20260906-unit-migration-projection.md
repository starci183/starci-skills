# Unit-bound migration verification

Accounting's accepted backend plan 31/1 partitioned fifteen operations into five dependency-ordered modules. Its admitted first generator 32/1 selected the schema module. The migration contract nevertheless required every architecture operation in mutations and checked every migration write root. This demanded dependent work before its planned admission.

The repaired consumer resolves the exact current accepted units producer, verifies its sealed evidence and backend-plan law, and binds its architecture bytes to the original complete contract fingerprint. The plan's existing Modules-table reader supplies the selected operation IDs. Only those operations determine mutation coverage and migration mutable roots. An unsplit branch retains complete coverage.

Read-only probes on the actual admitted request reproduce the old refusal and verify the selected operation, missing-operation refusal, foreign-operation refusal, changed-writer refusal, unknown-unit refusal, missing-plan refusal, unsplit completeness and selected migration roots. Nine cases passed without editing the peer request. The detailed observation belongs to the coordinating session's support bundle at `.worktrees/v25-unit-migration/actual-probe.json` in the runtime's host repository.

Portable regression checks exercise prerequisite versus dependent operation projection, ambiguous module refusal, and the existing full migration/original-producer validation. These are runtime gate proofs; they do not establish Accounting implementation, PostgreSQL behavior or browser UAT completion.
