# Goal impact and workflow ownership observations

On 2026-09-06, two independent live feature sessions had generic routed frontend/backend targets without structured route or code impact. One confirmation referred to coordinator delegation; the other included a synthetic prompt tag. Those historical records are retained as observations, not treated as new user confirmations. The parent mission retained the read-only observations and original hashes.

A second independent runtime occurrence was structural: session-open accepted a project-local sessions root while workspace-checkout required the Source-local coordinate. Helpers instead used hostBinding.worktree, and aliases used Source. The project declaration and resolver give these consumers one owner.

UPDATE classification: exact authorized scope was already required (enforcement gap); the frontend-publication long-flow rule was narrower than feature handoff (widen executable applicability); workflow storage and runtime authority had inconsistent homes (fold into one resolver). No product-specific route or implementation rule is added.

Validation is recorded by scripts/v23-workflow.spec.mjs alongside the existing runtime, planner, workspace and installer regressions. These are synthetic relocated-machine checks, not a claim that a physical laptop was tested. Full-suite and clean-install results are recorded in the parent mission upgrade evidence.
