# Declared application route binding

On 2026-09-06 the Nivo landing workflow had a portable and hydrated landing route and a matched 43-check preflight, but workspace.bind rejected its role before reading either declaration. The operator question and checkout selector independently narrowed roles to fe/be although the portable and binding schemas already use the same generic slug contract.

The repair reads that existing portable identifier schema for both project and role and keeps declared-route existence, Source identity, exact Git worktree membership, branch policy and source-role context checks. A synthetic public-site Git fixture proves canonical and session selection, request and receipt binding; absent declarations, traversal, invalid/oversized identifiers and mismatched hydrated roles refuse. Existing FE/BE regressions remain unchanged.

Focused proof: 12 checkout tests passed without skips; operator and template validators passed; 76 generated docs matched. The first new assertion failure was Windows path separator representation, corrected by comparing resolved paths. Full release validation and publication are owned by the coordinating task; this evidence does not claim they have completed.
