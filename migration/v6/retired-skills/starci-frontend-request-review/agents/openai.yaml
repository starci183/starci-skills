interface:
  display_name: "StarCi Frontend Request Review"
  short_description: "Approve or reject one feedback ledger entry"
  default_prompt: "Use $starci-frontend-request-review for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
