---
name: starci-frontend-ui-direction
description: Create and visualize three or four materially different UI directions after UX flow and interaction-container ownership are approved. Preserve the journey while proactively covering render states and responsive behavior.
---

# StarCi frontend UI direction

Frame the visual problem from fresh business authority plus approved UX flow and interaction-container ownership. Generate independent compositions, proactively render populated/empty/pending/validation/error/recovery/long-content and responsive states, compare tradeoffs, and recommend one without changing the journey, container choice, API behavior, or business policy. Current source is observation, not design authority. This skill ends with a typed review handoff and never edits product source.

Follow `analyze-input.md`, then execute `machine.json`. Load only the active operator and purge task-session drafts after handoff acknowledgement. Before returning the review handoff, resolve its validated HTML preview to an absolute executor-side path, generate the content reference only with `node <Source>/.claude/scripts/visualize-directive.mjs <absolute-path>`, and paste stdout unchanged. Never handwrite or interpolate the `visualize` JSON. Stop before the handoff or any `OK UI DIRECTION` command when the helper fails or the preview is not visibly rendered in the same response.
