interface:
  display_name: "StarCi Backend Solution Design"
  short_description: "Design one bounded backend solution"
  default_prompt: "Use $starci-backend-solution-design for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
