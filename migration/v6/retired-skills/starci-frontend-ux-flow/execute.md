# Execute frontend UX flow

1. Validate fresh business scope, known states, and the exact primary task; no UI direction is required.
2. Run only `fe/ux-flow`.
3. Return review handoff with typed potential signals or blocked.

## CONTEXT BY STATE

| State | Allowed | Forbidden |
| --- | --- | --- |
| `model` | business capabilities, known states, optional current-experience evidence, journey/state knowledge | UI direction, Grammar styling, raw implementation source, unapproved persistence |
