# Execute product potential

1. Validate hypothesis identities and resume target.
2. Run only `fe/product-potential`.
3. Return typed next candidates or blocked; never perform a routed mutation.

## CONTEXT BY STATE

| State | Allowed | Forbidden |
| --- | --- | --- |
| `assess` | potential signals, fresh business revision, exact evidence | implementation source, unapproved behavior, broad roadmap |
