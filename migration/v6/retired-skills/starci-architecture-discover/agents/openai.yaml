interface:
  display_name: "StarCi Architecture Discover"
  short_description: "Map observed architecture without choosing it"
  default_prompt: "Use $starci-architecture-discover for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
