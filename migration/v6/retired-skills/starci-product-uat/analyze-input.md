# Analyze starci-product-uat input

Global `@selection` has already selected this one-flow skill from prompt intent. Before any operator or default search retrieval, validate the invocation and verify `selection.skillId` equals `starci-product-uat`. Then perform these local checks:

1. Require a canonical flow review and immutable source, Behavior, UX and available UI authority revisions; missing UI detail may suspend only the UI lens.
2. Keep the canonical happy case separate, fold loading and refresh assertions into it when appropriate, and execute only unhappy branches that change product outcome, security, durable state, integration or user recovery.
3. Predeclare case, account or anonymous identity, fixture, precondition, expected outcome, Browser session and execution order before product execution.
4. Prohibit parallel visible-browser cases, account reuse across cases or reruns, shared authenticated browser contexts, unscoped fixture cleanup, and post-journey result manufacturing.
5. Deduplicate root causes only by authority, semantic owner, causal mechanism, corrective action, and source boundary.
6. Retest the discovering checkpoint, recovery path, all occurrences, and canonical happy smoke before closure.

Reject stale or missing authority/evidence, an ambiguous target, a write root outside scope, external mutation without approval, or an option outside the closed schema. Do not reconsider other skills here; return to global analysis if selection is wrong.

The fixed first state is `coverage`. Emit only normalized scope and facts as task-session data; do not choose a second mode or copy operator knowledge into context.

## Options

| Option | Values | Decision effect |
| --- | --- | --- |
| — | — | No additional option is loaded. |
