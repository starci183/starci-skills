# Analyze starci-frontend-quality-audit input

Global `@selection` has already selected this one-flow skill from prompt intent. Before any operator or default search retrieval, validate the invocation and verify `selection.skillId` equals `starci-frontend-quality-audit`. Then perform these local checks:

1. Compile a separate happy case and only material product-decision unhappy cases before browser execution.
2. Require every case identity and execution order to be declared before one-at-a-time visible Browser execution.
3. Keep Behavior, UX and UI verdicts independent even when they reuse the same runtime snapshot.
4. Permit SUSPENSE only in the UI lens and never count it as pass.

Reject stale or missing authority/evidence, an ambiguous target, a write root outside scope, external mutation without approval, or an option outside the closed schema. Do not reconsider other skills here; return to global analysis if selection is wrong.

The fixed first state is `coverage`. Emit only normalized scope and facts as task-session data; do not choose a second mode or copy operator knowledge into context.

## Options

| Option | Values | Decision effect |
| --- | --- | --- |
| — | — | No additional option is loaded. |
