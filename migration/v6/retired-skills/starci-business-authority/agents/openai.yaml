interface:
  display_name: "StarCi Business Authority"
  short_description: "Model and publish approved StarCi business truth"
  default_prompt: "Use $starci-business-authority for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
