# Execute `test/ui-audit`

Resolve only the canonical backend Source `.worktrees/uat` references and exact protocol files using default search. Audit UI principles and Grammar independently, then aggregate one failure-dominant UI result. Execute cases only in their predeclared contiguous order in the visible Browser, provision a fresh isolated account per case, and verify outcomes read-only without fixture finalization or post-journey mutation. Return only the typed v7 output. Stop when authority, evidence, order, isolation, or proof is incomplete.

For every approved destination, prove native link semantics and the same real non-null href across hover, focus, keyboard, and click. Preserve every contract-declared progress datum as an observable progress presentation. Reject any compact numeric fact whose visible rank equals or exceeds its owning section and content title. A static or button-only destination, missing declared progress, or fact-over-title hierarchy is `FAIL`.
