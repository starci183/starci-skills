# Execute `test/ux-audit`

Resolve only the canonical backend Source `.worktrees/uat` references and exact protocol files using default search. Audit orientation, feedback, recovery, refresh, resume, and completion using visible Browser evidence. Execute cases only in their predeclared contiguous order in the visible Browser, provision a fresh isolated account per case, and verify outcomes read-only without fixture finalization or post-journey mutation. Return only the typed v7 output. Stop when authority, evidence, order, isolation, or proof is incomplete.

