# Execute `test/flow-coverage-audit`

Resolve only the canonical backend Source `.worktrees/uat` references and exact protocol files using default search. Compile one flow into a minimal complete UAT case set with a separate canonical happy case. Execute cases only in their predeclared contiguous order in the visible Browser, provision a fresh isolated account per case, and verify outcomes read-only without fixture finalization or post-journey mutation. Return only the typed v7 output. Stop when authority, evidence, order, isolation, or proof is incomplete.

