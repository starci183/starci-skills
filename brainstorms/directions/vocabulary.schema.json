{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "starci-v3/brainstorms/directions/vocabulary.schema.json",
  "title": "Visual vocabulary inventory",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "root", "digest", "sources", "tokens"],
  "properties": {
    "schema": {"const": 1},
    "root": {"type": "string", "minLength": 1},
    "digest": {"type": "string", "pattern": "^[0-9a-f]{64}$", "description": "SHA-256 of the ordered source paths and exact token declarations."},
    "sources": {"type": "array", "items": {"type": "string", "minLength": 1}},
    "tokens": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "declarations"],
        "properties": {
          "name": {"type": "string", "pattern": "^--[A-Za-z_][A-Za-z0-9_-]*$"},
          "declarations": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["source", "value"],
              "properties": {
                "source": {"type": "string", "minLength": 1},
                "value": {"type": "string", "minLength": 1}
              }
            }
          }
        }
      }
    }
  }
}
