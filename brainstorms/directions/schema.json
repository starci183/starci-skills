{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "starci-v3/brainstorms/directions/schema.json",
  "title": "Visual direction batch",
  "description": "One transient round of visual-direction brainstorming. Schema 2 evidence-selects one recommendation; the owner approves it only through the layout hash that embeds it.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "envelope", "directions"],
  "properties": {
    "schema": {"enum": [1, 2]},
    "envelope": {"$ref": "#/$defs/envelope"},
    "directions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 4,
      "items": {"$ref": "#/$defs/direction"}
    },
    "recommended": {"$ref": "#/$defs/recommendation"},
    "refusal": {"$ref": "#/$defs/refusal"}
  },
  "$defs": {
    "envelope": {
      "type": "object",
      "additionalProperties": false,
      "required": ["round", "project", "surface", "prompt", "vocabularyAt"],
      "properties": {
        "round": {"type": "integer", "minimum": 1},
        "project": {"$ref": "#/$defs/slug"},
        "surface": {"$ref": "#/$defs/slug"},
        "prompt": {"type": "string", "minLength": 1},
        "vocabularyAt": {"$ref": "#/$defs/hash", "description": "The digest of the generated visual-token inventory these directions were checked against."}
      }
    },
    "direction": {
      "description": "A direction candidate. No timestamps, preview paths, or per-run ids; the recommended object is embedded unchanged in each layout candidate.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "vocabularyAt", "axes", "citesPrecedent", "personality", "roles", "rejects", "reason"],
      "properties": {
        "id": {"$ref": "#/$defs/slug"},
        "vocabularyAt": {"$ref": "#/$defs/hash", "description": "The digest of the frontend token state this direction was generated and validated against."},
        "axes": {"$ref": "#/$defs/axes"},
        "citesPrecedent": {
          "type": "string",
          "pattern": "^(none|[a-z0-9]+(-[a-z0-9]+)*(/[0-9]{4}-[0-9]{2}-[0-9]{2})?)$"
        },
        "personality": {
          "type": "array",
          "minItems": 3,
          "maxItems": 5,
          "items": {"$ref": "#/$defs/slug"}
        },
        "roles": {"$ref": "#/$defs/roles"},
        "lockedTokens": {
          "description": "Exact semantic token values imposed by a routed grammar visual contract. Omitted when the grammar leaves visual theme open.",
          "type": "object",
          "minProperties": 1,
          "propertyNames": {"$ref": "#/$defs/token"},
          "additionalProperties": {"type": "string", "minLength": 1}
        },
        "rejects": {
          "type": "array",
          "minItems": 1,
          "maxItems": 5,
          "items": {"type": "string", "minLength": 4}
        },
        "reason": {"type": "string", "minLength": 12}
      }
    },
    "axes": {
      "description": "The closed visual diversity set. Identical axis sets are one direction.",
      "type": "object",
      "additionalProperties": false,
      "required": ["contrast", "density", "shape", "depth", "motion"],
      "properties": {
        "contrast": {"enum": ["soft", "balanced", "strong"]},
        "density": {"enum": ["compact", "balanced", "spacious"]},
        "shape": {"enum": ["square", "soft", "rounded"]},
        "depth": {"enum": ["flat", "layered", "floating"]},
        "motion": {"enum": ["still", "measured", "expressive"]}
      }
    },
    "roles": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ground", "surface", "content", "mutedContent", "accent", "separator", "display", "body", "label", "radius", "elevation", "duration", "easing"],
      "properties": {
        "ground": {"$ref": "#/$defs/tokenDecision"},
        "surface": {"$ref": "#/$defs/tokenDecision"},
        "content": {"$ref": "#/$defs/tokenDecision"},
        "mutedContent": {"$ref": "#/$defs/tokenDecision"},
        "accent": {"$ref": "#/$defs/tokenDecision"},
        "separator": {"$ref": "#/$defs/tokenDecision"},
        "display": {"$ref": "#/$defs/tokenDecision"},
        "body": {"$ref": "#/$defs/tokenDecision"},
        "label": {"$ref": "#/$defs/tokenDecision"},
        "radius": {"$ref": "#/$defs/optionalTokenDecision"},
        "elevation": {"$ref": "#/$defs/optionalTokenDecision"},
        "duration": {"$ref": "#/$defs/optionalTokenDecision"},
        "easing": {"$ref": "#/$defs/optionalTokenDecision"}
      }
    },
    "tokenDecision": {
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["verdict", "token"],
          "properties": {
            "verdict": {"const": "reuse"},
            "token": {"$ref": "#/$defs/token"}
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["verdict", "token", "value", "why"],
          "properties": {
            "verdict": {"const": "new"},
            "token": {"$ref": "#/$defs/token"},
            "value": {"type": "string", "minLength": 1, "description": "The exact CSS value the preview and approval bind to."},
            "why": {"type": "string", "minLength": 12}
          }
        }
      ]
    },
    "optionalTokenDecision": {
      "oneOf": [
        {"$ref": "#/$defs/tokenDecision"},
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["verdict", "why"],
          "properties": {
            "verdict": {"const": "none"},
            "why": {"type": "string", "minLength": 12}
          }
        }
      ]
    },
    "recommendation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "reason"],
      "properties": {
        "id": {"$ref": "#/$defs/slug", "description": "The exact direction candidate selected as the evidence-backed default."},
        "reason": {"type": "string", "minLength": 12}
      }
    },
    "refusal": {
      "type": "object",
      "additionalProperties": false,
      "required": ["missing", "blocked"],
      "properties": {
        "missing": {"type": "string", "minLength": 12},
        "blocked": {"type": "array", "minItems": 1, "items": {"$ref": "#/$defs/slug"}}
      }
    },
    "token": {"type": "string", "pattern": "^--[A-Za-z_][A-Za-z0-9_-]*$"},
    "hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "slug": {"type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"}
  }
}
