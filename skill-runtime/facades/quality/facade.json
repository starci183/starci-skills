{
  "$schema": "../facade.schema.json",
  "schemaVersion": 1,
  "facadeId": "quality",
  "kind": "semantic-skill-facade",
  "executable": false,
  "description": "Separate read-only measurement and diagnosis from approved repair, debt repayment, and SonarQube enforcement setup.",
  "modes": [
    {
      "id": "stale-list",
      "intent": "Measure routed Source staleness and local check-only gates without repair.",
      "targetSkill": "starci-stale-list",
      "selectionFacts": ["inventory requested", "read-only machine measurement", "all routed roles or stale categories"],
      "excludes": ["repair requested", "trace of one skill flow"]
    },
    {
      "id": "diagnose-skill",
      "intent": "Trace one skill against the real machine without running or repairing it.",
      "targetSkill": "starci-diagnose",
      "selectionFacts": ["one skill flow", "where execution would stop", "environment versus skill defect"],
      "excludes": ["general stale inventory", "source repair"]
    },
    {
      "id": "repair",
      "intent": "Return a routed red or incompletely assured Source to green.",
      "targetSkill": "starci-repair",
      "selectionFacts": ["repair requested", "measured red or stale source", "delivery assurance gap"],
      "excludes": ["measurement-only request", "accepted debt scope with its own record"]
    },
    {
      "id": "debt",
      "intent": "Repay an owner-approved recorded debt scope and remove only proven green scopes.",
      "targetSkill": "starci-debt-repay",
      "selectionFacts": ["existing accepted debt record", "repayment requested", "scope exit criteria available"],
      "excludes": ["creating or accepting new debt", "unrecorded general repair"]
    }
  ],
  "permissions": {
    "selectionOnly": true,
    "writes": [],
    "externalMutations": false,
    "approvalTransfer": false
  },
  "dispatch": {
    "loadsTargetSkill": false,
    "startsTargetSeparately": true,
    "targetOwnsApprovalAndProof": true,
    "requiresOwnOrchestrationProfile": false
  },
  "output": {
    "fields": ["facadeId", "mode", "targetSkill", "selectionReason", "riskClass", "unresolvedFacts", "invocationEnvelope"]
  },
  "stops": [
    "request does not distinguish measurement from mutation",
    "project or role routing is unresolved",
    "repair or debt scope is undisclosed",
    "provider setup requires missing credential authority",
    "request requires a different capability owner"
  ]
}
