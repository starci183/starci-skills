{
  "schema": "starci/work-layout@2",
  "format": "Markdown with YAML 1.2 front matter for nodes; YAML 1.2 for workspace, resource, evidence and schema metadata",
  "node": "index.md",
  "identity": "stable metadata id; folder path is not identity",
  "shape": {
    "workspace": "workspace.yaml",
    "schemas": "_schema/work.schema.yaml",
    "resources": "_resources/<kind>/<id>/resource.yaml",
    "businessTree": "<business>/<work-item>/**/index.md",
    "nodeAssets": "<node>/assets/<file>",
    "evidence": "<node>/evidence/<id>/manifest.yaml",
    "localWorkflow": "_workflows/<run-id>/{goal,run}.yaml"
  },
  "rules": [
    "Each work folder has one index.md node; index is the node, not an extra directory listing.",
    "Split by independently owned questions and acceptance boundaries, not workflow stages. Names and depth stay flexible.",
    "The YAML front matter holds work/node@2 metadata; the Markdown body is the readable specification.",
    "Keep global parents thin. Business commonly separates independently checkable rules, actors, states, flows and acceptance. Architecture separates services, contracts, data, patterns, security, recovery, code and verification only where the selected scope needs independently investigable or acceptable children.",
    "Do not atomize by template. Create or retain a child when it owns a distinct question, decision, implementation boundary or acceptance result; otherwise keep the material together.",
    "Only leaves author state: uninvestigate, todo or done. todo is confirmed incomplete work and requires no investigation receipt. activity is auxiliary: idle, investigating, implementing or verifying. blockers[] explains impediments without becoming lifecycle state. Parents author no state or completion and derive progress from required descendants.",
    "When investigation occurs, investigation.contextDigest binds semantic context, excluding child rollup and auxiliary activity, blockers and history. A semantic parent change makes every affected descendant effectively uninvestigate while preserving stored done, completion, history and evidence.",
    "dependsOn uses stable node IDs. A semantic dependency change invalidates affected dependent nodes transitively; refs bind semantic inputs without implying execution order.",
    "Extend existing nodes and preserve stable IDs, history, assets and evidence. New required work prevents parent done; unrelated completed branches remain intact.",
    "Several workflows may target the same work nodes; no duplicated business tree per run. Local workflow tracking is ignored by validation/digests and Git.",
    "Declare semantic design/source files using node.assets with paths under own assets/. Their exact bytes contribute to semantic context; conservative hashing does not ignore presumed typo-only edits.",
    "Record output screenshots/videos in evidence assets with scope: node and paths under the primary owner assets/. Output evidence is not also declared as a semantic input, avoiding self-invalidating proof.",
    "Source code remains in bound repositories; code/index.md tracks requirements, exact source references and completion.",
    "Legacy index.yaml, node.yaml and node.md remain read compatibility. New and edited canonical nodes use index.md, with exactly one node file per folder; do not automatically migrate unrelated user Work."
  ],
  "example": {
    "vps/create/": [
      "index.md",
      "business/index.md",
      "business/rules/index.md",
      "business/actors/index.md",
      "business/states/index.md",
      "business/flows/index.md",
      "business/acceptance/index.md",
      "architecture/index.md",
      "architecture/services/index.md",
      "architecture/contracts/index.md",
      "architecture/data/index.md",
      "architecture/patterns/index.md",
      "architecture/security/index.md",
      "architecture/recovery/index.md",
      "architecture/code/index.md",
      "architecture/verification/index.md"
    ]
  }
}
