schema: starci/readme@3
version: 1.0.0-alpha.1
purpose: One skill routes complete verified jobs through bounded workflow matrices.
entry: SKILL.md
layout: INDEX.yaml
commands:
  test: npm test
  sites: npm run build:sites
runtime: Read the canonical YAML/MD/MJS sources directly — there is no .dist build; source code is tooling, not extra prompt documents.
status: alpha — draft material lives in .experiments/ until promoted; contracts freeze at 1.0.0.
legacy: Superseded trees move to legacy/; history remains in Git.
