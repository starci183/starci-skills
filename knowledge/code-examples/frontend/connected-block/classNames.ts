/** Unit-owned class strings for the example status block layout. */
export const exampleBlockBodyClassName = "flex min-w-0 flex-col gap-3 p-4"

/** Local action row; shared strings stay here, not inline in component.tsx. */
export const exampleBlockActionsClassName = "flex min-w-0 flex-wrap gap-2"
