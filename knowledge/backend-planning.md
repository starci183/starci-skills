# Backend boundary planning

| Field | Value |
| --- | --- |
| Knowledge ID | `be.boundary-planning` |
| Operators | `source-discovery, pattern-bind, boundary-plan, boundary-challenge, implementation` |
| Search tags | `backend, source discovery, pattern binding, exact files, approval boundary, implementation` |
| Dependencies | `business.authority-lifecycle, workspace.routing` |

## Record

Backend planning joins accepted behavior with one fresh business receipt and the exact routed backend source head. Default repository search locates the smallest relevant source slice; exact files, symbols, contracts, tests, and commit identity prove field names, owners, layering, exceptions, and conventions. Every reached pattern situation binds to those source references and evidence. No generated context index or shadow source authority is created.

The boundary plan names every create/modify/delete path, responsibility, dependency, branch, test, gate, exclusion, and proof before source writes. Challenge reopens all evidence. Implementation begins only from an approval bound to the canonical plan hash, a current source commit, and exact target hashes; only then may it open and mutate approved checkout files. Missing precedent is declared as a new family; source or boundary drift invalidates the approval and returns to planning.
