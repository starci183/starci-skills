# Backend implementation

| Field | Value |
| --- | --- |
| Knowledge ID | `be.implementation` |
| Operators | `implementation` |
| Search tags | `backend, implementation, approved boundary, source mutation, nestjs` |
| Dependencies | `be.plan-challenge` |

## Record

Implement only an explicitly approved backend revision and exact file boundary. Reverify route, source revision, approval hash, business head, and clean baseline immediately before the first product write.

## Source law

Mirror the observed sibling family and approved pattern bindings: module layering, transport, validation, authorization, data access, transaction boundary, concurrency, idempotency, event delivery, exception identity, naming, and type safety. New conventions require a returned planning discrepancy; they are never smuggled into implementation.

For persisted workflows, sessions, resumable jobs, carts, drafts, or other snapshots, enforce usability again when the snapshot is read. Creation-time validation is insufficient because entitlement, source membership, referenced records, and schema rules can drift afterward. A returned workflow must not contain an item the actor cannot complete. Reconcile stale members server-side, preserve stable order, remap positions and completed-state indexes atomically, and choose an explicit terminal state when no actionable member remains. Never make the client discover an unusable snapshot by receiving partial secret data, an empty required field, or an index that no longer names the returned collection.

Record every mutation with before/after hash and plan item. Do not widen roots, weaken gates, add suppression, skip tests, or repair a boundary decision locally. In-boundary code defects may loop back here; boundary drift must return to plan compilation.
