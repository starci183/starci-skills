SUSPECT examples/todo-app-backend/.starciwork/features/login/contract/identity-for-task/index.yaml: uncommitted withdraws "The session token travels as a custom "x-session-token" request header (implicit, never...", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/login/nfr/sign-in-timing/index.yaml: HEAD withdraws "The refusal for an unknown email and for a wrong password differ by less than 10 ms at ...", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/login/nfr/sign-in-timing/index.yaml: HEAD withdraws "node scripts/load/sign-in-timing.mjs with 1000 attempts of each shape", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/login/sds/session-store/index.yaml: uncommitted withdraws "POST /auth/sign-in with the pair, observable: http", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/login/sds/session-store/index.yaml: uncommitted withdraws "the session cookie and a redirect to the list, observable: http", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/plan/fr/downgrade/index.yaml: HEAD withdraws "No usage screen or downgrade action exists yet at app/plan for an owner to actually rea...", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/plan/fr/upgrade/index.yaml: HEAD withdraws "No usage screen or upgrade action exists yet at app/plan for an owner to actually reach...", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/plan/fr/usage/view/index.yaml: HEAD withdraws "No usage screen exists yet at app/plan/usage-screen.tsx for an owner to actually open; ...", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/plan/gap/sepay-not-reachable/index.yaml: HEAD withdraws "no module exists yet at src/plan/payments (gap.plan.unbuilt-module)", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/plan/gap/sepay-not-reachable/index.yaml: HEAD withdraws "no request has ever reached SePay's sandbox merchant account for real", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/plan/uat/upgrade-after-cap/index.yaml: HEAD withdraws "No implementation exists at app/plan/usage-screen.tsx or app/plan/upgrade-button.tsx ye...", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
SUSPECT examples/todo-app-backend/.starciwork/features/task/br/complete/once/index.yaml: HEAD withdraws "A complete task is never reopened.", which no older revision carries; the record's birth is not reachable within --limit 50, so the revision it names may exist outside what this run read [WITHDRAWS_NOT_VERBATIM]
