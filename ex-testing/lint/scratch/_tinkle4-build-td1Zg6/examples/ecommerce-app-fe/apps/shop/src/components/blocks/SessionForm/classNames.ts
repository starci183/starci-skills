/**
 * The session form's visual roles: the form region's frame, the stacked field column, and the
 * footer row holding the mode toggle and the way back to browsing.
 */
export const sessionFormClassNames = {
    frame: "w-full",
    fields: "mt-5 flex flex-col gap-4",
    foot: "mt-4 flex items-center gap-4",
} as const
