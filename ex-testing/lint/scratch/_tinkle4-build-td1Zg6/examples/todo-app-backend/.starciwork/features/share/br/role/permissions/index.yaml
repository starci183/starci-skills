schema: work/business-rule
id: br.share.role.permissions
title: A collaborator holds exactly one of two roles
state: done
statements:
  - A collaborator is a viewer or an editor, never both and never neither.
  - A viewer may read the task and its collaborators, and nothing else.
  - An editor may read the task and complete it, in addition to everything a viewer may do.
authorityRefs:
  - "owner-intent: Sharing extends the owner's own two capabilities on the task - read and complete -
    to somebody else; it does not extend delete, which stays owner-only regardless of role."
acceptance:
  - id: ac.share.role.permissions.viewer-read-only
    given: A task has an accepted viewer collaborator
    when: That collaborator attempts to complete the task
    then:
      - The attempt is refused.
      - The task is unchanged.
  - id: ac.share.role.permissions.editor-can-complete
    given: A task has an accepted editor collaborator
    when: That collaborator completes the task
    then:
      - The attempt succeeds.
      - The task reads complete for the owner too.
module: src/modules/bussiness/share
change:
  rev: 3
  kind: editorial
  at: 2026-09-19T15:59:33.966Z
  reason: impl.share.todo-app-backend.access is built (src/modules/bussiness/share, not the
    earlier-guessed src/share); completion-authority.spec.ts proves both acceptance criteria against a
    real TaskService wired to a real CompletionAuthorityRegistry with ShareCompletionAuthority
    registered, and scripts/live-proof-share.sh proves the same two outcomes live. gap.share.unbuilt-module's
    blockedBy is resolved.

    rev 3 (v11 ac-collapse) - acceptanceCriteria renamed to acceptance and now carries each criterion's id, given, when and then inline from the retired ac/ sub-records; refs to the old ac ids remap to br.share.role.permissions#<former-ac-id>. Encoding change only, no semantic change.
