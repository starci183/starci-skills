schema: work/business-rule
id: br.audit.retention
title: A log line is kept for 400 days
state: done
statements:
  - A line stays present and readable-or-tombstoned in storage for 400 days from its capture time.
  - Retention is a floor, not a promise of permanent readability; erasure may end a line's readability
    before the window closes, but the line itself is never dropped early.
authorityRefs:
  - "owner-intent: 400 days covers a year plus an audit cycle's slack; shorter windows have lost the
    line an investigation needed."
acceptance:
  - id: ac.audit.retention.available-through-window
    given: A line captured at time zero
    when: 400 days pass with no erasure request against its subject
    then:
      - The line is still present, still chained, and still readable.
      - Nothing in the storage layer evicts a line on a clock alone.
      - "The check that proves this: inject a fake clock into the retention sweep (never rely on real
        wall-clock delay in a test), advance it past 400 days, and assert the line count and chain
        validity are unchanged. A separate case must advance to well past the window with an active
        erasure against the line's subject and assert the line still exists but no longer decrypts -
        proving retention and erasure are independent axes, not one flag."
module: src/modules/bussiness/audit

change:
  rev: 3
  kind: editorial
  at: 2026-09-19T15:59:33.966Z
  reason: "gap.audit.unbuilt-module is closed: AuditLogService takes an injected Clock
    (src/modules/bussiness/audit/audit-log.service.ts) so audit-log.service.spec.ts can advance past 400
    days without a real wait and assert sweepRetention's count and chain validity are unchanged, and a
    second case proves retention and erasure are independent axes (the line still exists past the window
    but no longer decrypts once its subject is erased). module corrected from the drafted src/audit/log
    (never built) to the real Nest capability directory. rev 3 (v11 ac-collapse): acceptanceCriteria renamed to acceptance and now carries each criterion's id, given, when and then inline from the retired ac/ sub-records; refs to the old ac ids remap to br.audit.retention#<former-ac-id>. Encoding change only, no semantic change."

