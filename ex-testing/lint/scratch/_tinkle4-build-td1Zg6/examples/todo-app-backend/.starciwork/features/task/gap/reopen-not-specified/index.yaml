schema: work/gap
id: gap.task.reopen-not-specified
title: Nothing names the reopen code as proving the reversible statement
state: done
statement: br.task.complete.once rev 2 added "the owner may reopen a task they completed," and
  impl.task.todo-app-backend.reopen-task's code at src/features/reopen-task/** is real and passed its own
  unit tests, but no fr.task.reopen record existed to require it, so that implementation's own proves list
  stayed empty and nothing had ever run an acceptance check against the reversible statement itself.
  fr.task.reopen now names the reopen flow and composes br.task.complete.once against it, closing the
  naming absence this gap named. fr.task.reopen itself is not authored here as closedBy, even though it
  is the record that closed this gap, because it is todo, not done - a gap's closedBy names a completion
  that already happened, and fr.task.reopen existing is that completion; fr.task.reopen reaching done is
  a separate, still-open fact tracked on that record's own blockedBy, not on this gap.
verificationSource: authored-claim
because: This is an editorial fact about the tree's own bookkeeping - fr.task.reopen now exists and names
  the reopen code, which is independently verifiable by reading
  features/task/fr/reopen/index.yaml - not something a test run proves.
change: {rev: 1, kind: initial, at: 2026-09-18T10:00:00.000Z}
