schema: work/implementation
id: impl.recur.todo-app-frontend.schedule
title: The schedule screen
state: todo
repository: todo-app-frontend
# v7-3 (2026-09-19): route corrected to the directory that exists. Every page route in this app sits
# under the [lang] locale segment (locales en|vi); src/app/recur never existed. The rev-2 reason below
# claims the owners were already "corrected to the module-root directories that exist" - that held
# for src/components/recur and not for the route, and the comment naming "src/app/recur" as where the
# route now lives is stale for the same reason. Quoted because core/yaml.mjs rejects a bracketed
# plain scalar inside a flow mapping.
owners:
  - {role: route, path: "src/app/[lang]/recur"}
  - {role: block, path: src/components/recur}
proves: [ui.recur.schedule]
blockedBy:
  - {record: gap.recur.render-proof, because: "The real captures under ./assets fail the canonical render checks - entity-list-in-card on the active states (6 li items inside a starci-core-surface card) and palette-off-brand #6d97ff on no-rule/refused mobile. The defect lives in the page's markup and styles; until captures pass, the done claim is unproven."}
assets:
  - path: assets/capture.mjs
    role: capture-script
    sha256: 79c933efce7f979bbd7c9a020d0b0cd966a824d698bdb5637ef7bec008816f6c
    provenance: The replayable Playwright driver that produced every running-page capture below by
      driving the real served build through the page's own controls - no staged DOM.
  - path: assets/running-page-no-rule-desktop.png
    role: running-page
    sha256: 4cef2c2a062a7fde60cd9e98e2a0e24d62a9890309940bce74ac116e80497f8c
    provenance: Served build at desktop-1280, no-rule state; markup beside it at
      running-page-no-rule-desktop.html.
  - path: assets/running-page-refused-desktop.png
    role: running-page
    sha256: 9ced828182ec7d7acc8845d8776f5f9e9006309bf5be7b4fd6f1ac79757b042f
    provenance: Served build at desktop-1280, refused state - every-n-days submitted with n=0, all
      draft values retained; markup beside it at running-page-refused-desktop.html.
  - path: assets/running-page-active-desktop.png
    role: running-page
    sha256: 9adc5a32ef135faafc0fcdb3a38acb320139116a8f16395bce2cc3e2566d48f2
    provenance: Served build at desktop-1280, active state after a real makeRecurring +
      upcomingOccurrences against the live API; markup beside it at
      running-page-active-desktop.html.
  - path: assets/running-page-ended-desktop.png
    role: running-page
    sha256: 1d1739de3ab396613701d85e9980fc7e713b1a6540b600bf60bfb84305b451e0
    provenance: Served build at desktop-1280, ended state after a real endRecurrence against the
      live API; markup beside it at running-page-ended-desktop.html.
  - path: assets/running-page-no-rule-mobile.png
    role: running-page
    sha256: 832ac96cec54f0f51d8bb98587be75651b66213a62257dbfc79d92c7224bf19c
    provenance: Served build at mobile-390, no-rule state - compact header/navigation, fields
      stacked, no horizontal overflow; markup beside it at running-page-no-rule-mobile.html.
  - path: assets/running-page-refused-mobile.png
    role: running-page
    sha256: 319bb74c0213af2d81dd2beff2de88ee1753db2094e10c60db7f285620fc2499
    provenance: Served build at mobile-390, refused state; markup beside it at
      running-page-refused-mobile.html.
  - path: assets/running-page-active-mobile.png
    role: running-page
    sha256: c36fd8ab53821c97b278cccd243bb8d8740262c02c8badb8c4942b356758d553
    provenance: Served build at mobile-390, active state; markup beside it at
      running-page-active-mobile.html.
  - path: assets/running-page-ended-mobile.png
    role: running-page
    sha256: 95b6f307b36a5a1cebc717055733534dbec5cf309042713516b13733011e06fd
    provenance: Served build at mobile-390, ended state; markup beside it at
      running-page-ended-mobile.html.

# gap.recur.unbuilt-module closed at rev 2 (closedBy: impl.recur.todo-app-backend.engine, the backend
# half); the blockedBy edge that named it was removed at rev 1 because it was stale.
#
# rev 2 (this change) is the interface.implement run itself: the route and block named by owners
# above now exist under src/app/recur and src/components/recur in examples/todo-app-frontend, built
# on the installed @starci/grammar 0.4.13 exports the ui.recur.schedule map names (WorkspaceShell,
# PageContainer, SurfaceCard, SurfaceListCard, StaticStateRow, EmptyNotice, Input, Button, Text,
# TextAction, Heading). All four mapped states and both viewports are proven by the running-page
# captures listed under assets, each reached through the page's own controls against the live
# GraphQL API (makeRecurring/upcomingOccurrences/endRecurrence); evidence.yaml beside this file
# binds the check commands and the code digest. The owners entries were corrected from the record's
# originally intended file paths (app/recur/*.tsx) to the module-root directories the layout's
# owners vocabulary requires, matching how the code actually landed.
#
# Quality checks: lint (next build's starci-fe canon pass), typecheck (tsc --noEmit), tests (vitest,
# 39 green incl. 7 schedule-screen specs) and build (next build, /recur compiled) all pass and are
# the evidence assertions. Coverage has no configured runner or threshold in this example repo -
# recorded not-run, not pass. Sonar is likewise not-run: a SonarQube server responds at :9010 but no
# project binding, scanner runner or token reference exists in either example repository, so the
# required server quality gate cannot be proven from this checkout - reported, never fabricated.
#
# Integration note: the live RecurFrequency GraphQL enum travels under its registered keys
# (EveryWeekday/EveryNDays/MonthlyDay), not the data.recur.rule.frequency kebab-case domain values;
# the block maps domain<->wire at the transport call site. No business or schema change was needed.
change: {rev: 3, kind: editorial, at: 2026-09-19T21:30:00.000Z, reason: "v7-13: state rescinded done -> todo - the fresh captures v7-9 produced fail entity-list-in-card (active states) and palette-off-brand (#6d97ff on no-rule/refused mobile), so the done claim is unproven and the record is held by gap.recur.render-proof. Rev 2's build description stands as the record of what was built: built the /recur route and src/components/recur block on installed Grammar exports, captured all four mapped states at both viewports with real running-page captures under assets/, corrected owners to the module-root directories that exist, and bound checks + code digest in evidence.yaml. Coverage and Sonar recorded not-run for missing configured runners rather than falsely passed."}
