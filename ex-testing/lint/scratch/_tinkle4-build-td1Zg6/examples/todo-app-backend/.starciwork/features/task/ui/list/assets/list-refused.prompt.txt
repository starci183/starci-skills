Screen: The task list and its create form (task.list), route /tasks, actor: owner
State: refused - the owner submits an empty or whitespace-only title
Behavior to depict: a GrammarMessage(tone: danger) appears above the create form; nothing is created and the task list remains unchanged

Business grounding (accepted, do not invent beyond these):
  - fr.task.create
  - br.task.title.required

Brand (brand/index.yaml rev 2, read do not restate elsewhere):
  - primary: #2F6BFF
  - surface: #FFFFFF
  - typography: Inter, sans-serif
  - don't: Never place text on the primary colour below a 4.5:1 contrast ratio.

Relevant UI knowledge:
  - knowledge/ui/composition/collection.yaml (COLLECTION-1, COLLECTION-2)

Actual installed Grammar anatomy (@todo-app/grammar (examples/todo-app-frontend/packages/grammar/src/index.tsx and styles.css)) - depict exactly these components and styles, no invented ones:
  - GrammarTextField (task title)
  - GrammarButton (create task)
  - GrammarMessage (tone: danger, refused only)
  - grammar-row styling for plain task rows
  - GrammarButton {type, isDisabled?, onClick?, children: string}
  - GrammarTextField {id, label, kind?, value, onChange}
  - GrammarMessage {tone: 'danger', children: string | null} (role=alert on danger)

Layout note: The create form precedes a task-list page section with its own heading; the list is never wrapped in a card and each task is one plain row, per COLLECTION-1. Show an unchanged existing task list so the refused state covers both the form and collection anatomy.

Forbidden treatments: no card wrapper around a list of entities; no text on the primary colour below
4.5:1 contrast; no invented component outside the Grammar set named above; no fabricated brand mark
beyond brand/assets/**.

Viewport: desktop-1280, single column, product-shaped placeholder content only (no real person data).
