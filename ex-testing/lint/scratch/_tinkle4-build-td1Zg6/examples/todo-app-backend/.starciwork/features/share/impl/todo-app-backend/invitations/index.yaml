schema: work/implementation
id: impl.share.todo-app-backend.invitations
title: The invitation store and its lifecycle
state: done
repository: todo-app-backend
owners:
  - {role: module, path: src/modules/bussiness/share}
  - {role: module, path: src/features/todo/graphql/mutations/share}
  - {role: module, path: src/features/todo/graphql/queries/share}
proves: [br.share.invite.expiry, br.share.revoke.on-read, sds.share.invitation-lifecycle]
change:
  rev: 3
  kind: clarifying
  at: 2026-09-19T17:00:00.000Z
  reason: Built under nivo's real scaffold at src/modules/bussiness/share (invitation.service.ts,
    collaborator-cache.ts, ShareInvitationEntity + its migration), not the earlier-guessed src/share.
    Expiry and revocation are both enforced on read (never a sweep), matching
    sds.share.invitation-lifecycle's five transitions exactly. Proven by invitation.service.spec.ts,
    completion-authority.spec.ts and scripts/live-proof-share.sh against the running stack.
    gap.share.unbuilt-module's blockedBy is resolved.
    rev 3 (owners expansion) - owners now also claims the capability's shipped GraphQL transport,
    src/features/todo/graphql/mutations/share and src/features/todo/graphql/queries/share. The
    invite, accept-invitation, revoke-collaborator and collaborators doors each dispatch into this
    same module's commands/queries (InviteCommand, AcceptInvitationCommand, RevokeCollaboratorCommand,
    ListCollaboratorsQuery). Record metadata only; no code changed.
