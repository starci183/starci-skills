schema: work/data
id: data.notify.preference
title: Preference
state: done
fields:
  - {name: personId, type: text, required: true}
  - {name: channel, type: text, required: true, note: "e.g. email. One row per person per channel."}
  - {name: unsubscribed, type: boolean, required: true, default: false}
  - {name: digestWindowMinutes, type: integer, required: false, note: "Overrides the pipeline default from decision.notify.digest-window; null means use the default."}
invariants:
  - At most one row exists per (personId, channel) pair.
  - unsubscribed true suppresses every future notification on that channel until it is set back to false.
refs: [br.notify.unsubscribe.honored]
module: src/modules/platform/databases/postgresql/primary/entities/notify-preference.entity.ts
change: {rev: 2, kind: clarifying, at: 2026-09-18T12:00:00.000Z, reason: "NotifyPreferenceEntity exists exactly to this shape (notify_preferences table, primary key (person_id, channel)); gap.notify.unbuilt-module's blocker is resolved."}
verificationSource: authored-claim
because: "work/data's done is an authored claim by nature; NotifyPreferenceEntity's fields and invariants match this record exactly, and the table is exercised live by scripts/live-proof-notify.sh."
