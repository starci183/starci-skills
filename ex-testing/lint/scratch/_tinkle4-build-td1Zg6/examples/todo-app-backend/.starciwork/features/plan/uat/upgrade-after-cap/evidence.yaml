# Written by hand, same convention as uat.login.sign-in/uat.task.create/uat.share.invite-and-collaborate
# (scripts/example-evidence.mjs cannot emit the `run:` field a uat-flow needs). IMPORTANT DIFFERENCE:
# the named run settled fail, not pass - this file records which run exists and what it proved, it
# does NOT settle the record as done. The record stays inprogress on gap.plan.live-proof, which names
# the pending gateway leg (no simulated webhook per the owner directive) and the missing
# create-refusal feedback surface the run's `no` assertion documents.
schema: work/evidence
record: uat.plan.upgrade-after-cap
recordDigest: 0e1d936406c7fc8df16f48a457db56c69fb3b008296b9b3a5b642ba9697ad5f6
run: runs/20260919T143402Z-5c10a673
outcome: fail
assets:
  - path: runs/20260919T143402Z-5c10a673/videos/upgrade-after-cap.webm
    sha256: bca92d92b76f0f58783d59a715bcbbbfc32ad50078eeba86dadba8a20bb47a96
provenance:
  actor: playwright-harness
  tool: 'npx playwright test -c uat/playwright.config.ts uat.plan.upgrade-after-cap.spec.ts (reporter uat/lib/run-writer.ts)'
  environment: local
  capturedAt: '2026-09-19T14:34:02.000Z'
  stack: >-
    The shared stack already running on this machine (not booted by this lane): postgres:16 on
    127.0.0.1:5432 (database todo) in container compose-postgres-1, keycloak 26.0 on 8089 (realm todo,
    the two DEMO-ONLY committed users demo@todo.dev / demo2@todo.dev), redis on 6379, minio on 9000,
    this worktree's backend on 3001 and frontend on 3000 - the bytes proven are the manifest's
    frontendCommit/backendCommit (5c10a673 for both).
  authorization: >-
    UAT_LIVE_LOGIN_AUTHORIZED=true, UAT_DEMO_PASSWORD set to the DEMO-ONLY plaintext password already
    committed in .starcistacks/dev/infra/compose/realm-todo.json; UAT_PG_CONTAINER=compose-postgres-1
    for the read-only read-backs (sessions.person_id resolution, payment_intents/subscriptions counts)
    environment.todo-app.dev's allowedEffects authorize. No credential value is stored in this record
    or the run folder.
  observed_effect: >-
    One real sign-in wrote a session row (not counted as a run-owned resource, same caveat as the
    sibling flows). Twenty run-owned task rows were created through the app's own createTask mutation
    - the record's declared "seed 20 active task rows" effect, written through the real door - the
    21st create was refused on the wire as PLAN_CAP_EXCEEDED naming cap 20 and /plan/usage with
    nothing written, and the exercised upgradePlan call carried the gateway's real refusal (SePay
    create-intent HTTP 404, no credential configured) writing nothing either: payment_intents 0 -> 0,
    subscription status free -> free. All 20 seed rows were deleted by recorded id and verified absent
    by read-back; cleanup.json's triplets close out. The gateway leg and the create-refusal-feedback
    check are not-run/no - a fail outcome that proves the cap half only.
  recording: >-
    videos/upgrade-after-cap.webm, 161,964 bytes, WebM (EBML header verified), captured by Playwright
    with video: 'on'. Password fields render as the browser's own bullets and page.screenshot masks
    them whenever present; no credential is in the committed bytes.
