schema: work/gap
id: gap.share.frontend-unbuilt
title: No share invite screen exists under the Next.js layout yet
state: done
statement: gap.share.unbuilt-module closed on its backend half (src/modules/bussiness/share exists,
  proven by jest and scripts/live-proof-share.sh - lane be-share, backend-only per its brief). The
  frontend half it also named now exists too - the served route /tasks/<id>/share mounts
  features/pages/share -> components/blocks/share-invite, consuming the Grammar core package
  (WorkspaceShell, SurfaceCard, SurfaceListCard, Input, Button, Badge, Text, TextAction) over the
  real collaborators / invite / revokeCollaborator GraphQL operations, with running-page PNG+HTML
  captures for all five mapped states at desktop-1280 and mobile-390 in
  impl.share.todo-app-frontend.invite-screen's assets/. The impl record itself is held at todo by
  gap.share.render-proof - the captures are real but fail the canonical render checks - so the
  screen exists and serves while its done claim remains unproven; the absence this gap names stays
  resolved either way.
verificationSource: authored-claim
because: "the absence named - no share invite screen under the Next.js layout - is gone: the route
  serves and real captures exist. The closedBy edge to impl.share.todo-app-frontend.invite-screen
  was dropped at v7-13 because the impl's done claim was rescinded (gap.share.render-proof); this
  gap's own question - whether a screen exists at all - is answered by the served route and its
  captures, an authored-claim closure that does not depend on the impl record's state."
change:
  rev: 3
  kind: clarifying
  at: 2026-09-19T21:30:00.000Z
  reason: "impl.share.todo-app-frontend.invite-screen's done claim was rescinded at the v7-13 gate
    close (its real captures fail palette-off-brand and entity-list-in-card - gap.share.render-proof).
    The closedBy edge, which required that record to be done, is removed; the gap's closure now rests
    on the screen's existence itself, which the captures and the served route still prove. Rev 2's
    route correction (/tasks/<id>/share under the [lang] segment) stands."
