schema: work/gap
id: gap.share.unbuilt-module
title: No share module exists under the NestJS/Next.js layout yet
state: done
statement: The example is moving to a real NestJS backend and a Next.js frontend. src/share/access.service.ts
  and src/share/invitation.service.ts do not exist yet, and neither does the frontend invite screen, so no
  rule, flow, component or screen in this feature has anything to run acceptance criteria or a UAT walk
  against.
closedBy: [impl.share.todo-app-backend.access, impl.share.todo-app-backend.invitations]
verificationSource: authored-claim
because: "Both backend-half closers are done with real evidence: src/modules/bussiness/share (access.service.ts,
  completion-authority.ts, invitation.service.ts, collaborator-cache.ts, the CQRS commands/queries and
  the GraphQL mutations/queries under features/todo/graphql/{mutations,queries}/share/**) exists,
  builds, typechecks, and is proven by jest and scripts/live-proof-share.sh against the real running
  stack. impl.share.todo-app-frontend.invite-screen is a separate, still-unbuilt half this lane does not
  own (backend-only per its brief) - see the final report; the frontend invite screen remains its own
  todo record, unaffected by this gap closing."
change: {rev: 2, kind: clarifying, at: 2026-09-18T16:00:00.000Z}
