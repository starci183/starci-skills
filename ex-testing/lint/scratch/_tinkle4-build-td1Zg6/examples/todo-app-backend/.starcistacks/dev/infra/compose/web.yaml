services:
  web:
    # Same profile gate as api.yaml: no build context is declared, and this example runs the frontend on
    # the host via `npx next dev -p 3000`, per the dev README.
    image: todo-app/web
    profiles: [app]
    depends_on:
      - api
    ports:
      - "3000:3000"

