schema: work/catalog
id: ecommerce
description: Product catalog. One entry per feature; every record of a feature lives under its directory.
features:
  - id: checkout
    directory: features/checkout
    description: Cart, stock, payment and the confirmed order - the first thing that needs a second service.
  - id: identity
    directory: features/identity
    description: The person behind a session. Skeleton seeded beside checkout; its full design is a later pass.
