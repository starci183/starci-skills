import { TaskSharePage } from "@/components/pages/TaskSharePage"

/** The share route's thin shell; the page component reads the task the segment names. */
const Page = () => <TaskSharePage />

export default Page
