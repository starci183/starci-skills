import { LocaleRootPage } from "@/components/pages/LocaleRootPage"

/** The bare locale root's thin shell; the page component owns the redirect decision. */
const Page = () => <LocaleRootPage />

export default Page
