=== 1. is accb7b04 an ancestor of dfb7c74e (the REV_NOT_MONOTONIC ordering claim)? ===
  accb7b04 ancestor of dfb7c74e: NO
  dfb7c74e ancestor of accb7b04: NO
  accb7b04 2026-09-18 16:17:55 +0700 fix(login): name the Authorization header on the wire, refuse before any DB read
  dfb7c74e 2026-09-18 16:18:04 +0700 work(share): prove br/fr/sds/impl real, close gap.share.unbuilt-module and gap.task.collaborator-completi
  accb7b04 on first-parent mainline: NO
  dfb7c74e on first-parent mainline: NO
  2aba05ab on first-parent mainline: NO

=== 2. what actually moved in audit/fr/erasure/complete between HEAD and the worktree? ===
-    command: npm run test:e2e -- audit/erasure-complete
+    command: npm run test:e2e -- src/tests/e2e/audit/erasure-journey.e2e-spec.ts
-  rev: 2
-  kind: clarifying
-  at: 2026-09-18T12:00:00.000Z
-  reason: "fr.audit.erasure.request and gap.audit.unbuilt-module are both closed. This example has no
-    background worker; the GraphQL completeErasure mutation
-    (features/todo/graphql/mutations/audit/complete-erasure) exposes the worker's pickup directly for
-    the caller who requested it to trigger, dispatching CompleteErasureCommand to
-    AuditErasureService.execute (src/modules/bussiness/audit/audit-erasure.service.ts) - t-execute, the
-    unreadability confirmation, then t-complete, refusing with ERASURE_NOT_CONFIRMED rather than
-    completing if any line still decrypted (the exceptionFlow above), all proven in
-    audit-erasure.service.spec.ts and complete-erasure.handler.spec.ts, and exercised end to end by
-    scripts/live-proof-audit.sh. composes[].module corrected to the real capability directory.
-    requiresProof.uat stays unfulfilled: uat.audit.right-to-be-forgotten (gap.audit.live-proof) has not
-    run and is outside this backend lane's scope."
+  rev: 3
+  kind: editorial
+  at: 2026-09-19T00:00:00.000Z
+  reason: "requiresProof.e2e.command corrected to a real spec path: the jest positional pattern
+    'audit/erasure-complete' matched no file under src/tests/e2e; erasure-journey.e2e-spec.ts is the
+    spec that walks request -> verify -> complete -> anonymized against the assembled stack. Rev 2's
+    clarifying content stands; requiresProof.uat stays unfulfilled as before
+    (uat.audit.right-to-be-forgotten, gap.audit.live-proof)."

=== 3. what moved in audit/gap/emitted-events between aa186d5d and e9669075 (rev stayed 1)? ===
-state: todo
-statement: A task being created, completed or deleted, and a person signing in or out, are the events
-  fr.audit.log.append needs to seal a line for. login and task expose request/response contracts
-  (contract.login.identity-for-task, contract.task.list-for-dashboard) but, at this commit, neither
-  feature's implementation actually raises any of event.task.created, event.task.completed,
-  event.task.deleted, event.login.signed-in or event.login.signed-out - the event records specify the
-  shape each will have once emitted, not a signal that fires today.
+state: done
+statement: login and task now expose an emitted-event contract for audit to subscribe to -
+  impl.task.todo-app-backend.platform-events's typed in-process PlatformEventBus is done, and
+  create-task, complete-task, delete-task, sign-in and sign-out each publish event.task.created,
+  event.task.completed, event.task.deleted, event.login.signed-in and event.login.signed-out on it after
+  their respective writes succeed. fr.audit.log.append itself remains unbuilt - gap.audit.unbuilt-module
+  names that separately - so no subscriber exists yet to actually seal a line from these signals, but the
+  signals themselves fire today.
+closedBy: [impl.task.todo-app-backend.platform-events]
+verificationSource: authored-claim
+because: closedBy names an already-done implementation record, so no additional test run proves this
+  specific claim - that login and task raise these five events is independently verifiable by reading
+  create-task's, complete-task's, delete-task's, sign-in's and sign-out's use cases, which
+  impl.task.todo-app-backend.platform-events owns and proves.
