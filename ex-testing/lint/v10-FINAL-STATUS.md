# v10 FINAL STATUS — truth table over the example `.starciwork` trees

Generated by lane **v10-5**, 2026-09-19, final sweep window 22:57:34–22:58:29 (+0700),
after the last in-flight lane (v9-10) posted its marker at ~22:56.
Trees: `examples/todo-app-backend/.starciwork` (todo-be), `examples/ecommerce-app-be/.starciwork` (ec-be).
Every number below is a real run from `ex-testing/lint/scratch/v10-5-final2/*.txt`; nothing is
copied from an earlier report. Starting gate (per `briefs/v10/_common.md`): **9 refused / 23 suspect**.

## Preconditions

- Waited 22:06:26 → 22:42:30 for `done/v10-1..v10-4.done` (arrived 22:24:23, 22:27:54,
  22:29:30, 22:42:26). Poll log: `scratch/v10-5-wait.log`.
- Secondary markers: `v7-13`, `v7-15`, `v9-4`, `v9-7`, `v9-9` all landed during the wait;
  **v9-10** landed ~22:56 (this lane gave it a short bounded grace past the 30-min window so
  the final table is post-convergence); **v7-9 never landed** — v7-13 reports its render
  captures arrived and findings were absorbed.
- `_derived/` rebuilt both trees (`example-derive.mjs --write` + `example-critique.mjs --write`,
  final pass 22:57 after v9-10's last edit).
- `_derived/deep-baseline.json` seeded both trees via `check-work-deep.mjs --write-baseline`
  (final write 22:57) — DEP_STALE / NORM_UNRECORDED are now armed, not skipped.

## Check inventory — final counts

| check | command (from `.claude/`) | refused | suspect | info | exit |
| --- | --- | --- | --- | --- | --- |
| yaml parse | `node scripts/check-example-yaml.mjs` | 0 (906 files accepted) | – | – | 0 |
| **gate** | `node scripts/check-example-work.mjs` | **0** | 0 warned | 43 PAYLOAD_SKIPPED | 0 |
| derived freshness | `node scripts/check-example-derived.mjs` | 0 — "every derived index is fresh" | – | – | 0 |
| deep | `node scripts/check-work-deep.mjs` | 0 | 2 | 3 | 0 |
| replay dry-run | `node scripts/check-work-replay.mjs` | 3 | 123 | 268 | 1 |
| surfaces | `node scripts/check-work-surfaces.mjs` | 2 | 10 | 6 | 1 |
| history | `node scripts/check-work-history.mjs` | 42 | 53 | 5 | 1 |
| consistency | `node scripts/check-work-consistency.mjs` | 15 | 23 | 10 | 1 |
| artifacts | `node scripts/check-work-artifacts.mjs` | 38 | 58 | 6 | 1 |
| structure (layout) | `node cli/main.mjs work-layout check --work <tree>` | report-only: todo-be 517 findings, ec-be 203 | – | – | 0 |
| code-structure | `scripts/check-code-structure.mjs` | **N/A — never delivered** (v8-7 produced no script/report/marker) | | | |

Gate summary line (verbatim): `311 record(s), 2770 ref(s), 127 evidence file(s), 43 artifact
payload(s) skipped: every id matches its place, every ref resolves, and every new-concept rule
is satisfied`.

## Findings by code (refused tier)

| check | refused breakdown |
| --- | --- |
| replay | 3 × NOT_REPLAYABLE — all `s/x-session-token:/Authorization: Bearer/` sed probes on the retired auth header (notify/pipeline, share/access, share/invitations evidence) — the known dead live-proof scripts v7-13 flagged |
| surfaces | 2 × UI_ROUTE_GHOST — `ui.audit.privacy` claims `/privacy`, `ui.recur.schedule` claims `/tasks/:taskId/schedule`; no `page.tsx` serves either in todo-fe |
| history | 39 × CHANGE_UNRECORDED (normative content moved, `change.rev` flat — mostly `HEAD -> uncommitted`, i.e. fleet edits not yet committed) + 3 × WITHDRAWS_NOT_VERBATIM |
| consistency | 9 × AC_NAMING_ASYMMETRY (ec-be `br.checkout.cart`, `br.checkout.place-order`, `br.identity.account`, `br.identity.sign-in` carry inline acceptance entries while `acceptanceCriteria` is `[]`) + 4 × STATE_VOCABULARY_UNKNOWN (`inprogress`/`uninvestigate` vs schema enum `[todo, done]`; v9-10's `done` settlement cleared the fifth) |
| artifacts | 32 × ASSET_DIGEST (declared sha256 ≠ bytes on disk — recaptured captures/runs whose manifests weren't re-stamped) + 6 × ASSET_MISSING |

Suspect totals: replay 123 (94 ASSERTION_NO_CWD, 29 NOT_REPLAYABLE-under-guessed-cwd);
surfaces 10 (7 UI_ROUTE_UNDECLARED, 1 UI_ROUTE_GHOST, 1 SUBSCRIPTION_UNDECLARED,
1 SUBSCRIPTION_NOT_WIRED); history 53 (43 CHANGE_KIND_SUSPECT, 10 WITHDRAWS_NOT_VERBATIM);
consistency 23 (7 PROVES_ASYMMETRY, 5 CATALOG_TITLE_DRIFT, 4 PROOF_COVERAGE_GAP,
2 each GAP_CLOSED_BY_NOTHING / GAP_CLOSURE_STALE / PROOF_UNCLAIMED, 1 PROOF_COVERAGE_THIN);
artifacts 58 (34 INPUT_BYTES_MOVED, 16 EVIDENCE_ARTIFACT_GHOST, 6 RUN_MEDIA_FAKE,
2 PROMPT_INPUT_GHOST).

## Structure findings (report-only — `work-layout check`)

| tree | total | WORK_UNKNOWN_RECORD_ENTRY | WORK_RECORD_MISSING_INDEX | WORK_EVIDENCE_MISPLACED | WORK_UNKNOWN_FAMILY | WORK_ROOT_UNKNOWN_ENTRY |
| --- | --- | --- | --- | --- | --- | --- |
| todo-be | 517 | 431 | 44 | 25 | 15 | 2 |
| ec-be | 203 | 161 | 24 | 12 | 4 | 2 |

Dominant causes: `evidence.yaml` inside record dirs and `_derived`/`_resources` at root are
conventions `schemas/work-layout.yaml`'s executable checker does not enumerate; `contract`,
`gap`, `event` family dirs are likewise unknown to its FAMILIES list; `runs/<id>/screens` leaf
dirs are counted as records needing `index.yaml`. Report-only by design (exit always 0).

## Remaining suspects that are real product gaps (not noise)

- todo-be `/health` + `/webhooks` http routes: CAPABILITY_WITHOUT_SPEC — shipped infra doors no
  spec record names (v10-2 claimed the graphql surface; these two remain listed orphans).
- 32 dead replay assertions: mostly cwd-unstamped evidence (no `cwd`/`repository`/`commit`
  recorded — 110 + 17 evidence files carry no stamp, the EVIDENCE_CONTEXT_MISSING infos).

## Caveats — read before trusting any single number

- Derived freshness is **as of 22:57** — `_derived` is a pure function of the tree; any later
  record or product-source edit re-stales it (v7-8 §7's structural race). It is clean now.
- `history`'s 42 refused are dominated by `HEAD -> uncommitted` transitions — the fleet's edits
  are uncommitted working-tree changes; that class clears when work is committed with
  `change.rev` bumps, not by editing records.
- `artifacts` ASSET_DIGEST/ASSET_MISMATCH refusals are real byte-vs-declaration drift left by
  the capture/UAT lanes — listed, not patched (fleet rule: fixing them means re-running proofs,
  which is an evidence lane's job).
- `.dist` is stale: `bin/starci.mjs work-layout` dispatches to `.dist/cli/main.mjs` which
  predates the command; the same check was run from source `cli/main.mjs` (identical module).
- **The v11 format-compression fleet gated on this lane's marker** (`briefs/v11/_common.md`:
  wait for v10-1..v10-5 + v9-2..v9-10) and began rewriting both trees at ~22:59:34 the moment
  `v10-5.done` posted — collapsing `ac/` sub-records and re-binding evidence digests. Every
  number above is the **v10-convergence snapshot** (22:57–22:58), exactly the point this lane
  exists to capture; anything v11 changes afterward is that fleet's closer's (v11-5's) problem.
- Pre-convergence baseline for comparison: `scratch/v10-5-pre.log` (22:13, mid-flight).
