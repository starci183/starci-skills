v7-14 done: mechanical residual sweep, both trees, applied after all five phase-1 markers (v7-4 landed 19:47:27, inside the window; no timeout used). 76 line pairs across 28 files, four classes the gate never checks: repository field vs real sibling directory (17 todo BE impl records -> todo-app-backend, plus workspace.yaml's be entry, so dir == id == field == workspace == disk; repoRootFor resolution re-verified unchanged, still 0 OWNER_PATH_MISSING); short-sha revision -> full 40-hex (5, each resolved by git rev-parse to exactly one commit); machine-absolute paths in records (53 -> 0 across brand + the 7 todo ui records, every conversion pin-verified — post-write re-measure: 186 pins still match current bytes, 0 unresolvable); composes[].module / module / owners[].path / inputRefs / assets / command paths swept tree-wide with existence checked per value, and the classes phase-1 had already cleared confirmed independently (composes 8 -> 0, owners 6 -> 0, apps/ 52 -> 0, commands 8 -> 0 after fixing my own cross-repo base bug). Report-only, with measurements: the 16 ecom ui provenance paths (pins match NO file here, so a retarget asserts a false byte identity — recipe + per-entry target included), 61 records in 4 families whose schema file does not exist, 51 records failing their schema's required set of which 23 are mutually unsatisfiable (data/nfr require appliesTo that the gate refuses on those schemas), the ecommerce-app id collision between workspace.yaml and catalog, 3 measurement.how values naming a nonexistent k6 harness (task's has no gap record), and v6-3 #17's featureCatalog contradiction. Cost disclosed: 23 records I edited had a fresh recordDigest at write time and now refuse — nothing marked stale, no evidence file opened, no digest hand-recomputed; 4 were re-bound by v7-6 before I sealed. Gate 130 refused / 4 warned before my writes -> 142 two minutes after -> 111 / 0 warned sealed (exit ≠ 0, marker file, after other lanes' regeneration); tests 46 pass / 0 fail. Report: ex-testing/lint/v7-14-REPORT.md
