.github\workflows\todo-app-example.yml:13:      - 'scripts/check-example-*.mjs'
.github\workflows\todo-app-example.yml:14:      - 'checks/**'
.github\workflows\todo-app-example.yml:19:      - 'scripts/check-example-*.mjs'
.github\workflows\todo-app-example.yml:20:      - 'checks/**'
.github\workflows\todo-app-example.yml:52:        run: node scripts/check-example-yaml.mjs
.github\workflows\todo-app-example.yml:55:        run: node scripts/check-example-work.mjs
.github\workflows\todo-app-example.yml:134:        run: node scripts/check-scoped-lint.mjs --profile nest --root examples/todo-app-backend --architecture-config architecture.json --all
.github\workflows\todo-app-example.yml:207:          node scripts/check-scoped-lint.mjs --profile next --root examples/todo-app-frontend --architecture-config architecture.json --all | tee scoped-lint-frontend.json
docs\5-plus.md:604:`uat.verify` observes the identified running build in a browser. `checks/render.mjs` audits those implementation
docs\5-plus.md:765:| `checks/` | machine checks over bytes: `brand.mjs`, `render.mjs`, `proof.mjs` | `execution/brand-checks.mjs`, `execution/verify-proof.mjs` |
docs\5-plus.md:789:| `profiles/`, `execution/` | `model/`, `kernel/`, `hosts/`, `models/`, `checks/` |
docs\installation.md:79:| Agent asks for retired `INDEX.md` | Read actual host entry; run `node .claude/scripts/check-entry.mjs /absolute/host`. Do not recreate a retired entry. |
goal.md:256:    `scripts/check-scoped-lint.mjs`, `checks/code-patterns/*.mjs`, `checks/architecture/*.mjs`,
goal.md:257:    `checks/acceptance.mjs`, `checks/proof.mjs`. `code.refactor` must declare the regression
goal.md:270:  `.stacks` env/service file must conform to the declared topology (`checks/stacks.mjs`).
goal.md:272:  executable check under `checks/code-patterns/` (or an explicit `check: manual` marker with a
docs\grammar-guard-checks.md:3:`checks/code-patterns/grammar-guards.mjs` executes a bounded behavior probe against
docs\yaml-migration-map.md:16:Machine check: `node scripts/check-json-exceptions.mjs` (see end).
docs\yaml-migration-map.md:249:node scripts/check-json-exceptions.mjs
docs\yaml-migration-map.md:252:node scripts/check-json-exceptions.mjs --ignore-lockfiles
docs\workflow-store.md:27:  checks/<opId>.json        the checks a worker actually ran
docs\workflow-kernel.md:53:| `checks/render.mjs` | the canon rules, read from the bytes a drawing left | [Render checks](#render-checks) |
docs\workflow-kernel.md:959:`checks/render.mjs` remains a compatibility and implementation-capture audit. Design directions stay in the
docs\brand-checks.md:9:`checks/brand.mjs` re-derives each of those claims from the artefacts themselves and reports every
docs\brand-checks.md:139:`checks/render.mjs` reads the two artefacts a downstream browser capture leaves behind — the PNG and the
docs\verify-proof.md:9:`checks/proof.mjs` supplies that half. It never edits the operation's worktree, never commits, and
docs\v5-plan.md:122:  checks/<op>      what the kernel re-ran, and what it got
docs\next-error-state-check.md:3:`checks/code-patterns/next-errors.mjs` implements five adopted mechanical rules for a selected Next application. It reads source and installed declarations; it never executes application code or a provider.
docs\nest-test-discovery-check.md:3:`checks/code-patterns/nest-metadata.mjs` owns `NEST_JEST_ALIAS_PARITY` and
docs\nest-test-code-check.md:3:`checks/code-patterns/nest-tests.mjs` checks the source form of selected colocated
docs\examples\handoff\op-work-author.md:16:node scripts/check-example-work.mjs
docs\examples\handoff\op-work-author.md:48:- **Do not edit the runtime**: `ops/`, `schemas/`, `checks/`, `scripts/`, `kernel/`, `core/`,
docs\examples\handoff\op-work-author.md:62:`node scripts/check-example-yaml.mjs` (all accepted), `node scripts/check-example-work.mjs`
docs\examples\handoff\op-work-author.md:67:`node scripts/check-example-derived.mjs`. Commit in slices ending with
docs\nest-syntax-checks.md:3:The runtime-owned `checks/code-patterns/nest.mjs` module implements three adopted mechanical code rules. The code-pattern runner supplies the exact applicable relative files and rule IDs; the module uses TypeScript installed in that target repository and returns `starci/code-pattern-script@1`. It neither modifies source nor runs application code.
docs\examples\handoff\op-interface-draw.md:56:- **Do not edit the runtime**: `ops/`, `schemas/`, `checks/`, `scripts/`, `kernel/`, `core/`,
docs\examples\handoff\op-interface-draw.md:68:Run `node scripts/check-example-yaml.mjs` and `node scripts/check-example-work.mjs` from the
docs\nest-error-identity-check.md:3:`checks/code-patterns/nest-error-identity.mjs` is the read-only source adapter
docs\examples\handoff\op-e2e-verify.md:64:- **Do not edit the runtime**: `ops/`, `schemas/`, `checks/`, `scripts/` (the repository root's),
docs\examples\handoff\op-e2e-verify.md:75:`node scripts/check-example-yaml.mjs`, `node scripts/check-example-work.mjs` (paste the summary; it
docs\examples\handoff\op-e2e-verify.md:79:`node scripts/check-example-derived.mjs`. Commit in slices ending with
docs\nest-error-checks.md:3:`checks/code-patterns/nest-errors.mjs` is the read-only machine adapter for the
docs\source-staleness.md:3:`scripts/check-stales.mjs` is the read-only source/Work/stack freshness gate. It reads an existing canonical Work tree, runs the canonical validator, selects the declared inspection closure, and compares current `inputDigest`, completion evidence, `sourceRefs`, and `starci/source-identity@1` coverage with explicitly mapped Git repositories.
docs\source-staleness.md:6:node scripts/check-stales.mjs --work <work-root> \
docs\nest-boundary-check.md:3:The reusable check lives in `checks/code-patterns/nest-boundaries.mjs`. It uses
docs\examples\grit\second-example-gates.md:5:not edited - `scripts/`, `checks/`, `.github/` and `tests/` are shared-file territory that lane
docs\examples\grit\second-example-gates.md:22:`videos/`, one stale `blockedBy` rev), and `scripts/check-example-derived.mjs` refuses the todo
docs\examples\grit\second-example-gates.md:116:| `scripts/check-example-yaml.mjs` | exit 0 - "453 yaml file(s) under examples: all accepted" |
docs\examples\grit\second-example-gates.md:122:| `scripts/check-example-work.mjs` | **exit 1** - 4 refused, 15 warned; every line names `examples/todo-app-backend/...`, and no output line mentions `ecommerce-app-be` (grep-verified) |
docs\examples\grit\second-example-gates.md:123:| `scripts/check-example-derived.mjs` | **exit 1** - "2 work tree(s) checked: 2 refused"; both refusals name `todo-app-backend/_derived`, from the counting in the gate itself (`problems.length`, not tree count) |
docs\examples\grit\second-example-gates.md:124:| `scripts/check-scoped-lint.mjs --profile nest` | **exit 1** for both examples - see below |
docs\examples\grit\second-example-gates.md:125:| `scripts/check-json-exceptions.mjs` | **exit 1** before visiting any example - see #7 |
docs\examples\grit\second-example-gates.md:200:`node scripts/check-json-exceptions.mjs` exits 1 with
docs\examples\grit\ecommerce-fe.md:24:- **`architecture.json` had no schema to lean on.** Nothing in `checks/` reads `devPort` or
docs\examples\todo-app-handoff.md:52:| `node scripts/check-example-yaml.mjs` | 447 files, all accepted |
docs\examples\todo-app-handoff.md:53:| `node scripts/check-example-work.mjs` | **4 refused**, 15 warned |
docs\examples\todo-app-handoff.md:83:   rendered state and compare it against the direction; `checks/render.mjs` and `checks/brand.mjs`
docs\examples\todo-app-handoff.md:119:  kernel is out of scope. The tree's real gates are the `scripts/check-example-*.mjs` family.
docs\ledger-db.md:155:-- today's checks/<op>.json
docs\ledger-db.md:290:`reports/*` ? `reports`; `contracts/*` ? `contracts`; `checks/*` ? `checks`; `inbox/*` ? `inbox`; locks ? nothing
docs\examples\todo-app-grit.md:20:| 1 | `scripts/check-example-work.mjs` | Renaming the impl directories to `todo-app-backend`/`todo-app-frontend` left 13 records whose `id` still read `todo-app`; the YAML gate accepted all of them because each file parsed. | lied | fixed `ded62a70` | An id-vs-directory check belongs in the gate, not just in the YAML parser. |
docs\examples\todo-app-grit.md:21:| 2 | `scripts/check-example-yaml.mjs`, `core/yaml.mjs` | A lenient convenience YAML parser accepted a flow mapping like `{note: a, b}` by inventing two keys with null values and truncating at the comma; six records carried that shape and reported clean. | lied | fixed `e2048b55` | Example verification must run the runtime's own loader, never a second, more tolerant one. |
docs\examples\todo-app-grit.md:23:| 4 | `checks/architecture/contracts.mjs` (`callableMembers`) | Walked ambient `Error` statics (`prepareStackTrace`/`captureStackTrace` from `@types/node`) as if they were the repository's own public contract, so a domain exception extending `Error` could never be public. | blocked | fixed `8866b230` | The public-contract walk must restrict to members declared inside the checked repository. |
docs\examples\todo-app-grit.md:24:| 5 | `checks/architecture/contracts.mjs` (`contractTypeStatusFromType`) | TypeScript represents `boolean` as the union `true \| false` internally; the checker classified every public `boolean` param/return as an authored inline union and refused it. | blocked | fixed `330127e2` | A structural type check must special-case TS's own primitive unions before the union/intersection branch. |
docs\examples\todo-app-grit.md:25:| 6 | `checks/architecture/contracts.mjs` (`BE_MODULE_IMPORTS_APP`, `BE_APP_COMPOSITION_ONLY`, `BE_APP_BUSINESS_ROLE`) | `backend.apps` could not describe `src/main.ts` + `src/app.module.ts` beside `src/features`/`src/modules` in one application without either swamping every feature file as "app source" or leaving the composition files uncovered. | no-home | fixed `dcb30bba` | A single-application layout with composition-only entry files needs first-class support, not a workaround of declaring the whole `src` root as `apps`. |
docs\examples\todo-app-grit.md:28:| 9 | `checks/stacks.mjs` | Hardcodes `.stacks/application-stacks.yaml` (verified at lines 141–144, 192–194, 237, 340, 343 on this checkout) after the owner renamed the on-disk convention to `.starcistacks`; the real example ships `.starcistacks/...`, verified by the example's own gates. | drift | lane `stacks-rename` (no unique commits found: `git log --oneline main..ex-stacks-rename` is empty; the branch sits at `af3dfbbc`, same as three other untouched lanes) | `checks/stacks.mjs` needs the same rename `73ef0459`/`28590b5b` already gave the example tree. |
docs\examples\todo-app-grit.md:30:| 11 | `schemas/work-layout.yaml` (`shape.businessSRS`/`shape.architectureSDS`, lines 58 and 61; also lines 383, 389, 443–468) | Still declares `features/<feature>/business/srs/**/index.yaml` and `features/<feature>/architecture/sds/**/index.yaml`, but the actual example tree and `scripts/check-example-work.mjs`'s own `FAMILIES` set place every record in a flat family folder directly under `features/<feature>/` (`features/task/br/...`, `features/task/sds/...`, no `business/`/`architecture/` segment) — confirmed by listing `examples/todo-app-backend/.starciwork/features/login/`. `kernel/reconciliation.mjs` (lines 306, 309) independently still refers to the same nested `business/srs/business-rules/policy-decisions` path. `docs/examples/knowledge-update-delivery.md` uses the nested shape too, so three sources disagree with the one actually checked-in and gated example. | drift | fixed `11bf0d68` (lane `ex-trust`, merged to `main`) — `schemas/work-layout.yaml`'s `shape` block rewritten to one entry per flat family, matching the real tree; a new `FAMILIES_DRIFT` gate rule guards against the schema and the code silently redescribing two shapes again | The schema's `shape` block, the kernel's own path comments, and the doc example must all match the flat family layout the gate and the real tree actually use. |
docs\examples\todo-app-grit.md:37:| 18 | `scripts/check-example-work.mjs` | The gate never checks that a `proves` target is still `done`, nor that `owners[].path` exists on disk. Confirmed still true on this checkout: `impl.task.todo-app-backend.ownership` is `state: done`, `proves: [br.task.single-owner, sds.task.ownership-guard]`, and both targets are `state: todo` at `rev: 2` — the gate is silent. Independently, `docs/examples/todo-app-standard.md` (lane `ex-guide`, commit `9bf15616`, unmerged) documents the same finding: three `done` impl records (`impl.login.todo-app-backend.auth`, `impl.task.todo-app-backend.list`, `impl.task.todo-app-backend.ownership`) name six `owners[].path` values from a pre-refactor flat layout (`src/auth/sign-in.service.ts` etc.) that do not exist under `examples/todo-app-backend/src/`. | lied | fixed `83d920c2` (lane `ex-trust`, merged to `main`) — new rules `PROVES_TARGET_NOT_DONE`/`OWNER_PATH_MISSING` in `scripts/check-example-work.mjs`; both fire against this exact record on the real tree | The gate needed two more checks, and now has them: a `proves` edge's target must still be `done`, and every `owners[].path` must resolve to a real file. |
docs\examples\todo-app-grit.md:39:| 20 | `checks/code-patterns/next.mjs` (`FE_READONLY_PROPS_CONTRACT`) | Rejects any type reference outside the selected file set, including React's own `ReactNode`. Confirmed by `git log --all -S GrammarCard`: commit `d2936f56` deleted `GrammarCard` (whose only prop was `readonly children: ReactNode`) from the vendored Grammar package entirely, replacing every call site with a plain `<div>`. | blocked | owner decision | Whether the readonly-props contract should special-case well-known external ambient/library types (`ReactNode`, `ButtonHTMLAttributes`, ...) or the example must keep avoiding them is unresolved. |
docs\examples\todo-app-grit.md:50:| 31 | `scripts/example-derive.mjs`, `scripts/check-example-derived.mjs`, `examples/todo-app-backend/.starciwork/_derived/index.yaml` | `blockedBy` edges can form a ring with no root. Confirmed on `ex-derive` (`ea01e0ac`): `br.task.single-owner` ? `sds.task.ownership-guard`/`contract.share.completion-guard-for-task` ? back to `br.task.single-owner`, tagged `cyclic: true` 16 times in the generated `_derived/index.yaml`. `scripts/check-example-derived.mjs` (read in full) only gates derived-index freshness and forbids authored derived-vocabulary fields — it never turns a `cyclic: true` blocker into a refusal, so four records can block each other forever with no `gap` or open decision anywhere in the ring, and no frontier item can ever appear for them. | no-home / lied | fixed `756c95c8` (lane `ex-trust`, merged to `main`) — `BLOCKER_CYCLE` refuses a cycle outright; the commit's own message reports the real tree "had *three* cycles, not the one the brief named," and fixes the task/login ring this row and item 45 describe by adding `gap.task.collaborator-completion` and repointing three `blockedBy` entries. A second new rule, `BLOCKER_UNROOTED`, only warns (does not refuse) roughly a dozen other dead-end blockers across the tree that terminate at an ordinary record instead of a `gap`/decision — see item 49 | The schema now states that `blockedBy` is a DAG rooted in a `gap` or an open decision, and the gate refuses a cycle; whether every unrooted dead-end should also be refused, not merely warned, remains open. |
docs\examples\todo-app-grit.md:58:| 39 | `examples/todo-app-backend/.starciwork/features/notify/impl/todo-app-backend/pipeline/index.yaml`, `scripts/example-derive.mjs` (`EDGE_FIELD_NAMES`) | `impl.notify.todo-app-backend.pipeline` authors `willProve` where every other `impl` record authors `proves` (confirmed: `grep -rn willProve examples/` finds exactly this one file). Neither name is a classified edge kind: `EDGE_FIELD_NAMES` (line 37 of `scripts/example-derive.mjs`, confirmed on `ex-critique`) lists `refs, composes, dependsOn, subscribes, extends, appliesTo, conflictsWith, blockedBy, tension` — no `proves`, no `willProve`. The generated `_derived/index.yaml`'s `unclassifiedEdges` list (confirmed: `field: proves` at ~40 lines, `field: willProve` at 5 lines) shows both landing in the same catch-all bucket. Confirmed still true on the latest `main`: `EDGE_FIELD_NAMES` is unchanged and `impl.notify.todo-app-backend.pipeline` still authors `willProve`. This now has a concrete, checked consequence: item 18's new `PROVES_TARGET_NOT_DONE` rule (`scripts/check-example-work.mjs`, `if (Array.isArray(data.proves) && data.state === 'done')`) reads `data.proves` literally, so this one record's real proof edges are invisible to the very check the rest of the tree now enforces — a `willProve`-authored record could go `done` over a not-done target and the new rule would never see it. | drift | lane `trust` (classify `proves` as a real edge kind; refuse `willProve` as a second vocabulary for the same thing; still pending on the latest `main`) | `proves` needs to be a first-class edge kind (it is arguably the most safety-critical one, feeding items 18/31/36's "does the target still hold" checks); a schema-refused `willProve` closes the two-vocabulary drift. |
docs\examples\todo-app-grit.md:60:| 41 | `examples/todo-app-backend/.starciwork/**/evidence.yaml`, `schemas/work-layout.yaml` (line 289, `staleSince?`) | Of the 20 stale evidence files in the tree, 0 carry a `staleSince` naming the record whose breaking change staled them — confirmed on `ex-critique`'s "Stale evidence by cause" section: all 20 findings are `stale-evidence-bulk-edit`, and three of them (`br.task.complete.once`, `br.task.single-owner`, `sds.task.ownership-guard`) are called out explicitly as having "no `staleSince` at all... even where the record's own `staleReason` prose describes a real revision" — i.e. the one case (`br.task.single-owner` rev 2) this tree was deliberately built to demonstrate as a *designed* stale is structurally identical, by every field, to a plain bulk-edit digest mismatch. `staleSince` is optional in the schema (`staleSince?: {record: <id>, rev?: <n>}`) and `scripts/check-example-work.mjs` has no reference to `staleSince` at all — nothing requires or checks it. | drift | lane `nivo-shape` (regenerating login/task evidence) + lane `trust` (gate: `stale: true` requires `staleSince`) | A gate rule that `stale: true` requires a real `staleSince` would make a "why is this stale" claim checkable instead of prose-only, closing the same gap item 34 already flagged in the ops-count estimate: an honest-sounding narrative is not a substitute for a field the gate reads. |
docs\examples\todo-app-grit.md:61:| 42 | `examples/todo-app-frontend/architecture.json` (`frontend.grammar`), `checks/architecture/index.mjs` (`grammarContract`, lines ~135–146), `checks/architecture/frontend.mjs` (`ARCH_GRAMMAR_CONTRACT_INVALID`), `model/code-patterns.yaml` (`NEXT-GRAMMAR-CONTRACT`, line 706) | The frontend example vendors `@todo-app/grammar` at `examples/todo-app-frontend/packages/grammar` — confirmed 3 exported JSX primitives (`GrammarButton`, `GrammarTextField`, `GrammarMessage`; the coordinator's message said four, this session counts three on `main` at `b037b5fe` — `GrammarCard` was the fourth before it was removed in `d2936f56`, item 20) plus a separate `guards.mjs` rule-vocabulary module — not `@starci/grammar@0.4.13` with HeroUI, which the owner's real nivo-fe consumes. `architecture.json` declares `frontend.grammar.package: "@todo-app/grammar"`; `checks/architecture/index.mjs`'s `grammarContract` coverage is `{status: 'checked', package: config.frontend.grammar.package}` whenever *any* `frontend.grammar` object is declared — it never compares that package name against anything. `checks/architecture/frontend.mjs`'s `ARCH_GRAMMAR_CONTRACT_INVALID`/`ARCH_GRAMMAR_EXPORT_BYPASS` (read in full) only check that the manifest declares the package as a dependency, that the declared entry/style-entry paths actually export, that peers match the declared peer list, and that style imports stay inside the declared style entry — every check is "does the declared shape hold," none is "is the declared package the real one." `model/code-patterns.yaml`'s `NEXT-GRAMMAR-CONTRACT` obligation (`ruleIds: [ARCH_GRAMMAR_EXPORT_BYPASS, ARCH_GRAMMAR_CONTRACT_INVALID]`) inherits exactly that same blind spot, so it reports `covered`. | lied | fixed `12c59eb4` (on branch `ex-prompts-v2`, not `ex-grammar-real` — that branch still has zero unique commits; the real swap landed as a prerequisite of the prompts-v2 lane's own work instead: `@todo-app/grammar` deleted, `@starci/grammar@0.4.13` + `@heroui/react@^3.2.1` installed, `architecture.json`'s `frontend.grammar.package` now `@starci/grammar`, peers `[react, @heroui/react]`); owner decision on whether `frontend.grammar.package` should be pinned to a specific value or a `knowledge/` rule should name the house package remains open for the *general* case (this one example is fixed, but nothing stops the next example from declaring another fake package) | Either `architecture.json`'s schema should refuse (or `checks/architecture/index.mjs` should flag) a `frontend.grammar.package` that isn't the project's registered design-system package, or the runtime needs an explicit, named exception for "this example intentionally vendors a stub," because right now the two are indistinguishable to the checker. |
docs\examples\todo-app-grit.md:62:| 43 | `examples/todo-app-backend/.starciwork/features/login/impl/todo-app-frontend/sign-in/index.yaml`, `features/login/ui/sign-in/index.yaml`, `docs/kinds.md:158` | `impl.login.todo-app-frontend.sign-in` is `state: done` (`proves: [ui.login.sign-in]`, `revision: bfc3cdab...`), but `ui.login.sign-in` is `state: todo`, `blockedBy: [{record: gap.login.direction-not-drawn}]` — its own `ui.gaps` field says "No direction has been generated yet." `interface.draw` never ran for this screen, yet the frontend was implemented and marked done anyway. `docs/kinds.md` line 158 states the mandatory sequence exactly: `` `implementation/frontend` \| `implementation` + frontend side \| `frontend.implement` -> `uat.verify`, held until the feature's `ui` node is done ``, and `kernel/contract.mjs`'s `LANE` text repeats the same rule for the kernel's own live node@2 workflow model — but no rule in `scripts/check-example-work.mjs` or `scripts/check-example-derived.mjs` encodes it for this example's flat-family tree, so the inversion passed every static check. (`ex-critique`'s generic `done-proves-not-done:impl.login.todo-app-frontend.sign-in` finding, confirmed present, already catches this specific case as an instance of item 18's general missing check — this row names the more specific rule and the process cause.) No rule literally named `IMPL_BEFORE_DIRECTION` exists anywhere in this checkout (`git grep -i IMPL_BEFORE_DIRECTION main` is empty) — it is proposed, not landed. (Verified against `b037b5fe`; by the later `132e01ec` used to verify item 44 below, `interface.draw` actually had run for this screen — see item 45.) | lied / process | fixed `756c95c8` (lane `ex-trust`, merged to `main`) — the exact rule `IMPL_BEFORE_DIRECTION` landed, confirmed firing against this exact record ("impl.login.todo-app-frontend.sign-in is done while ui.login.sign-in it proves is todo... Left red; a ui-evidence lane settling ui.login.sign-in clears it"); the lead-process-error half is unchanged and still unverifiable against a commit | `docs/kinds.md`'s documented lane order needs a matching gate for the example's own tree: a `work/implementation` with a frontend `owners` role must refuse `state: done` while its `proves` target `work/ui-screen` is not itself `done` — the same fix item 18 already calls for, specialized to the one ordering `kinds.md` already promises. |
docs\examples\todo-app-grit.md:64:| 45 | `examples/todo-app-backend/.starciwork/features/{login,audit,notify,plan,recur,share}/ui/*/index.yaml` (`assets`/`gaps`/`blockedBy`), sibling `gap/direction-not-drawn/index.yaml` records | Found independently while verifying item 44 against the later commit `132e01ec`: all 6 of these `ui-screen` records (not `task`, which has no `direction-not-drawn` gap) now carry a real generated direction in `assets` — `generation: {tool: image_gen.imagegen, ...}`, `provenance: {actor: codex, profile: gpt-5.6-sol, capturedAt: "2026-09-18T05:58:...Z"}` (confirmed on all 6, e.g. `login/ui/sign-in/index.yaml`) — `interface.draw` demonstrably ran. Yet every one of the 6 still carries `gaps: ["No direction has been generated yet; drawing is deferred to gap.<feature>.direction-not-drawn."]` verbatim, and the matching `gap.<feature>.direction-not-drawn` record (confirmed on all 6) is still `state: todo` with a statement still reading "no `interface.draw` run has invoked built-in `image_gen.imagegen` against it yet." The gap record's own `change.at` (`2026-09-18T08:00:00.000Z`) postdates the image's `capturedAt` (`05:58:...Z`), so the contradiction is not a same-instant race: whoever last touched the gap did so after the direction already existed and still wrote that it didn't. | drift | fixed `4a855db4` ("examples: remove the seven v1 ui direction images (owner rejected)"), merged to `main` via `00dd31f6` — the fake-anatomy images were deleted rather than the prose corrected, which restores consistency (`gaps`/`blockedBy` were already accurate about nothing having been drawn once the false `assets` entries they contradicted were gone) but only by chance of the owner rejecting on content grounds (item 44/46), not because any gate caught the field-level contradiction itself — the missing check this row calls for is still pending, verified absent from `scripts/check-example-work.mjs` on the latest `main` | A `work/ui-screen` (or its `gap`) with a real `generation.tool` entry in `assets` should refuse authoring `gaps: ["No direction has been generated yet"]` or a `blockedBy` on the matching `direction-not-drawn` gap — a record's own two fields should not be allowed to disagree with each other, let alone silently; this time the underlying content defect got caught and fixed the contradiction as a side effect, but a future one might not be. |
docs\examples\todo-app-grit.md:65:| 46 | `examples/todo-app-backend/.starciwork/features/*/ui/*/index.yaml` and `assets/*.prompt.txt` (pre-fix, all 7), `scripts/check-example-work.mjs` (`ui.coverage.map`, line ~253) | Verified against branch `ex-prompts-v2` (`b54b1480`, which re-authored all 7) and the pre-fix records on `main`. Every pre-fix record asserted `GrammarMessage(tone: danger, role="alert")` as the refusal component; the real `@starci/grammar@0.4.13` common family has none — confirmed verbatim by both the fix commit's own message ("refusal copy is a plain Text with `live=\"assertive\"` (no danger tone, no role=alert, no Message/Alert renderer at all)") and the re-authored `sign-in-refused.prompt.txt` itself, which spells out `TextTone is default | muted | accent only` and that Common "sets no `role=\"alert\"`". `ui.task.list`'s pre-fix `refused` state is confirmed to have been the *wrong* representative direction: the fix's own record now says the real block "renders `refused` as ONLY an assertive-live Text line, hiding the create form and the row list entirely, so refused is this screen's LOWEST-anatomy state, not its densest" — `many-tasks` replaced it. Two narrower claims in the coordinator's message could not be confirmed as literally stated: pre-fix records named a `GrammarButton (toggle proxy: on/off submit)` for `notify/preferences` — an explicit, hedged stand-in for a toggle, not a bare claim that a toggle primitive exists — and this session found no pre-fix record naming a select/dropdown or a named "destructive" button variant at all (closest: `share/invite`'s plain, unstyled "revoke"/"delete" `GrammarButton`); these are noted as unverified rather than repeated. `scripts/check-example-work.mjs` line ~253 confirms the only existing `ui.coverage.map` check is that every listed state has a map entry — nothing compares a map entry's `components` strings against any resolved export list, real or fake. | lied | fixed `b54b1480`/`e8f83d07` on `ex-prompts-v2`, merged to `main` via `48f94491`; owner decision open on whether `ui.coverage.map[*].components` should be gate-checked against the resolved Grammar exports `ARCH_GRAMMAR_CONTRACT_INVALID` already computes | `checks/architecture/frontend.mjs` already resolves the real Grammar package's exports to validate `ARCH_GRAMMAR_*`; feeding that same resolved export list into a new check against every `ui.coverage.map[*].components` string would catch a fictitious component name mechanically, instead of relying on the owner eyeballing a screenshot. |
docs\examples\todo-app-grit.md:66:| 47 | `examples/todo-app-backend/src/modules/integrations/postgres/postgres.exception.ts` and every other `*.exception.ts`; `src/modules/domain/{session,task}/*.repository.ts`; `find examples/todo-app-backend/src -name '*.module-definition.ts'`; `checks/architecture/**`, `model/code-patterns.yaml` | The owner rejected the backend example on sight against three concrete defects, all confirmed on `main` at `f7ab26f0`: (1) exceptions are scattered per-domain/per-integration (`session.exception.ts`, `task.exception.ts`, `keycloak.exception.ts`, `postgres.exception.ts`, plus the shared `abstract.exception.ts`), each constructor passing a positional literal message string straight to `super(...)` (e.g. `super('The database could not be reached.', 'POSTGRES_UNAVAILABLE', {reason})`) — versus nivo-backend's (`D:/Repositories/nivo-backend`) `src/modules/shared/exceptions/errors/abstract.ts` plus one file per exception grouped by domain under `errors/<domain>/<name>.ts` (confirmed: `expert/exceptions/errors/classroom/course-not-found.ts` takes a single destructured `{courseRef, originalError}: CourseNotFoundExceptionMetadata` object, never a positional string). (2) `@InjectRepository(TaskEntity)`/`@InjectRepository(SessionEntity)` are injected directly into `@Injectable()` repository classes that also carry business logic (confirmed: `task.repository.ts`, `session.repository.ts`) — versus nivo's `@InjectPrimaryEntityManager()` over a named connection (confirmed: real usage in `chatbot-workspace-gateway.service.ts` injects `EntityManager` from `@modules/databases/postgresql/primary/primary.decorators`, not `@nestjs/typeorm`'s `InjectRepository` at all). (3) zero `*.module-definition.ts` files exist anywhere under `examples/todo-app-backend/src` (confirmed via `find`) — versus nivo-backend, where exactly 84 of 95 `.module.ts` files have a sibling `.module-definition.ts` (confirmed by counting both under `src/modules` specifically: `Get-ChildItem -Recurse -Filter '*.module-definition.ts'` = 84, `-Filter '*.module.ts'` = 95, matching "84 of 95" precisely). None of the three has a rule anywhere in this checkout: `checks/architecture/source-names.mjs` merely lists `module-definition` as one *accepted* naming suffix, never a *required* one; no checker or `model/code-patterns.yaml` obligation mentions `InjectRepository`, `InjectPrimaryEntityManager`, or a positional-vs-metadata-object exception constructor shape. The one checker structurally closest — `checks/code-patterns/nest-error-identity.mjs`'s `NEST_ERROR_DECLARATION_IDENTITY` ("a literal code and one static metadata object to super at the declared zero-based positions") — requires an authored `starci/nest-error-identity@1` contract the example's `package.json` never configures, so it never ran. `checks/architecture/**` and the `nest` code-pattern profile encode the runtime doc's (`docs/backend-source-pattern.md`, item 23) generic folder shape, not any house style, so a tree that violates nivo's actual conventions in all three ways still passes every rule that ran. | lied / no-home | lane `nivo-shape` is real and active (`ex-nivo-shape`, 5 commits not yet merged to `main`: `84bc5241` stops conflating the domain-first doc's folder names with the only valid shape, `750bd4b1` reshapes the backend to nivo's module/transport shape, `89ab1f2e` moves the frontend api module onto a GraphQL fetcher matching nivo-fe, plus a merge from `ex-trust` and a test-restoration commit); MASTER-PLAN (a house-style profile derived from nivo) not found anywhere in this checkout | A house-style profile (exception grouping + metadata-object constructors, `@InjectPrimaryEntityManager`-over-named-connection, one `module-definition.ts` per module) needs to be derived from nivo-backend and encoded as its own checked contract — `docs/backend-source-pattern.md`'s generic folder shape and `checks/architecture/**`'s structural rules are necessary but not sufficient; they cannot by themselves tell a domain-first tree that merely looks plausible from one that actually matches the house the owner ships. |
docs\examples\todo-app-grit.md:67:| 48 | `examples/todo-app-backend/.starciwork/features/login/gap/migration-not-applied/index.yaml`, `scripts/check-example-work.mjs` (`gap.closedBy` list + closer-done check, from `e5e29204`) | Found while verifying the fix for item 29: `gap.login.migration-not-applied` is `state: done`, `closedBy: gap.login.live-proof`, but `gap.login.live-proof` (confirmed) is still `state: todo`. Confirmed directly against the latest `main`, not only from the commit message. The fixing commit's own message names this exact refusal and explains why it was left red rather than silently resolved: "closing a gap by folding it into another gap is an editorial fact... not something a test run proves," and fixing the underlying judgment (whether the migration-not-applied absence is really gone) is "a substantive state judgment, not the mechanical field gap this lane is scoped to add." | lied | lane `trust` found and reported it (`e5e29204`, merged to `main`); the record itself is unfixed — confirmed still `done`/`closedBy: todo` on the latest `main` | `gap.login.migration-not-applied` needs to revert to `todo` (or `gap.login.live-proof` needs to actually close) before this tree's own new rule stops refusing it; this is exactly the kind of premature-done claim item 28 already found once, caught this time by a mechanical check instead of an agent's manual read. |
docs\examples\todo-app-grit.md:68:| 49 | `examples/todo-app-backend/.starciwork/features/{login,task}/**/evidence.yaml` (24 files), `scripts/check-example-work.mjs` (`PROOF_NOT_REPLAYABLE`, from `0d1ba1fe`) | The same `ex-trust` lane added `command`/`exit` tracking to every assertion and a `PROOF_NOT_REPLAYABLE` refusal for any assertion missing a `command` to replay. Running it against the real tree refuses all 24 pre-existing `evidence.yaml` files (42 assertions total, per the fixing commit's own count) because every one predates command-tracking and carries only a human `observation` string. Left deliberately red rather than mechanically backfilled: the commit's message reports that several existing observations quote a command (e.g. `npm run test:unit -- src/auth/session/accept`) that does not correspond to any real script in `examples/todo-app-backend/package.json` (which defines `test`, not `test:unit`) — authoring a `command` field that does not actually replay "would satisfy the gate while being a second, worse lie than the missing field it replaces." | lied | lane `trust` added the check and surfaced the backlog (`0d1ba1fe`, merged to `main`); the 24 files themselves are unfixed | Every one of the 24 `evidence.yaml` files needs its `command` re-derived from a real, currently-runnable script and re-verified via `scripts/example-verify.mjs`, not authored from the old observation text — this is the login/task lane's actual verification work, the same distinction item 28 drew between a real fix and a second false claim. |
docs\examples\todo-app-grit.md:69:| 50 | `checks/architecture/{registration,source-names}.mjs`, `examples/todo-app-backend` (reshaped, `ex-nivo-shape`) | Reproduced live in this session, not just read from a report. Extracted a clean scratch copy of `ex-nivo-shape` at `e180ccfe` (`git archive`, no shared worktree touched), installed dependencies, and ran `node cli/main.mjs architecture check examples/todo-app-backend --config architecture.json` twice. With the branch's own checker (including `84bc5241`'s relaxations): `ok: true`, 0 violations. After swapping in `main`'s pre-fix `checks/architecture/registration.mjs` and `checks/architecture/source-names.mjs` (extracted from `9a5ee8d0`, `main`'s current head) and rebuilding: `ok: false`, exactly 41 violations, broken down exactly as claimed - `BE_FEATURE_LAYOUT_INVALID` 28, `BE_MODULE_HANDLER_REGISTRATION` 7, `BE_SOURCE_NAME_INVALID` 6. Sampled messages confirm the causes: `BE_FEATURE_LAYOUT_INVALID` fires on GraphQL DTOs/action folders with no `application/` layer (e.g. `src/features/todo/graphql/mutations/session/sign-in/graphql-types/input.ts`); all 6 `BE_SOURCE_NAME_INVALID` violations are "GraphQL request arguments use the literal name request" (nivo's resolvers use `@Args("input")`, confirmed on the reshaped tree); `BE_MODULE_HANDLER_REGISTRATION` fires because CQRS handlers (`src/modules/bussiness/session/sign-in.handler.ts`, matching nivo's own `bussiness` spelling) are "not registered as a direct class-token provider in a resolved Nest module" - the static-registration pattern the claim names. One correction: the tree does have two `types/` folders (`modules/bussiness/{session,task}/types`), but none of the 6 `BE_SOURCE_NAME_INVALID` violations trace to them in this run - all 6 are the `@Args` literal-name cause alone. Also ran the branch's own spec suite after installing root dependencies: `tests/architecture.spec.mjs` 45/45 green; `tests/architecture*.spec.mjs`+`tests/nest*.spec.mjs` together 190/190 green; broader combinations adding `scoped-lint`/`next-code-patterns`/`code-patterns-cli` specs (225, 227, 259, 261 tests across four different combinations tried) were all green too, but none hit exactly "241" - the precise glob the lane's own report used could not be reproduced, though every combination tried was 100% green with zero failures, consistent with the claim. At the time this row was first written, `ex-nivo-shape` was not yet an ancestor of `main`, and "the owner's examples-only ruling" and any "MASTER-PLAN" document were unverifiable. Both are now confirmed while verifying item 51: `main` has since merged `ex-nivo-shape` (`a4e1f6f9`) and a named owner-ruling commit, `5b743c12` "integrate ex-nivo-shape examples-only: restore main's checker and docs (owner ruling)", which reverts `84bc5241`'s relaxation while keeping the reshaped example - see item 52 for what that leaves `main` in right now. No "MASTER-PLAN" document exists yet. | blocked / drift | fix exists at `84bc5241` (real, reproduced) and was deliberately reverted from `main` by a confirmed owner ruling (`5b743c12`) that kept the reshaped example without it - the combination now fails `main`'s own CI-equivalent check, per item 52 | The runtime's own checker, not just its example, encodes one doc's folder names as the only valid backend shape; `84bc5241`'s relaxation needs either to land on `main` for real (the ruling kept the half that needs it and reverted the half that satisfies it) or `examples/todo-app-backend` needs to revert its own reshape until the fix does land - one or the other, not neither, which is the state item 52 finds `main` in right now. |
docs\examples\todo-app-grit.md:71:| 52 | `main`'s current head (verified live, not from a report), `checks/architecture/registration.mjs`/`source-names.mjs`, commit `5b743c12` | Found independently while verifying item 51's branch ancestry: `main` has since merged both `ex-nivo-shape` (`a4e1f6f9`, the reshaped backend from items 47/50) and a follow-up, `5b743c12` "integrate ex-nivo-shape examples-only: restore main's checker and docs (owner ruling)" - a one-line-message commit that reverts `84bc5241`'s checker relaxation (confirmed: `checks/architecture/registration.mjs` and `source-names.mjs` on the current `main` are byte-identical to the pre-fix `9a5ee8d0` versions used in item 50's comparison) while keeping the reshaped example content (confirmed: 39 files under `examples/todo-app-backend/src` still reference the `bussiness` module path). Reproduced live in this session: extracted a fresh scratch copy of the actual current `main` head via `git archive` (no shared worktree touched), installed dependencies, and ran the exact command `.github/workflows/todo-app-example.yml` runs in CI (confirmed at line 96, which fails the job on `ok: false`): `node cli/main.mjs architecture check examples/todo-app-backend --config architecture.json` returns `ok: false`, the identical 41 violations from item 50 (`BE_FEATURE_LAYOUT_INVALID` 28, `BE_MODULE_HANDLER_REGISTRATION` 7, `BE_SOURCE_NAME_INVALID` 6). This is not a held-off fix or a pending lane; it is the present state of `main`'s own checked-in example against `main`'s own checker, right now. | lied / blocked | owner ruling confirmed (`5b743c12`) reverted the checker fix but kept the reshaped example that needs it, and this session could not find any record of the resulting break being tracked or accepted anywhere in this checkout | Either the checker fix needs to go back in (the owner ruling was presumably about scope/timing, not about the fix being wrong - it passed 45/190+ specs clean in item 50's own reproduction), or `examples/todo-app-backend` needs to revert its own layout until the fix lands, because right now neither side of that pairing is true and `main`'s own CI-equivalent command fails on `main`'s own head. |
docs\examples\todo-app-grit.md:74:| 55 | `checks/render.mjs`, `checks/brand.mjs`, `ops/interface.implement/operator.yaml` (capture/render-compare requirements, confirmed at multiple lines including 405-409, 428, 459-479), `scripts/check-example-work.mjs` (concept 11, line ~364) | Confirmed both `checks/render.mjs` (reads an implementation capture's PNG bytes plus its kept markup and checks them against brand tokens pixel-for-pixel) and `checks/brand.mjs` exist in the runtime, but neither is referenced anywhere in `scripts/*.mjs` or `.github/workflows/todo-app-example.yml` - never run against this example. No file anywhere under `examples/` is named or shaped like an implementation capture (`git ls-tree | grep -i capture` is empty). `ops/interface.implement/operator.yaml` already designs for exactly this: "capture every required screen/state/viewport... Open the captures and compare each mapped draw with its corresponding rendered evidence... Complete only when required implementation and rendered proof pass" - the op's own text requires it, but nothing mechanical enforces it on this tree. `scripts/check-example-work.mjs`'s concept 11 (the only capture-adjacent gate rule that exists) checks only that an *existing* implementation asset does not carry ImageGen provenance - it never requires that a capture exist at all before a frontend impl record reaches `done`. (As of the latest `main`, `impl.login.todo-app-frontend.sign-in` itself is honestly `state: todo`, not `done` - the specific record item 43 found is no longer wrongly claiming done, but the missing capture-and-compare gate this row describes is unrelated to that record and still absent generally.) | lied | open - MASTER-PLAN candidate | A `work/implementation` with a frontend `owners` role needs to require real captures (matching `checks/render.mjs`'s PNG-plus-markup shape) and a passing `render`/`brand` check against the direction and the brand record before `state: done` - the design in `ops/interface.implement/operator.yaml` already says this; it needs a mechanical gate, the same class of fix items 18/31/43/51 already established for other `proves`/`done` edges. |
docs\examples\todo-app-grit.md:75:| 56 | `examples/todo-app-backend/.gitignore` (the `.starciwork/**/runs/**/videos/*` rule), `scripts/check-example-work.mjs` (concept 12, lines ~399-402), `examples/todo-app-frontend/uat/lib/run-writer.ts` (lines ~74-79), `examples/todo-app-frontend/uat/playwright.config.ts` (line 41, `video: 'on'`), `.github/workflows/todo-app-example.yml` (lines ~517-531), `ops/uat.verify/operator.yaml` (`writes.evidence` line 157, `outputs.completion` line 390) | The gate refuses a `done` uat-flow whose settled run has no `runs/<runId>/videos/` with at least one file in it, and the harness really writes one (`playwright.config.ts` records video for every page, `run-writer.ts` copies the `video/webm` attachment to `videos/<flow>.webm` and hashes it into `manifest.yaml`) — and then `.gitignore` refused to track exactly that path, so on any fresh clone the settled run had screens and a manifest naming a video sha256 and no video. This worktree started with that contradiction firing: `features/login/uat/sign-in` was one of the four refusals, "run runs/20260918T090054Z-240aa95e has no videos/ with at least one playable recording", against a run folder whose four screens are tracked. The ignore's own justification is false twice over: (1) it says "a CI job uploads the actual .webm as a build artifact instead of committing it", but the upload step's five paths are all under `examples/todo-app-frontend/uat/**/runs/**`, while `UAT_WORK_ROOT` is unset in CI, so `paths.ts`'s `resolveWorkRoot()` falls back to `examples/todo-app-backend/.starciwork` and every run lands there instead — the glob can never match the file, and `if-no-files-found: ignore` makes that silence a success; (2) the same comment claims this matches "layoutPolicy in ops/uat.verify/operator.yaml", which says the opposite — that op's `writes.evidence` lists `E/videos/*` as evidence written into the node's own run folder, and its completion gate requires "Every executed flow has finalized playable genuine video". Owner ruling on this pass: the recording is acceptance evidence an outsourcer must be able to open, so it belongs in the repository; the rule is removed and a real run's `.webm` is committed with its sha256 named in the record that settles on it. | lied / no-home | fixed `efb582f7` (branch `starci183/ex-work-settle`, "feat(example): track UAT run videos and settle uat.login.sign-in on a real run") — `.gitignore` no longer ignores `runs/**/videos/*`, and the settled run now carries a 41,771-byte VP8 recording verified playable (52/52 frames decoded with zero errors by Playwright's own bundled ffmpeg) | The layout needs to state where a run's bytes live — tracked in the repository, or an external artifact store named by a field the gate can resolve — because right now `check-example-work.mjs` and `ops/uat.verify/operator.yaml` both assume "in the folder" while the ignore file and the CI comment both assume "not in the folder," and nothing checks that the two agree. A gate that requires a file on disk is unsatisfiable by construction on a clone that never had it. |
docs\examples\todo-app-grit.md:76:| 57 | `examples/todo-app-frontend/uat/lib/run-writer.ts` (lines 64, 110-124, 198-201), `scripts/check-example-work.mjs` (concept 12's `/outcome:\s*pass/i` test on `result.md`) | Reproduced live on this pass. A run whose Playwright test **threw partway through** was written into the tracked tree as `outcome: pass`. `onTestEnd` consults `result.status` for exactly one value — `skipped`, to return early — and never for `failed`; `outcome` is derived only from the assertions recorded *before* the throw (`observedCount > 0 && failedCount === 0 && observedCount === assertions.length`), and `assertions` is the list of checks the walk happened to reach. The run that produced it: the api was down, the flow's second step (`signed-in`) timed out on `page.waitForResponse`, one of six assertions had been recorded, and both `manifest.yaml` and `result.md` say pass. Concept 12 keys on the word in `result.md` and on the *existence* of screens and a video — all three of which an aborted run also has (it has a 147,341-byte recording of the failure) — so a `done` uat-flow could settle on a run where the browser never got past step 2. The folder is kept in the tree (`runs/20260918T173922Z-023dd8d9/`) rather than deleted, precisely because it is the only reproducible artifact of this hole; the record settles on the later, genuinely complete run instead. | lied | open — nothing in `tests/` or in any gate compares a run's `outcome` against the Playwright test's own status, and `result.md`'s `Outcome:` line is the sole thing concept 12 reads | `run-writer.ts` should fold `result.status !== 'passed'` into the outcome (an aborted walk is `inconclusive` at best, with the thrown step named in the manifest), and concept 12 should require every assertion the flow record declares to appear in the run's `manifest.yaml` with `observed: yes` — existence of a folder is not evidence that the flow was walked. |
docs\examples\todo-app-grit.md:78:| 59 | `scripts/check-example-work.mjs` (`walk` line 33, `collect` lines 62-69), `scripts/example-ownership.mjs` (`loadRecords`, lines ~105-118) | The gate's headline census counts the generated tree as authored input. `check-example-work.mjs`'s `walk` has no `_derived` exclusion, and `collect` pushes **any** string whose whole trimmed value is id-shaped as a ref — so every record id inside `_derived/index.yaml` and `_derived/critique.yaml` is read back as a reference and re-validated against the map built from... those same generated files. Measured on this pass by moving `_derived` out of the tree and re-running the same command: `258 record(s), 2127 ref(s), 76 evidence file(s)` with it, `258 record(s), 727 ref(s), 76 evidence file(s)` without it, `2127` again when restored. 1,400 of the 2,127 refs the gate reports as "every ref resolves" — two thirds of the number — are derived bytes pointing at themselves; the authored count is 727. The two walkers also disagree with each other: `example-ownership.mjs`'s `loadRecords` describes itself as "built the same way `scripts/check-example-work.mjs`'s own walk does" and then adds exactly the exclusion the gate lacks (`rel === '_derived' || rel.startsWith('_derived/') || rel.endsWith('/evidence.yaml')`), so the evidence writer and the gate do not share a definition of what a record is. Visible side effect on this pass: the same tree reported `2308`, `2312` and `2127` refs across three runs whose authored content differed by four files, because the derived index in the tree changed between them. | drift | open — nothing in `tests/` asserts the census excludes `_derived`; found and measured on this pass, reported rather than fixed because `scripts/` is out of bounds for an example-settlement op | Either share one walker (the `example-ownership.mjs` one, which already carries the exclusion) between the gate, the evidence writer and the derive step, or have `collect` skip any file under `_derived/`; a check whose own count is 66% self-referential cannot be quoted as evidence about the authored graph, which is precisely what item 34's "a count needs its command beside it" was about. |
docs\examples\todo-app-grit.md:94:catch: `checks/render.mjs`/`checks/brand.mjs` exist and can compare an implementation capture against brand
docs\examples\todo-app-grit.md:159:3. **Require captures and a passing render/brand check before a frontend implementation reaches `done`, and make `uat.verify` a required prover for every `fr`.** `ops/interface.implement/operator.yaml` already designs for exactly this; `checks/render.mjs`/`checks/brand.mjs` already exist to do the comparing; nothing wires them together or enforces it, and item 55's predecessor (items 44/46) needed a human watching a UAT video to catch what this gate would have caught mechanically - closes items 51 (required-prover half) and 55.
docs\examples\todo-app-standard.md:149:gate. `scripts/check-example-work.mjs` enforces exactly this: "state is done with no sibling evidence.yaml
docs\examples\todo-app-standard.md:181:flow spans — never a single `directory`/`files` pair, which `scripts/check-example-work.mjs` refuses outright
docs\examples\todo-app-standard.md:199:reconciliation, and the same one `scripts/check-example-work.mjs` recomputes to refuse a mismatched, non-stale
docs\examples\todo-app-standard.md:261:| `node scripts/check-example-yaml.mjs examples` | Any `.yaml`/`.yml` under `examples` the runtime's own strict loader cannot parse — a permissive parser accepting silently-invented keys is exactly the bug this gate exists to catch |
docs\examples\todo-app-standard.md:262:| `node scripts/check-example-work.mjs` | An id that doesn't match its directory place; a `ref`/`blockedBy`/`conflictsWith`/`appliesTo`/`subscribes`/`extends` pointing at an id nothing owns; a stale `blockedBy` (target already `done` at or past the cited rev); `blockedBy` authored as prose instead of `{record, rev?, because}`; a `work/gap` missing `state`/`statement`; a `work/policy-decision` with an invented `outcome` or a `chosen` not in `options`, or a `targetModule`; an unclosed `change.kind`; a `done` record with no evidence and no authored-claim declaration; `appliesTo` on the wrong schema or pointing nowhere; a malformed `work/event` or a `subscribes`/`extends` pointing at the wrong schema; `work/implementation` using legacy `directory`/`files` instead of `owners`; and an `evidence.yaml` whose `recordDigest` no longer matches its sibling's current bytes without `stale: true` |
docs\examples\todo-app-standard.md:311:  it, `scripts/check-example-work.mjs`'s own `FAMILIES` set (`br, ac, fr, nfr, data, journey, decision, sds,
docs\examples\todo-app-standard.md:332:  neither the staleness check nor `scripts/check-example-work.mjs` verifies that an `owners[].path` actually
docs\examples\todo-app-standard.md:338:  change. `scripts/check-example-work.mjs` checks that `blockedBy`/`conflictsWith`/`refs`/`appliesTo`/
docs\examples\todo-app-standard.md:362:  `scripts/check-example-work.mjs`) allows exactly one `closedBy`, so a gap spanning two implementation
cli\main.mjs:196:      const {scopedLintMain}=await import('../scripts/check-scoped-lint.mjs');
cli\main.mjs:200:      const {checkStalesMain}=await import('../scripts/check-stales.mjs');
cli\main.mjs:211:      const {checkArchitecture}=await import('../checks/architecture.mjs');
cli\main.mjs:225:      const {checkWorkChange}=await import('../checks/work-change.mjs');
cli\main.mjs:240:      const {checkApplicationStacks}=await import('../checks/stacks.mjs');
cli\main.mjs:254:      const {checkWorkLayout}=await import('../checks/work-layout.mjs');
cli\main.mjs:308:      const {runBrandChecks,formatBrandChecks}=await import('../checks/brand.mjs');
cli\main.mjs:330:      const {runRenderChecks,formatRenderChecks}=await import('../checks/render.mjs');
package.json:27:    "checks/",
ops\security.verify\operator.yaml:52:    path: scripts/check-scoped-lint.mjs + checks/code-patterns/*.mjs + checks/architecture/*.mjs
ops\review.verify\operator.yaml:1126:        path: scripts/check-stales.mjs + docs/source-staleness.md
ops\request.analyze\operator.yaml:57:        (goal.json, checks/, contracts/, inbox/, reports/) is never op output.
ops\release.deliver\operator.yaml:265:          en: Inspect current remote and local refs, branch policy and required checks/hooks. If integration
ops\goal.revise\operator.yaml:66:        state (checks/, contracts/, inbox/, reports/) is never op output - rev bumped by exactly
ops\common.yaml:221:        `goal.json` (starci/workflow-goal@1), `goal.md`, `checks/`, `contracts/`, `inbox/`,
ops\code.refactor\operator.yaml:63:    path: scripts/check-scoped-lint.mjs + checks/code-patterns/*.mjs + checks/architecture/*.mjs +
notes\srs-3.md:3:Branch `srs/3` owns `schemas/work-layout.yaml` and `checks/work-layout.mjs`. The per-record schemas
notes\srs-3.md:50:  leaf-only field, add it to `STATE_KEYS` in `checks/work-layout.mjs` rather than to a second list.
notes\s7.md:11:  `reports/contracts/checks/inbox` ? their tables, plus the retired journal's `jobs/leases/events/
notes\s4.md:60:kernel.lock, stop.flag, this op's checks/reports, and the ledger-wide supervisor.lock/runtime-loads/
notes\s3.md:227:`verifyAnchor` call). Roughly in order: ported kernel.mjs's own reports/contracts/checks/inbox/signals
notes\s1.md:51:                                                  // goal.md/goal.json, reports/<dispatch>.json, contracts/, checks/
SKILL.md:90:On resumption or an entry-path error, reread the actual host bootstrap before diagnosing an installation failure. `node scripts/check-entry.mjs <explicit-host> [claimed-entry]` distinguishes a missing runtime from a stale `INDEX.md` reference. Do not recreate retired entrypoints or require a push because the runtime is dirty. A conflicting explicit instruction needs a replacement instruction from its authority, not a runtime rewrite; do not override it merely because disk contents differ.
scripts\runtime-modules.txt:5:checks/code-patterns/nest.mjs
scripts\runtime-modules.txt:6:checks/code-patterns/nest-metadata.mjs
scripts\runtime-modules.txt:7:checks/code-patterns/nest-boundaries.mjs
scripts\runtime-modules.txt:8:checks/code-patterns/nest-tests.mjs
scripts\runtime-modules.txt:9:checks/code-patterns/nest-errors.mjs
scripts\runtime-modules.txt:10:checks/code-patterns/nest-error-identity.mjs
scripts\runtime-modules.txt:11:checks/code-patterns/grammar-guards.mjs
scripts\runtime-modules.txt:29:checks/brand.mjs
scripts\runtime-modules.txt:30:checks/architecture.mjs
scripts\runtime-modules.txt:31:checks/architecture/index.mjs
scripts\runtime-modules.txt:32:checks/architecture/config.mjs
scripts\runtime-modules.txt:33:checks/architecture/typescript.mjs
scripts\runtime-modules.txt:34:checks/architecture/backend.mjs
scripts\runtime-modules.txt:35:checks/architecture/contracts.mjs
scripts\runtime-modules.txt:36:checks/architecture/frontend.mjs
scripts\runtime-modules.txt:37:checks/architecture/next-data.mjs
scripts\runtime-modules.txt:38:checks/architecture/owners.mjs
scripts\runtime-modules.txt:39:checks/architecture/registration.mjs
scripts\runtime-modules.txt:40:checks/architecture/source-names.mjs
scripts\runtime-modules.txt:41:checks/stacks.mjs
scripts\runtime-modules.txt:42:checks/work-layout.mjs
scripts\runtime-modules.txt:43:checks/work-change.mjs
scripts\runtime-modules.txt:44:checks/render.mjs
scripts\runtime-modules.txt:100:checks/acceptance.mjs
scripts\runtime-modules.txt:111:checks/proof.mjs
scripts\runtime-modules.txt:129:scripts/check-scoped-lint.mjs
scripts\runtime-modules.txt:130:scripts/check-stales.mjs
scripts\runtime-modules.txt:156:checks/code-patterns/next.mjs
scripts\runtime-modules.txt:157:checks/code-patterns/next-errors.mjs
scripts\example-verify.mjs:21: * (the same condition scripts/check-example-work.mjs's gate refuses as PROOF_NOT_REPLAYABLE - this script
scripts\example-render-proof.mjs:5:import {decodePng, checkPalette, checkEntityListInCard, checkMascotSlot, cardClassesOf} from '../checks/render.mjs';
scripts\example-render-proof.mjs:9: * Grit item 55 (docs/examples/todo-app-grit.md): checks/render.mjs and checks/brand.mjs are real - a
scripts\example-render-proof.mjs:18: * The check functions are imported from checks/render.mjs itself and composed here rather than calling
scripts\example-render-proof.mjs:78: * Deliberately not checks/brand.mjs's readBrandRecord: that reader demands schema `work/node@2`, while the
scripts\example-render-proof.mjs:144:    problems.push('state is done but keeps no running-page capture under its assets/ - a frontend implementation is proven by a browser PNG plus the markup it rendered, the shape checks/render.mjs reads, and a design/direction image is not an implementation capture [RENDER_CAPTURE_MISSING]');
scripts\example-ownership.mjs:9: * once here rather than three times with three chances to disagree. Used by scripts/check-example-work.mjs,
scripts\example-ownership.mjs:239: * scripts/check-example-work.mjs's own walk does, for callers (scripts/example-evidence.mjs) that need
scripts\example-evidence.mjs:35: * change stale a proof without anyone touching the record: scripts/check-example-work.mjs refuses an
scripts\example-evidence.mjs:95: * scripts/check-example-work.mjs already made for the same reason.
scripts\example-derive.mjs:16: * exists to make impossible (enforced separately by scripts/check-example-derived.mjs).
scripts\example-derive.mjs:21: * This tree's simpler schema (scripts/check-example-work.mjs's FAMILIES) has no `invalid`/`uninvestigate`
scripts\example-derive.mjs:28:/** Every id-shaped string the layout's rules make a real edge, exactly as scripts/check-example-work.mjs's
scripts\example-derive.mjs:310:  // one-closer case and is normalised the same way here as scripts/check-example-work.mjs's gate treats it.
scripts\example-derive.mjs:394: * The one call a caller (this file's CLI, or scripts/check-example-derived.mjs) needs: compute the tree,
scripts\example-critique.mjs:16: * of freshness gate (scripts/check-example-derived.mjs, extended below).
scripts\check-work-history.mjs:10:import {classifyChange} from '../checks/work-change.mjs';
scripts\check-work-history.mjs:16: * disk holds exactly one side of it. checks/work-change.mjs already owns the semantics of the transition and
scripts\check-work-history.mjs:23: * been committed. This script is that bridge - it hands checks/work-change.mjs the baseline it has been asking
scripts\check-work-history.mjs:38: * It reports and never repairs - the precedent check-stales.mjs and checks/work-change.mjs both set.
scripts\check-work-history.mjs:57: * changes either. checks/work-change.mjs's exported `normative()` is deliberately NOT used for this comparison
scripts\check-work-history.mjs:454:    // classifyChange over empty criteria maps is checks/work-change.mjs's own verdict for a record read on its
scripts\check-work-history.mjs:461:      + `declared kind: editorial while ${describeDelta(added, removed)} - checks/work-change.mjs reads the `
scripts\check-work-artifacts.mjs:372:  // say how many candidates were opened and found whole, the way scripts/check-example-work.mjs's summary does.
scripts\check-stales.mjs:6:const HELP=`Usage: node scripts/check-stales.mjs --work <canonical-work-root> --repo <id>=<git-root> [--repo ...] [--target <work-node-id> ...]
scripts\check-scoped-lint.mjs:8:import {checkArchitecture} from '../checks/architecture/index.mjs';
scripts\check-scoped-lint.mjs:9:import {loadTargetTypeScript} from '../checks/architecture/typescript.mjs';
scripts\check-scoped-lint.mjs:10:import {discoverNestMetadataInputs} from '../checks/code-patterns/nest-metadata.mjs';
scripts\check-scoped-lint.mjs:160:  let loaded;try{loaded=await import(new URL(`../checks/code-patterns/${moduleName}.mjs`,import.meta.url));}catch(error){throw Object.assign(Error(`Static code-pattern checker ${moduleName} is unavailable: ${error.message??error}`),{code:'SCRIPT_CHECKER_UNAVAILABLE'});}
modules\schemas\relationships.yaml:122:     source: "scripts/check-scoped-lint.mjs:12-13"}
modules\schemas\relationships.yaml:124:     note: "check-scoped-lint aggregates per-script results emitted as code-pattern-script@1 by checks/code-patterns/*.mjs — an unschematized sibling id",
modules\schemas\relationships.yaml:125:     source: "checks/code-patterns/*.mjs result.schema; check-scoped-lint.mjs:176"}
scripts\check-json-exceptions.mjs:7: *   node scripts/check-json-exceptions.mjs
scripts\check-json-exceptions.mjs:8: *   node scripts/check-json-exceptions.mjs --ignore-lockfiles
scripts\check-example-work.mjs:508:  // checks/render.mjs + checks/brand.mjs are the canon for "the running page looks like the brand and the
modules\schemas\index.yaml:65:      - checks/work-layout.mjs (the report-only `work-layout check` behind bin/starci.mjs)
modules\schemas\index.yaml:66:      - scripts/check-example-work.mjs (reads shape.families; FAMILIES_DRIFT if gate and schema disagree)
modules\schemas\index.yaml:67:      - scripts/check-work-consistency.mjs, ops/validate.mjs, scripts/work-example.mjs
modules\schemas\index.yaml:107:    usedBy: [scripts/check-work-artifacts.mjs (record walk), scripts/check-work-consistency.mjs]
modules\schemas\index.yaml:122:    usedBy: [scripts/check-work-artifacts.mjs (parent records may not author state)]
modules\schemas\index.yaml:137:    usedBy: [scripts/check-work-artifacts.mjs]
modules\schemas\index.yaml:168:      - scripts/check-work-consistency.mjs (reads its $defs.state enum to refuse invented states; additionalProperties:false, NO requiresProof)
modules\schemas\index.yaml:183:    usedBy: [scripts/check-work-consistency.mjs (rule lists criterion id tails)]
modules\schemas\index.yaml:198:    usedBy: [scripts/check-work-consistency.mjs (PROOF_DEMAND_SCHEMAS member — done requires declared proof)]
modules\schemas\index.yaml:213:    usedBy: [scripts/check-work-consistency.mjs (PROOF_DEMAND_SCHEMAS)]
modules\schemas\index.yaml:228:    usedBy: [scripts/check-work-consistency.mjs (PROOF_DEMAND_SCHEMAS)]
modules\schemas\index.yaml:243:    usedBy: [scripts/check-work-consistency.mjs (two done records may not leave the tension open)]
modules\schemas\index.yaml:258:    usedBy: [scripts/check-work-consistency.mjs]
modules\schemas\index.yaml:274:      - scripts/check-work-consistency.mjs (PROOF_DEMAND_SCHEMAS; owners/module-root rule)
modules\schemas\index.yaml:305:    usedBy: [scripts/check-work-artifacts.mjs, check-work-consistency.mjs (proves-edge records exempt from requiresProof — proof is outbound)]
modules\schemas\index.yaml:320:    usedBy: [scripts/check-work-artifacts.mjs (accounts cross-check vs work/disposable-accounts)]
modules\schemas\index.yaml:335:    usedBy: [scripts/check-work-artifacts.mjs (uat-flow.accounts must resolve to this fixture)]
modules\schemas\index.yaml:504:    usedBy: [kernel/source-staleness.mjs (emits it), scripts/check-stales.mjs (exit 0 clean / 1 findings / 2,3 errors), kernel/contract.mjs + kernel/audit.mjs (interpret exit codes — nonzero never authorizes writes)]
modules\schemas\index.yaml:610:    usedBy: [docs/application-stacks.md (checker shares its read-only findings), checks/stacks.mjs]
modules\schemas\index.yaml:625:    usedBy: [checks/stacks.mjs (loads BOTH compiled .dist schema and source yaml), docs/application-stacks.md]
modules\schemas\index.yaml:708:    usedBy: [scripts/check-scoped-lint.mjs (PROFILE_SCHEMA const; status enum implemented|missing|conflict)]
modules\schemas\index.yaml:723:    usedBy: [scripts/check-scoped-lint.mjs (CODE_PATTERN_REPORT const); checker scripts emit the sibling starci/code-pattern-script@1 per-file results it aggregates]
modules\schemas\index.yaml:776:    usedBy: [scripts/check-json-exceptions.mjs (inventory-only; offenders listed), scripts/compile-declarative.mjs (excludeRelative)]
modules\schemas\index.yaml:790:  - "starci/code-pattern-script@1 is emitted by every checks/code-patterns/*.mjs script and consumed by check-scoped-lint.mjs, but has no schema file — only the aggregate starci/code-pattern-check@1 is schematized."
scripts\check-example-derived.mjs:10: * A separate gate rather than a line inside scripts/check-example-work.mjs: another lane owns that file's
scripts\check-entry.mjs:21: if(!host){process.stderr.write('Usage: node scripts/check-entry.mjs <explicit-host> [claimed-entry]\n');process.exitCode=1;}
upgrades\5.0.0-plus.md:38:`checks/` (the machine checks that read bytes). `.dist` mirrors that same shape, so a path you may have read
upgrades\1.0.0.md:553:and exclude checks/report control transport from reported product files. Adopt these corrections
UPDATE.yaml:76:        the model functions, `checks/` for the machine checks that read bytes. `execution/` itself
hosts\orca\launch.mjs:767:    checks/) from the workflow's ledger rows, for a reader or a tool that still wants files on disk.
MASTER.md:16:| Validator debt | `starci validate examples/todo-app-backend/.starciwork` = 2 916 errors (UNKNOWN_FIELD 1 226, SCHEMA_VALUE 705, SCHEMA 217, NODE_KIND 216, REQUIRED 216, EMPTY_SPEC 201); the example gate `scripts/check-example-work.mjs` passes the same tree (217 records, 723 refs) |
MASTER.md:83:| B3 core validator | `core/` accepts `work/*` records by their own schema; `starci/work-layout@3` shapes stay readable, never authoring authority | `starci validate examples/todo-app-backend/.starciwork` = 0 errors; `scripts/check-example-work.mjs` rules move into `core/` (one validator, the example spec becomes a fixture test) |
MASTER.md:95:| C3 knowledge/checks/stacks inputs | each kind's declared `knowledge`, `checks`, `stacks` lists resolve to real files; pattern-coverage parity: every `knowledge/patterns/{fe,be}/*` has a `checks/code-patterns/*` or a `check: manual` with a reason |
MASTER.md:169:- `scripts/check-example-work.mjs` is not a second validator; its rules move into `core/` and
modules\ops\ops\code.refactor.yaml:67:    path: scripts/check-scoped-lint.mjs + checks/code-patterns/*.mjs + checks/architecture/*.mjs +
kernel\audit.mjs:3:import {CODE_PATTERN_REPORT,verifyCodePatternReportDigest} from '../scripts/check-scoped-lint.mjs';
kernel\candidate-bridge.mjs:245:    add(`contracts/${id}.md`,'receipt');add(`checks/${id}.json`,'receipt');add(`checks/${id}-kernel.json`,'receipt');add(`checks/${id}.credential-request.json`,'receipt');
kernel\candidate-bridge.mjs:563:    `${root}/checks/${identity.opId}.json`,`${root}/checks/${identity.opId}.credential-request.json`,`${root}/reports/wait-state.json`,
sqlite\schema.sql:246:-- checks — the kernel's re-run check results per op attempt (was checks/<op>.json).
sqlite\design.yaml:176:    purpose: "kernel's re-run check results per op attempt (was checks/<op>.json)"
modules\goal\legality.yaml:26:#   scripts/check-work-deep.mjs     — DEP_STALE / NORM_UNRECORDED baseline semantics
modules\goal\legality.yaml:336:    citation: "existing.yaml extend-vs-add + ownership-check; scripts/check-work-deep.mjs DEP_STALE"
modules\goal\legality.yaml:350:    citation: "existing.yaml ownership-check; scripts/check-work-deep.mjs"
modules\goal\existing.yaml:18:#   scripts/check-work-deep.mjs    — DEP_STALE: a done record whose referenced
modules\goal\existing.yaml:51:    - scripts/check-work-deep.mjs
modules\goal\anatomy.yaml:87:    - scripts/check-work-deep.mjs (normDigest/DEP_STALE — staleness of a done variable)
modules\goal\anatomy.yaml:118:        (DEP_STALE semantics, scripts/check-work-deep.mjs)
modules\goal\anatomy.yaml:143:    - scripts/check-work-deep.mjs (DEP_STALE)
model\records.yaml:67:    validator: [schemas/work.schema.yaml, schemas/work-layout.yaml, checks/brand.mjs]
model\records.yaml:108:    validator: [schemas/work.schema.yaml, schemas/work-layout.yaml, checks/acceptance.mjs]
model\records.yaml:117:    validator: [schemas/work.schema.yaml, checks/stacks.mjs]
model\kinds.yaml:42:  # `application-stacks`); `checks` names executable check scripts (`scripts/check-scoped-lint.mjs`,
model\kinds.yaml:43:  # `checks/code-patterns/*.mjs`, `checks/architecture/*.mjs`, `checks/acceptance.mjs`); `stacks` names the
model\kinds.yaml:300:    checks: [scripts/check-scoped-lint.mjs, checks/code-patterns/*.mjs, checks/architecture/*.mjs]
model\kinds.yaml:315:    checks: [scripts/check-scoped-lint.mjs, checks/code-patterns/*.mjs, checks/architecture/*.mjs]
model\kinds.yaml:350:    checks: [checks/code-patterns/*.mjs]
model\kinds.yaml:370:    checks: [checks/acceptance.mjs]
model\kinds.yaml:388:    checks: [checks/acceptance.mjs]
model\kinds.yaml:407:    checks: [checks/acceptance.mjs]
model\kinds.yaml:425:    checks: [scripts/check-scoped-lint.mjs, checks/code-patterns/*.mjs, checks/architecture/*.mjs, checks/acceptance.mjs]
model\kinds.yaml:531:    checks: [scripts/check-scoped-lint.mjs, checks/code-patterns/*.mjs, checks/architecture/*.mjs]
model\kinds.yaml:548:    checks: [scripts/check-scoped-lint.mjs, checks/code-patterns/*.mjs, checks/architecture/*.mjs]
model\kinds.yaml:566:    checks: [checks/code-patterns/*.mjs]
model\kinds.yaml:581:    checks: [checks/acceptance.mjs]
model\kinds.yaml:651:    checks: [checks/code-patterns/*.mjs]
model\kinds.yaml:669:    checks: [scripts/check-scoped-lint.mjs]
model\kinds.yaml:705:    checks: [scripts/check-scoped-lint.mjs]
model\kinds.yaml:722:    checks: [checks/stacks.mjs, checks/proof.mjs]
README.md:160:| `checks/` | machine checks over bytes — the brand record, a drawing's capture and markup, a proof bundle |
README.md:201:| An old instruction requests `.claude/INDEX.md` | Read the current bootstrap and run `node .claude/scripts/check-entry.mjs /absolute/host`. Do not recreate the retired file. |
PARALLEL-AMAP.md:31:4. **`.claude` runtime is read-only** (`ops/`, `schemas/`, `checks/`, `scripts/`,
PARALLEL-AMAP.md:78:| 19 | `ex-capture` | devin | `scripts/check-example-render.mjs` wiring, `features/*/impl/**` captures | wire `render`/`brand` checks into the gate (item 55) — compares renders against direction PNGs, needs vision |
tests\application-stacks-integration.spec.mjs:22:  for(const file of ['checks/stacks.mjs','schemas/application-stacks.schema.json','knowledge/application-stacks.json',
tests\acceptance-gates.spec.mjs:7:import {EVIDENCE_PACKET,evaluateAcceptance,kernelVerificationReceipt,requiredGatesFor,resolveEvidencePacket} from '../checks/acceptance.mjs';
SUPERVISOR.md:114:Ðã truy t?i t?n g?c. `source:oracle-189` = `scripts/check-fe-architecture.spec.mjs`,
SUPERVISOR.md:118:node node_modules/vitest/vitest.mjs run --changed a01a7bd7... && node --test scripts/check-fe-architecture.spec.mjs
tests\application-stacks-kit.spec.mjs:6:import {checkApplicationStacks} from '../checks/stacks.mjs';
tests\architecture-contracts.spec.mjs:7:import { checkArchitecture } from '../checks/architecture.mjs';
tests\architecture-contracts.spec.mjs:8:import { loadArchitectureConfig } from '../checks/architecture/config.mjs';
tests\architecture-contracts.spec.mjs:9:import { checkBackendContracts, PUBLIC_CONTRACT_RULE_ID, READONLY_BOUNDARY_RULE_ID } from '../checks/architecture/contracts.mjs';
tests\architecture-contracts.spec.mjs:10:import { buildTypeScriptContext } from '../checks/architecture/typescript.mjs';
tests\architecture-input-scope.spec.mjs:7:import {checkScopedLint} from '../scripts/check-scoped-lint.mjs';
tests\architecture-input-scope.spec.mjs:8:import {checkArchitecture} from '../checks/architecture/index.mjs';
knowledge\coding-reference.yaml:107:        - scripts/check-scoped-lint.mjs
modules\ops\ops\goal.revise.yaml:70:        state (checks/, contracts/, inbox/, reports/) is never op output - rev bumped by exactly
tests\architecture.spec.mjs:8:import { checkArchitecture } from '../checks/architecture.mjs';
tests\architecture-next-data.spec.mjs:7:import { loadArchitectureConfig } from '../checks/architecture/config.mjs';
tests\architecture-next-data.spec.mjs:8:import { checkArchitecture } from '../checks/architecture.mjs';
tests\architecture-next-data.spec.mjs:9:import { checkFrontendDataLifecycle, SWR_KEY_RULE_ID, SWR_MUTATION_RULE_ID } from '../checks/architecture/next-data.mjs';
tests\architecture-next-data.spec.mjs:10:import { buildTypeScriptContext } from '../checks/architecture/typescript.mjs';
knowledge\patterns\enforcement.yaml:71:      `node scripts/check-scoped-lint.mjs <repository> <explicit-owned-file> [...]`.
knowledge\patterns\enforcement.yaml:81:        - scripts/check-scoped-lint.mjs
knowledge\grammars\common\DNA.yaml:71:        are absent from `tokens[]` by design, not by omission. `checks/brand.mjs` `tokens-in-grammar`
knowledge\grammars\common\DNA.yaml:165:      `checks/render.mjs` `cardClassesOf` keeps (a `starci-core-*` name ending in `-surface` or
knowledge\grammars\common\DNA.yaml:1360:      — the rule `checks/render.mjs` `cardClassesOf` applies — does not see an accordion card as a card,
knowledge\grammars\common\DNA.yaml:1364:      - checks/render.mjs:337
tests\brand-checks.spec.mjs:15:} from '../checks/brand.mjs';
modules\ops\ops\security.verify.yaml:56:    path: scripts/check-scoped-lint.mjs + checks/code-patterns/*.mjs + checks/architecture/*.mjs
modules\ops\ops\review.verify.yaml:1130:        path: scripts/check-stales.mjs + docs/source-staleness.md
modules\ops\ops\request.analyze.yaml:61:        (goal.json, checks/, contracts/, inbox/, reports/) is never op output.
modules\ops\ops\release.deliver.yaml:269:          en: Inspect current remote and local refs, branch policy and required checks/hooks. If integration
tests\candidate-multi-root.spec.mjs:135:  write(accepted.owner,'.starciwork/_local/workflows/wf/checks/frontend.json','[]\n');
tests\candidate-multi-root.spec.mjs:141:    `${owner}/.starciwork/_local/workflows/wf/checks/frontend.json`,
tests\candidate-multi-root.spec.mjs:162:  foreign.write('checks/foreign-op.json','{}\n');
tests\candidate-multi-root.spec.mjs:163:  foreign.write('checks/foreign-op-kernel.json','{}\n');
tests\candidate-multi-root.spec.mjs:169:    'work','other-workflow',['.starciwork/_local/workflows/other-workflow/checks/foreign-op-kernel.json','.starciwork/_local/workflows/other-workflow/checks/foreign-op.json',
tests\candidate-multi-root.spec.mjs:201:  assert.ok(forgedArtifactsRefused.reasons.includes('work:custody-path-touched:.starciwork/_local/workflows/other-workflow/checks/foreign-op.json'));
tests\check-stales-cli.spec.mjs:11:  const script=run(['scripts/check-stales.mjs',...args]);
workflows\transitions.yaml:62:    A passing support response resumes FE; FE must rerun affected checks/render before its own response can pass.
knowledge\architecture-rules.yaml:195:        - scripts/check-stales.mjs
knowledge\architecture-rules.yaml:196:        - scripts/check-scoped-lint.mjs
knowledge\architecture-rules.yaml:208:        - scripts/check-stales.mjs
schemas\work-layout.yaml:14:    nesting; the real example tree and the gate that checks it (scripts/check-example-work.mjs's own
schemas\work-layout.yaml:16:    scripts/check-example-work.mjs reads shape.families from this file at gate time and refuses
schemas\work-layout.yaml:72:    a record, has no id) and refused (scripts/check-example-derived.mjs) the moment it goes stale against
schemas\work-layout.yaml:116:    provenance (see checks/render.mjs's capture-identity rule) - and a generation-bearing asset
tests\code-patterns-cli.spec.mjs:20:  const direct = run(['.dist/scripts/check-scoped-lint.mjs', '--profile', 'nest']);
packages\eslint\fe\file-layout.test.mjs:140:      { filename: "D:/repo/scripts/check-quality.spec.mjs", code: "export const x = 1" },
tests\example-critique.spec.mjs:10: * different-drive os.tmpdir() breaks the relative-path handling scripts/check-example-work.mjs relies on.
kernel\kernel.mjs:26:import {planProtectedProof,proofApplies,proofFinding,proofPlan,protectedProofFinding,runAtBase,runProtectedProof} from '../checks/proof.mjs';
kernel\kernel.mjs:27:import {evaluateAcceptance,kernelVerificationReceipt,resolveEvidencePacket} from '../checks/acceptance.mjs';
kernel\kernel.mjs:51:import {renderChecksFor} from '../checks/render.mjs';
tests\example-derive.spec.mjs:7:import {checkExampleDerived} from '../scripts/check-example-derived.mjs';
tests\example-derive.spec.mjs:13: * (scripts/check-example-derived.mjs). Fixtures live on the same drive as the repo, exactly like
tests\example-work-gate.spec.mjs:6:import { checkWorkTree, checkFamiliesDrift, FAMILIES } from '../scripts/check-example-work.mjs';
tests\example-work-gate.spec.mjs:9: * One fixture tree per new-concept rule in scripts/check-example-work.mjs, proving each rule refuses the
tests\frontend-source-layout.spec.mjs:7:import {checkArchitecture} from '../checks/architecture.mjs';
tests\grammar-guards.spec.mjs:9:import { checkGrammarGuards, GRAMMAR_GUARD_RULES } from '../checks/code-patterns/grammar-guards.mjs';
tests\helpers\png.mjs:6: * rather than in `checks/`. Like the decoder it carries no dependency: the CRC32 of the format is twenty
tests\integration.spec.mjs:364:    '.claude/hosts/index.mjs', '.claude/hosts/orca/launch.mjs', '.claude/models/functions.mjs', '.claude/checks/brand.mjs']) {
tests\json-exceptions.spec.mjs:14:assert.equal(fs.existsSync(checkerFile), true, 'scripts/check-json-exceptions.mjs is required');
tests\maintenance-regressions.spec.mjs:9:import {checkEntry} from '../scripts/check-entry.mjs';
tests\nest-boundaries.spec.mjs:7:import { checkNestBoundaries, NEST_BOUNDARY_RULES } from '../checks/code-patterns/nest-boundaries.mjs';
tests\nest-code-patterns.spec.mjs:7:import { checkNestPatterns, NEST_SCRIPT_RULES } from '../checks/code-patterns/nest.mjs';
tests\nest-error-identity.spec.mjs:7:import { checkNestErrorIdentity, NEST_ERROR_IDENTITY_RULES } from '../checks/code-patterns/nest-error-identity.mjs';
tests\nest-errors.spec.mjs:7:import { checkNestErrors, NEST_ERROR_RULES } from '../checks/code-patterns/nest-errors.mjs';
tests\nest-errors.spec.mjs:8:import { defaultScriptChecker } from '../scripts/check-scoped-lint.mjs';
tests\nest-identity-aggregate.spec.mjs:7:import { checkScopedLint } from '../scripts/check-scoped-lint.mjs';
tests\nest-metadata.spec.mjs:7:import { checkNestMetadata, discoverNestMetadataInputs } from '../checks/code-patterns/nest-metadata.mjs';
tests\nest-test-aggregate.spec.mjs:7:import {checkScopedLint} from '../scripts/check-scoped-lint.mjs';
tests\nest-test-patterns.spec.mjs:7:import { checkNestTests, NEST_TEST_RULES } from '../checks/code-patterns/nest-tests.mjs';
tests\next-adapter-aggregate.spec.mjs:9:import {checkScopedLint} from '../scripts/check-scoped-lint.mjs';
tests\next-adapter-aggregate.spec.mjs:10:import {checkArchitecture} from '../checks/architecture/index.mjs';
tests\next-code-patterns.spec.mjs:8:import { checkNextPatterns, NEXT_SCRIPT_RULES } from '../checks/code-patterns/next.mjs';
tests\next-error-context.spec.mjs:7:import { checkNextErrors } from '../checks/code-patterns/next-errors.mjs';
tests\next-error-patterns.spec.mjs:7:import { checkNextErrors, NEXT_ERROR_RULES } from '../checks/code-patterns/next-errors.mjs';
tests\next-required-value-aggregate.spec.mjs:7:import {checkScopedLint} from '../scripts/check-scoped-lint.mjs';
tests\npm-package.spec.mjs:36:  assert.ok(receipt.files.some(file=>file.path==='.dist/checks/stacks.mjs'));
tests\pattern-coverage.spec.mjs:13: *    (a script obligation routed into checks/code-patterns/, an ESLint obligation or an
tests\pattern-coverage.spec.mjs:20: *    referenced by the manifest (checks/stacks.mjs bounds-checks each reference), or a
tests\pattern-coverage.spec.mjs:39:const scriptSource=read('scripts/check-scoped-lint.mjs');
tests\pattern-coverage.spec.mjs:41:// The adapter table of check-scoped-lint.mjs routes script ruleIds into checks/code-patterns/.
tests\pattern-coverage.spec.mjs:81:          coverage='script';detail=`${obligation.id} -> checks/code-patterns/${module}.mjs`;
tests\pattern-coverage.spec.mjs:125:    const file=path.join(root,'checks/code-patterns',`${adapter.module}.mjs`);
tests\pattern-coverage.spec.mjs:126:    assert.ok(fs.existsSync(file),`checks/code-patterns/${adapter.module}.mjs is missing`);
tests\pattern-coverage.spec.mjs:127:    const loaded=await import(`${new URL(`../checks/code-patterns/${adapter.module}.mjs`,import.meta.url)}`);
tests\pattern-coverage.spec.mjs:197:  const {checkApplicationStacks}=await import('../checks/stacks.mjs');
tests\pattern-coverage.spec.mjs:198:  assert.equal(typeof checkApplicationStacks,'function','checks/stacks.mjs must export checkApplicationStacks');
tests\pattern-coverage.spec.mjs:228:  assert.deepEqual(uncoveredFiles,[],'every env/service file under .stacks/ must be referenced by the manifest (checks/stacks.mjs bounds-checks it) or be a non-service artifact');
tests\pattern-coverage.spec.mjs:249:  {file:'checks/<op>.json',schema:'worker check-result list',validator:'kernel checks intake via store.checksPath',source:['kernel/kernel.mjs','checksPath']},
tests\pattern-coverage.spec.mjs:250:  {file:'checks/<op>-kernel.json',schema:'starci/workflow-kernel-checks@1',validator:'kernel kernel-check writer',source:['kernel/kernel.mjs','workflow-kernel-checks@1']},
tests\pattern-coverage.spec.mjs:251:  {file:'checks/<op>-audit.json',schema:'starci/audit-measurement@1',validator:'kernel audit writer',source:['kernel/kernel.mjs','audit-measurement@1']},
tests\pattern-coverage.spec.mjs:252:  {file:'checks/<op>.credential-request.json',schema:'credential-request record',validator:'housekeeping-declared runtime file',source:['kernel/candidate-bridge.mjs','credential-request.json']},
tests\pattern-coverage.spec.mjs:303:  assert.ok(posix(store.checksPath('op1')).startsWith(`${localRoot}/checks/`));
tests\pattern-coverage.spec.mjs:336:  for(const declared of ['state.json','events.jsonl','checks/opx.json']){
tests\render-checks.spec.mjs:15:} from '../checks/render.mjs';
tests\runtime-import-closure.spec.mjs:9:    ['checks/index.mjs', 'import { check } from "./owners.mjs"; export {check};'],
tests\runtime-import-closure.spec.mjs:12:    ['checks/index.mjs', 'export { check } from "./owners.mjs";'],
tests\runtime-import-closure.spec.mjs:18:    ['checks/index.mjs', `import './owner.mjs';
tests\runtime-import-closure.spec.mjs:23:    ['checks/owner.mjs', 'export const identity = 1;'],
tests\scoped-lint.spec.mjs:11:import {checkScopedLint,codePatternExitCode,inspectLintResults,parseScopedLintArgs,scopedLintMain,verifyCodePatternReportDigest} from '../scripts/check-scoped-lint.mjs';
tests\scoped-lint.spec.mjs:125:test('the lint audit protocol accepts a typed findings report as measurement but rejects unavailable or contradictory output',async t=>{const {root,source}=fixture(t),report=await checkScopedLint(root,['src/component.tsx'],{profile:'next',profileCatalog:catalog({status:'missing'}),runtime:runtime(source),architecture}),op={id:'audit-code-patterns',kind:'review.verify',operation:'lint',checks:[{name:'code patterns',command:'node scripts/check-scoped-lint.mjs --profile next --root repo -- src/component.tsx'}]};const measured=measureAuditChecks(op,()=>({status:1,stdout:JSON.stringify(report),stderr:''}));assert.equal(measured.ok,true);assert.equal(measured.outcome,'findings');assert.equal(measured.findings,report.issues.length);const publicCommand={...op,checks:[{name:'code patterns',command:'node bin/starci.mjs code-patterns check --profile next --root repo --all'}]},publicMeasured=measureAuditChecks(publicCommand,()=>({status:1,stdout:JSON.stringify(report),stderr:''}));assert.equal(publicMeasured.ok,true);assert.equal(publicMeasured.outcome,'findings');const unavailable={...report,status:'unavailable'};delete unavailable.reportDigest;const sealed={...unavailable,reportDigest:sha256(canonicalJSON(unavailable))};assert.equal(measureAuditChecks(op,()=>({status:2,stdout:JSON.stringify(sealed),stderr:''})).ok,false);assert.equal(measureAuditChecks(op,()=>({status:0,stdout:JSON.stringify(report),stderr:''})).ok,false);});
tests\source-staleness.spec.mjs:11:import {checkStalesMain} from '../scripts/check-stales.mjs';
tests\source-staleness.spec.mjs:13:const script=path.resolve(new URL('../scripts/check-stales.mjs',import.meta.url).pathname.replace(/^\/(?:[A-Za-z]:)/,value=>value.slice(1)));
tests\stacks.spec.mjs:9:import {checkApplicationStacks} from '../checks/stacks.mjs';
tests\stacks.spec.mjs:196:  fs.copyFileSync(new URL('../checks/stacks.mjs',import.meta.url),path.join(dist,'checks','stacks.mjs'));fs.copyFileSync(new URL('../core/yaml.mjs',import.meta.url),path.join(dist,'core','yaml.mjs'));
tests\store.spec.mjs:348:  for(const name of ['state.json','events.jsonl','goal.md','goal.json','reports/ctx_1.json','contracts/op-1.attempt-1.md','contracts/op-1.attempt-2.md','checks/op-1.json'])
tests\verify-proof.spec.mjs:7:import {PROOF_POLICY,isSpecPath,policyFor,proofApplies,proofFinding,proofPlan,runAtBase,planProtectedProof,runProtectedProof,protectedProofFinding} from '../checks/proof.mjs';
tests\work-artifacts.spec.mjs:6:import {checkWorkArtifacts} from '../scripts/check-work-artifacts.mjs';
tests\work-artifacts.spec.mjs:10: * One fixture tree per byte rule in scripts/check-work-artifacts.mjs, proving each refuses the exact
tests\work-change.spec.mjs:7:import {checkWorkChange,classifyChange,normativeDigest,normative,readWorkTree,CHANGE_KINDS,WorkChangeInputError} from '../checks/work-change.mjs';
tests\work-consistency.spec.mjs:5:import {checkWorkConsistencyTree, checkTreeParity, declaredStateValues} from '../scripts/check-work-consistency.mjs';
tests\work-consistency.spec.mjs:7:import {walk} from '../scripts/check-example-work.mjs';
tests\work-consistency.spec.mjs:10: * One fixture tree per cross-record rule in scripts/check-work-consistency.mjs, proving each rule fires on the
tests\work-history-check.spec.mjs:6:import {checkWorkHistoryTree} from '../scripts/check-work-history.mjs';
tests\work-history-check.spec.mjs:10: * scripts/check-work-history.mjs refuses the transition it targets - and that the cases it must NOT refuse (a
tests\work-ledger.spec.mjs:169:    assert.match(unchecked.reason,/extensions\.work3\.checks/);
tests\work-replay.spec.mjs:5:import {checkTree, planCommand, runAssertion, collectAssertions, analyzeAssertion} from '../scripts/check-work-replay.mjs';
tests\work-replay.spec.mjs:8: * Fixture trees for scripts/check-work-replay.mjs - one per behavior the replay engine promises:
tests\work-surfaces.spec.mjs:6:import {checkWorkSurfaces} from '../scripts/check-work-surfaces.mjs';
tests\work-surfaces.spec.mjs:179:  const run = spawnSync(process.execPath, ['scripts/check-work-surfaces.mjs', '--tree', workRoot],
tests\workflow-kernel-shared-ledger.spec.mjs:547:    for(const row of runtime.journal.db.prepare('SELECT op_id FROM checks WHERE workflow_id=?').all(run.state.id))observedCustody.add(`checks/${row.op_id}`);
tests\workflow-kernel-shared-ledger.spec.mjs:596:    assert.ok(observedCustody.has(`checks/${op.id}`),`worker checks custody for ${op.id}`);
tests\workflow-kernel-shared-ledger.spec.mjs:597:    assert.ok(observedCustody.has(`checks/${op.id}-kernel`),`kernel checks custody for ${op.id}`);
tests\workflow-kernel.spec.mjs:11:import {brandColours} from '../checks/render.mjs';
tests\workflow-kernel.spec.mjs:3447:    assert.match(author.goal,/^Complete the Work record of demo\.sales\.implementation\.backend\.refund so the kernel can launch it: Node .* declares no implementation\.changes\[\]\.files and no extensions\.work3\.checks/);
tests\workflow-kernel.spec.mjs:3539:    assert.match(still.detail,/^ledger incomplete: Node demo\.sales\.implementation\.backend\.refund declares no implementation\.changes\[\]\.files and no extensions\.work3\.checks/);
