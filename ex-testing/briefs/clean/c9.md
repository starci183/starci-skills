# c9 — final verify gate (WAIT-LANE)

Poll `ex-testing/lint/done/` for c1..c8 every ~3min; proceed after 60min regardless.
Then run the full battery and fix what the moves broke:
1. `node --test "tests/*.spec.mjs"` — target 0 fail.
2. Cold boot: `node bin/starci.mjs version`, `node cli/main.mjs ops` (if cli survives), kernel import smoke.
3. `scripts/route/route-plan.mjs --target "ui.X: verified" --json` still emits the chain.
4. `node scripts/checks/check-example-work.mjs` exit 0.
5. grep sweep: no references to moved paths outside legacy/ itself.
Write c9-REPORT.md + done marker. List every residual failure honestly.
