# c5 — triage: skills/, fixtures/, worktrees/, junk files, stray models/

Owns: `skills/`, `fixtures/`, `worktrees/`, top-level `models/` (stray — canon is `modules/models/`), junk paths at root (`--help/`, `0`, `m[1]).join('/')`, and any other top-level file/dir not in the target layout nor owned by c1-c4/c6-c10.
- `skills/` vs SKILL.md — is it the agent-skills catalog? Check providers/tests refs.
- `fixtures/` is canon target — keep dir, sweep dead members.
- `worktrees/` — old agent snapshots (data). Move to `legacy/worktrees` or delete if pure scratch — prefer legacy.
- `models/` top-level: inspect; if duplicate of modules/models → delete (untracked) or legacy.
- Junk: verify untracked + garbage, then `rm -rf`.
