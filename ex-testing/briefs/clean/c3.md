# c3 — triage: providers/, runtime/, upgrades/

Owns: `providers/`, `runtime/`, `upgrades/`.
- `providers/*.yaml` — SKILL.md references them (auth/provider profiles). Likely LIVE — keep, verify refs.
- `runtime/` — contains engine/builds sealed cache? `runtime/engine/builds/*` is gitignored local state — leave the dir but document. Other members: triage.
- `upgrades/` — old upgrade scripts/notes → likely `legacy/upgrades`.
