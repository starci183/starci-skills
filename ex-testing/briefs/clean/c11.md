# c11 — scripts/ root must be empty (pattern: scripts/<folder>/<file>)

STANDARD: `scripts/` holds ONLY folders. Every loose `scripts/*.mjs` moves into a domain folder.

Owns: `scripts/*.mjs` (loose files only — checks/, route/, goal/, kernel/ are settled).

Moves (rename to drop redundant prefixes):
- example-{critique,derive,evidence,ownership,render-proof,verify}.mjs → `scripts/example/{critique,derive,evidence,ownership,render-proof,verify}.mjs`
- work-example.mjs, work-remap-path.mjs → `scripts/example/` or `scripts/work/` (read the file, pick the domain it actually serves)
- plan.mjs, present-goal.mjs → `scripts/goal/` (read first — plan.mjs may be dead; dead → legacy/scripts-loose/)
- ledger-migrate.mjs → `scripts/kernel/`
- config.mjs → `scripts/dev/` if a real shared helper, else legacy
- probe-reference-conventions.mjs, sanitize-orca-fixture.mjs → `scripts/dev/` or legacy if one-off

After EVERY move: grep the moved path repo-wide (`scripts/<oldname>`) and update ALL references — code imports, yaml executable: fields, SKILL.md/docs mentions, recorded assertion commands in examples' .starciwork evidence (update the recorded command path — it's a path fix, not evidence tampering; note it in report).

Verify: node --check each moved file; run tests/example-work-gate.spec.mjs + check-example-yaml; confirm `ls scripts/` shows folders only.
Marker: ex-testing/lint/done/c11.done + c11-REPORT.md
