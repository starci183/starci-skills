# c7 — triage: tests/ dead specs + node_modules sanity

Owns: `tests/` (sweep only — it's canon dir, don't move the dir itself).
- Delete/move specs that only assert retired behavior (`.dist` build pipeline, ops/ at old path, yaml-dist migration = already deleted). Check `git log`/content; specs testing legacy/* paths should move to `legacy/` with their subjects or be deleted if the subject is gone.
- Keep: all specs covering kernel/, modules/, scripts/checks, example gates — must still pass.
- Also verify root `node_modules/` is a real install (not stale junk): `npm ls --depth=0` quick probe; report only.
