# c8 — root files + package.json + INDEX/README/SKILL final layout (WAIT-LANE)

Owns: `package.json`, `INDEX.yaml`, `README.md`, `README.yaml`, `SKILL.md`, `VERSION`, root loose files (`*.json`, `*.yaml`, `*.mjs` at root, `.gitignore`, `.npmrc`...).
- Poll `ex-testing/lint/done/` for c1..c7 every ~3min; proceed after 45min regardless noting who's missing.
- Then: `package.json` files/exports must reflect FINAL layout (no moved dirs). INDEX.yaml folder tree rewritten. SKILL.md/README preamble sweep for any still-listed moved dir.
- Root loose files: classify live/legacy/junk same rules.
