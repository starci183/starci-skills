# c1 — triage: cli/, init/, mcp/, sites/

Owns: `cli/`, `init/`, `mcp/`, `sites/` (+ their references).
- `cli/main.mjs` was migrated to source-YAML by a prior lane — check if `cli/` is the live entry (package.json bin) or dead weight next to `bin/`.
- `init/` templates: referenced by installer? `bin/starci-skills.mjs prepareInstalledRuntime`?
- `mcp/` server + `sites/` (github-pages catalog?): likely legacy/junk — verify nothing imports them, then move.
