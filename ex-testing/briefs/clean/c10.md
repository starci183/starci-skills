# c10 — examples/ + ex-testing/ hygiene sweep

Owns: `examples/**` scratch files + `ex-testing/` tidy (keep briefs/, lint/ reports+done — they're the durable record).
- Delete scratch leftovers like `examples/todo-app-backend/_w8-cov.cjs`, `_w8-uncov*.cjs`, `ex-testing-cov-v8/` dirs, `*.tmp`, `nul` files, `--help/` junk under examples.
- `ex-testing/scratch/` (w7/r2 driver scripts): keep under lint/scratch (evidence) — just confirm no stray dirs elsewhere.
- examples dirs themselves are canon — do NOT move example apps. Only junk/scratch inside.
- Run `node --test tests/example-work-gate.spec.mjs` + `scripts/checks/check-example-yaml.mjs` after.
