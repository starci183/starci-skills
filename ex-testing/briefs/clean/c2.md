# c2 — triage: workflows/, execution/, contracts/

Owns: `workflows/`, `execution/`, `contracts/`.
- `workflows/*.mjs` (matrix, frontend, lifecycle) are imported by kernel/tests — likely LIVE, keep dir but sweep dead members inside.
- `execution/` + `contracts/` — old JSON-workflow era; check kernel/workflows imports first. If superseded by modules/kernel contracts → `legacy/`.
- Careful: `workflows/frontend.mjs` was just fixed (w7) to read legacy/ops — it's live.
