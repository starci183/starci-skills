# c6 — triage: scripts/ root (non-checks/non-route members)

Owns: loose `scripts/*.mjs` (NOT `scripts/checks/`, `scripts/route/` which are canon).
- Builders were moved to `legacy/builders/` already. Whatever remains at scripts/ root — classify each file: live tool (plan.mjs? config.mjs? example-*.mjs used by checks) stays; dead → `legacy/scripts-loose/`.
- `scripts/plan.mjs:48` still does distPath+JSON.parse of yaml (w3 flagged) — fix or legacy it.
- After moves: repo-wide grep old paths, fix refs in scope.
