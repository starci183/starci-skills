# w2 — route-plan + dispatch-op (was tinkle-11 + tinkle-12)

Read `_common.md` + `tinkle/tinkle-11.md` + `tinkle/tinkle-12.md`. Execute sequentially:
1. `scripts/route/route-plan.mjs` — chain builder: S0 survey + S* target → backward-chain via needs/produces → topo order → validate. produces: already in ops yaml.
2. `scripts/route/dispatch-op.mjs` — build dispatch packet (op, brief path, records, owned_paths, model, budget, lease) + spawn Orca terminal named `[Op] <id>` via orca CLI. Reconcile with real `orca terminal create --worktree --title --command` flags.
Marker `done/w2.done` + `w2-REPORT.md`.
