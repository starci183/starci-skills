# w3 — kernel reads source + ops→legacy (was tinkle-14 + tinkle-17)

Read `_common.md` + `tinkle/tinkle-14.md` + `tinkle/tinkle-17.md`. Owns: `kernel/**`, `core/**`, `bin/`, `cli/`, `init/`, `ops/**`, `legacy/**` — including model-policy.mjs and dispatcher/candidates (consolidated, no more split).
1. Repoint every .dist ref in kernel to source dirs; schemas load via core/yaml.mjs from schemas/*.yaml; ops resolve from modules/ops/ops/*.yaml with legacy/ops fallback
2. `git mv ops legacy/ops`
3. Cold-boot test: kernel entry runs with .dist renamed away
Marker `done/w3.done` + `w3-REPORT.md`.
