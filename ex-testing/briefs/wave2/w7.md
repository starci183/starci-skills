# w7 — consistency gate + .dist deletion + E2E verify (WAIT-LANE, was tinkle-13+22+23)

Read `_common.md` + `tinkle/tinkle-13.md` + `tinkle/tinkle-22.md` + `tinkle/tinkle-23.md`.
WAIT for done markers w1..w6 (poll ex-testing/lint/done/ every 2min; proceed after 40min regardless, noting who's missing).
Then:
1. Consistency gate: modules ↔ schemas ↔ kernel contracts ↔ scripts coherent (tinkle-13's 8 checks)
2. Zero `.dist` refs sweep; `rm -rf .dist`
3. Cold boot + route smoke + checks suite + one example gate E2E + all modules yaml parse
Marker `done/w7.done` + `w7-REPORT.md`.
