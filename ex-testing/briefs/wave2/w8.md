# w8 — backend test depth (was exup-1 + exup-2)

Read `_common.md` + `exup/_common.md` + `exup/exup-1.md` + `exup/exup-2.md`. Owns: `examples/todo-app-backend` seeds dirs + src spec files.
1. Full seed set: edge cases + volume tier + idempotent (exup-1)
2. Branch coverage 55%→≥75% on business-critical modules via real specs (exup-2)
Marker `done/w8.done` + `w8-REPORT.md`.
