# w4 — model + schemas consolidation (was tinkle-15 + tinkle-16)

Read `_common.md` + `tinkle/tinkle-15.md` + `tinkle/tinkle-16.md`. Owns: `model/**`, `modules/models/**`, `schemas/**`, `kernel/model-policy.mjs`, schema readers in `scripts/**`/`tests/**` (NOT kernel/** — w3 owns it; report kernel-side needs).
1. Merge model/*.yaml → modules/models (port gaps), model-policy.mjs reads modules/models yaml directly, `git mv model legacy/model`
2. Readers of .dist/schemas/*.json → schemas/*.yaml via parseYaml; dedupe json/yaml in schemas/ keeping yaml canon
Marker `done/w4.done` + `w4-REPORT.md`.
