# w5 — dirs triage + builders + tests (was tinkle-18 + 19 + 21)

Read `_common.md` + the three briefs. Owns: `docs/ workflows/ hosts/ providers/ execution/ contracts/ approvals/ scripts/build-* scripts/compile-* ensure-build.mjs package.json config*.yaml tests/** fixtures/**` + unclassified dirs `bin/ cli/ init/ mcp/ runtime/ sites/ skills/ specifications/ upgrades/ notes/ packages/` (packages/ is LIVE — shared libs, keep).
1. Per-dir verdict: live-runtime (keep) | legacy (git mv) | merge target. Kill .dist refs in each.
2. Builders → legacy/builders/; remove build call-sites you own; package.json build entries removed
3. tests/fixtures: repoint .dist refs to source; run affected tests
Marker `done/w5.done` + `w5-REPORT.md` with full per-dir table.
