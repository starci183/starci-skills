# w1 — kernel module (was tinkle-9)

Read `_common.md` + `tinkle/tinkle-9.md`. Execute tinkle-9 fully:
`modules/kernel/` with driver-loop.yaml, dispatch.yaml (packet contract), verdict-contract.yaml.
Operating model: [Kernel] agent = 1/project long-lived, plan→dispatch→settle→replan; [Op] = 1 op = 1 ephemeral agent dies at verdict settle; chat = trigger only. Include failure handling seen in practice: agent-child-exit detection via output staleness, respawn policy, permission-stall handling.
Marker `done/w1.done` + `w1-REPORT.md`.
