# v10-3 — owners expansion: ec-be transport + ops surfaces

Same law as v10-2 but for `examples/ecommerce-app-be`: impl records must own their transport doors (graphql/http dirs under the ec-be src layout — discover the actual layout first, it differs from todo-be). Also resolve `CAPABILITY_WITHOUT_SPEC` suspects for `/internal`, `/health`, `/webhooks`: each gets either (a) an owning record (integration/contract record that declares it), or (b) a named exemption in the workspace/exemption convention — do NOT leave silent. Re-run gate + deep check on ec tree; report the final suspect count.
