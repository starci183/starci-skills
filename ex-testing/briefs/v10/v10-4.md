# v10-4 — notify manifest dangling assertion + run hygiene

Gate refuses 3 run manifests under `features/notify/uat/digest-and-unsubscribe/runs/*/manifest.yaml`: `assertions.id` = `uat.notify.digest-and-unsubscribe.inbox-arrival` which no record owns. CHECK FIRST whether lane v9-2 is still iterating (its marker `done/v9-2.done` absent = running) — if running, WAIT (poll ~3min, up to 20min) rather than fight it.

When free: the honest fix depends on the assertion's semantics — if `inbox-arrival` is a real checkable assertion the spec intends, add it as a proper assertion record/sub-record the uat owns; if it was an ad-hoc id, remap manifests to the assertion ids the uat record actually declares. Read `uat.notify.digest-and-unsubscribe/index.yaml` first — its steps/assertions are the source of truth. Never delete a run folder to hide the refusal.
