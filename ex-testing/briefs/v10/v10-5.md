# v10-5 — derived rebuild + deep-check baseline + final sweep

WAIT LANE: poll until `v10-1`, `v10-2`, `v10-3`, `v10-4` done markers exist (and v7-9, v7-13, v9-* as feasible — they're other lanes' scope, note their status but don't block on them beyond 30min).

Then: (1) rebuild `_derived/` for both trees (`scripts/` has the derived rebuild path — find it from v7-8's report); (2) run `check-work-deep.mjs --write-baseline` on both trees to seed `deep-baseline.json` NOW that the tree is clean — this enables DEP_STALE/NORM_UNRECORDED checks; (3) run full gate + all v8 checks (replay dry-run, surfaces, history, consistency, artifacts, structure) and produce `v10-FINAL-STATUS.md` tallying every check's count. Report is the truth table, not vibes.
