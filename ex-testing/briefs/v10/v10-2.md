# v10-2 — owners expansion: todo-be graphql transport doors

Deep check flags 22 UNCLAIMED_SURFACE: `src/features/todo/graphql/{queries,mutations}/<feature>/<op>/` dirs are shipped API doors no record owns — impl records only own `src/modules/bussiness/*`. Changing an endpoint silently stales nothing.

Fix: for each impl record in `examples/todo-app-backend/.starciwork`, extend `owners:` to also cover its matching transport dirs — e.g. `impl.share.invitations` gains `{role: module, path: src/features/todo/graphql/mutations/share/**}` + queries equivalent. Map each graphql op dir (task/create-task, share/invite, plan/upgrade-plan, audit/audit-log, notify/*, recur/*...) to the impl record that owns that capability's business logic. Ops with NO owning impl → report `TRANSPORT_ORPHAN` (don't invent a record). Also cover `src/features/todo/http/**` (webhooks/sepay, health). Adding owner paths changes recordDigest → re-run evidence refresh for touched records (`scripts/example-evidence.mjs`) so digests settle. Re-run gate + `check-work-deep.mjs`: UNCLAIMED_SURFACE count must drop to ~0 (except genuine orphans, listed).
