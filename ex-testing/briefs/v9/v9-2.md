# v9-2 — implement + run `uat.notify.digest-and-unsubscribe` (todo)

Read `v9/_common.md` + `v7-11-REPORT.md`. Spec stub: `uat/flows/uat.notify.digest-and-unsubscribe.spec.ts` — steps: 2 notify-worthy events in one digest window → inbox shows ONE message covering both → unsubscribe in preferences.

Verify first: does the app have a notify inbox UI? preferences screen with unsubscribe? digest batching on BE (RECUR_TICK_CRON-style override may exist — check `integration.notify` records for a digest-window env override). Missing surface → `APP_CAPABILITY_MISSING`, gap update, leave todo. Present → walk + run + video + evidence + close `gap.notify.live-proof` per v7-11's convention.
