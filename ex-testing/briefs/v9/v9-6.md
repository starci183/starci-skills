# v9-6 — ec-fe: build sign-in + session handoff (real feature work)

Read `v9/_common.md` + `v7-12-REPORT.md` (names the absences). Scope: `examples/ecommerce-app-fe` — OUTSIDE `.starciwork`, real product code.

Facts from v7-12: `apps/shop` `AccountPage` is read-only (`fetchCurrentUser` reads `/me` with ambient credentials); NO register/sign-in form exists; the landing→shop session handoff is "an explicitly unsettled contract" (`apps/shop/src/modules/api/identity.ts` comment). ec-be identity service runs host-side (`npm run start:identity`) on its own port — find it in `.starcistacks`/`application-stacks.yaml` or package.json scripts.

Build: (1) sign-in + register forms in `apps/shop` matching the existing module style (read `apps/shop/src/modules/` first); (2) the session handoff landing→shop — read both apps' api modules and the be identity endpoints to settle the contract concretely (token in cookie? query handoff? pick what be already serves — do NOT invent a new auth scheme); (3) wire `NEXT_PUBLIC_*_API_URL` defaults. Verify manually: boot `start:identity` + shop dev server, register + sign in + land on `/account` showing the real user. Then update the ec `.starciwork` records this proves: `ui.identity.sign-in` (uninvestigate→ its honest state), `gap.identity.live-proof` statement (the form now exists — narrow the gap to "no uat run yet"). Report real blockers, not workarounds.
