# v9-1 — implement + run `uat.audit.right-to-be-forgotten` (todo)

Read `v9/_common.md` + `v7-11-REPORT.md` first. Spec stub: `examples/todo-app-frontend/uat/flows/uat.audit.right-to-be-forgotten.spec.ts` — its doc comment lists the 4 intended steps (erasure request → export shows nothing → operator reads unnamed log lines).

Before writing the walk: verify the app actually HAS these surfaces (erasure request UI? data export? operator audit log view?). If any are missing → do NOT fake: report `APP_CAPABILITY_MISSING` naming the exact surface, update the owning gap record statement, leave record `todo`, done. If present → implement the walk matching `uat.task.create.spec.ts` style, run it (`npx playwright test uat/flows/uat.audit.right-to-be-forgotten.spec.ts` with the run env), verify `runs/<id>/` has screens+video+`result.md` pass+cleanup, write evidence.yaml + settle the record + close `gap.audit.live-proof` the way v7-11 did.
