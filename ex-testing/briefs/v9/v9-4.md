# v9-4 — implement + run `uat.recur.make-recurring` (todo)

Read `v9/_common.md` + `v7-11-REPORT.md`. Spec stub: `uat/flows/uat.recur.make-recurring.spec.ts` — steps: owner turns a task into a weekday-09:00 rule → occurrences appear. NOTE: this record has NO `blockedBy` — the skip is a stub artifact, the feature may be fully built.

Verify first: recur UI exists? (`features/recur/ui/schedule` record claims a screen). RECUR_TICK_CRON env override exists in AppConfigService — use a fast tick so the run observes a real occurrence. Missing surface → `APP_CAPABILITY_MISSING`. Present → walk + run + video + evidence + settle.
