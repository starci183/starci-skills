# v9-10 — ec `uat.checkout.place-order` spec + run (WAIT LANE)

Read `v9/_common.md` + `v7-12-REPORT.md`. Poll `ex-testing/lint/done/v9-6.done` + `v9-7.done` + `v9-8.done` every ~3min (requires: working sign-in, real cart, checkout path, harness rig). Do not start early.

Then: implement `uat.checkout.place-order.spec.ts` walking the record's steps (browse → add line → confirm → replay idempotent → empty-cart refusal → beyond-stock refusal naming product/qty/stock → order on account page — read `uat.checkout.place-order/index.yaml` + `fr/br.checkout.place-order` for exact claims). Run on the real stack (identity + order services + shop). Produce `runs/<id>/` screens+video+pass+cleanup → evidence.yaml → settle done → close `gap.checkout.live-proof`.
