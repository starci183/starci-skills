# v9-3 — implement + run `uat.plan.upgrade-after-cap` (todo)

Read `v9/_common.md` + `v7-11-REPORT.md`. Spec stub: `uat/flows/uat.plan.upgrade-after-cap.spec.ts` — steps: owner at 20-task cap → 21st refused naming cap+upgrade path → checkout via gateway sandbox → usage screen shows no cap.

Verify first: is the 20-cap enforced (seed may need 20 tasks — check seed.yaml or create via API)? Does a paid-plan checkout UI + sandbox gateway exist (sepay module is loaded — check `features/plan/` records for the sandbox path)? Missing → `APP_CAPABILITY_MISSING`. Present → walk + run + evidence + close `gap.plan.live-proof`.

## Amendment — sepay is inprogress, no callback simulation
Do NOT simulate the sepay webhook to complete the checkout leg. Walk only what the app serves:
cap refusal + upgrade-path surface. `integration.plan.sepay` and the uat record settle at
`inprogress` (never `done`); the gap names the pending gateway leg. See `_common.md` amendment.
