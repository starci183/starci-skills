# v9-9 — ec `uat.identity.sign-in` spec + run (WAIT LANE)

Read `v9/_common.md` + `v7-12-REPORT.md`. Poll `ex-testing/lint/done/v9-6.done` AND `v9-8.done` every ~3min (also require: shop app serves a working sign-in form + harness rig exists). Do not start early — a spec against an unbuilt form is fabricated evidence.

Then: implement `apps/shop`-side flow spec `uat.identity.sign-in.spec.ts` walking the record's steps (register pair → wrong-pair uniform refusal → missing-half refusal → taken-email conflict → second sign-in new session — read `uat.identity.sign-in/index.yaml` + `fr.identity.sign-in` for exact claims). Run against the real stack (identity service + shop dev server — boot them the way the be package.json scripts say). Produce `runs/<id>/` with screens+video+pass+cleanup → write evidence.yaml → settle record done → close `gap.identity.live-proof`.
