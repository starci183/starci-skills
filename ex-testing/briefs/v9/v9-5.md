# v9-5 — implement + run `uat.share.invite-and-collaborate` (todo)

Read `v9/_common.md` + `v7-11-REPORT.md`. Spec stub: `uat/flows/uat.share.invite-and-collaborate.spec.ts` — steps: owner creates task → invites editor + viewer → editor accepts + completes → viewer accepts + cannot complete.

Verify first: invite UI + accept flow exist? (`features/share/ui/invite` + impl records claim so). Needs both demo accounts (owner=demo, editor/viewer=demo2 or a third account — check keycloak realm-todo.json for a third seeded user; if only two accounts exist, note it and pick the closest honest walk). Missing → `APP_CAPABILITY_MISSING`. Present → walk + run + video + evidence + close `gap.share.live-proof`.
