# v9-8 — ec UAT harness (port the run-writer rig to ec-fe)

Read `v9/_common.md` + `v7-11-REPORT.md` + `v7-12-REPORT.md`. Scope: `examples/ecommerce-app-fe` — create the `uat/` rig mirroring `todo-app-frontend/uat/`: `lib/run-writer.ts`, `run-context.ts`, `steps.ts`, `flow-records.ts`, `paths.ts`, `playwright.config.ts`, `flows/` dir.

PORT, don't copy-paste blindly: read the todo rig fully first, adapt roots (ec tree is `ecommerce-app-be/.starciwork`), adapt baseURL (shop app port — find it in `apps/shop/package.json` dev script or next config), adapt accounts (`uat.identity.sign-in/accounts.yaml` uses `{role, username, password}` shape — no keycloak identity refs like todo). Add `uat` script to package.json. Write ONE smoke spec (`uat.smoke.spec.ts` visiting shop `/browse` and asserting the catalog renders — the one already-connected route per v7-12) to prove the rig works end-to-end: run it, confirm `runs/<id>/` writes screens+video+result.md under `ecommerce-app-be/.starciwork/features/.../uat/...` (match where todo writes runs — check `paths.ts` destination logic). Do NOT implement the two real flow specs — v9-9/v9-10 own them after v9-6/v9-7 land the features.
