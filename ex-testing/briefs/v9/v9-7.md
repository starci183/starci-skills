# v9-7 — ec-fe: cart persistence + checkout completion (real feature work)

Read `v9/_common.md` + `v7-12-REPORT.md`. Scope: `examples/ecommerce-app-fe` product code + verifying against `ecommerce-app-be` order service (`npm run start:order`).

Facts: shop `cart` route renders an honest empty state (persistence deferred); `checkout` renders a blocked state (session handoff + non-empty cart preconditions unsettled). ec-be HAS the order/cart capability (records: `cart/add-cart-item`, `cart/clear-cart`, `contract.checkout.order-for-identity` — `GET /internal/buyers/:personId` after v7-2's fix).

Build: (1) cart add/remove/view wired to the real be endpoints; (2) checkout confirm path → order created → visible on account page; (3) the refusal states the UAT record expects: empty-cart refused, beyond-stock refused naming product/qty/stock. Match existing module style; read `uat.checkout.place-order` record steps first — build what the record claims, not more. Verify end-to-end manually with curl/UI. Update records this proves (`ui.checkout.*` states, `gap.checkout.live-proof` narrowing). If a be endpoint is missing/broken → report `BE_CAPABILITY_MISSING` naming it, do not fake.
