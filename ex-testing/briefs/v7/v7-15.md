# read-v7-15 — topology re-audit (READ group — waits for v7-13)

READ-ONLY lane. Read `_common.md` + your group's prior reports `v6-3-REPORT.md`/`v6-4-REPORT.md`. **WAIT for `done/v7-13.done`** (the closer's summary).

Mission: re-run the v6-3 topology audit questions against the FIXED trees and answer honestly — did the fixes land semantically, or did they just clear the gate?

For each v6-3 finding, verify:
- Are the new FRs real statements describing actual code behavior, or filler written to satisfy "fr exists"?
- Did contract surfaces get corrected to real wire paths — re-grep the controllers.
- Are `[lang]` owner paths real (ls them)?
- Is `provenBy` actually gone (or does it persist in subtler form)?
- Did blockers get rerooted to real gaps/decisions, or repointed to another dead-end?
- NEW: did the fixes introduce new contradictions? (fr's added without checking they don't conflict with contracts; decisions authored that code doesn't honor.)

Method: same as v6-3 — read records AND the code they claim to describe. The point is cross-checking claims against reality, not re-running the gate (v7-13 did that).

Write `ex-testing/lint/v7-15-REPORT.md` — verdict per original finding (fixed / partially / still broken / regressed) + new issues found. Do NOT edit any tree file.
Done → `done/v7-15.done`.
