# v7-9 — todo-fe render re-capture (Phase 3 — start now)

Read `_common.md` + v6-3 §"render proofs". Scope: `todo-app-backend/.starciwork/features/**/ui/**` — captures, assets, and `ui/**/evidence.yaml`. You own ui records' evidence (v7-6 skips ui/).

Context: 47 `RENDER_CHECK_FAILED` across both trees — captures are stale (palette off-brand, pre-rebuild screenshots) + some `RENDER_PROOF_INCOMPLETE` needed `common` DNA (v6-2 authored `knowledge/grammars/common/DNA.yaml` — verify it exists and the render check now loads it).

Work:

1. Find the capture pipeline: `scripts/example-render-proof.mjs`, `checks/render.mjs`, `checks/brand.mjs`, `examples/todo-app-frontend/capture-share.mjs` + `verify-captures.mjs`. Read `--help`/source to learn invocation (likely: boots the fe app, screenshots declared ui states, writes `captures/` + updates ui evidence).
2. Bring the app up: todo-app-frontend needs its backend + keycloak + db. Check `src/tests/infra` / `E2EStackService` / docker-compose for the real topology — reuse it, don't invent ports.
3. Re-capture every `ui/*` record's declared states for the todo app. Each capture must pass `render.mjs`/`brand.mjs` checks — palette, tokens, layout.
4. Update `ui/**/evidence.yaml` via the tool (not by hand). Re-run gate → `RENDER_CHECK_FAILED`/`RENDER_PROOF_INCOMPLETE` for todo ui records → 0.
5. If a declared state can't be rendered (missing fixture, dead route), don't fake a capture — log it in report as unprovable.

This is a heavy lane — imagegen/capture calls are slow. Budget accordingly; a partial pass with honest gaps beats fabricated screens.
Done → `done/v7-9.done` + report.
