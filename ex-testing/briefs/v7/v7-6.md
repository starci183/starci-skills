# v7-6 — todo-be evidence re-proof (Phase 2 — wait for phase-1)

Read `_common.md` + v6-4 report §Q1-Q3. **WAIT for `done/v7-1.done`, `v7-3.done`, `v7-4.done`, `v7-5.done`** before re-proving (your digests hash index.yaml content those lanes are editing).

Scope: `todo-app-backend/.starciwork` — every `evidence.yaml` sibling of records EXCEPT `ui/**` (v7-9 owns) and `uat/**` (v7-11 owns).

Work:

1. List stale evidence: run the gate, collect `CODE_DIGEST_STALE` + `RECORD_DIGEST_STALE` refusals in your scope.
2. Re-proof each: `node scripts/example-evidence.mjs` (or `work-example.mjs` — check `--help`; the tool re-runs the record's assertions and rewrites evidence.yaml with fresh digests). Run from the right cwd.
3. For records whose `requiresProof`/assertion commands are dead (v7-1 fixed paths — if a command still can't run), DO NOT mark stale — leave evidence stale, flip record to `todo` if gate demands, and log the record id in your report under "unprovable".
4. After re-proof: re-run gate, confirm your scope's digest refusals → 0. Note remaining refusals verbatim in report.

Evidence digests cover `src/` code — do not edit product code to make digests pass. If code legitimately changed behavior vs the record's statements, flag as contradiction in report, don't paper over.
Done → `done/v7-6.done` + report.
