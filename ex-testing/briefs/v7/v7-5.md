# v7-5 — tree hygiene + payload/schema edges (Phase 1)

Read `_common.md` + v6-3 §"sanctioned layout". Scope: BOTH trees — root-level files/dirs, `assets/**` payloads, catalog `index.yaml`. (Root index.yaml is yours — v7-1/v7-2 are told not to touch it.)

Fix:

1. **Unsanctioned roots** — `business-rules/` empty dir in todo tree, `DIRECTIONS.md` at ec tree root. Delete the empty dir; move/remove `DIRECTIONS.md` content into a proper record or delete if stale prose. Anything else at tree root not in `work-layout.yaml`'s sanctioned list — same treatment; list each in report.
2. **Asset payloads refused as records** — `assets/generation-receipts.yaml`, `assets/direction-check.yaml` (×6 refusals `id is undefined`). These are payloads, not records. Correct fix: add an explicit payload marker the gate recognizes — check `check-example-work.mjs` for how it walks dirs; if it keys on `schema:`/`id:`, give payloads their real `schema: starci/generation-receipts@1` AND confirm gate skips non-`work/*` schemas; if it doesn't, note it in report (gate fix is out of your scope — do NOT fake record ids like the earlier fix did; revert fake ids if you find them).
3. **Catalog ↔ dirs** — root `index.yaml` (`work/catalog`) must list every `features/<name>/` dir and vice versa. Add/remove entries to match reality; verify `featureCatalog` path disagreement (`work-layout.yaml` says `features/index.yaml`, trees use `.starciwork/index.yaml`) — report, don't fix the schema file.
4. **`_resources` realm/repository naming** — coordinate boundary: v7-3 owns `_resources` files; you only flag inconsistencies you notice, don't edit.

Done → `done/v7-5.done` + report.
