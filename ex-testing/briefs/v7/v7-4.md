# v7-4 — edges, blockers, decisions (Phase 1)

Read `_common.md` + v6-3 §"edges"/"contradictions" + v6-4 Q4. Scope: BOTH trees — `blockedBy`, `conflictsWith`, `refs`, `gap/`, `decision/` records. Index.yaml edits only.

Fix:

1. **BLOCKER_UNROOTED ×3** — `blockedBy` chains ending at `br.task.single-owner` (todo-because-evidence, not a gap). Reroot: either the chain genuinely ends at an existing `gap.*`/`decision.*` (repoint), or author the missing `gap`/`decision` record describing the real blocker. Do not point blockedBy at a record that's merely unfinished.
2. **`conflictsWith` tensions without decisions** — `data.audit.log-line` vs `br.task.delete.final` (retention vs stays-gone) is recorded as accepted tension in prose only. Author a `decision/` record that resolves it explicitly (which rule wins, under what condition — read `audit-log.service` + `delete-task.handler` to ground it). Same for the plan-cap FR/contract tension in ec-be (coordinate: v7-2 fixes the fr; you author the decision if one is needed).
3. **`conflictsWith` misuse** — v6-3 found `conflictsWith` used to mean "not implemented" rather than actual contradiction. Convert those to `gap` records or drop the edge; list each in your report.
4. **`fr.login.sign-out` composing a sign-in-only rule** — repoint `composes` to the rule that actually constrains sign-out, or author the missing br if none exists.
5. **`proves`/`refs` sanity** — spot-check every edge your edits touch resolves to the semantically right record (not just an existing id).

Do NOT create evidence.yaml or mark anything stale. Your fixes change index.yaml digests — that's expected; evidence lanes run after you.
Done → `done/v7-4.done` + report listing every edge you added/removed/rerooted.
