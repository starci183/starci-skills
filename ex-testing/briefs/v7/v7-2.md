# v7-2 — ec-be record content (Phase 1)

Read `_common.md` + v6-3/v6-4 reports first. Scope: `ecommerce-app-be/.starciwork` — all record families EXCEPT `ui/**`, `uat/**`, `assets/**`, `evidence.yaml` files.

Fix:

1. **Contract wire paths** — `contract.checkout.order-for-identity` declares `GET /buyers/:personId` but code serves `GET /internal/buyers/:personId` (`buyer.controller.ts` `@Controller("internal/buyers")`, `order.client.ts` calls `/internal/buyers/...`). Fix the contract record to the real wire shape. Sweep ALL contract surface declarations vs controllers/resolvers.
2. **Missing FRs** — cart add/clear/query mutations ship in code without records. Create `fr/cart/*` (+ac) mirroring real resolver behavior.
3. **Plan-cap contradiction** — `fr.task.create`-equivalent: an `fr` record missing a precondition that contract+code enforce. Resolve by editing the record to state the precondition (code is truth here), or author a `decision/` record if it's genuinely contested.
4. **`provenBy` cleanup** — same rule as v7-1: remove hand-authored blocks.
5. **Any remaining `apps/order|identity/...` owner/module paths** — v6-1 did a first pass; verify zero remain in your scope (gate `OWNER_PATH_MISSING` should be 0 for ec-be).

Do NOT touch: `ui/`, `uat/`, `evidence.yaml`, `_derived/`, `_resources/`, root `index.yaml`.
Done → write `done/v7-2.done` + report.
