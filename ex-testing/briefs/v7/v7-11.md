# v7-11 — todo UAT runs (Phase 3 — start now)

Read `_common.md`. Scope: `todo-app-backend/.starciwork/features/**/uat/**` — `runs/` artifacts + `uat/**` evidence (v7-6 skips uat/).

Context: `uat.task.create` is `state: todo` yet `fr.task.create` claims `provenBy.uat: [uat.task.create]` — a false proof claim (v7-1 removes the claim; you produce the actual proof). UAT tooling is real: `examples/todo-app-frontend/uat/flows/*.spec.ts` (7 Playwright flows) + `uat/lib/run-writer.ts` writes `runs/<runId>/{screens,videos,result.md}`.

Work:

1. Read `uat/playwright.config.ts` + `uat/lib/*` — learn how runs produce screens+videos+result.md, and what a "settled run" needs per `schemas/work-uat-flow.schema.yaml`.
2. Bring the stack up for real: backend api + keycloak + postgres + frontend. Reuse `src/tests/infra`/`E2EStackService`/docker-compose — the apps already boot for e2e.
3. Run the UAT flows with video+screenshot capture (`video: 'on'` or whatever run-writer expects). Each `uat-flow` record needs a settled `runs/<runId>/` with `result.md` recording `outcome: pass`.
4. Cover ALL uat records in the todo tree that code can actually satisfy — `uat.task.create` is the critical one (fr.task.create depends on it). Sign-in/out, share invite, recur, plan-cap, notify digest, audit erasure — run each that has a spec.
5. Flows that fail because the app genuinely can't do them → leave uat `todo`, write a `gap.*` record naming the missing capability. Never edit a run's result.md to pass.

This is the heaviest lane — full stack + playwright + video. If the stack won't boot, document exactly where it fails rather than stubbing.
Done → `done/v7-11.done` + report.
