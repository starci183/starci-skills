# v7-12 — ec UAT runs (Phase 3 — start now)

Read `_common.md`. Scope: `ecommerce-app-be/.starciwork/features/**/uat/**` — `runs/` + `uat/**` evidence (v7-7 skips uat/).

Same procedure as v7-11 (read its brief) but for ecommerce:

1. Check whether `ecommerce-app-fe/uat/` exists — if ec has no UAT spec suite yet, that's a finding: report it, and only settle uat records that have executable flows. Do NOT write Playwright specs as part of this lane (that's app work) — but DO record which uat records are unprovable for lack of specs.
2. ec-be is now a single merged `src/` app — verify boot topology before running.
3. Settled runs need `runs/<runId>/{screens,videos,result.md}` with `outcome: pass`.
4. Failures → gap records, never edited results.

Done → `done/v7-12.done` + report.
