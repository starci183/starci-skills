# v7-8 — `_derived/` rebuild both trees (Phase 2 — wait for phase-1)

Read `_common.md`. **WAIT for `done/v7-1.done`..`v7-5.done`** — derived indexes must reflect final record content.

Scope: `_derived/**` in BOTH trees (todo-be + ec-be).

Work:

1. Find the derive tool: `node scripts/check-example-derived.mjs` checks freshness; look for `example-derive.mjs` / a `--write`/`--regen` flag or sibling script that writes `_derived/` (check `scripts/` listing + `--help`). If only a checker exists and no writer, read the checker to learn the expected shape, then regenerate by hand per that shape — and flag "no derive writer exists" prominently in your report.
2. Rebuild `_derived/index.yaml` (+ any per-feature derived files) for both trees.
3. Run `check-example-derived.mjs` → must be clean for both trees.
4. Also verify: does `_derived` include `proves` edges? v6-4 noted they're filed under `unclassifiedEdges` — if so, that's a derive-tool gap; report it, don't hand-patch the output to look complete.

Done → `done/v7-8.done` + report.
