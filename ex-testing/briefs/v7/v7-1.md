# v7-1 — todo-be record content (Phase 1)

Read `_common.md` + v6-3/v6-4 reports first. Scope: `todo-app-backend/.starciwork` — all record families EXCEPT `ui/**`, `uat/**`, `assets/**`, `evidence.yaml` files (other lanes own those).

Fix, in `features/task` (and `login`, `plan`, `share`, `audit`, `notify`, `recur` as findings apply):

1. **Missing FRs for shipped capability** — code has delete-task + list/count handlers but no `fr` record. Create `fr/task/delete/` and `fr/task/list/` (+ `ac/` children where the code enforces something checkable). Statements must describe real behavior — read the handlers first, don't invent.
2. **`composes[].module` dangling paths** — e.g. `fr.task.complete` points at `src/features/complete-task/**`, `src/share/access.service.ts`, `src/modules/domain/task/**`, none of which exist. Rewrite to real paths under `src/modules/`. Sweep EVERY `composes[].module` and `module:` field in your scope.
3. **`requiresProof` commands** — replace `npm run test:e2e -- tasks/complete`-style commands with real spec paths under `src/tests/e2e/` (verify each file exists). Fix `unit.forEach`/spec globs the same way.
4. **Event records** — `event.login.signed-in` / `signed-out` are `todo` but code emits them. Set state honestly: if producers/consumers exist in code, wire the record correctly and leave state for the evidence lane to promote.
5. **`provenBy`** — remove hand-authored `provenBy` blocks (per the model they are kernel-derived; gate doesn't check them, so they're lies waiting to happen). Note each removal in your report.

Do NOT touch: `ui/`, `uat/`, `evidence.yaml`, `_derived/`, `_resources/`, `index.yaml` catalog root.
Done → write `done/v7-1.done` + report.
