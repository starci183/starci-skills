# v7-3 — FE impl owner paths + resource naming (Phase 1)

Read `_common.md` + v6-3 report §3. Scope: BOTH trees, only `impl/<fe-app>/**/index.yaml` records + `_resources/**` files. No `evidence.yaml`, no `ui/`, no `uat/`.

Fix:

1. **`[lang]` route ownership** — 5 `done` fe impl records in todo-be tree own `src/app/tasks` etc., but real routes are `src/app/[lang]/...`. Verify each `owners[].path` exists on disk (`ls`), rewrite to real paths. Same sweep in ec-be tree (`ecommerce-app-fe` routes).
2. **`repository` field consistency** — impl dirs say `todo-app-frontend`/`ecommerce-app-fe`; check `repository:` fields and `.workspaces`/`workspace.yaml` entries agree. Report any disagreement, fix record-side.
3. **Realm naming** — todo `_resources` declares `realms/todo-app`; code uses `realms/todo`. Fix the resource record to match code (code is truth), and note it: a wrong realm target that still answers 200 is a silent hazard.
4. **Absolute paths in `inputRefs`** — v6-4 found `C:/Users/...` leaked into ui asset inputRefs. If any leak into YOUR owned files, normalize to repo-relative.

Verify: `node scripts/check-example-work.mjs` — your owned records must not add refusals. Report count delta.
Done → `done/v7-3.done` + report.
