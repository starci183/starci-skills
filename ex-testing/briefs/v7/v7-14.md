# v7-14 — mechanical residual sweep (Phase 4 — wait for phase-1 only)

Read `_common.md`. **WAIT for `done/v7-1.done`..`v7-5.done`** (records must be final before you touch paths/digests that reference them). Do NOT wait for the heavy lanes.

Scope: mechanical residuals BOTH trees — the stuff that's pure path/string consistency, no judgment.

Fix:

1. Any `module:`/`owners[].path`/`composes[].module`/`inputRefs` strings still pointing at non-existent paths — the gate warns on owners but `composes[].module` is unchecked; sweep it yourself with `ls`/`Test-Path` on every value.
2. `repository:` fields vs actual sibling dir names (`todo-app-backend` vs `todo-app` mismatch class) — make them match reality.
3. `schema:` declarations vs actual record shape — any record claiming a schema whose required fields it lacks (spot-check, gate may not enforce all).
4. Absolute machine paths anywhere in tree yaml → repo-relative.
5. Anything else the gate still refuses/warns that's a pure mechanical fix — don't do judgment calls (that's v7-13); if a refusal needs a decision, report it, don't decide.

Done → `done/v7-14.done` + report.
