# v7-7 — ec-be evidence re-proof (Phase 2 — wait for phase-1)

Read `_common.md` + v6-4 report. **WAIT for `done/v7-2.done`, `v7-3.done`, `v7-4.done`, `v7-5.done`** before re-proofing.

Scope: `ecommerce-app-be/.starciwork` — every `evidence.yaml` EXCEPT `ui/**` (v7-10) and `uat/**` (v7-12).

Work: identical procedure to v7-6:

1. Collect `CODE_DIGEST_STALE`/`RECORD_DIGEST_STALE` refusals in scope (v6-1 fixed owner paths but left evidence stale — expect ~8+ records).
2. Re-proof via `node scripts/example-evidence.mjs`/`work-example.mjs` from ec-be cwd.
3. Dead assertion commands → don't mark stale, leave + report as "unprovable".
4. Re-run gate; confirm scope digest refusals → 0.

Watch for: records whose `owners[].path` was rewritten `apps/* → src/*` — evidence `files[]` may pin old paths; regenerate rather than hand-editing digests.
Done → `done/v7-7.done` + report.
