# v7-13 — final gate verify + residual fixes (Phase 4 — waits for all)

Read `_common.md` + both v6 audit reports. **WAIT for `done/v7-1.done` through `v7-12.done`** (v7-9..v7-12 are heavy; if timeout hits, proceed and note which markers were missing).

Scope: whole-gate residual. You are the closer.

Work:

1. Run `node scripts/check-example-work.mjs` on both trees; diff refusals against the audit's baseline (~200). Anything the phase lanes left → fix it yourself if it's a small record edit (missing ref, bad path, wrong state); if it needs a capture/UAT run that failed, write a `gap.*` record and report it — don't fabricate.
2. `check-example-derived.mjs` clean on both trees.
3. Confirm zero: `OWNER_PATH_MISSING`, `CODE_DIGEST_STALE`, `RECORD_DIGEST_STALE`, `RENDER_CHECK_FAILED`, `RENDER_PROOF_INCOMPLETE`, `BLOCKER_UNROOTED`, payload `id is undefined`, `PROOF_NOT_REPLAYABLE`, `PROVES_TARGET_NOT_DONE` where the target genuinely is done.
4. Sanity contradictions: re-check the v6-3 headline items are actually gone (contract path, `[lang]` owners, provenBy removed, missing FRs now exist).
5. Write a `v7-FLEET-SUMMARY.md`: baseline→final refusal counts by class, what each lane landed, what remains genuinely unprovable, and whether the trees now answer "what does the code actually do" truthfully.

Done → `done/v7-13.done` + report.
