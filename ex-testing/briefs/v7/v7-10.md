# v7-10 — ec-fe render re-capture (Phase 3 — start now)

Read `_common.md` + v6-3 §"render proofs". Scope: `ecommerce-app-be/.starciwork/features/**/ui/**` — captures, assets, `ui/**/evidence.yaml` (v7-7 skips ui/).

Identical procedure to v7-9 (read its brief for context) but for the ecommerce app:

1. `scripts/example-render-proof.mjs` + `checks/render.mjs`/`brand.mjs`; ecommerce-app-fe captures live under `examples/ecommerce-app-fe/captures`.
2. ec-be topology differs: identity + order are merged into one `src/` app now — verify how the fe talks to it (env/config) before booting.
3. Re-capture all declared ui states; update evidence via tooling.
4. `common` DNA: v6-2 authored `knowledge/grammars/common/DNA.yaml` — verify render check loads it for ec brand (`family: common`).
5. Unprovable states → report, never fabricate.

Done → `done/v7-10.done` + report.
