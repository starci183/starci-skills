# v11-4 — evidence/run consolidation audit (both trees)

Read v11/_common.md (WAIT GATE). Scope: `evidence.yaml` + `runs/` layout across both trees.
Goal: one manifest per settled run — verify every `runs/<id>/` has exactly {manifest.yaml, result.md, screens/, videos/} and nothing stray; stray files → move into the manifest or report. Check every `evidence.yaml` references its run(s) by relative path that exists. Do NOT merge evidence into index.yaml yet (digest churn) — just produce the consolidation map + fix strays. Report: runs audited, strays fixed, evidence↔run link integrity.
