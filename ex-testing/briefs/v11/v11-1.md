# v11-1 — collapse ac/ sub-records: todo-app-backend tree

Read v11/_common.md first (WAIT GATE applies). Scope: `examples/todo-app-backend/.starciwork` only.
Walk every `features/**/ac/*/index.yaml`; for each: merge its statements/assertions into the parent record's `acceptance:` (or `statements:` if no acceptance field — match existing field shapes) as `id: <ac-id>` entries. Delete the ac file only after the parent carries its content AND all inbound refs are remapped. Report: counts before/after, any ac kept separate (with reason).
