# v11-3 — strip authored derived-fields (both trees)

Read v11/_common.md (WAIT GATE). Scope: BOTH trees' index.yaml files.
Remove hand-authored `provenBy:` blocks entirely (the deep check derives them; authored claims are the bug class). For `state:` fields keep authored todo/inprogress/done/uninvestigate — that's intent, not derivation. Where a record has a `derived:` block an agent wrote, strip it too. Where removing a field would break the current gate (check `check-example-work.mjs` for required-field reads on those keys first), leave the field and add a comment `# derived — do not author` instead. Report what was stripped vs comment-marked, per tree.
