# v11-5 — update checks for the compact format (LAST)

Read v11/_common.md (WAIT GATE + also wait for v11-1..4 markers). Scope: `scripts/check-*.mjs`, `scripts/example-*.mjs`.
After the tree compacts: update every check so an inline `acceptance:` entry resolves like a former ac record (refs `parent#ac-id` must resolve), `provenBy` absence is not flagged (its presence IS flagged — authored provenBy stays refused), and record-count expectations in any check output cope with fewer files. Re-run: gate + deep + artifacts + surfaces + consistency on both trees; produce `v11-FINAL-STATUS.md` with before/after counts for every check. No check may go silent — every former check must still run and report.
