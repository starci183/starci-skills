# Lane E2E-02 — todo task flows

SCOPE (exclusive): `D:/Repositories/starci-academy-backend/.claude/D:/Repositories/starci-academy-backend/.claude/examples/todo-app-backend/test/e2e/task/`

Specs: `task-lifecycle.e2e-spec.ts` (sign-in → create → list → complete → reopen → counts → sign-out), `task-isolation.e2e-spec.ts` (two users, verify cross-user invisibility — out-of-band verify via E2EDbService where API can't see it). Complete journeys only.
