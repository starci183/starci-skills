# v8-2 — `scripts/check-work-surfaces.mjs` (contract↔code surface truth)

check-work-deep.mjs has heuristic SUSPECT surface checks (UNCLAIMED_SURFACE, GHOST_SURFACE, CAPABILITY_WITHOUT_*). Your lane is the rigorous version, standalone.

Script: `scripts/check-work-surfaces.mjs`

Checks:
- **HTTP surface diff**: extract every NestJS route (`@Controller` prefix + `@Get/@Post/...` subs, including bare decorators and `@All`) from each bound repo's `src/`. Extract every contract record's declared http surface (both `surface.http {method,path}` objects and `surface.shape`/`requests` text entries matching `METHOD /path`). Diff both ways:
  - declared-not-served → REFUSE `CONTRACT_GHOST_ROUTE` (the `/buyers` vs `/internal/buyers` class — a done contract describing a wire that doesn't exist)
  - served-not-declared → SUSPECT `UNDECLARED_ROUTE` (some routes are legitimately internal/ops — `/health` is real-but-uninteresting; report it but explain the tier choice)
- **GraphQL ops**: enumerate `graphql/{queries,mutations}/<cap>/<op>/` dirs (they nest under `src/features/<f>/graphql/`) + `@Query/@Mutation` decorated methods; cross-check against contract records that declare graphql surfaces and against impl `proves` coverage.
- **FE routes**: `src/app/**/page.tsx` in each `role: fe` repo → route list (normalize `[lang]`); compare against `ui-screen` records' route/entry claims → `UI_ROUTE_GHOST`/`UI_ROUTE_UNDECLARED`.
- **Events**: `class XxxEvent` declarations + `@EventsHandler`/subscriber registrations vs `work/event` records both directions (declared-not-emitted, emitted-not-declared). Event class naming is inconsistent (feature prefix sometimes dropped) — handle both mappings before suspecting.
- Normalization: `:param` segments compare positionally (`/buyers/:personId` == `/buyers/:id`); trailing slashes, method casing normalized.
- Emit a `SURFACE MAP` section in output: per repo, count routes/ops/fe-routes/events and how many are claimed — the audits' "the tree must explain every public behavior" made measurable.

Done → `done/v8-2.done` + report.
