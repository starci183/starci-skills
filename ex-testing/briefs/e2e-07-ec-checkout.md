# Lane E2E-07 — ecommerce checkout journey

SCOPE (exclusive): `D:/Repositories/starci-academy-backend/.claude/D:/Repositories/starci-academy-backend/.claude/examples/ecommerce-app-be/test/e2e/checkout/`

Specs: `checkout-journey.e2e-spec.ts` (register → browse catalog → add to cart → checkout → pay → order confirmed → cart empty), `payment-failure.e2e-spec.ts` (payment declined → order pending → retry → success or cancel per api contract). Code against E2E-06 contract (ANALYSIS.md §5).
