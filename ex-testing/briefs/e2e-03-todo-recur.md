# Lane E2E-03 — todo recur flows

SCOPE (exclusive): `D:/Repositories/starci-academy-backend/.claude/D:/Repositories/starci-academy-backend/.claude/examples/todo-app-backend/test/e2e/recur/`

Specs: `recurrence-lifecycle.e2e-spec.ts` (create task → make-recurring → upcoming occurrences → edit rule → occurrences reflect edit → end rule → no new occurrences). One journey per spec, sequential steps inside the `it`.
