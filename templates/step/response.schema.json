{
  "$id": "step:response",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "operatorId",
    "step",
    "parallel",
    "status",
    "fields",
    "fallbacks",
    "commits",
    "next"
  ],
  "properties": {
    "interaction": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "decisionId", "options"],
      "properties": {
        "kind": { "type": "string", "description": "Allowed question kinds come from resources/interaction.json." },
        "decisionId": { "type": "string", "minLength": 1 },
        "options": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["id", "label", "tradeoff"],
            "properties": {
              "id": { "type": "string", "minLength": 1 },
              "label": { "type": "string", "minLength": 1 },
              "tradeoff": { "type": "string", "minLength": 1 }
            }
          }
        }
      }
    },
    "schemaVersion": {
      "const": 9
    },
    "operatorId": {
      "type": "string",
      "pattern": "^[a-z]+(?:\\.[a-z]+)+$"
    },
    "step": {
      "type": "integer",
      "minimum": 1
    },
    "parallel": {
      "type": "integer",
      "minimum": 1
    },
    "exchange": {
      "type": "string",
      "pattern": "^[a-z][a-z-]*$"
    },
    "status": {
      "enum": [
        "running",
        "done",
        "blocked",
        "waiting"
      ],
      "description": "running is the skeleton the orchestrator writes at dispatch and never routes; the agent replaces it with done, blocked or waiting, and an agent that exits leaving it in place is RECEIPT_MISSING"
    },
    "stop": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9_]+$"
    },
    "awaiting": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "exchange",
        "kind"
      ],
      "properties": {
        "exchange": {
          "type": "string",
          "pattern": "^[a-z][a-z-]*$"
        },
        "kind": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        }
      }
    },
    "fallbacks": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[A-Z][A-Z0-9_]+$"
      },
      "uniqueItems": true
    },
    "fields": {
      "type": "object",
      "additionalProperties": {
        "oneOf": [
          {
            "type": "string",
            "pattern": "^(?:[a-z][a-z-]*/)?response/(?:(?:[a-z][a-z-]*/)*[A-Za-z0-9_.-]+\\.md|data/[A-Za-z0-9_./-]+\\.json|artifacts/[A-Za-z0-9_./-]+)$"
          },
          {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "pattern": "^(?:[a-z][a-z-]*/)?response/(?:data/[A-Za-z0-9_./-]+\\.json|artifacts/[A-Za-z0-9_./-]+)$"
            }
          }
        ]
      }
    },
    "commits": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[0-9a-f]{7,40}$"
      }
    },
    "next": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^(?:[a-z]+(?:\\.[a-z]+)+|user|external)$"
      },
      "uniqueItems": true
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2000,
      "description": "One paragraph for a person: why the branch blocked or waits, when the kind files could not be written"
    },
    "boundProfile": {
      "type": "string",
      "pattern": "^[a-z][a-z-]*$",
      "description": "The profile operator.json binds; recorded with ranProfile when the processor ran a stand-in (resources/orchestrator.json#profileEquivalents)"
    },
    "ranProfile": {
      "type": "string",
      "pattern": "^[a-z][a-z-]*$",
      "description": "The profile that actually ran the branch"
    },
    "goalCheck": {
      "description": "The agent's own check against the branch goal (request.json.goal.doneWhen): whether the done-when line is now evidenced, and by which of this response's declared files. Required on a done response whose request carried goal.doneWhen; every evidence path is one the fields list and exists on disk; achieved true needs at least one (scripts/validate-response.mjs#goalCheckErrors). Only a validator-accepted goalCheck reaches state.json.brief.proven.",
      "type": "object",
      "additionalProperties": false,
      "required": ["achieved", "evidence"],
      "properties": {
        "achieved": { "type": "boolean" },
        "evidence": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^(?:[a-z][a-z-]*/)?response/(?:(?:[a-z][a-z-]*/)*[A-Za-z0-9_.-]+\\.md|data/[A-Za-z0-9_./-]+\\.json|artifacts/[A-Za-z0-9_./-]+)$"
          }
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "status": {
            "const": "blocked"
          }
        }
      },
      "then": {
        "required": [
          "stop"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "status": {
            "const": "waiting"
          }
        }
      },
      "then": {
        "required": [
          "awaiting"
        ]
      }
    }
  ]
}
