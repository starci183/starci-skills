# identity.provision

Converges one approved account set at a supplied provider and proves real product sign-in. It records
stable references, roles and memberships without credential values. Uncertain provider effects are
observed before retry so a recovery cannot create duplicate accounts.
