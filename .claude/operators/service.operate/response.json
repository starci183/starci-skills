{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "service.operate response",
  "$ref": "works:response",
  "description": "A done response contains one service receipt binding the declaration and authority, desired and observed states, declared probe, holder when up, port-free proof when down, and whether the action was a proved no-op.",
  "allOf": [
    {
      "if": {"properties": {"status": {"const": "done"}}, "required": ["status"]},
      "then": {"properties": {
        "outputs": {"contains": {"properties": {"id": {"const": "service-receipt"}, "type": {"const": "file"}, "file": {"properties": {"mediaType": {"const": "application/json"}}}}, "required": ["id", "type", "file"]}},
        "observations": {"not": {"contains": {"properties": {"result": {"const": "failed"}}, "required": ["result"]}}}
      }}
    }
  ]
}
