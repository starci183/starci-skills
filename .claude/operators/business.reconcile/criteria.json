{
  "limits": { "feedbackRounds": 2 },
  "history": "Retain every provenance, delivered claim, matrix or publication defect with exact evidence and changed input. Never republish or reassess unchanged failing delivery as progress.",
  "items": [
    { "id": "request-valid", "expect": "The request schema passes.", "check": { "kind": "machine", "ref": "request-shape" }, "onFail": { "action": "ask", "fromStep": "bind-delivery-authority", "instruction": "Ask for the malformed request field." } },
    { "id": "files-valid", "expect": "Delivered and authority files match their digests.", "check": { "kind": "machine", "ref": "request-files" }, "onFail": { "action": "ask", "fromStep": "bind-delivery-authority", "instruction": "Ask for the missing or changed input artifact." } },
    { "id": "delivery-bound", "expect": "Delivered receipt, source role and frozen checkout head agree.", "check": { "kind": "review", "ref": "delivered-provenance" }, "onFail": { "action": "ask", "fromStep": "bind-delivery-authority", "instruction": "Name the exact provenance mismatch and request the correct delivered receipt." } },
    { "id": "delivered-claims-grounded", "expect": "Every delivery claim binds actual source evidence and contradictions remain discrepancies.", "check": { "kind": "review", "ref": "claims-delivered" }, "onFail": { "action": "retry", "fromStep": "extract-delivered-claims", "instruction": "Correct or remove the unsupported claim and preserve any contradiction as a discrepancy." } },
    { "id": "matrix-fully-compared", "expect": "Every frozen matrix row has delivered evidence or a concrete discrepancy, including unchecked limits.", "check": { "kind": "review", "ref": "matrix-reconciled" }, "onFail": { "action": "retry", "fromStep": "compare-matrix", "instruction": "Repair the omitted comparison row; a standing discrepancy remains mismatch." } },
    { "id": "republication-valid", "expect": "Only a discrepancy-free legal reconcile model is republished once.", "check": { "kind": "review", "ref": "republication-legal" }, "onFail": { "action": "block", "fromStep": "republish-reconciled-head", "instruction": "Record the lifecycle or authority conflict and publish no model." } },
    { "id": "response-valid", "expect": "The response schema passes.", "check": { "kind": "machine", "ref": "response-shape" }, "onFail": { "action": "retry", "fromStep": "emit-reconciliation", "instruction": "Repair only the response shape; do not republish." } },
    { "id": "response-bound", "expect": "The response binds the request revision.", "check": { "kind": "machine", "ref": "request-binding" }, "onFail": { "action": "retry", "fromStep": "emit-reconciliation", "instruction": "Correct the request binding; do not republish." } },
    { "id": "outputs-unique", "expect": "Output ids are unique.", "check": { "kind": "machine", "ref": "unique-outputs" }, "onFail": { "action": "retry", "fromStep": "emit-reconciliation", "instruction": "Remove the duplicate output record." } },
    { "id": "artifacts-valid", "expect": "Output artifacts are scoped and digest-bound.", "check": { "kind": "machine", "ref": "output-files" }, "onFail": { "action": "retry", "fromStep": "emit-reconciliation", "instruction": "Repair the invalid artifact record without republishing." } },
    { "id": "observations-valid", "expect": "Observations bind criteria and evidence.", "check": { "kind": "machine", "ref": "observation-binding" }, "onFail": { "action": "retry", "fromStep": "emit-reconciliation", "instruction": "Correct the invalid observation without republishing." } },
    { "id": "reconcile-input-present", "expect": "Feature, source role, target, delivered receipt and authority are available.", "check": { "kind": "event", "ref": "essential-input-missing" }, "onFail": { "action": "ask", "fromStep": "bind-delivery-authority", "instruction": "Ask for the exact missing delivery or authority input." } },
    { "id": "capabilities-present", "expect": "Required source and publication capabilities are available.", "check": { "kind": "event", "ref": "capability-unavailable" }, "onFail": { "action": "block", "fromStep": "extract-delivered-claims", "instruction": "Name the unavailable capability and publish nothing." } },
    { "id": "transient-complete", "expect": "Transient execution completes without duplicate republication.", "check": { "kind": "event", "ref": "transient-execution-failure" }, "onFail": { "action": "retry", "fromStep": "republish-reconciled-head", "instruction": "Re-read the content address and current head, then perform only a missing idempotent publication action." } }
  ],
  "stopWhen": "Return done only when every criterion passes and the accepted model is republished exactly once. A standing discrepancy returns mismatch with reconciliation and claims but no model. After two failed rounds or authority conflict, block without publication."
}
