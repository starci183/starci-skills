{
  "limits": { "feedbackRounds": 2 },
  "history": "Retain each concrete uncovered family, collision, target defect or rollback defect with the units changed and reassessment result. Do not retry unchanged seed definitions.",
  "items": [
    { "id": "request-valid", "expect": "The request schema passes.", "check": { "kind": "machine", "ref": "request-shape" }, "onFail": { "action": "ask", "fromStep": "bind-data-goal", "instruction": "Ask for the malformed request field." } },
    { "id": "files-valid", "expect": "Declared planning files exist and match digests.", "check": { "kind": "machine", "ref": "request-files" }, "onFail": { "action": "ask", "fromStep": "bind-data-goal", "instruction": "Ask for the missing or changed plan input." } },
    { "id": "families-covered", "expect": "Every required flow and data family has exactly one seed unit.", "check": { "kind": "review", "ref": "coverage-complete" }, "onFail": { "action": "retry", "fromStep": "compose-seed-units", "instruction": "Name the missing or extra unit and repair coverage without pre-creating tested outcomes." } },
    { "id": "namespaces-disjoint", "expect": "Namespaces and cleanup ownership are disjoint.", "check": { "kind": "review", "ref": "namespace-isolation" }, "onFail": { "action": "retry", "fromStep": "compose-seed-units", "instruction": "Rename the colliding namespace and update only its fixture and rollback references." } },
    { "id": "contracts-complete", "expect": "Each seed unit has valid targets, attribution, volume, read-back and rollback.", "check": { "kind": "review", "ref": "seed-contract-complete" }, "onFail": { "action": "retry", "fromStep": "compose-seed-units", "instruction": "Repair the named unit field from observed backend declarations." } },
    { "id": "response-valid", "expect": "The response schema passes.", "check": { "kind": "machine", "ref": "response-shape" }, "onFail": { "action": "retry", "fromStep": "emit-seed-plan", "instruction": "Repair only the response shape." } },
    { "id": "response-bound", "expect": "The response binds the request revision.", "check": { "kind": "machine", "ref": "request-binding" }, "onFail": { "action": "retry", "fromStep": "emit-seed-plan", "instruction": "Correct the request binding." } },
    { "id": "outputs-unique", "expect": "Output ids are unique.", "check": { "kind": "machine", "ref": "unique-outputs" }, "onFail": { "action": "retry", "fromStep": "emit-seed-plan", "instruction": "Remove the duplicate output id." } },
    { "id": "artifacts-valid", "expect": "Output files are scoped and digest-bound.", "check": { "kind": "machine", "ref": "output-files" }, "onFail": { "action": "retry", "fromStep": "emit-seed-plan", "instruction": "Repair the invalid artifact record." } },
    { "id": "observations-valid", "expect": "Observations bind request criteria and evidence.", "check": { "kind": "machine", "ref": "observation-binding" }, "onFail": { "action": "retry", "fromStep": "emit-seed-plan", "instruction": "Correct the invalid observation." } },
    { "id": "goal-present", "expect": "Mission goal, feature and target environment are available.", "check": { "kind": "event", "ref": "essential-input-missing" }, "onFail": { "action": "ask", "fromStep": "bind-data-goal", "instruction": "Ask for the exact missing planning input." } },
    { "id": "capabilities-present", "expect": "File and source inspection capabilities are available.", "check": { "kind": "event", "ref": "capability-unavailable" }, "onFail": { "action": "block", "fromStep": "inspect-data-boundaries", "instruction": "Name the unavailable capability and target evidence it prevents." } },
    { "id": "transient-complete", "expect": "Transient file operations complete.", "check": { "kind": "event", "ref": "transient-execution-failure" }, "onFail": { "action": "retry", "fromStep": "inspect-data-boundaries", "instruction": "Retry only the interrupted read or artifact write once." } }
  ],
  "stopWhen": "Return done only when every criterion passes. After two failed feedback rounds, return mismatch with the remaining concrete seed defects and no partial plan; never execute data changes."
}
