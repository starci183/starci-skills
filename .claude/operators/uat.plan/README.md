# uat.plan

Turn each mission journey into one bounded UAT flow with explicit access, fixture and source-role prerequisites. The human plan, machine case sheet and units stay aligned. Planning performs no browser, identity or data action.

Run the repository operator build, then `python tests/test_contract.py` from this directory.
