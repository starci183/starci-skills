{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "uat.plan response",
  "$ref": "works:response",
  "properties": { "outputs": { "items": { "properties": { "type": { "enum": ["text", "file"] } } } } },
  "allOf": [{ "if": { "properties": { "status": { "const": "done" } }, "required": ["status"] }, "then": { "properties": {
    "outputs": { "allOf": [
      { "contains": { "properties": { "id": { "const": "uat-plan" }, "type": { "const": "file" }, "file": { "properties": { "mediaType": { "const": "text/markdown" } } } }, "required": ["id", "type", "file"] } },
      { "contains": { "properties": { "id": { "const": "uat-case-sheet" }, "type": { "const": "file" }, "file": { "properties": { "mediaType": { "const": "application/json" } } } }, "required": ["id", "type", "file"] } },
      { "contains": { "properties": { "id": { "const": "units" }, "type": { "const": "file" }, "file": { "properties": { "mediaType": { "const": "application/json" } } } }, "required": ["id", "type", "file"] } }
    ] }, "observations": { "not": { "contains": { "properties": { "result": { "const": "failed" } }, "required": ["result"] } } }
  } } }],
  "description": "A done response contains the human UAT plan, machine case sheet and matching flow units. A journey without a valid entry remains mismatch and emits no partial plan."
}
