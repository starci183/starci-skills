# library.update

Repairs one authorized package with a paired regression, reproducible archive and exact consumer
proof. Owner and consumer mutations have separate ceilings. Registry publication occurs only when
explicitly requested, and an uncertain publish is observed by version and integrity before retry.
