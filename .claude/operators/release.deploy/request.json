{
  "$schema":"https://json-schema.org/draft/2020-12/schema",
  "title":"release.deploy request",
  "$ref":"works:request",
  "properties":{"operation":{"const":"release.deploy"},"expected":{"minItems":1},"inputs":{"properties":{"files":{"minItems":1}}}},
  "description":"Bind passing quality evidence, immutable release and manifest digests, one target, existing deploy authorization, steady-state probes and deadline, exact rollback identity, and migration proof when required."
}
