{
  "limits": { "feedbackRounds": 2 },
  "history": "Retain each check id, status, evidence, wall and owner action. Reassess only when its declaration, process or host observation changes; never loop on unchanged walls.",
  "items": [
    { "id": "request-valid", "expect": "The request schema passes.", "check": { "kind": "machine", "ref": "request-shape" }, "onFail": { "action": "ask", "fromStep": "bind-preflight-scope", "instruction": "Ask for the malformed request field." } },
    { "id": "files-valid", "expect": "Declared readiness files exist and match digests.", "check": { "kind": "machine", "ref": "request-files" }, "onFail": { "action": "ask", "fromStep": "bind-preflight-scope", "instruction": "Ask for the missing or changed declaration." } },
    { "id": "vocabulary-complete", "expect": "Every required closed-vocabulary check appears exactly once.", "check": { "kind": "review", "ref": "closed-check-vocabulary" }, "onFail": { "action": "retry", "fromStep": "inspect-runtime-host-services", "instruction": "Add only the omitted check or remove the duplicate, preserving existing observations." } },
    { "id": "skips-valid", "expect": "Skipped checks follow dependency, flow and runtimeRoles rules.", "check": { "kind": "review", "ref": "skip-semantics" }, "onFail": { "action": "retry", "fromStep": "inspect-runtime-host-services", "instruction": "Correct the named invalid skip and run that check only when applicable." } },
    { "id": "runtime-host-grounded", "expect": "Runtime, host and service results use the declared bindings and actual probes.", "check": { "kind": "review", "ref": "runtime-and-host-evidence" }, "onFail": { "action": "retry", "fromStep": "inspect-runtime-host-services", "instruction": "Repeat only the unsupported probe against the declared endpoint or host capability." } },
    { "id": "report-complete", "expect": "The report retains all walls, evidence and owner actions and exposes no secret.", "check": { "kind": "review", "ref": "complete-wall-report" }, "onFail": { "action": "retry", "fromStep": "compile-readiness-report", "instruction": "Repair the missing or inconsistent report row from existing observations." } },
    { "id": "response-valid", "expect": "The response schema passes.", "check": { "kind": "machine", "ref": "response-shape" }, "onFail": { "action": "retry", "fromStep": "emit-readiness", "instruction": "Repair only the response shape." } },
    { "id": "response-bound", "expect": "The response binds the request revision.", "check": { "kind": "machine", "ref": "request-binding" }, "onFail": { "action": "retry", "fromStep": "emit-readiness", "instruction": "Correct the request binding." } },
    { "id": "outputs-unique", "expect": "Output ids are unique.", "check": { "kind": "machine", "ref": "unique-outputs" }, "onFail": { "action": "retry", "fromStep": "emit-readiness", "instruction": "Remove the duplicate output id." } },
    { "id": "artifacts-valid", "expect": "Output artifacts are scoped and digest-bound.", "check": { "kind": "machine", "ref": "output-files" }, "onFail": { "action": "retry", "fromStep": "emit-readiness", "instruction": "Repair the invalid artifact record." } },
    { "id": "observations-valid", "expect": "Observations bind criteria and evidence.", "check": { "kind": "machine", "ref": "observation-binding" }, "onFail": { "action": "retry", "fromStep": "emit-readiness", "instruction": "Correct the invalid observation." } },
    { "id": "declarations-present", "expect": "Required environment and route declarations are available.", "check": { "kind": "event", "ref": "essential-input-missing" }, "onFail": { "action": "ask", "fromStep": "bind-preflight-scope", "instruction": "Record the missing declaration as a wall and ask for it while completing independent checks." } },
    { "id": "capabilities-present", "expect": "Required probe capabilities are available.", "check": { "kind": "event", "ref": "capability-unavailable" }, "onFail": { "action": "block", "fromStep": "inspect-runtime-host-services", "instruction": "Record the unavailable capability as a wall and continue independent checks." } },
    { "id": "transient-complete", "expect": "Transient probes complete.", "check": { "kind": "event", "ref": "transient-execution-failure" }, "onFail": { "action": "retry", "fromStep": "inspect-runtime-host-services", "instruction": "Retry only the interrupted idempotent probe once." } }
  ],
  "stopWhen": "Return done only when every criterion passes and the report has zero walls. After two feedback rounds or any unchanged wall, return blocked with the complete report and owner actions; repair nothing."
}
