{
  "machine": [
    { "id": "request-shape", "check": "request-schema", "phase": "request" }, { "id": "request-files", "check": "input-files", "phase": "request" },
    { "id": "response-shape", "check": "response-schema", "phase": "response" }, { "id": "request-binding", "check": "request-binding", "phase": "response" },
    { "id": "unique-outputs", "check": "unique-output-ids", "phase": "response" }, { "id": "output-files", "check": "output-files", "phase": "response" },
    { "id": "observation-binding", "check": "observation-references", "phase": "response" }
  ],
  "review": [
    { "id": "closed-check-vocabulary", "instruction": "Verify every requested role has one result for declaration, hydration, Git policy, worktree cleanliness, installed dependencies and ancestor isolation; every declared operation class has one approval result; and identity, browser, Playwright, container, service and applicable runtime checks each appear once as ok, wall or skipped." },
    { "id": "skip-semantics", "instruction": "Verify a role with an unresolved declaration has dependent checks skipped, an unnamed flow has account and sign-in skipped, and a role absent from runtimeRoles has runtime checks skipped rather than wall. Applicable checks may not be skipped." },
    { "id": "runtime-and-host-evidence", "instruction": "Verify runtime checks compare registry head containment and projected ports, detect foreign listeners, and service probes follow the declaration. Host checks identify the actual browser, Playwright resolution, container reachability, lockfile install state and ancestor type isolation." },
    { "id": "complete-wall-report", "instruction": "Verify all checks ran or were validly skipped after the first wall, every wall includes a concrete owner action, declaration path and digest are recorded, done contains zero walls, and no credential value appears." }
  ],
  "failurePolicy": "Continue independent checks after a wall and retain the complete report. Re-run only checks whose underlying declaration, process or host state changed; unchanged walls block and are never treated as progress."
}
