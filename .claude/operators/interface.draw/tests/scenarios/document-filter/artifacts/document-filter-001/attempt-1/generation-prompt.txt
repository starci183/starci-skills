Use case: ui-mockup
Asset type: high-fidelity desktop dashboard feature-extension direction
Primary request: Edit the supplied baseline screenshot of the Nivo Accounting workbench. Add a clearly usable document search and filter control to the existing Documents panel. Make the smallest coherent interface change and preserve the rest of the screen as faithfully as possible.
Input images: Image 1 is the edit target and authoritative visual baseline.
Surface and viewport: Preserve the supplied 1536 x 1024 desktop dashboard composition and crop. Return a complete full-screen PNG at the same aspect ratio.
Audience: Business owner or financial approver reviewing accounting evidence and immutable ledger history.
Hierarchy and placement: Inside the existing Documents card, keep the Documents title and the All, Draft, Submitted, Approved, and Posted tabs exactly where they are. Insert one compact horizontal filter row directly below the tabs and above the table header. The row should read as part of the Documents card, with comfortable 12-16 px spacing, precise alignment, and no extra card nesting.
Controls and exact visible text: Add a wide search field with a small leading magnifying-glass icon and placeholder text "Search file name". Add a select labeled "Classification" showing "All classifications". Add a select labeled "Accounting month" showing "All months". Add a compact tertiary action reading "Clear filters" at the right edge. Keep the existing status tabs as the status filter. The controls are a visual specification for later native implementation; do not depict hover menus, fabricated results, or claims that filtering has already run.
Source-grounded fields: The Nivo backend document row supports file_name, classification, period_key, and status. Use only those concepts for searching or filtering. Do not add vendor, uploader, owner, free-form date range, amount range, tags, OCR, AI search, or any unsupported filter.
Style/medium: High-fidelity SaaS product UI screenshot, clean white canvas, subtle warm-gray page background, thin light-gray borders, restrained shadows, rounded 6-8 px controls, modern neutral sans-serif typography, dark navy text, muted gray helper text, and the existing navy-to-blue interaction color.
Composition/framing: Keep the same dense but breathable desktop information hierarchy. Fit the new filter row without crowding the right rail or displacing unrelated panels. If vertical room is needed, slightly tighten only the whitespace inside the Documents card while keeping every current section visible.
Text fidelity: Render all new labels exactly as specified and keep existing readable content unchanged. Avoid garbled, duplicated, or pseudo-text.
Implementation feasibility: Search and selects should resemble standard native web controls with visible focus boundaries and sufficient contrast; this image is a design direction, not working frontend behavior.

Preserve checklist (verbatim):
- Preserve the full 1536 x 1024 desktop canvas and existing two-column Accounting workbench layout.
- Preserve the existing application typography, white surfaces, fine gray borders, spacing system, and dark navy-to-blue action palette.
- Preserve the Current ledger panel and its immutable snapshot (view-only) meaning, lock icon, ledger version controls, balance, and applied setup details.
- Preserve the existing Documents status tabs and the three visible document rows with their current data and status badges.
- Preserve Add evidence document, Reconciliation, Close period, Applied context, Pending corrections, Propose correction, and Accounting setup panels without changing their content or purpose.

Change checklist (verbatim):
- Add one compact filter row inside the Documents panel between the existing status tabs and the document table.
- Add a file-name search input labeled by placeholder text Search file name with a leading magnifying-glass icon.
- Add a Classification select set to All classifications and an Accounting month select set to All months.
- Add a small Clear filters action aligned with the filter controls so the reset behavior is discoverable.
- Keep the filter controls visibly native, keyboard-friendly, and proportioned so the table remains easy to scan at the supplied desktop viewport.

Constraints: Change only the Documents panel by inserting the filter row. Preserve the immutable/read-only ledger meaning. Preserve all unrelated controls, values, panels, tab labels, table values, and the overall Nivo visual language. No logos added, no watermark, no decorative illustration, no dark mode, no mobile redesign, no charts, and no new operational claims.
