Use case: ui-mockup
Asset type: high-fidelity responsive product UI direction
Primary request: Revise Image 1, the original Nivo Accounting workbench baseline, into a polished mobile web page for a true 390 CSS-pixel-wide viewport. Image 2 is the rejected first mobile candidate: retain its excellent header, balance card, pending-correction explanation, stacked document cards, text fidelity, palette, and overall visual polish, but correct its lower-half breakpoint defect. Produce a straight-on long portrait scrolling-page screenshot with no phone hardware or decorative background.
Input images: Image 1 is the required edit target and authoritative baseline. Image 2 is an additional rejected-candidate reference only. Do not replace the original baseline with Image 2.

Required targeted correction: from “Add evidence document” downward, EVERY section must span the full content width in ONE vertical column. Never place cards, forms, fields, or panels side by side. Stack these full-width cards in this order: Add evidence document; Propose correction; Reconciliation; Close period; Applied context; Accounting setup. Use 16px page gutters and at least 12px vertical gaps. Full-width inputs and 44px buttons must have enough room for readable 15–16px labels. If page height is constrained, use compact disclosure rows for Applied context and Accounting setup, but keep their titles, statuses, and actions visible. Do not create a two-column tablet layout.

Keep the successful upper hierarchy from Image 2: compact “Accounting workbench” header with green “Applied setup” and “Ledger v7”; balance card “VND 128,450,000” and “Current immutable balance”; lock-marked “Immutable snapshot (view-only)”, “As of 30 Sep 2026”, ledger version field, “View as of”, “Return to current”, and “Audit details”; amber “1 pending” card stating “A pending proposal has no ledger effect until distinct approval”, “Reclassify consulting fee”, “26 Sep 2026”, and “View all pending corrections”; Documents with horizontal filters “All (3)”, “Draft (1)”, “Submitted (1)”, “Approved (1)”, “Posted (1)” and three stacked document cards rather than a table.

Document facts to retain: “Invoice_0926.pdf” / “25,000,000 VND” / Expense / Sep 2026 / Submitted / Business owner / 26 Sep 2026 10:15; “Bank_Statement_0926.xlsx” / “15,000,000 VND” / Expense / Sep 2026 / Approved / Financial approver / 26 Sep 2026 14:32; “Utility_Bill_0926.pdf” / “2,500,000 VND” / Expense / Sep 2026 / Posted / System / 26 Sep 2026 14:40; and “View all documents”.

Full-width action-card contents: “Add evidence document” with Choose file, Classification / Expense, Amount (VND) / 25,000,000, Accounting month / Sep 2026, Notes (optional), and “Add document”. “Propose correction” with signed amount helper, Amount (VND) / -5,000,000, Correction reason / Select reason, and “Submit correction proposal”. “Reconciliation” with Source balance (VND) / -128,450,000, signed-or-zero helper, and “Save reconciliation”. “Close period” with Month to close / Sep 2026 and a visibly disabled “Close period” button. “Applied context” keeps Immutable ledger / Ledger v7, As of / 30 Sep 2026 23:59, Owner / Business owner, Approver / Financial approver, Setup status / Applied, and “Audit details”. “Accounting setup” keeps Verified owner continuity, Verified approver continuity, Applied configuration, and “View setup details”.

Interaction and business semantics: retain only supported operations. Documents progress Draft → Submitted → Approved → Posted. Approval is performed by the distinct financial approver; never imply self-approval or autonomous assistant approval. Posted ledger history is immutable. A correction proposal remains Pending with zero ledger effect until distinct approval appends a correction entry. Reconciliation is VND-scoped at the current ledger high-water and accepts signed or zero source amounts. Period close remains an owner action and is disabled in this shown state. Controls must look like native web controls backed by live server data.

Visual execution: narrow phone layout at 390 CSS-pixel intent, one column only, long vertical scroll, 16px gutters, 44px touch targets, readable compact sans-serif type, white background and cards, thin warm-gray borders, restrained radius and shadow, near-black headings, muted helper text, dark navy primary buttons, outline secondary buttons, green success badges, blue submitted badge, amber pending accents. No gradients, illustrations, extra metrics, bottom navigation, invented business actions, desktop sidebar, squeezed tables, split cards, or side-by-side forms.

Preserve checklist (verbatim from request):
- Preserve the Nivo Accounting workbench identity, restrained white card system, thin gray borders, compact sans-serif typography, dark navy primary actions, green applied/approved/posted states, and amber pending state.
- Preserve the immutable view-only ledger model, Ledger v7, the VND balance, the as-of snapshot controls, and the separation between current and historical views.
- Preserve the document lifecycle labels Draft, Submitted, Approved, and Posted, plus evidence intake, pending correction, correction proposal, reconciliation, period close, audit details, applied context, and accounting setup.
- Preserve explicit four-eyes semantics: owner and financial approver remain distinct, approval is explicit, and no self-approval is implied.
- Preserve the baseline sample amounts, dates, document names, classifications, owners, and status labels where they remain visible.
- Preserve native-control and live-data intent; this image is a design direction, not evidence of implemented interaction.

Change checklist (verbatim from request):
- Recompose the interface for a single 390 CSS-pixel-wide mobile viewport with comfortable touch targets, a clear vertical reading order, and scrollable page depth.
- Replace the seven-column desktop document table with stacked document cards under horizontally scrollable status filters; never squeeze the desktop table into the narrow viewport.
- Move the desktop right rail into the main flow as compact mobile cards or disclosure sections, surfacing the one pending correction before lower-priority setup details.
- Group long forms into focused mobile action cards with full-width inputs and buttons while keeping destructive or unavailable period close visibly disabled until its prerequisite state is met.

Output constraints: one polished raster UI direction only; long portrait mobile screenshot; preserve exact spelling of visible labels and filenames where possible; no watermark; no annotations outside the UI; no claim that this is implemented.
