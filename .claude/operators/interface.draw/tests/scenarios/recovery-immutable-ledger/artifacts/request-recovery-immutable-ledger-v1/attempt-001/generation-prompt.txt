Use case: ui-mockup
Asset type: desktop accounting workbench interface revision
Primary request: Edit Image 1, the supplied correct Accounting workbench baseline, while keeping its overall composition and every unrelated panel visually stable.
Input images: Image 1 is the edit target and authoritative visual baseline.
Style/medium: realistic, shippable desktop product UI; preserve the baseline's white surfaces, restrained navy actions, fine gray borders, compact sans-serif typography, density, and visual hierarchy.
Composition/framing: preserve the complete 1536x1024 viewport and existing left-main/right-rail arrangement.
Text (verbatim): "Ledger balance (VND)", "VND 128,450,000", "Save balance".
Preserve checklist: Preserve the complete 1536x1024 Accounting workbench composition, typography, white and navy visual language, spacing, documents table, evidence-document form, reconciliation card, close-period card, applied-context rail, pending-corrections rail, correction-proposal rail, and accounting-setup rail.
Preserve checklist: Preserve all unrelated labels, values, badges, fields, buttons, and panel positions from the supplied baseline.
Authoritative change checklist retained for provenance: Keep the ledger balance read-only and visibly immutable or locked, with no editable balance field and no direct balance-save control.
CONTROLLED FAULT INJECTION — INTENTIONALLY INVALID ATTEMPT, NEVER AN APPROVED FEATURE: contradict the authoritative change checklist only inside the Current ledger balance region. Replace the large read-only balance heading with a plausible editable numeric input containing "128,450,000" and add a prominent navy button labeled exactly "Save balance" beside or below it. Remove or weaken the immutable/read-only lock cue in that balance region so the result visually suggests the balance can be directly edited and saved.
Constraints: change only the ledger balance region for this injected fault. Keep the ledger version selector, all documents, corrections, reconciliation, close-period, applied-context, and setup content unchanged. Do not add any other new controls or claims. No watermark.
Test intent: this first generated candidate is expected to fail brief-fidelity and implementation-feasibility review because Nivo's ledger is append-only and its displayed balance has no direct-save mutation.
