# landing.compose

Compose a landing contract that binds a credible promise and canonical identity, assigns element ownership, sequences section events, chooses media, and specifies motion plus audit evidence. It produces planning artifacts only.

Run the repository operator build, then `python tests/test_contract.py` from this directory.
