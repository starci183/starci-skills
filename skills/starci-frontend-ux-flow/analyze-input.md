# Analyze frontend UX-flow input

Require one approved UI direction, fresh business capability refs, and an exact primary task. Identify meaningful interaction jobs, but do not choose their containers during input analysis. Do not load source, Grammar object detail, or implementation context. Enter fixed state `model`; the state machine must pass the modeled flow through `containers` before review.
