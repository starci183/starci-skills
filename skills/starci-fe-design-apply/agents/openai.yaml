interface:
  display_name: "StarCi FE Design Apply"
  short_description: "Apply the approved FE architecture delta"
  default_prompt: "Use $starci-fe-design-apply to commit the current baseline, implement only the approved component and props delta in source, and reconcile every row with the resulting diff."
