interface:
  display_name: "StarCi Deployment"
  short_description: "Adopt and deploy one immutable release"
  default_prompt: "Use $starci-deployment for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
