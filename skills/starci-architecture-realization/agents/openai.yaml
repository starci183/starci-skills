interface:
  display_name: "StarCi Architecture Realization"
  short_description: "Bind architecture to deployable implementation"
  default_prompt: "Use $starci-architecture-realization for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
