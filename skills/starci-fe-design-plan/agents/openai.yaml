interface:
  display_name: "StarCi FE Design Journey"
  short_description: "Orchestrate layout, block and exact execution from immutable JSON decisions."
  default_prompt: "Use $starci-fe-design-plan to start or resume the hash-bound FE design journey."
