interface:
  display_name: "StarCi Business Reconcile"
  short_description: "Reconcile delivered source with business truth"
  default_prompt: "Use $starci-business-reconcile for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
