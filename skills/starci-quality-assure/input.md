# Input

A closed v7 mission envelope with one bounded objective. FE-origin assurance requires the exact
route-issued final blind visual PASS, its unchanged source heads, source/evidence fingerprints and
`audit.md`, plus `debtPolicy: forbidden`, empty write roots, and no external mutation.
