interface:
  display_name: "StarCi Frontend Maintenance Apply"
  short_description: "Apply approved frontend maintenance safely"
  default_prompt: "Use $starci-frontend-maintenance-apply for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
