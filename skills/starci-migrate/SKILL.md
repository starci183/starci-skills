---
name: starci-migrate
description: Import one selected business pilot from existing source and work records into Work 3, preserving provenance and marking inferred intent unapproved.
---

# starci-migrate

Use for Work-format adoption from inspected source, not a database or production migration.

## Load the actual contract

Read [the main entry](../../SKILL.md), [Work runtime guide](../../v3/README.md), [core contract](../../v3/core/README.md), and [metadata schema](../../v3/schemas/work.schema.json). Read [this fixed recipe](recipe.json). Resolve its selected op IDs through [the actual operator catalogue](../../v3/ops/catalog.json), then read the catalogue's commonDocument and each selected English document completely. Missing or inconsistent installed files block execution; never substitute guessed operators, paths or fields.

The recipe is the single sequence authority. Choose exactly one mode from the user's requested outcome; apply one consistent named branch selection across all its waves. Do not activate unselected branches. Read-only questions are answered/inspected without launching this mutating preset.

## Bound execution

State the specific goal, actual target IDs, selected mode/branches, source/resource write ceiling and done-when briefly. Reuse existing scope authorization; ask only for missing material choices or new effects. Map all consumers to existing scope-owned nodes. Only the selected scope/planning op may declare its explicitly selected downstream targets; a missing target or unmet dependency stops that branch, not an extra setup op.

The common protocol's wave/width budget is shared across every selected skill, worker and retry for this prompt. Do not reset it per recipe. Parallel invocations require disjoint ownership and sessions/data; three agents are not mandatory. Workers return results without dispatching successors. The coordinator may advance only already selected eligible waves, never invent another workflow. Recipe ordering is scheduling, not authority to add .work dependsOn or refs.

## Domain boundaries

Inventory one selected business pilot, not the whole repository. Source-inferred behavior is an observation, never approved intent: imported leaves use state:suspended and concrete suspensionReason, with no fabricated completion. Business review may only approve what the owner actually accepted; stop there when a material intent decision is missing. Workflow review distinguishes imported coverage from product acceptance. Removing .worktrees requires explicit exact cleanup authority, verified preservation of commits, untracked work, images, evidence and sealed account references, no active worker/worktree, and proper Git removal for registered worktrees. Otherwise retain it and report the cleanup gap.

## Result

Use the selected op's exact write matrix and completion profile. Finish semantic edits before verification; put actual observations in its evidence bundle, not frozen node prose. Keep failed, suspended and untested work visible. Return the actual files/node IDs, real commits when applicable, inspected evidence and remaining gap. No request.json/response.json, hidden successor, manufactured done or product effects from merely loading this skill.
