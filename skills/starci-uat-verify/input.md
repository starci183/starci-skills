# Input

Closed mission input with the exact canonical `quality/delivery-proof=pass` RETURN, its owning CALL,
unchanged source heads, source/evidence fingerprints, and required `audit.md`. Direct invocation from
arbitrary strings or a non-final Quality result is rejected.
