# Analyze frontend mission

Compile and validate the complete frozen mission `scope` against `scope.yaml`, then validate one
intent, objective, target hints, authority refs, and optional exact resume receipt. Read
`knowledge/ux-ui-change-levels.md` and require exactly one `frontend.ux-ui.change-level` dimension for
frontend UX/UI work. When any material scope dimension is unresolved, ask one focused boundary
question before validating or entering the machine. The fixed first state is `compile` only after
scope is frozen.
