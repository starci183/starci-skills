interface:
  display_name: "StarCi Workspace Manage"
  short_description: "Manage routed workspaces and task sessions"
  default_prompt: "Use $starci-workspace-manage to prepare or resume this routed workspace boundary."
policy:
  allow_implicit_invocation: true
