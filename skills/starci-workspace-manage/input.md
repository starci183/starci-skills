# Input

A closed v7 mission envelope with one bounded objective.

For `mission.intent=uat-account`, provide `uatAccountRequest` with the canonical feature, flow, run,
learner role, runtime owner, localhost origin, fixture namespace, source fingerprint, and future
`snapshot.json#account` reference. Do not include a username, password, token, cookie, or OTP.
