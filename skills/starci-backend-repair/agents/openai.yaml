interface:
  display_name: "StarCi Backend Repair"
  short_description: "Repair one approved backend source boundary"
  default_prompt: "Use $starci-backend-repair for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
