# Analyze input

Compile a frozen mission scope before opening target content. Bind the learner audience, current source evidence, requested natural languages, zero to four implementation-language tracks, visual requirement, code requirement, E2E requirement, and review-round limit. Ask one focused question only when a missing authority would materially change the learning contract.

For resume, verify the exact receipt/evidence references and select only the requested stage. Any upstream revision invalidates dependent downstream artifacts according to `machine.json`.
