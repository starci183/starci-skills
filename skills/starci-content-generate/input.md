# Input

Provide one frozen educational-content mission with exact request, artifact, and evidence references. Declare the audience, natural languages, at most four implementation languages, artifact modes, and maximum review rounds. `resumeTarget` is required only when `intentMode` is `resume`.
