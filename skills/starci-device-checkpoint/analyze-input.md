# Analyze starci-device-checkpoint input

Global `@selection` has already selected this one-flow skill from prompt intent. Before any operator or Qdrant retrieval, validate the invocation and verify `selection.skillId` equals `starci-device-checkpoint`. Then perform these local checks:

1. Resolve one verified project-role route and the exact mission-owned checkout set.
2. Require explicit stop/checkpoint authority for source pushes and private release publication.
3. Block dirty, behind, diverged, detached or force-push source state.
4. Quiesce every Source-declared Docker volume, stream encrypted archives, restart containers and prove one complete private release manifest.

Reject stale or missing authority/evidence, an ambiguous target, a write root outside scope, external mutation without approval, or an option outside the closed schema. Do not reconsider other skills here; return to global analysis if selection is wrong.

The fixed first state is `route`. Emit only normalized scope and facts as task-session data; do not choose a second mode or copy operator knowledge into context.

## Options

| Option | Values | Decision effect |
| --- | --- | --- |
| — | — | No additional option is loaded. |
