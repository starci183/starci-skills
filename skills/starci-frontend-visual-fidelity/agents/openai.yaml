interface:
  display_name: "StarCi Frontend Visual Fidelity"
  short_description: "Compare implementation with approved design"
  default_prompt: "Use $starci-frontend-visual-fidelity for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
