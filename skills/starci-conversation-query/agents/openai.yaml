interface:
  display_name: "StarCi Conversation Query"
  short_description: "Query bounded conversation provenance safely"
  default_prompt: "Use $starci-conversation-query for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
