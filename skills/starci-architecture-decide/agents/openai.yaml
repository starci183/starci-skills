interface:
  display_name: "StarCi Architecture Decide"
  short_description: "Resolve difficult cross-system architecture choices"
  default_prompt: "Use $starci-architecture-decide for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
