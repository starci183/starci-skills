interface:
  display_name: "StarCi Sonar Service Reconcile"
  short_description: "Reconcile shared Sonar quality enforcement"
  default_prompt: "Use $starci-sonar-service-reconcile for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
