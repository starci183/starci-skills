# Analyze frontend UI-detail input

Require one approved UI direction, one approved UX flow, page refs, Grammar package, and current generated frontend-context hash. Do not load raw source or implementation context. Enter fixed state `freeze`.
