interface:
  display_name: "StarCi Journey Delivery"
  short_description: "Ship complete cross-stack product journeys"
  default_prompt: "Use $starci-frontend-layout-delivery for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
