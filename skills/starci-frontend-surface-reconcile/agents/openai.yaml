interface:
  display_name: "StarCi Frontend Surface Reconcile"
  short_description: "Align a closed set of frontend surfaces"
  default_prompt: "Use $starci-frontend-surface-reconcile for this selected flow and execute its state machine."
policy:
  allow_implicit_invocation: true
