# Execute

Follow `machine.json`. Operators execute one job; the machine routes. Challenge method while preserving outcome. Emit typed CALL at peer handoff and resume only from matching typed RETURN. Stop for user input only at an authority dead end. A design/redesign result is not ready until `knowledge/direction-visualization.md` has produced a visible `visualize` comparison of three or four architecture directions.
Bind the choice with `operators/architecture/review-widget.md` and render it through `visualize` before requesting `OK ARCHITECTURE`.
