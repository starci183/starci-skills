# Execute

Follow `machine.json`. Operators execute one job; the machine routes. Challenge method while preserving outcome. Emit typed CALL at peer handoff and resume only from matching typed RETURN. Stop for user input only at an authority dead end.

