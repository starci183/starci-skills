# Input

Use the exact v7 selection envelope: `analyzerVersion: 2`, this local `skillId`, `confidence`, `interactionPolicy: ask-only-when-stuck`, active input refs, and passive context refs. Mission options bind intent, prerequisites, canonical runtime receipt/history, and executable resume target. A WAIT is valid only at a machine-declared genuine authority dead end.

