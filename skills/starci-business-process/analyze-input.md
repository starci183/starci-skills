# Analyze input

Compile and validate a complete frozen mission `scope` against `scope.yaml`. If any material
dimension is unclear, ask one focused boundary question before Skill execution or target-source
inspection.

Validate intent, exact authority, typed peer envelope, and progress fingerprint before loading operator context. Reject unchanged progress and misaddressed RETURN envelopes.
