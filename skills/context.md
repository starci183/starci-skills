# Skill runtime router

## LOADS

None.

## Routes

The sixteen capability entries each live in their own capability folder as `SKILL.md`. Select them from their frontmatter
descriptions and load exactly one binding entry unless the owner explicitly requests separate runs.

`skill-shape/context.md` is shared runtime policy, not a capability entry.
