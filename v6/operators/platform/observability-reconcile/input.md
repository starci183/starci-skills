# platform/observability-reconcile input

Reconcile declared runtime metrics collection and remote write.

Accept only the declared state and exact artifact references: `stackRef`, `metricsDestinationRef`, `credentialReceiptRef`. Validate before knowledge retrieval or side effects.
