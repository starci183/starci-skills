{
  "$id": "https://starci.dev/v6/operators/platform/tunnel-plan/input.schema.json",
  "title": "platform/tunnel-plan operator input",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "runId",
    "stage",
    "status",
    "facts",
    "payload"
  ],
  "allOf": [
    {
      "oneOf": [
        {
          "properties": {
            "stage": {
              "const": "platform.tunnel.plan"
            },
            "status": {
              "const": "ready"
            }
          },
          "required": [
            "stage",
            "status"
          ]
        }
      ]
    }
  ],
  "properties": {
    "schemaVersion": {
      "const": 6
    },
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "stage": {
      "enum": [
        "platform.tunnel.plan"
      ]
    },
    "status": {
      "enum": [
        "ready"
      ]
    },
    "facts": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "routeReceiptRef",
        "hostnameRef",
        "originRef",
        "ownershipRef"
      ],
      "properties": {
        "routeReceiptRef": {
          "type": "string",
          "minLength": 1
        },
        "hostnameRef": {
          "type": "string",
          "minLength": 1
        },
        "originRef": {
          "type": "string",
          "minLength": 1
        },
        "ownershipRef": {
          "type": "string",
          "minLength": 1
        }
      }
    }
  }
}
