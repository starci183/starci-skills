# platform/sonar-service-reconcile output

Return `sonarServiceReceiptRef`, one typed decision (`proved`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
