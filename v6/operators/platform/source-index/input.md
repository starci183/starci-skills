# platform/source-index input

Index routed source partitions into Qdrant.

Accept only the declared state and exact artifact references: `mcpConfigReceiptRef`, `routeSetRef`, `exclusionPolicyRef`. Validate before knowledge retrieval or side effects.
