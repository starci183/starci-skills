# platform/tunnel-apply output

Return `tunnelReceiptRef`, one typed decision (`proved`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
