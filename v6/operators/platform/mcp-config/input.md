# platform/mcp-config input

Compile value-safe source-context MCP/Qdrant configuration.

Accept only the declared state and exact artifact references: `routeSetRef`, `collectionRef`, `modelRef`, `credentialRosterRef`. Validate before knowledge retrieval or side effects.
