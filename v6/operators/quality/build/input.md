# quality/build input

Build the exact routed role using its declared entrypoint.

Accept only the declared state and exact artifact references: `changeReceiptRef`, `buildCommandRef`. Validate before knowledge retrieval or side effects.
