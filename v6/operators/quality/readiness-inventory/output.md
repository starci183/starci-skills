# quality/readiness-inventory output

Return `inventoryReceiptRef`, one typed decision (`green`, `findings`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
