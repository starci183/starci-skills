# quality/delivery-proof output

Return `sourceDeliveryProofRef`, one typed decision (`pass`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
