# quality/typecheck input

Run the declared typecheck entrypoint without source mutation.

Accept only the declared state and exact artifact references: `changeReceiptRef`, `typecheckCommandRef`. Validate before knowledge retrieval or side effects.
