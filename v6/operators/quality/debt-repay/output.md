# quality/debt-repay output

Return `debtReceiptRef`, one typed decision (`closed`, `progress`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
