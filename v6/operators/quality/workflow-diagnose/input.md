# quality/workflow-diagnose input

Trace another workflow against the real machine without running it.

Accept only the declared state and exact artifact references: `skillRef`, `invocationRef`, `observedEnvironmentRef`. Validate before knowledge retrieval or side effects.
