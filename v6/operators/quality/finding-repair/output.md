# quality/finding-repair output

Return `repairReceiptRef`, one typed decision (`repaired`, `boundary-drift`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
