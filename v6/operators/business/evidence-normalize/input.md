# business/evidence-normalize input

Separate facts, intent, examples, unknowns, and contradictions.

Accept only the declared state and exact artifact references: `routeReceiptRef`, `requestRef`, `ownerEvidenceRef`, `sourceEvidenceRefs`. Validate before knowledge retrieval or side effects.
