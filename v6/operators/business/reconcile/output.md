# business/reconcile output

Return `reconciliationReceiptRef`, one typed decision (`implemented`, `discrepancy`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
