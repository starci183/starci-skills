# business/reconcile input

Reconcile delivered source with the exact business head.

Accept only the declared state and exact artifact references: `businessHeadRef`, `deliveryProofRef`, `finalSourceHeadsRef`. Validate before knowledge retrieval or side effects.
