# business/model input

Reconcile one evidence-backed feature model.

Accept only the declared state and exact artifact references: `evidencePackRef`, `previousHeadRef`. Validate before knowledge retrieval or side effects.
