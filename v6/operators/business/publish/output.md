# business/publish output

Return `businessHeadRef`, one typed decision (`architecture-required`, `direct-plan`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
