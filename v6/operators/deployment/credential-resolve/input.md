# deployment/credential-resolve input

Resolve required credential names through existing custody.

Accept only the declared state and exact artifact references: `executionPlanRef`, `credentialRosterRef`, `custodyPolicyRef`. Validate before knowledge retrieval or side effects.
