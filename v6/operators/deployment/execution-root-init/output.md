# deployment/execution-root-init output

Return `executionRootReceiptRef`, one typed decision (`ready`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
