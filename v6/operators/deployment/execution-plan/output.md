# deployment/execution-plan output

Return `executionPlanRef`, one typed decision (`execute`, `approval-required`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
