# deployment/migration output

Return `migrationReceiptRef`, one typed decision (`applied`, `not-applicable`, `rollback`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
