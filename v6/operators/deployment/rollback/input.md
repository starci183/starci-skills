# deployment/rollback input

Restore the declared prior artifact/runtime/domain identity without unapproved data loss.

Accept only the declared state and exact artifact references: `executionPlanRef`, `rollbackPointRef`, `failureEvidenceRef`. Validate before knowledge retrieval or side effects.
