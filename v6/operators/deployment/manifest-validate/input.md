# deployment/manifest-validate input

Validate durable deployment intent and ownership.

Accept only the declared state and exact artifact references: `deploymentContractRef`, `manifestRef`, `routeReceiptRefs`. Validate before knowledge retrieval or side effects.
