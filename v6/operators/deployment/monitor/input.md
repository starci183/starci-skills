# deployment/monitor input

Observe workflow, host, service, TLS/DNS, and application readiness through the steady window.

Accept only the declared state and exact artifact references: `executionPlanRef`, `rolloutReceiptRef`, `probeSetRef`, `steadyWindowRef`. Validate before knowledge retrieval or side effects.
