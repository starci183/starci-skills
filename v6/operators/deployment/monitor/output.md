# deployment/monitor output

Return `monitorReceiptRef`, one typed decision (`steady`, `recover`, `rollback`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
