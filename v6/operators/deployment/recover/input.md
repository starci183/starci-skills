# deployment/recover input

Repair the smallest owned recoverable deployment boundary.

Accept only the declared state and exact artifact references: `executionPlanRef`, `monitorReceiptRef`, `failureEvidenceRef`, `ownedBoundaryRef`. Validate before knowledge retrieval or side effects.
