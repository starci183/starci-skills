# deployment/artifact-build input

Build immutable artifacts from declared contexts.

Accept only the declared state and exact artifact references: `executionPlanRef`, `sourceRevisionRefs`, `artifactBuildRefs`. Validate before knowledge retrieval or side effects.
