# workspace/routes-hydrate input

Hydrate rebuildable machine-local routes from portable declarations.

Accept only the declared state and exact artifact references: `compiledDeclarationsRef`, `localRepositoryMapRef`. Validate before knowledge retrieval or side effects.
