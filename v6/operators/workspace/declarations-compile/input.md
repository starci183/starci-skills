# workspace/declarations-compile input

Validate and compile portable project/role declarations.

Accept only the declared state and exact artifact references: `bootstrapReceiptRef`, `portableDeclarationsRef`. Validate before knowledge retrieval or side effects.
