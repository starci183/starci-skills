# workspace/bootstrap-verify input

Prove the minimal agent bootstrap routes to one maintained Source index.

Accept only the declared state and exact artifact references: `identityReceiptRef`, `agentBootstrapRef`, `sourceIndexRef`. Validate before knowledge retrieval or side effects.
