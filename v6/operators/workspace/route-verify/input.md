# workspace/route-verify input

Resolve one verified project/role checkout and source-context partition.

Accept only the declared state and exact artifact references: `projectRef`, `roleRef`, `requestRef`. Validate before knowledge retrieval or side effects.
