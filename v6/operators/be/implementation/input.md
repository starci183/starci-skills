# be/implementation input

Implement only one approved backend boundary.

Accept only the declared state and exact artifact references: `approvedBoundaryRef`, `approvalReceiptRef`, `businessHeadRef`, `baselineCommitRef`. Validate before knowledge retrieval or side effects.
