# source/conversation-record input

Record one provider-neutral immutable conversation provenance snapshot.

Accept only the declared state and exact artifact references: `providerRef`, `conversationSnapshotRef`, `artifactRefs`, `redactionReceiptRef`. Validate before knowledge retrieval or side effects.
