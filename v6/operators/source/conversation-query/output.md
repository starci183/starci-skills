# source/conversation-query output

Return `conversationQueryReceiptRef`, one typed decision (`found`, `empty`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
