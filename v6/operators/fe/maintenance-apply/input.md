# fe/maintenance-apply input

Apply approved frontend maintenance to product source and prove the correction first.

Accept only the declared state and exact artifact references: `feedbackRef`, `approvedBoundaryRef`, `baselineRef`. Validate before knowledge retrieval or side effects.
