# fe/learning-resolve output

Return `learningReceiptRef`, one typed decision (`complete`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
