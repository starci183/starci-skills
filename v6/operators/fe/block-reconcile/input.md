# fe/block-reconcile input

Reconcile one component-impact block inside its complete parent page.

Accept only the declared state and exact artifact references: `pageRef`, `blockRef`, `sourceContractRef`. Validate before knowledge retrieval or side effects.
