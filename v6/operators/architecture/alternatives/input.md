# architecture/alternatives input

Create materially viable alternatives under one criteria set.

Accept only the declared state and exact artifact references: `decisionFrameRef`, `currentStateRef`. Validate before knowledge retrieval or side effects.
