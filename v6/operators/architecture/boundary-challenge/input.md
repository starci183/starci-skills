# architecture/boundary-challenge input

Independently challenge the exact backend revision.

Accept only the declared state and exact artifact references: `boundaryPlanRef`, `sourceReceiptRef`, `patternBindingRef`. Validate before knowledge retrieval or side effects.
