# architecture/decision-challenge output

Return `challengeReceiptRef`, one typed decision (`ready`, `revise`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
