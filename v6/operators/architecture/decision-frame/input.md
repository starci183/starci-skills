# architecture/decision-frame input

Freeze the exact cross-system decision and constraint classes.

Accept only the declared state and exact artifact references: `businessHeadRef`, `objectiveRef`, `constraintEvidenceRefs`. Validate before knowledge retrieval or side effects.
