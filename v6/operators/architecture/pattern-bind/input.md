# architecture/pattern-bind input

Bind every accepted backend fact to fixed pattern situations.

Accept only the declared state and exact artifact references: `businessHeadRef`, `planningContextRef`, `sourceReceiptRef`. Validate before knowledge retrieval or side effects.
