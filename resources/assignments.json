{
  "schemaVersion": 8,
  "note": "One entry per operator. `profile` names the single execution profile (declared in agents/profiles/*.json) that runs the operator end to end; an operator never splits across profiles. `requires` lists the runtime grants the operator's execute.md actually uses; the validator rejects a profile that does not permit them. `policy` answers the three standing questions for that operator and must agree with `requires`.",
  "operators": {
    "workspace.bind": {
      "profile": "sonnet",
      "requires": [],
      "policy": {
        "webSearch": "never",
        "grammarBound": false,
        "imageGeneration": "never"
      },
      "why": "Route resolution reads canonical files and a runtime registry; nothing here is a judgement call."
    },
    "business.decide": {
      "profile": "sol-fresh",
      "requires": [
        "webSearch"
      ],
      "policy": {
        "webSearch": "bounded",
        "grammarBound": false,
        "imageGeneration": "never"
      },
      "why": "An unfamiliar business or pricing model may need bounded reference research before the coverage matrix can be frozen; the critique is a separate fresh execution."
    },
    "architecture.decide": {
      "profile": "sol-fresh",
      "requires": [
        "webSearch"
      ],
      "policy": {
        "webSearch": "bounded",
        "grammarBound": false,
        "imageGeneration": "never"
      },
      "why": "Alternatives and compatibility need evidence beyond the repository; the operator's schema already pins the decision model."
    },
    "backend.implement": {
      "profile": "opus",
      "requires": [
        "sourceWrite"
      ],
      "policy": {
        "webSearch": "never",
        "grammarBound": false,
        "imageGeneration": "never"
      },
      "why": "Writes product source inside a frozen mutation contract; it follows patterns/be and never invents business behaviour."
    },
    "fe.direction.decide": {
      "profile": "sol-fresh",
      "requires": [
        "webSearch",
        "browser"
      ],
      "policy": {
        "webSearch": "bounded",
        "grammarBound": true,
        "imageGeneration": "authority-only"
      },
      "why": "Bounded research runs only when the domain or interaction model is unfamiliar and the tree holds no reference. The direction renders an inspectable page, not a picture; product artwork is generated only when product authority names it."
    },
    "fe.presentation.resolve": {
      "profile": "sonnet",
      "requires": [],
      "policy": {
        "webSearch": "never",
        "grammarBound": true,
        "imageGeneration": "never"
      },
      "why": "A lookup against a closed rule inventory. Nothing may be searched for, because a value the knowledge does not publish is RULE_MISSING, not a research task."
    },
    "fe.source.apply": {
      "profile": "opus",
      "requires": [
        "sourceWrite"
      ],
      "policy": {
        "webSearch": "never",
        "grammarBound": true,
        "imageGeneration": "never"
      },
      "why": "Writes only classes the resolution receipt already contains, following patterns/fe. It decides nothing."
    },
    "fe.surface.audit": {
      "profile": "sol-reviewer",
      "requires": [
        "browser"
      ],
      "policy": {
        "webSearch": "never",
        "grammarBound": true,
        "imageGeneration": "never"
      },
      "why": "Capture is mechanical; judgement is a fresh reviewer that sees rasters and claims only. A claim is never evidence of passing."
    },
    "quality.verify": {
      "profile": "sonnet",
      "requires": [],
      "policy": {
        "webSearch": "never",
        "grammarBound": false,
        "imageGeneration": "never"
      },
      "why": "Runs declared gates and reports measured results; it repairs nothing."
    },
    "uat.verify": {
      "profile": "sol-reviewer",
      "requires": [
        "browser"
      ],
      "policy": {
        "webSearch": "never",
        "grammarBound": false,
        "imageGeneration": "never"
      },
      "why": "Drives a real journey with an auto-provisioned learner and never asks a person to sign in; the verdict comes from a fresh reviewer per lane."
    },
    "release.deploy": {
      "profile": "opus",
      "requires": [
        "sourceWrite"
      ],
      "policy": {
        "webSearch": "never",
        "grammarBound": false,
        "imageGeneration": "never"
      },
      "why": "Immutable release under declared authorization with recovery and rollback in the same pass."
    },
    "platform.operate": {
      "profile": "opus",
      "requires": [
        "sourceWrite"
      ],
      "policy": {
        "webSearch": "never",
        "grammarBound": false,
        "imageGeneration": "never"
      },
      "why": "Shared observability, Sonar, or tunnel work from exact evidence; never product deployment."
    },
    "content.generate": {
      "profile": "luna",
      "requires": [
        "webSearch",
        "imageGeneration",
        "sourceWrite"
      ],
      "policy": {
        "webSearch": "bounded",
        "grammarBound": false,
        "imageGeneration": "required"
      },
      "why": "The teacher brief may research the topic; production writes, codes, and generates images to a stated claim; the critique is independent of the producer. The operator's schema pins both models."
    },
    "git.publish": {
      "profile": "sonnet",
      "requires": [
        "sourceWrite"
      ],
      "policy": {
        "webSearch": "never",
        "grammarBound": false,
        "imageGeneration": "never"
      },
      "why": "Non-force publication of an approved boundary; the schema forbids every destructive operation."
    }
  }
}
