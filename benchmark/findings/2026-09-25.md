# Đánh giá pool model — 72h tính đến 2026-09-25

- Snapshot: `benchmark/snapshots/2026-09-25-72h.json` (`scripts/agent/model-scorecard.mjs`, cửa sổ 72h từ
  2026-09-21T19:09Z)
- Repo: nivo-backend, mia-mia-backend, starci-next
- Tổng: **1389 job op**
- Kỳ vọng so sánh: `benchmark/expectations.yaml` (runtime commit 52e284716)
- Trạng thái: **owner đã duyệt đề xuất (`ok`, 2026-09-25)**. Xem mục Quyết định.

Cách đọc số: pass/fail/blocked tính trên các job đã settle, và blocked gồm cả cancelled. Median là thời gian
từ `op-dispatched` đến `op-settled`.

## Theo pool

| Pool | Model | Jobs | Pass | Fail | Blocked | Median |
|---|---|---:|---:|---:|---:|---:|
| codex | GPT-6 Luna/Sol | 451 | 53% | 18% | 29% | 8m |
| claude | Claude Opus 5.5 | 422 | 70% | 14% | 15% | 5m |
| devin | SWE-2 Max | 179 | 65% | 30% | 5% | 20m |
| qwen | DeepSeek V4.1 Flash (trước 24/09 là qwen3.8-flash) | 115 | 66% | 21% | 12% | 18m |
| unrouted | — | 222 | — | — | — | — |

Có 222 job unrouted, tức là chưa từng được route: bị drop hoặc bị hủy trước khi route. Chúng không tính vào
chất lượng của pool nào.

## So sánh cùng loại việc (pass %)

| Kind | Kết quả |
|---|---|
| business.decide | claude **72** (108 job), codex 60 (112) |
| architecture.decide | claude **73** (95), codex 64 (104) |
| backend.scaffold | qwen **92** (36), devin 73 (45), codex 43 (23) |
| backend.implement | devin **70** (47), qwen 46 (37) |
| interface.implement | devin **68** (38), qwen 42 (14) |
| interface.draw | codex 51 (72 job), 35% blocked. Chỉ codex vẽ được. |
| interface.audit | devin 11 (18 job), 83% fail |

## Kỳ vọng so với thực tế

- **Claude (Opus 5.5): đúng kỳ vọng.** Claude là ghế suy nghĩ và dẫn đầu mọi kind decide: pass cao nhất,
  median ngắn nhất (5m).
- **Codex (GPT-6 Sol): dưới kỳ vọng ở decide.** Trên cùng loại việc, codex thấp hơn claude khoảng 10 điểm
  (business 60 so với 72, architecture 64 so với 73), và tỉ lệ blocked cao hơn. Codex là **duy nhất** cho
  interface.draw, nên không có pool nào khác để so; 35% blocked ở đó là điểm yếu của cả đường vẽ.
- **Devin (SWE-2 Max): đúng kỳ vọng ở implement.** Devin dẫn đầu backend.implement (70) và
  interface.implement (68). Devin **rất kém ở interface.audit**: 11% pass, 83% fail trên 18 job.
- **DeepSeek (qwen-agent): tốt hơn kỳ vọng ở scaffold** (92%, cao nhất, 36 job), **yếu hơn Devin ở
  implement** (backend 46 so với 70, interface 42 so với 68). **Chưa có dữ liệu decide/review** cho DeepSeek:
  pool này đứng cuối mọi thứ tự think nên gần như không nhận việc đó (business.decide 2 job, chưa settle).

## Lưu ý khi đọc số

- **Rework 70–80%** ở hầu hết pool phần lớn đến từ lỗi runtime đã sửa trong 2026-09-24/25 (liveness,
  dead-worker). Đó không phải chất lượng model; snapshot sau các bản sửa mới cho rework thật.
- **"Blocked" thường là chờ owner hoặc chờ gate** (awaiting-owner, provision.ask, gate thiếu input), không
  phải model làm hỏng. Ví dụ: provision.ask của codex có 82% blocked, của claude 57%.
- **Cửa sổ của qwen trộn hai model.** Trước 2026-09-24 pool chạy qwen3.8-flash, sau đó là DeepSeek V4.1 Flash
  (log token: 24/09 có 2 request qwen3.8-flash và 8703 request deepseek-v4.1-flash). Con số của qwen chưa phải
  số thuần của DeepSeek.

## Đề xuất

- Tỉ trọng: **devin 35 / qwen 35 / claude 20 / codex 10** (hiện tại qwen 40 / claude 20 / codex 20 / devin 20).
- Implement: devin trước.
- Scaffold và việc nhỏ: qwen.
- Decide và review: claude trước, rồi codex. Bỏ qwen khỏi think.
- Draw: codex.
- **Bỏ devin khỏi interface.audit.**

## Quyết định

**Owner đã duyệt (`ok`, 2026-09-25)** toàn bộ đề xuất trên:

- shares devin 35 / qwen 35 / claude 20 / codex 10;
- implement: devin trước;
- scaffold và việc nhỏ: qwen;
- decide và review: claude, rồi codex (bỏ qwen khỏi think);
- draw: codex;
- bỏ devin khỏi interface.audit.

Thay đổi hợp đồng routing được land qua lane riêng (routing-evidence). Khi lane đó land xong, cập nhật
`benchmark/expectations.yaml` (`approvedChange` chuyển vào `history`). Snapshot 72h tiếp theo sẽ kiểm tra
lại quyết định này.
