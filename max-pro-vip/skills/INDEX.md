# Skills

Two lanes, one per axis of the same reasoning: **data decides everything**.

| Skill | Decides | Enter with |
|---|---|---|
| `starci-fe-story-audit-block` | which library entry a **data shape** demands | one block: a name, a cropped screenshot, a story id, a feature sentence |
| `starci-fe-story-audit-composition` | which arrangement a **data volume** forces | a whole page, overlay, or layout |

## Routing

```
one block on the table            -> audit-block
how blocks sit together           -> audit-composition
composition settled its regions   -> hands each region to audit-block
either lane finds a gap           -> propose it as a widget, teacher decides
either lane closes                -> reflect, patch canon in the same turn
```

There is no build lane and no audit lane. The same judgement applies whether the screen exists yet
or not — a data shape demands the same entry either way.

## Two rules that outrank both skills

**Draw, do not ask.** Candidates are rendered as widgets, never listed in prose. Two or more valid
options get drawn side by side; exactly one gets drawn alone, with the reason there is no second.

**Propose, do not enact.** A missing library entry is drawn as a proposal. The teacher decides what
enters the library.

## Before every lane

[`references/house-rules.md`](../references/house-rules.md) — five rules: small context · measure,
don't look · match requirements · visualize on feedback · a feedback fix must be able to patch the
rule layer.
