# StarCi Skills 2.2.0

Cây này chính là runtime. Đọc tiếp `SKILL.md` (bản tiếng Anh là bản được nạp; `SKILL.vi.md` chỉ để người đọc); đó là cửa vào duy nhất,
đóng băng phạm vi của một nhiệm vụ, chọn đúng một operator sở hữu kết quả, rồi định tuyến giữa các
operator dựa trên kết quả có kiểu.

## Thứ tự nạp

0. [`UPDATE.md`](UPDATE.md) (bản soi [`UPDATE.vi.md`](UPDATE.vi.md)): đọc trước khi sửa cây này, không
   bao giờ để chạy một nhiệm vụ. Đó là chuẩn cập nhật một cây skills hình dạng này: bốn câu hỏi theo
   thứ tự, được thêm gì và không được thêm gì, sửa một id thế nào và cho một id nghỉ thế nào, mức bằng
   chứng, luật ngôn ngữ, cưỡng chế trước lời khuyên, file nào được sinh ra, phát hành nghĩa là gì, và
   danh mục kiểm trước khi commit. Một thay đổi chưa đi qua nó thì chưa commit được.
1. `SKILL.md`: cửa vào, vòng lặp định tuyến, và tuyên bố về thẩm quyền.
2. `routing.json`: bảng đóng ánh xạ mọi domain mà mã dừng bàn giao tới (xem bảng Mã dừng trong
   `operators/INDEX.md`) sang bước kế tiếp. Bảng được kiểm đối chiếu với bảng Dừng của các operator,
   nên thiếu một route là lỗi build. `workflows/README.md` nêu cách suy chuỗi từ nhiệm vụ.
3. `resources/`: profile thực thi nào chạy từng vai trò của operator, nó được dùng quyền nào lúc
   chạy, và câu trả lời thường trực về tìm mạng, ràng Grammar, sinh hình. Cũng được kiểm.
4. Đúng một operator mà nhiệm vụ cần: `operator.md` (Việc, Xong khi, Context, Đầu vào, Yêu cầu, Các bước,
   Đầu ra, Dừng, Kế tiếp) cùng `operator.json` (id, domain, resources). Mã dừng tra ở bảng Mã dừng trong `operators/INDEX.md`.
5. Knowledge operator bind; quy trình UI/Grammar kiểm manifest canonical đầy đủ và độ phủ từng Case trước dispatch.

Nạp context theo operator. Việc UI/Grammar khóa manifest canonical đầy đủ cùng catalog và calibration bằng scripts/knowledge-manifest.mjs; việc khác bind topic cần cho quyết định. Mỗi binding có fingerprint và danh sách rule đầy đủ; operator không được viện dẫn ID ngoài inventory.

## Bố cục

```text
SKILL.md                 một cửa vào, operator được liệt kê ở operators/INDEX.md, một bảng định tuyến
routing.json             route operator đóng, bảy loại: operator | resume | chain | user | external | helper | bank (helper và bank vào từ người dùng, không là đích của stop)
alias/                   alias.json (sổ cho máy: vị trí, scheme, bind, ai ghi, vùng) + INDEX.md (bản đồ sinh theo vùng); operator chỉ đọc qua alias
resources/               settings.example.json (cài đặt của người: ngôn ngữ hiển thị, mặc định vi; chép thành settings.json để đè, không track) + tools.json (sổ tool đóng: mode và hỗ trợ theo runtime, gọi bằng @tools/<id>) + agents/profiles/{openai,claude}.json (các profile reasoning/working và profile đọc receipt cũ, quyền theo tool) + orchestrator.json (chế độ dispatch inline | fresh, tối đa 3 agent, chờ theo hoàn tất, khung running, brief, budget, profile tương đương) + interaction.json (loại câu hỏi và dạng báo cáo); có kiểm
workflows/               chỉ còn README.md: cách scripts/plan-chain.mjs suy chuỗi từ các dòng "xong khi" của nhiệm vụ và scripts/validate-chain.mjs từ chối gì; runtime không đọc ví dụ nào — các ví dụ 2.0.0 sống tiếp làm fixture của planner trong tests/chains/
operators/INDEX.md       sinh tự động: mỗi operator đọc gì, tiêu thụ và sinh kind nào, số bước, và mọi mã dừng kèm cách xử lý; operators/errors.json giữ mã dùng chung
operators/<id>/          operator.md (+vi) một file viết tay cho mỗi operator, operator.json (id, domain, resources gồm mode), errors.json (mã riêng), validate.mjs, self-test.mjs, brief.md (sinh tự động: prompt dispatch cho một agent mới, tối đa orchestrator.json#briefBytes)
helpers/INDEX.md         sinh tự động: tầng hỗ trợ bên cạnh các operator — mỗi helper đọc gì, được ghi ở đâu, các bước và mã dừng của nó
helpers/<id>/            helper.md (+vi) một file viết tay cho mỗi helper (Việc, Xong khi, Đọc, Ghi, Yêu cầu, Các bước, Đầu ra, Dừng), helper.json (id, resources gồm profile và mode, writes, primaryOutput), errors.json, validate.mjs, self-test.mjs, brief.md (sinh tự động). Helper dùng context của host session, không tạo user session mới, không ghi source sản phẩm, không chạm runtime và không publish: resources/orchestrator.json#helpers là luật của nó và scripts/validate-helper.mjs từ chối gói nào ra khỏi luật ấy
knowledge/
  ui/composition/        cây phải chứa gì, trước khi nó tồn tại      -> interface.plan, interface.generate
  ui/presentation/       ranh giới do app sở hữu lấy giá trị CSS nào -> interface.generate
  ui/proof/              thứ chỉ đúng sai sau khi đã render          -> interface.audit
  patterns/fe, be        quy ước code trích từ hai source thật
  findings/              sổ <family>.jsonl ghi lỗi audit và đi thử -> scripts/record-findings.mjs ghi, interface.generate trả lời, scripts/promote-findings.mjs soạn proposals/
  grammars/<họ>/         cách một họ hình ảnh hiện thực Common
templates/               mỗi loại tài liệu một template; mỗi template mang khối json template-contract dùng để kiểm cả cây;
                         kinds/ định kiểu mọi file đi qua giữa các bước (<kind>.contract.json + <kind>.skeleton.md cho markdown, <kind>.schema.json cho dữ liệu); step/ giữ hai gate request.json và response.json
scripts/                 validate-routing.mjs, validate-resources.mjs, validate-knowledge-citations.mjs, validate-alias.mjs, validate-templates.mjs, validate-operator.mjs, plan-chain.mjs (chuỗi từ nhiệm vụ; slot import tính là đã sinh), validate-chain.mjs (chuỗi đối chiếu bảng, kế hoạch và request), validate-walk.mjs (cổng của lượt đi thử và sweep của nó), browser-walk.mjs (@tools/browsercontrol chế độ playwright: runner của một lượt đi khai báo), record-findings.mjs, promote-findings.mjs, validate-request.mjs, validate-response.mjs, validate-step.mjs, validate-session.mjs (cả sổ: brief, budget, nhánh bị bỏ rơi, chuỗi), sweep-secrets.mjs (nhà duy nhất của các mẫu hình dạng bí mật, gate response chạy), merge-resolution.mjs (+ spec: bộ luật đóng để giải một xung đột merge, dùng chung bởi lần serve và lần publish, tên luật đọc từ kind delta), generate-operator-briefs.mjs, run-operator-self-tests.mjs (mọi họ gói: operator và helper), validate-helper.mjs, generate-helpers-index.mjs, generate-helper-briefs.mjs, bank.mjs (+ spec: hàng đợi, hash của nó và nhiệm vụ kế tiếp của kho một sản phẩm);
                         unchecked.mjs và record-unchecked.mjs (+ spec), sổ chưa kiểm được đọc và được ghi;
                         device-state.mjs và workspace-portable.mjs (+ spec), thứ package.json của backend gọi tới
readiness/               các schema workspaces/ mà khai báo route portable và hydrate gọi tên trong $schema
```

Sổ chưa kiểm sống ngoài cây, dưới alias riêng của nó `@worktrees/unchecked`
(`<Source>/.worktrees/unchecked/<product>/<featureId>.jsonl`, mỗi mục một dòng chỉ nối thêm, đối chiếu
với `templates/kinds/unchecked.schema.json`; `@worktrees/debts` cũ là một khái niệm khác và giữ nhà
riêng của mình, nơi các khoản nợ gate đã được chủ duyệt mà `quality.verify` đọc). Việc kiểm chứng của
một nhiệm vụ phủ những đơn vị mà hành trình "xong khi" của nó đi qua: bản kế hoạch xếp mọi đơn vị còn
lại là `secondary` kèm lý do, việc toả nhánh không bao giờ gửi một đơn vị như thế tới một làn kiểm
chứng, và `scripts/record-unchecked.mjs` ghi nó xuống thay vào đó — để một lượt chạy thu hẹp phần phủ
của mình trên giấy trắng mực đen và bản kế hoạch kế tiếp đọc lại được thứ vẫn còn chưa kiểm
(`scripts/unchecked.mjs`, `scripts/validate-session.mjs#uncheckedLedgerErrors`).

Kho nhiệm vụ cũng nằm ngoài cây, dưới `@worktrees/banked` (`<Source>/.worktrees/banked/<product>/`:
`queue.json` là thứ tự, `<missionId>/mission.json` và `mission.md` là nội dung, `approvals.json` là câu
trả lời duy nhất của người trên cả hàng đợi). Đó là thứ một helper phác ra và harness nhận việc từ đó:
một phê duyệt tính là goal-confirm của mọi nhiệm vụ hàng đợi liệt kê, mỗi sản phẩm chỉ chạy một nhiệm
vụ một lúc, `dependsOn` được chờ, và hash của phê duyệt bao phủ thành phần hàng đợi cùng nội dung các
nhiệm vụ chứ không bao phủ tiến độ — nên chạy và xong không đổi gì, còn thêm, đổi thứ tự, bỏ hay sửa
goal thì trả câu hỏi về cho người (`scripts/bank.mjs`, `scripts/validate-session.mjs#bankRefErrors`).
Bên cạnh nó, `@worktrees/helpers/<id>/runs/<runId>/run.json` là toàn bộ dấu vết một lần chạy helper để
lại, và mọi kho đều gọi tên lần chạy đã phác ra nó.

`npm test` chạy kiểm định tuyến, kiểm resources, kiểm trích dẫn knowledge, kiểm template, kiểm brief sinh ra, mọi self-test của operator và helper, và các spec của script. Head đã publish phải xanh, không xanh
thì không được publish.

## Luật áp dụng khắp nơi

- Một operator làm đúng một việc trong một lượt tuyến tính. Nó không gọi operator khác, không điều
  phối workflow, không tự dừng giữa chừng, không trả chỉ dẫn điều khiển dạng văn xuôi. Chỉ cha mới
  ánh xạ một output đã validate sang bước chuyển kế tiếp.
- Chỉ trường đã validate mới định tuyến được. Văn xuôi trong receipt, kết quả kể bằng lời, hay
  output trượt validator đều không định tuyến.
- Thẩm quyền nằm trong schema của operator, không nằm ở file này hay ở `SKILL.md`. `git.publish`
  không diễn đạt được force push; `release.deploy` không chạy nổi khi thiếu authorization đã khai;
  `uat.verify` không có trường nào chứa nổi credential.
- File `.md` tiếng Anh là authority duy nhất lúc chạy. File `.vi.md` cùng tên là bản đọc cho người,
  không bao giờ vào context manifest, danh sách phụ thuộc, hay binding của
  operator; validator đối chiếu chỉ đọc chúng để chứng minh bản gương không lệch, không bao giờ coi là authority.
- Mã rule là địa chỉ công khai ổn định. Chỉ thêm; không đánh số lại, không dùng lại, không lặng lẽ
  đổi nghĩa.

## Dòng dõi

2.2.0 (2026-09-05): session gắn với host của người dùng, workflow từ goal đã xác nhận, attempt khóa expected rồi đối chiếu actual, tối đa ba worker cô lập và đóng phiên bằng compact cùng bundle đã kiểm chứng. Mỗi operator/helper có quy trình cụ thể; UAT tách owner account và seed, UI/Grammar kiểm manifest knowledge và mindset family, knowledge.repair khép vòng sửa luật. Suy luận dùng Sol/Fable; thực thi dùng Sol/Opus. Bằng chứng và giới hạn: tests/evidence/20260905-starci-2.2-release.md.

2.1.4 (2026-09-05): ba phiên chạy thật — hành trình AgentOS Setup được chứng minh và publish, hành trình tự phục hồi workspace được chứng minh qua chính bộ e2e của repo, và bản sửa đóng nốt một finding của nó (`tests/evidence/20260905-nivo-setup-uat-on-2.1.2.md`, `20260905-nivo-recovery-e2e-on-2.1.2.md`, `20260905-nivo-recovery-operations-fix-on-2.1.3.md`) — gặp bảy bức tường thuộc về cây chứ không thuộc về công việc, và mỗi bức đóng bằng một cửa kèm một spec. Một bản phát hành có thể khai họ hình ảnh nó hiện thực hoá, và bản khai họ là một **bản phát hành trình bày**: thứ nó sửa chỉ tồn tại khi đã render, nên lần consume lấy hai nửa trước và sau từ hai nhánh `interface.audit` của cùng phiên thay vì từ một spec consumer viết ra cho dịp này — spec ấy chỉ chứng minh cái dấu chứ không chứng minh bề mặt. `templates/kinds/dependency-plan.schema.json#regression` có thêm hình dạng `audit` bên cạnh hình dạng spec, và `operators/library-update/validate.mjs#auditProofErrors` giữ cặp ấy đúng là hai nửa của một proof: cả hai ref giải được, cả hai chấm đúng những lời khai ấy theo định danh, version mỗi bên quan sát là version nửa của nó đại diện, lời khai của nửa trước fail và được định tuyến về chủ họ như một grammar gap, lời khai của nửa sau pass, và nửa sau đo tại chính commit mà nhánh này commit. Một nhánh cấp phát trọn dàn diễn viên của luồng: `resources/identity-plan.schema.json` mang `accounts`, mỗi mục một alias mà luồng gọi tên, luật plan-sha giữ nguyên, và `planCastErrors` từ chối một hồ sơ công bố ít alias hơn kế hoạch đòi hoặc một alias kế hoạch chưa từng đòi — một luồng chỉ đúng khi hai vai gặp nhau không còn phải đi hai nhánh mà không gì nối hai kế hoạch lại. Giá trị ô mật khẩu được che trong bản ghi DOM và bản chụp accessibility trước khi ghi, nên một lần đăng nhập bị từ chối — mà form vẫn còn giữ thứ vừa gõ vào — là một trạng thái lượt đi chụp được, thay vì bị sweep bí mật từ chối cả bức; sweep vẫn đọc mọi byte của mọi file một lượt chạy để lại. Dòng phạm vi của khối goal đếm mọi kế hoạch phiên đã cho tới nơi chứ không riêng kế hoạch mới nhất (`scripts/validate-session.mjs#missionScopeErrors`, tách khỏi kiểm tra ngân sách vốn tự bỏ qua khi `budget.units` vắng), vì một nhiệm vụ vẽ bề mặt rồi vẽ luồng thì có hai kế hoạch và một phạm vi phủ, và đọc theo kế hoạch mới nhất đã để một kế hoạch toàn journey xoá mười một bề mặt đã hoãn khỏi đúng dòng người đọc. `git.publish` giải xung đột merge bằng đúng bộ bốn luật đóng mà chủ runtime dùng cho một lần merge tích hợp, dùng chung `scripts/merge-resolution.mjs` với tên luật đọc ra từ kind delta chứ không chép: mọi hunk đã giải được ghi trên biên bản dưới `## Resolutions` kèm file, dải dòng và luật, `MERGE_RESOLVED` và `MERGE_GATED` được ghi cùng, phía của phiên đi vào chỉ được lấy trong file mà chính `changes` của nó gọi tên, và một hunk ngoài bộ luật vẫn là `NON_FAST_FORWARD` cho người — một baseline lint định dạng lại đúng những dòng một bản sửa ba dòng chạm vào không còn tốn cả một nhiệm vụ. Kho được chạy thật: route `bank` (`routing.json#kinds.bank`, Entry của `SKILL.md`) lấy `scripts/bank.mjs#next`, đánh dấu mục `running:<sessionId>`, mở phiên với `mission.bankRef`, và chỉ đánh dấu `done:<sessionId>` khi phiên ấy kết thúc done, nên một nhiệm vụ dừng vì người thì ở lại `running` và tạm dừng cả kho; `bankRefErrors` từ chối một hàng đợi đọc ngược lại những gì đã xảy ra. Và bản sửa nóng đang nằm trong cây được hoàn tất tại chỗ: `interface.audit` khai `feature`, nên một audit hoãn phủ phải gọi tên một sổ hoặc bị từ chối trước khi tới bề mặt; một lens thừa kế hợp lệ khi không mang anchor nào của riêng nó; và writer sổ chưa kiểm không còn in ra một file nó chưa từng mở.

2.1.3 (2026-09-05): việc chuẩn bị được tách khỏi việc bàn giao, và được cho một tầng riêng với những lời từ chối của riêng nó. Một **helper** là việc hỗ trợ nằm ngoài mọi quy trình — nó không mở phiên, không ghi source sản phẩm, không chạm runtime, không publish và không hỏi gì — và `helpers/<id>/` là gói của nó bên cạnh `operators/<id>/`: một `helper.md` viết tay (+ bản đối chiếu) với Việc, Xong khi, Đọc, Ghi, Yêu cầu, Các bước, Đầu ra và Dừng, cùng `helper.json`, `errors.json`, `validate.mjs`, `self-test.mjs` và một `brief.md` sinh tự động, được liệt kê trong `helpers/INDEX.md` sinh tự động. `scripts/validate-helper.mjs` đọc gói ấy đúng như `validate-operator` đọc một gói operator — Tham số đối chiếu Yêu cầu, Các bước đối chiếu Dừng đối chiếu sổ mã, Đầu ra đối chiếu schema của kind, tiếng Anh đối chiếu bản mirror — và thêm ba lời từ chối làm nên tầng này: mọi alias trong bảng Ghi phải là alias mà `alias/alias.json` đánh dấu `helperWritable`, thứ mà không route source sản phẩm nào, không runtime owner nào và không đích publish nào mang, nên sổ alias trả lời chứ không phải một danh sách nằm trong cửa kiểm; mọi tool phải là một chế độ mà `resources/orchestrator.json#helpers.tools` cho phép, nên `git` dừng ở `read` và mọi chế độ ghi checkout, vận hành runtime, publish hay giải bí mật đều vắng mặt chứ không phải khai `never`; và chế độ chỉ là `inline` hoặc `isolated`, vì helper vào từ người dùng — kind `helper` mới của `routing.json`, `/helper <id> <args>` — chứ không bao giờ từ một bức tường, nên không bảng route nào gọi tên nó. Mỗi lần chạy để lại đúng một dấu vết, `@worktrees/helpers/<id>/runs/<runId>/run.json` (`helper-run`): helper nào, tham số gì, mọi đầu vào kèm head đã đọc, mọi đầu ra, profile và hai mốc thời gian. Thứ một helper phác ra là một **kho**, `@worktrees/banked/<product>`: `queue.json` là thứ tự (`bank-queue`), `<missionId>/mission.json` và `mission.md` là nội dung (`banked-mission`: khối goal mà một phiên chép nguyên vào `state.json.mission`, các route, môi trường, và `evidenceRefs` — ít nhất một mục chưa kiểm, finding, lượt đi thử, lượt chạy API, đường dẫn source hay ghi chú, vì nhiệm vụ không gọi tên được cái nào chỉ là một ý nghĩ và bị từ chối bằng `BANK_UNGROUNDED`), và `approvals.json` là câu trả lời duy nhất của người trên cả hàng đợi (`bank-approvals`). Câu trả lời duy nhất ấy là goal-confirm của mọi nhiệm vụ hàng đợi liệt kê: một phiên mở ra từ một mục trong kho ghi `state.json.mission.bankRef`, in khối goal ra và không chờ, tự ghi goal-confirm của mình với phê duyệt làm `sourceRef` (`SKILL.md` bước 1). `scripts/validate-session.mjs#bankRefErrors` giữ cho lối tắt ấy trung thực — mục phải còn đó, phải gọi tên chính phiên này, phải mang đúng từng chữ cái goal mà phiên đang chạy, và phê duyệt phải còn phủ được kho như nó đang là — còn hash mà phê duyệt lấy trên đó phủ thành phần hàng đợi cùng nội dung các nhiệm vụ chứ không phủ tiến độ, nên chạy và xong không đổi gì, trong khi thêm, đổi thứ tự, bỏ hay sửa goal thì trả câu hỏi về cho người. `scripts/bank.mjs` là nhà duy nhất của bố cục ấy: `next` trả về mục banked đầu tiên có mọi `dependsOn` đã done và không trả gì cả khi còn một mục anh em đang chạy, nên mỗi sản phẩm chỉ chạy một nhiệm vụ một lúc và nhiệm vụ dừng lại vì người sẽ tạm dừng cả kho bằng cách cứ ở `running`; không gì trong kho ghim head, và mỗi nhiệm vụ tự preflight và tự vẽ chuỗi của nó khi mở ra. Helper đầu tiên là `generate-banks`, chạy trên profile đọc: nó đọc route của sản phẩm, sổ chưa kiểm, các finding chưa ai trả lời, các lượt đi thử và lượt chạy API đã hỏng, model tính năng và ghi chú của người, rồi phác một kho xếp theo thứ mỗi nhiệm vụ chờ, rồi theo ưu tiên, rồi theo id, mang theo tier của mục chưa kiểm như một gợi ý mà kế hoạch có quyền ghi đè.

2.1.2 (2026-09-05): một phiên chạy trọn cả thang — hai checkout đã định tuyến, một lần serve, hai lần audit dưới runner đi thử, các gate và một lượt đi tới được bề mặt — gặp bảy khiếm khuyết của chính cây chứ không phải của công việc, và mỗi cái được đóng bằng một gate kèm spec (`tests/evidence/20260905-nivo-reachability-fix.md`). Sweep của lượt đi đem câu hỏi origin ra hỏi chính bản ghi của runner, nên mọi bức chụp một trang có icon nội tuyến hay chạy framework ở chế độ phát triển đều bị từ chối, và sổ finding chịu chung số phận: phép kiểm origin giờ chỉ đọc thứ agent tự viết, `scripts/validate-walk.mjs#PAGE_RECORD` giữ bản ghi DOM, ảnh cây accessibility, bản đo và biên nhận host ở ngoài đúng phép kiểm ấy trong khi sweep bí mật và sweep mã trình duyệt vẫn đọc từng byte một lượt chạy để lại (`scripts/browser-walk.spec.mjs`), còn `operators/interface-audit/self-test.mjs` đưa một biên nhận do runner lái kèm các bản ghi ấy qua validator của operator rồi vào thẳng `knowledge/findings/<family>.jsonl` — khiếm khuyết thứ hai được đóng bởi chính cái thứ nhất. `@tools/host` được dừng bằng một marker mà server đang chạy tự dò và tuân theo — đặt tên theo pid của nó trong thư mục temp của hệ thống, địa chỉ duy nhất mà một bên dừng chỉ cầm pid tính ra được — vì tín hiệu trên một số nền tảng là kết liễu cứng và một biên nhận đóng kiểu ấy không bao giờ có `stoppedAt` (`scripts/host-artifacts.mjs#requestStop`, spec trên đúng hình dạng đã ghi). `brief.proven` nhận cả nhánh mở đường cho nhánh khác bên cạnh dòng "xong khi": `templates/step/state.schema.json#/$defs/provenEntry` công bố cả hai cách viết và `scripts/validate-session.mjs#provenErrors` đọc chính pattern ấy thay vì giữ một bản sao, giải `prerequisite:<N/M>` qua đúng cái sổ goal ấy. Một bản giao không bố cục gì thì không còn bị phán như thể nó có: `TASTE-12` Case 5 cho tiêu chí tham chiếu thôi áp dụng khi direction khai delta trình bày là `none` — hàng ghi `n/a`, nằm ngoài điểm trung bình lẫn tập chặn cửa — và bản thân ống kính thẩm mỹ được mang từ lần audit đã phán chính bố cục ấy, trích dẫn dưới `## Calibration` là `inherited` kèm nhánh đã chấm (các ô đã nới của hợp đồng audit), nên không nợ mốc nào, và một `fix-first` mang theo kiểu ấy để finding đứng nguyên chỗ nó được nêu và cho `next` gọi tên `quality.verify`, còn một fix-first trên bố cục mà bản giao có chạm thì giữ nguyên luật cũ. Và một nhiệm vụ publish hai route kết thúc trong một chuỗi: `uat.verify` bàn giao sang `quality.verify` khi một checkout đã định tuyến khác còn chờ gate của nó, planner xếp một ranh giới khi mọi nhánh chưa xếp đều là ranh giới, và thứ tự các dòng "xong khi" của chính nhiệm vụ phân xử consumer đọc nhánh nào của một operator lặp lại, nên lượt đi đọc các gate đứng trước nó chứ không phải các gate đứng sau (`tests/chains/two-route-publish.json`, và `scripts/plan-chain.spec.mjs` giờ đọc mỗi ví dụ như một dãy con của các bậc đã xếp).

2.1.1 (2026-09-05): phần phủ cố ý không lấy thì được ghi ra, để không có gì bị bỏ qua trong im lặng và không lượt chạy nào render mọi màn hình cho chắc. Một bản kế hoạch xếp tier cho từng đơn vị theo chính hành trình "xong khi" của nhiệm vụ (`templates/kinds/units.schema.json`: `tier: journey | secondary`, vắng thì đọc là `journey`, và đơn vị `secondary` mang `deferral.reason`), nói điều đó hai lần thành một lời duy nhất — cột `Tier` mà người đọc và `tier` mà việc toả nhánh chạy trên đó (`scripts/unchecked.mjs#tierErrors`) — rồi các làn kiểm chứng đi theo đúng lời ấy: `scripts/unchecked.mjs#VERIFY_LANES` là nhà duy nhất của việc operator nào chứng minh làn nào, `scripts/validate-request.mjs#unitGateErrors` từ chối một đơn vị `secondary` bị gửi tới `interface.audit`, `uat.verify` hay `api.verify`, và `budget.units` chỉ đếm phần hành trình (`#budgetUnitsOf`, `scripts/validate-session.mjs#unitBudgetErrors`), nên một nhiệm vụ thu hẹp phần phủ của mình cũng thu hẹp ngân sách trong cùng một động tác, trong khi `interface.generate` vẫn dựng hết những gì kế hoạch liệt kê, vì phạm vi sinh là mục tiêu của người còn phạm vi chứng minh mới là của hành trình. Thứ không được chứng minh thì nằm lại là chưa kiểm trên một sổ chỉ nối thêm: `@worktrees/unchecked/<product>/<featureId>.jsonl` (`templates/kinds/unchecked.schema.json`, một alias của riêng nó, đứng cạnh những khoản nợ gate đã được chủ duyệt của `@worktrees/debts` — cái tên và ý nghĩa cũ ấy giữ nguyên không đụng tới), do `scripts/record-unchecked.mjs` ghi ở đúng transition nhận biên nhận, y như `record-findings.mjs`, được tất toán bằng một dòng thứ hai gọi tên nhánh cuối cùng đã phủ nó, và bị `scripts/validate-session.mjs#uncheckedLedgerErrors` từ chối khi thiếu. Hoãn chỉ hợp lệ ở ngoài hành trình: `interface.audit` không được hoãn một state mà người đọc gặp trên đường đi qua một bề mặt thuộc hành trình (`audit-scope.schema.json#journeyStates`, tập đóng mà cổng đọc chứ không chép lại), và dừng bằng mã mới `UNCHECKED_UNLAWFUL` trên domain mới `plan`, quay về `interface.plan`, vì cách sửa là một lần xếp tier khác chứ không phải một lượt audit nhỏ hơn; mọi state được hoãn đều mang lý do (`auditScope.deferrals`) để sổ chép lại thay vì tự bịa. Phần chưa kiểm sau đó được tiêu chứ không bị quên: `interface.plan` và `uat.plan` bind sổ và bị từ chối nếu bỏ một mục đang mở khỏi danh sách thay vì kiểm nó ở tier `journey` hay gia hạn nó ở tier `secondary`; `business.reconcile` mang phần còn mở của tính năng vào mục mới `## Unchecked` và công bố lại một head còn hành trình chưa kiểm ở `in-progress` chứ không phải `implemented` — đúng chữ mà vòng đời vốn đã có cho một lời hứa mới được mang đi xa đúng bằng mức nó đã được mang; và `release.deploy` từ chối một lần release production trên một hành trình chưa kiểm (`UNCHECKED_OPEN`, về tay người, người ấy hoặc cho chạy làn ấy hoặc chấp nhận dòng ấy), trong khi một mục `secondary` và mọi đích không phải production đều đi qua. Người chỉ gặp phần thu hẹp đúng một lần, ở dòng thứ năm của khối goal — `scope: <n> journey units, <m> unchecked`, điền từ chính các con số đếm của kế hoạch qua `state.json.mission.scope` — và không bao giờ bị hỏi lại về nó nữa.

2.1.0 (2026-09-05): điều người gặp đầu tiên được chốt một lần. `resources/settings.example.json` mang ngôn ngữ hiển thị (`vi`) và người đè nó trong `resources/settings.json` không track (`scripts/settings.mjs`, gate trong `validate-resources`; `interaction.json#language`); một prompt mới hỏi đúng một câu, khối goal, và từ đó lượt chạy phải mượt — log transition in ra mà không chờ, vẽ lại dưới cùng goal và vào lại theo route không hỏi gì, và chỉ route `user`, một `budget-choice` hay goal được sửa mới dừng để hỏi người (`interaction.json#asks`). Một bản giao đã chứng minh thì tự phát hành: `library.update` mode `publish` kết thúc ở một `library-release` qua tool mode `registry:publish` (`LIBRARY_PUBLISH_REJECTED` khi registry từ chối), `git.publish` đưa nhánh session lên nhánh đích bằng fast-forward hay merge commit qua chính hook của kho, và không hỏi thẩm quyền của ai cho một lần phát hành mà các gate đã chứng minh. Profile được chia theo việc: Opus 5 (`opus`) và Sol 5.6 (`sol-fresh`) sản xuất; Fable 5.1 (`fable`) và Astra 6 (`astra`) đọc và phán xét, nên `environment.preflight`, `interface.plan`, `interface.audit`, `quality.verify`, `uat.plan`, `uat.verify`, `data.plan` và `backend.plan` bind `astra`, và `orchestrator.json#profileEquivalents` ghép opus với sol-fresh và fable với astra. Hai operator catalogue còn thiếu: `service.operate` (domain service, inline, `sol-fresh`; kind `service-receipt`; `SERVICE_UNPROVEN` quay về dịch vụ, `SERVICE_APPROVAL_REQUIRED` tới người; môi trường khai `services` và báo cáo sẵn sàng thêm họ `service`) vận hành observability, một dịch vụ Sonar hay một tunnel cạnh runtime đang phục vụ; `api.verify` (domain api, isolated, `astra`; kind `api-verification`; `API_CASE_FAILED` tới backend, `API_NAMESPACE_LEAK` tới caller) chạy bộ kiểm end-to-end của chính kho như một client đối với runtime đang phục vụ trên namespace seed đã đặt, đọc lại mọi bản ghi qua API chứ không bao giờ qua store, và phát hành một hồ sơ lượt chạy chỉ-thêm dưới `@worktrees/e2e/<flow>/runs/<runId>/` — nhà bằng chứng e2e mới cạnh `@worktrees/uat`, nơi giữ các lượt đi trình duyệt — được tới từ `runtime.serve`, `quality.verify` và `data.seed` và được `business.reconcile` đọc; `tests/chains/service-fanout.json` và `backend-e2e.json` chứng minh chỗ đặt. `business.reconcile` phát hành head businesses qua `scripts/business-registry.mjs` (địa chỉ nội dung JCS của object, self-fingerprint của model) và một head `implemented` có thể được rebind sang một head `implemented` sau. Bốn nợ 2.0.4 đóng trong `scripts/workspace-checkout.mjs`: operator ghi source ghi `Preflight`, `Reflog before` và `Reflog after` trong `## Binding` của changes.md, và một stash, reset, force, clean, checkout nhánh khác, rebase hay am bên trong checkout đã route bị từ chối bởi chính reflog nó để lại (`orchestrator.json#sourceWrites.history`); một bind được kiểm lại theo tổ tiên (`git merge-base --is-ancestor`) thay vì head bằng nhau; một junction `node_modules` trỏ ra ngoài checkout cần `sharedInstall` được khai, `## Checkout` ghi `Installed tree`, và không gì dưới checkout như thế bị xoá tay.

2.0.3 (2026-09-05): một lượt đi thử gõ địa chỉ, gọi mutation hay bấm xuyên DOM để lại một biên nhận đọc y hệt một biên nhận thật thà, nên trình duyệt không còn là thứ agent được lập trình — `@tools/browsercontrol` có thêm chế độ `playwright`: agent viết một lượt đi khai báo (kind dữ liệu `uat-walk`: target chỉ là vai trò và tên, route vào đúng một lần ở bước 1, thông tin đăng nhập theo tên mà runner phân giải từ tham chiếu niêm phong ngay lúc điền và không bao giờ in ra) rồi `scripts/browser-walk.mjs` chạy nó trên Chromium headless từ bản cài Playwright duy nhất của máy chủ (`resources/tools.json#browsercontrol.install`, đứng cạnh cây và không bao giờ nằm trong cây), mỗi lượt một ngữ cảnh mới tinh mang viewport, tỉ lệ, tông màu và tuỳ chọn chuyển động của chính lượt đi, dừng ở bước hỏng đầu tiên mà không thử lại và không đoán locator, và ghi `walk-result`, sổ từng bước, mỗi bức chụp một ảnh kèm ảnh cây truy cập và bản ghi DOM, một trace, còn với lượt đi UAT là `uat-capture` mà mọi điều khiển đều chép từ lượt đi; `scripts/validate-walk.mjs` và sweep của nó từ chối selector, goto sau bước 1, bí mật viết chữ, URL ngoài origin của route và mã trình duyệt; `uat.verify` và `interface.audit` có thêm dòng viết lượt đi và dòng chạy nó, validator của hai operator giữ biên nhận ghi chế độ này đúng chế độ ấy — lượt đi đứng cạnh kết quả ở đúng digest đã chạy, điều khiển bằng target của lượt đi, kết cục là của runner, bức chụp không có lượt đi bị từ chối; `environment.preflight` báo `host.playwright` và validator của nó đọc lại bản cài từ máy chủ. Lượt đi sản phẩm đầu tiên (tests/evidence/20260905-nivo-setup-uxui-on-2.0.3.md) cho thấy một bức chụp không phải một phép đo — không giá trị tính toán nào tới được người audit dưới một chế độ cấm nó viết mã trình duyệt — nên runner, vốn là mã của cây, đo ở mỗi lần chụp: một lần đánh giá phía trang ghi kind dữ liệu `capture-measurements` cạnh ảnh chụp (hộp, style đã tính, nền hiệu dụng, độ tương phản WCAG, mỗi phần tử của cây truy cập và mọi phần tử mang id, data-contract hay data-testid một mục, giới hạn theo kind), `walk-result` nêu tên nó, `validate-walk` kiểm hình dạng của nó và sweep vẫn chỉ đọc những gì một lượt chạy đã ghi, còn validator của `interface.audit` từ chối một kết quả presentation, contrast, accessibility hay responsive trên bức chụp runner đã lái mà không trích dẫn `ref` và giá trị của bản ghi, hoặc nêu một con số runner chưa ghi (`verdicts.results[].measurement`). Sổ từng bước gắn nhãn mỗi bước bằng điều khiển chính bước ấy nêu — entry cho goto, null cho bước chụp, bước chờ hay kỳ vọng chỉ có url — không bao giờ là target của bước trước (`validate-walk#stepOwnControl`). Alpha: runner được chứng minh bằng `scripts/browser-walk.spec.mjs` trên một trang tĩnh phục vụ qua loopback, và bằng một lượt đi sản phẩm.

2.0.2 (2026-09-05): hai nhiệm vụ Nivo chạy lại trên 2.0.0 (tests/evidence/20260905-nivo-recovery-on-2.0.0.md, 20260905-nivo-setup-uxui-on-2.0.0.md) gặp những cánh cửa đóng chứ không phải bức tường chậm, và 2.0.2 mở mỗi cửa bằng một gate. Reachability: `validate-operator` từ chối operator mà không bảng Next nào dẫn tới từ `environment.preflight` (D1, D6, D7), và các dòng làm cây tới được đã có — `workspace.bind` giao cho `architecture.decide`, `interface.plan` và `data.plan`; `architecture.decide` cho `backend.plan`; `runtime.serve` và `quality.verify` cho `uat.plan`. Cặp plan/execute đủ bộ: `data.plan` (kind `seed-plan`, mỗi luồng một đơn vị seed với namespace, khối lượng và rollback) và `backend.plan` (kind `backend-plan`, mỗi module contract một đơn vị) đứng cạnh `interface.plan` và `uat.plan`, và `tests/chains/seed-fanout.json`, `backend-fanout.json` chứng minh việc toả ra. Producer đã import là producer: `plan-chain` đọc các slot import của phiên và in "imported from <session> step N", `validate-chain` chỉ tính kind qua `validate-request#validateImportedInput` (D2), cổng origin của `producer-import` kiểm output và byte chứ không kiểm `next`, nên bundle 1.x import được (D5); nhiệm vụ dạng "làm thứ đã quyết" vẽ chuỗi không cần `business.decide`. Kế hoạch chốt requirement của nhánh thành `state.json.planned` trước khi request tồn tại, cổng chuỗi đọc role của bind từ kế hoạch cho tới khi request được ghi, và request dispatch lệch kế hoạch bị từ chối (D3); `gitPolicy` có kiểu đúng object mà route khai và sai hình bị từ chối gọi tên field (D4). Auditor được hiệu chuẩn: `knowledge/ui/proof/calibration/` giữ ba sheet mốc với dải điểm TASTE và UX mong đợi, receipt `interface.audit` có chấm gu hay UX mang `## Calibration` và `## Ranked against`, điểm lệch dải quá dung sai là `CALIBRATION_OFF`, làn đo được chấm mù từng đơn vị còn gu chấm một lần cho cả tính năng (TASTE-13 Case 9). Cây học từ finding của mình: kind `findings`, `knowledge/findings/<family>.jsonl` được `scripts/record-findings.mjs` nối thêm sau mỗi audit hay lượt đi thử done, `validate-session#findingsLedgerErrors`, `interface.generate` bind các finding còn mở của bề mặt và trả lời từng cái dưới `## Findings answered`, `scripts/promote-findings.mjs` soạn đề xuất rule từ finding chưa có rule gặp ở hai phiên. Self-test đọc `.stacks/<env>` dùng host giả của chính chúng. Giữ từ 2.0.1-alpha.1: `fixSize` theo số lượng, loại và độ kiên nhẫn; đồ thị operator đóng. Package owner và consumer có thể nằm ở hai repo: `library.update` nhận `mode` — `full` như cũ, `publish` chỉ nửa owner và kết ở bản ghi `library-release` (kind dữ liệu mới: tên, version, digest sha512, đường dẫn archive, commit package) cạnh `library-archive` mà nó định danh, `consume` chỉ nửa consumer trên một bản phát hành mà phiên anh em đã sinh và đã import — nên nhiệm vụ anh em của route `chain` publish còn nhiệm vụ bị chặn vào lại để consume; preflight chỉ phân giải đường dẫn package ở nơi nửa package chạy, và mục receipt của nửa mà nhánh không chạy bị từ chối (`operators/library-update/validate.mjs#modeSectionErrors`). Còn mở, ở TODO 4: Playwright (2.0.3).

2.0.1-alpha.1 (2026-09-05): ranh giới giữa fix và sinh lại là của orchestrator theo số lượng, loại và độ kiên nhẫn — `resources/orchestrator.json#fixSize` nêu maxFiles, các topic knowledge và tiền tố rule không bao giờ là fix (composition; TASTE, UX), và escalateAfter; `validate-request` từ chối request `interface.fix` cho phát hiện composition hay gu, hoặc cho phát hiện đã fix trong phiên (`FIX_TOO_LARGE` trước khi dispatch); đồ thị operator đóng — `validate-operator` từ chối Input bắt buộc không operator nào khác sinh ra và primary output không bảng Inputs nào đọc trừ khi operator kết một chuỗi (dòng `user`, publish, deploy), nhờ đó thêm `business-reconciliation` vào `git.publish`, `seed-receipt` vào `uat.verify` và `interface.audit`, `uat-plan` vào `uat.verify`, `library-source-application` vào `interface.generate`, và điểm kết ở người cho `content.generate`. Alpha: hai nhiệm vụ Nivo chạy lại trên 2.0.0 là bằng chứng; phản hồi của chúng định hình 2.0.2, bản 2.0 đầu tiên để dùng. Còn nợ 2.0.2: `data.plan` và `backend.plan`, hiệu chuẩn auditor, sổ finding.

2.0.0 (2026-09-05): năm nhiệm vụ Codex trên cây 1.7–1.9 tiêu phần lớn thời gian vào chờ theo đồng hồ, dispatch lại cùng một operator và những bức tường không ai sở hữu (tests/evidence/20260904-codex-five-tasks-retrospective.md); 2.0 trả lời mỗi bức tường bằng một gate, và dựng lại cây quanh một luật: một agent mù làm đúng một việc trên đúng một đơn vị với một goal kiểm được. Goal của nhiệm vụ được người xác nhận trước bước 1 (`state.json.mission`: goal, gồm, không gồm, các dòng xong khi kèm producer; lựa chọn `goal:<session>:v<n>`; vẽ lại là transition `replanned`); mọi operator nêu `## Done when` trong một câu gọi tên `primaryOutput` của nó; mọi nhánh nêu goal (`request.json.goal`: một dòng xong khi hay nhánh nó mở đường) và receipt mang `goalCheck` được validator đối chiếu với output thật, ba nhánh done phục vụ một dòng xong khi mà không thêm bằng chứng thì chuỗi dừng, và mỗi transition in ra chat gốc đúng hai dòng. Một operator một việc: `platform.operate` tách thành `identity.provision`, `data.seed` và `runtime.serve`; `release.deploy` giữ image còn `migration.release` giữ bộ migration; `business.decide` giữ mô hình còn `business.reconcile` đối chiếu lời hứa với thứ đã giao; bộ ba frontend gộp thành `interface.generate` (mù, một trang hay một modal), `frontend.surface.audit` thành `interface.audit` (mù) và `interface.fix` nhận finding dưới cỡ fix (`FIX_TOO_LARGE` khi vượt); `library.source.apply` và `dependency.update` gộp thành `library.update`; `backend.source.apply` thành `backend.generate` với `scope: full|fix`; `workspace.bind` bỏ nửa runtime và `git.publish` bỏ việc dừng server. Mọi operator chạy `inline` hay `isolated` (`resources.mode`, `dispatch` dành cho orchestrator, tối đa một); agent isolated chỉ thấy alias mà bảng Context của nó bind và file mà `request.json` nêu, và `brief.md` của nó nói rõ điều đó; mọi bảng Steps là một quy trình tối đa mười hai dòng mở bằng động từ. Cặp plan/execute cho mọi thứ có N đơn vị: `interface.plan` và `uat.plan` in danh sách `units` ra chat gốc, operator execute chạy mỗi nhánh một đơn vị, tối đa ba nhánh một bậc, và budget nới theo đơn vị (`budget.perUnit`). Workflow ví dụ không còn trong runtime: `scripts/plan-chain.mjs` suy chuỗi từ các dòng xong khi qua bảng Đầu vào, Context và Kế tiếp (preflight khi có tool tác dụng phụ, vai được bind, plan trước khi toả ra, audit và đi thử giữa bề mặt đã áp và lần publish), in ra hai dòng mỗi nhánh, và `scripts/validate-chain.mjs` chấp nhận nó trong `validate-session` sau mỗi transition; mười một chuỗi 1.x là fixture của planner dưới `tests/chains/`; route `chain` gọi tên operator mà phiên anh em phải sinh primary output của nó. Full Sol: mọi operator bind `sol-fresh` hay `sol-reviewer`, `luna` và `sonnet` nghỉ. Giữ từ bản cắt 2.0 đầu: `environment.preflight` mở mọi chuỗi và báo mọi bức tường cùng lúc; direction khai `Presentation delta`; `backend.generate` sở hữu theo ranh giới và ghi mở rộng cần thiết là `OWNER_WIDENED`; seed quy được về luồng bằng sở hữu tài khoản hay tiền tố; orchestrator ghi khung `running` và agent thoát không receipt là `RECEIPT_MISSING`; `state.json.brief` là trí nhớ duy nhất của orchestrator và `state.json.budget` giới hạn số bậc và số lần vào lại (`BUDGET_EXHAUSTED`); `business.decide` và `architecture.decide` dừng với `RESTATEMENT_UNCONFIRMED` cho tới khi người xác nhận câu nói lại; mỗi lượt kết thúc bằng một trong ba dạng báo cáo; agent mới nhận `brief.md` sinh ra dưới 3 KB; gate response quét mọi receipt tìm giá trị hình dạng bí mật; môi trường khai `external-upload`. Observability, dịch vụ Sonar và tunnel chưa có operator trong catalogue này cho tới `service.operate` ở 2.1.0. Major: hợp đồng định tuyến thêm một loại, trạng thái response thêm `running`, một mã dừng có thể do orchestrator ghi, id operator đổi, và `workflows/*.json` không còn được đọc.

1.9.0 (2026-09-04): sửa thư viện owner và cập nhật dependency đã kiểm có operator giới hạn cùng bằng chứng regression; provisioning bind đúng custody provider và chứng minh đăng nhập sản phẩm; audit UI ưu tiên các bề mặt chính đã chọn và giữ rõ giới hạn của các state phụ để sau.

1.8.0 (2026-09-04): tương tác có một chính sách giao tiếp cùng gate câu hỏi/lựa chọn có kiểu; người dùng chọn hướng khác biệt dựa trên bằng chứng render, lựa chọn đã ghi được dùng tiếp, và bỏ hỏi xác nhận thường lệ trong việc đã được giao; quyền thao tác và định tuyến giữ nguyên.

1.7.9 (2026-09-04): mở phiên là hành động đầu tiên của một nhiệm vụ có viết, không phải câu hỏi đặt cho người và không phải việc làm sau lần viết đầu tiên — nhiệm vụ đã lỡ viết ngoài phiên thì mở phiên rồi chuyển việc lên nhánh của nó; một lần bind thấy source chưa commit trên nhánh mutation thì từ chối, dù write root đã khai có phủ nó hay không.
1.7.8 (2026-09-04): một giá trị niêm phong chỉ được phân giải tại đúng chỗ tiêu thụ — một lệnh chẩn đoán chứng minh tham chiếu phân giải được thì báo kết quả, tên, độ dài hay digest, không bao giờ báo giá trị, và lệnh nào không viết được như vậy thì không chạy; receipt trước đây chỉ có mục fallback nay có mục finding để ghi sự cố ấy.
1.7.7 (2026-09-04): một lượt đi bộ chỉ là bằng chứng cho thứ nó đã bấm — mỗi khẳng định được chấm trong một capture UAT phải nêu tên control trên bề mặt mà bước đó tác động, nên một bước làm cho việc gì đó xảy ra không qua bề mặt thì không được chấm.
1.7.6 (2026-09-04): điểm là một lời khẳng định về chính ứng viên được chấm — quyết định khai rõ ứng viên không mang gì, và tiêu chí đã khai là không thoả thì không được chấm ở đầu đạt, cũng không được bỏ trống.
1.7.5 (2026-09-04): một lần chạy UAT khởi động vì nhu cầu và lấy thẩm quyền từ khai báo môi trường cho lớp seed và định danh, không phải từ một người được nêu tên trong request; topic đọc theo trạng thái — composition, accessibility, lens taste — bị blocked kèm tên các khoảng thiếu khi ma trận không phủ đủ trạng thái direction khai, và một vòng thu hẹp không được chốt vòng lặp hay tuyên đã cạn ngân sách; bậc serve chạy bộ gate giao hàng do sản phẩm khai, gồm patch coverage đo theo base đã merge.
1.7.4 (2026-09-03): thẩm quyền đến từ khai báo môi trường — .stacks/<env>/environment.json đánh dấu từng lớp thao tác là declared hay person, ngoài production mặc định declared cho cấp tài khoản và runtime, production mặc định person, và trường approval nhận tham chiếu khai báo kèm hash; nhiều ứng viên đã render được xếp hạng theo rubric proof và ứng viên trội được chọn, chỉ hỏi người khi điểm số chứng minh hoà; tiêu chí phụ thuộc khối lượng dữ liệu đo ở khối lượng seed đại diện của flow, thiếu thì route sang seed, đủ mà vẫn fail thì là data-bound; tiêu chí mà lựa chọn in ra của người đã biết là fail thì là person-accepted và không chặn quality hay uat.
1.7.3 (2026-09-03): quyết định trao cho người được in thành ứng viên đã render — mỗi lựa chọn một ứng viên, ít nhất ba cho composition hay taste, một ảnh mỗi viewport, câu hỏi một dòng — và validator của direction và audit từ chối route user in ít hơn; family bind theo grammarId của route dưới dạng @knowledge/grammars/<family>; hàng Steps nêu việc và kind, cơ chế nằm ở contract của kind; UPDATE.md mang hai nguyên tắc viết (không cụ thể, không đính chính) và cả cây được quét theo cả hai.
1.7.2 (2026-09-03): khởi động lại không phải là build lại — scripts/serve-runtime.mjs ghi head đã serve và digest của manifest/lockfile theo route, xoá cache build của framework khi chúng đổi, khi head trước không rõ hoặc khi được yêu cầu --clean, và dừng trọn cây tiến trình của server rồi kiểm tra bằng kết nối rằng cổng đã trống; receipt platform-operation bắt buộc có hàng cache và validator từ chối cache giữ nguyên trên head trước không rõ.
1.7.1 (2026-09-03): INTEGRATION_CONFLICT nghỉ hưu vào INTEGRATION_FAILED — serve tự giải xung đột merge theo bốn luật đóng, ghi từng cách giải trên lần merge, chạy gate giao hàng trên head đã gộp trước khi khởi động lại, và chỉ dừng khi gate đỏ; Served surface của audit nêu phiên bản family quan sát được và phiên bản lúc resolve, lệch thì ghi vào bằng chứng của verdict có thể lật.
1.7.0 (2026-09-03): runtime là nhánh tích hợp uat của mỗi sản phẩm trên cổng cố định — platform.operate leo đủ thang stack-up → locate → start-role → serve → attest, serve = merge nhánh phiên vào uat rồi khởi động lại idempotent theo head, server chạy ngầm có pid/log (scripts/serve-runtime.mjs), lease theo thứ tự merge, RUNTIME_BUSY và INTEGRATION_CONFLICT; bind theo tổ tiên (head serve chứa commit đã pin); audit có input route và mục Served surface; UAT có isolation trong snapshot; luật hai phiên một sản phẩm ở một chỗ; schema projection cổng với sessionSlots mặc định 0.

1.6.1 (2026-09-03): @tools/print — direction in URL và ảnh mỗi candidate theo viewport trước khi viết quyết định, audit in sheet + ảnh tệ nhất mỗi topic + bảng Verdict, UAT in tóm tắt ảnh từng bước; receipt có bảng ## Printed và validator từ chối quyết định mà người chưa thấy.

1.6.0 (2026-09-03): hồ sơ UAT thiếu thì được tạo, không báo lỗi — platform.operate cấp tài khoản tại identity provider của registry với mật khẩu chung niêm phong và seed dữ liệu; registry runtime theo <project>/<role> (owner.schema.json) có identity; audit đăng nhập bằng tài khoản của flow, IDENTITY_MISSING chuyển sang provisioning; kiến trúc thư mục UAT (flow.md, accounts.<env>.json, seed, snapshots golden, runs append-only, latest.json, history.md) là contract; tham số env ở uat/audit/platform; workflow ví dụ staging-uat.

1.5.4 (2026-09-03): host dò cổng bằng cách kết nối trước khi bind (Windows cho hai server cùng bind một cổng loopback); 1.5.3 đã phát hành với spec host đỏ.

1.5.3 (2026-09-03): @tools/host có script đi kèm (scripts/host-artifacts.mjs), không phiên nào phải tự viết server nữa; .gitattributes khoá LF.

1.5.2 (2026-09-03): quyết định direction khai surface class (coverage.surfaceClass + ## Surface class, đọc từ COVERAGE-1), audit chép class từ decision thay vì tự khai.

1.5.1 (2026-09-03): rule kết luận của topic contrast lấy tiền tố của chính topic (CONTRAST-1); COLOR-6 nghỉ, trỏ sang nó.

1.5.0 (2026-09-03): UPDATE.md, chuẩn cập nhật trung lập cho một cây skills, đứng đầu thứ tự nạp và đi theo gói cài; session-first và SESSION_MISSING; git.publish đòi receipt; mọi workflow ví dụ là luồng dài (bind runtime → audit chụp màn → quality → uat → publish); lens taste (TASTE) và experience (UX) tự kết luận trong topic của mình, verdict cuối là bảng trong receipt của quality.verify; @tools/host phục vụ candidate theo viewport; pass gom: 44 chỗ → 19 cho các khái niệm thêm trong ngày, ui.md và FE-TEST-7 nghỉ.

1.3.0 (2026-09-03): gate quét presentation chạy máy (scripts/sweep-presentation.mjs: APP_OVERRIDE, APP_REIMPLEMENTATION, OFF_SCALE, SHELL_GEOMETRY) nối vào frontend.source.apply và quality.verify; phạm vi resolve gồm mọi thư mục leaf/branch app sở hữu; luật twin, spec cặp và hằng số triển khai; luật viết product-agnostic.

1.2.0 (2026-09-03): vòng test thứ hai (tests/) và các sửa từ đó: state.json có schema và liên kết resume được kiểm; workflow khai asks; response.next phải nằm trong bảng Next; UNKNOWN_STOP phát được; quyết định kiến trúc công bố các operation mà kế hoạch backend tiêu thụ và trích dẫn dimension của ma trận bao phủ thay cho BA-<n>; uat.verify chấp nhận lời từ chối hợp luật của chính nó; chủ đề radius.

1.1.0 (2026-09-03): cây phát hành thành gói npm @starci/skills; npx @starci/skills init cài nó làm runtime .claude của một repository kèm bootstrap CLAUDE.md và AGENTS.md, update giữ sửa đổi tay, doctor chạy validator trên bản đã cài; @starci/grammar 0.4.2 (entry core re-export Common).

1.0.3 (2026-09-03): sổ tool thay cho grant và policy (resources/tools.json, @tools/<id> trong Các bước, hỗ trợ theo runtime, profile tương đương); mọi operator ràng profile OpenAI cho processor Codex; vòng test đầu (tests/) và các sửa từ đó; docs/ và sites/ trở lại.

1.0.2 (2026-09-03): mọi operator là một operator.md viết tay với layout nhánh request/response, hợp đồng kind JSON, sổ mã dừng có cách xử lý, workflow mẫu, và tên frontend.* / backend.source.apply; 1.0.1 là vòng chạy khô đã lộ ra dạng cũ.


Cây này thay thế runtime v7.6 vào ngày 2026-09-02. Toàn bộ cây v7.6, gồm 13 skill, 113 operator,
template và các contract runtime, được giữ nguyên trên nhánh `v7` của cùng repository. Tài liệu v8
nào dẫn tới một file chỉ có ở v7 thì gọi tên nhánh đó.
