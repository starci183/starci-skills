---
title: "dependency-maintenance"
description: "Một bản phát hành package đã kiểm cần được tiêu thụ qua metadata dependency frontend chính xác, không đổi source hay presentation. Bằng chứng tiêu thụ package và chất lượng đi trước audit runtime và UAT đã bind riêng; riêng chuỗi này không xác nhận bàn giao luồng người dùng."
---

import { Callout } from 'nextra/components'

# dependency-maintenance
<Callout type="info">
Generated by `docs/scripts/generate-docs.mjs` from [`workflows/dependency-maintenance.json`](https://github.com/starci183/starci-skills/blob/main/workflows/dependency-maintenance.json). Edit the source, not this page.
</Callout>

## Khi nào

> Một bản phát hành package đã kiểm cần được tiêu thụ qua metadata dependency frontend chính xác, không đổi source hay presentation. Bằng chứng tiêu thụ package và chất lượng đi trước audit runtime và UAT đã bind riêng; riêng chuỗi này không xác nhận bàn giao luồng người dùng.

## Chuỗi

| Bước | Nhánh | Operator | Preset | Fan-out | Song song tối đa |
| --- | --- | --- | --- | --- | --- |
| 1 | 1/1 | [`workspace.bind`](/reference/operators/workspace-bind) | `role` = `"fe"`<br />`runtimeNeed` = `"none"` | — | — |
| 2 | 2/1 | [`dependency.update`](/reference/operators/dependency-update) | — | — | — |
| 3 | 3/1 | [`quality.verify`](/reference/operators/quality-verify) | — | — | — |

## Vòng lặp

Chuỗi này không có vòng lặp.

## Kết thúc ở

`user`
