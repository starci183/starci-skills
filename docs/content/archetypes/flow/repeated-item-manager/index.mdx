# Repeated item manager

## LOADS

None.

## Record

### Identity

| Field | Value |
|---|---|
| Archetype ID | `repeated-item-manager` |
| Family | `flow` |
| Dominant task | Create and maintain a small collection of same-schema records before confirming that the collection is complete. |
| Search aliases | `add another pattern`, `small repeated records`, `collection summary editor` |
| Authority | Product-neutral macro topology and behavior contract. |

### Invariants

- The archetype owns only the dominant task, required regions, region relationships, responsive transformations, interaction parity, and state families.
- Grammar owns product nouns, semantic owners, domain rules, and state transitions.
- Principles own exact geometry, measure, gap, alignment, overflow values, and responsive thresholds.
- Direction owns visual character; the template is only one neutral conforming realization.
- Reading order, DOM order, and focus order retain one semantic sequence across every topology.

## Recognition

### Situation codes

| Code | Situation | Verdict |
|---|---|---|
| `RIM-01` | Create and maintain a small collection of same-schema records before confirming that the collection is complete. | required positive |
| `RIM-02` | All required regions and their named ownership relationships are necessary for the task. | required positive |
| `RIM-03` | Wide adjacency fails while intermediate and compact transformations preserve task, state, and recovery. | conditional positive |
| `RIM-90` | Reject selectable bulk tables or open-ended asset browsers. | reject |
| `RIM-91` | Reject single answer groups, priced carts, or centered tasks. | reject |

### Selection rule

- Return `accept` only when `RIM-01` and `RIM-02` are evidenced and no 90–99 code is present.
- Return `reject` when `RIM-90` or `RIM-91` is evidenced, or an adjacent archetype owns the dominant task.
- Return `needs-evidence` when the task, required region, relationship, or responsive failure trigger remains unresolved.

## Region graph

```text
item-manager
├─ collection-context
├─ item-summary-list
├─ contextual-item-actions
├─ add-another-action
├─ collection-completeness
└─ continue
```

- **Shared relationship:** Each item identity encloses its values and contextual actions; collection completeness derives from the item set; Continue is gated by the explicit completeness answer.
- `item-manager -> collection-context`: `collection-context` consumes the named context or revision from `item-manager` and exposes an explicit return or reconciliation path.
- `collection-context -> item-summary-list`: `item-summary-list` consumes the named context or revision from `collection-context` and exposes an explicit return or reconciliation path.
- `item-summary-list -> contextual-item-actions`: `contextual-item-actions` consumes the named context or revision from `item-summary-list` and exposes an explicit return or reconciliation path.
- `contextual-item-actions -> add-another-action`: `add-another-action` consumes the named context or revision from `contextual-item-actions` and exposes an explicit return or reconciliation path.
- `add-another-action -> collection-completeness`: `collection-completeness` consumes the named context or revision from `add-another-action` and exposes an explicit return or reconciliation path.
- `collection-completeness -> continue`: `continue` consumes the named context or revision from `collection-completeness` and exposes an explicit return or reconciliation path.

### Region obligations

| Region | Obligation |
|---|---|
| `item-manager` | Owns the complete item manager transaction boundary, shared revision, and recovery context; child regions cannot commit outside it. |
| `collection-context` | Owns the collection context orientation and immutable basis that qualifies every downstream decision. |
| `item-summary-list` | Owns the derived item summary list state; it names its source revision and cannot contradict the input or evidence owners. |
| `contextual-item-actions` | Owns the contextual item actions orientation and immutable basis that qualifies every downstream decision. |
| `add-another-action` | Owns the add another action commitment boundary and prevents duplicate or stale commitment; it consumes the reviewed shared revision. |
| `collection-completeness` | Owns the collection completeness input or decision and updates the shared transaction revision while preserving its label, status, and contextual actions. |
| `continue` | Owns the continue commitment boundary and prevents duplicate or stale commitment; it consumes the reviewed shared revision. |

## Responsive contract

### Wide

- **Failure trigger:** A required region can no longer remain simultaneous without squeezing, truncating, or separating context from action.
- **Topology response:** Summary rows expose key values and contextual actions without turning the collection into a dense data grid.
- **Navigation replacement:** Every region that loses adjacency receives a named route to the same content and state.
- **Sticky boundary:** Persistence yields before it can obscure content, focus, validation, or actions in short-height conditions.
- **Overflow owner:** The page owns vertical overflow; only an explicitly named two-dimensional task region may own bounded overflow.

### Intermediate

- **Failure trigger:** The lowest-priority supporting region can no longer persist while retaining readable measure and action order.
- **Topology response:** Actions wrap inside each item boundary while long values expand vertically.
- **Navigation replacement:** Every region that loses adjacency receives a named route to the same content and state.
- **Sticky boundary:** Persistence yields before it can obscure content, focus, validation, or actions in short-height conditions.
- **Overflow owner:** The page owns vertical overflow; only an explicitly named two-dimensional task region may own bounded overflow.

### Compact

- **Failure trigger:** Adjacency or multiple primary panes no longer work under zoom, localization, text growth, or short height.
- **Topology response:** Each item becomes identity, values, and actions; add or remove restores focus to a stable item or status.
- **Navigation replacement:** Every region that loses adjacency receives a named route to the same content and state.
- **Sticky boundary:** Persistence yields before it can obscure content, focus, validation, or actions in short-height conditions.
- **Overflow owner:** The page owns vertical overflow; only an explicitly named two-dimensional task region may own bounded overflow.

### Reflow

- Semantic and DOM order is `item-manager` → `collection-context` → `item-summary-list` → `contextual-item-actions` → `add-another-action` → `collection-completeness` → `continue`.
- CSS does not reorder regions or the focus sequence at a topology transition.
- Compact navigation replaces adjacency with a named primary pane and restores the exact trigger, state, and scroll context.

### Interaction parity

- Wide, intermediate, and compact retain the same actions, state meanings, recovery paths, and consequence.
- Dynamic status is announced without moving focus automatically.
- Error recovery moves focus to a summary or repairable field and then returns to the meaningful continuation.

## State obligations

| State | Required behavior | Responsive presentation |
|---|---|---|
| `empty` | Distinguish a valid absence from missing required input and expose the next available start path. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `one item` | Expose the state with its owner, consequence, and a deterministic continuation or recovery path. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `many items` | Expose the state with its owner, consequence, and a deterministic continuation or recovery path. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `add pending` | Announce progress without moving focus, preserve the current revision, and prevent only the duplicate operation. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `edit pending` | Announce progress without moving focus, preserve the current revision, and prevent only the duplicate operation. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `duplicate warning` | Expose the state with its owner, consequence, and a deterministic continuation or recovery path. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `incomplete item` | Expose the confirming evidence and the next or handoff action without erasing review context. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `remove confirm` | Expose the state with its owner, consequence, and a deterministic continuation or recovery path. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `remove undo` | Expose the state with its owner, consequence, and a deterministic continuation or recovery path. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `external reorder` | Expose the state with its owner, consequence, and a deterministic continuation or recovery path. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `permission unavailable` | Explain the unavailable boundary, retain readable context, and expose an eligible alternate or recovery path. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |
| `collection complete` | Expose the confirming evidence and the next or handoff action without erasing review context. | Wide keeps the state beside its owner; intermediate removes low-priority persistence; compact places the state and recovery in the current primary pane and restores its anchor. |

## Boundaries

### Accept

- Use for a small pre-transaction collection whose records share one schema.
- Required regions must share one transaction state model and one provable recovery path.

### Reject

- Reject selectable bulk tables or open-ended asset browsers.
- Reject single answer groups, priced carts, or centered tasks.
- Reject when the difference is only a product noun, card count, density, color, component, or state variation.

### Boundary verdict

- Default `needs-evidence`; `accept` is valid only under the executable selection rule above.

## Handoff

- **Grammar:** Supplies product actors, nouns, semantic owners, domain rules, eligibility, transitions, and consequences.
- **Principles:** Resolve exact grid, measure, gap, size, alignment, overflow, sticky offsets, and content-driven thresholds.
- **Direction:** Resolve visual character without changing topology or ownership.

## Non-binding research evidence

### Evidence boundary

The sources below are advisory comparison evidence. They are not product truth, do not select a Grammar owner, do not authorize copied geometry or component trees, and do not override Source authority.

### Sources

| Source | What it supports | What it does not prove |
|---|---|---|
| [GOV.UK Design System — Summary list](https://design-system.service.gov.uk/components/summary-list/) | Repeated key-value records preserve contextual actions. | It does not require a table or define collection completeness. |
| [NHS service manual — Check answers](https://service-manual.nhs.uk/design-system/patterns/check-answers) | Review preserves answer-to-change association before submission. | It does not define non-health product truth. |
| [Carbon Design System — Data table usage](https://carbondesignsystem.com/components/data-table/usage/) | Dense row-column relationships may own bounded horizontal overflow. | It does not make a small record collection a data table. |
| [W3C WAI — Focus Order](https://www.w3.org/WAI/WCAG22/Understanding/focus-order.html) | Logical focus follows relationships and preserves operability. | It does not select this archetype or define product facts. |

## Output

Return one resolved record with exactly these runtime fields:

```json
{
  "archetypeId": "repeated-item-manager",
  "situationCodes": [
    "RIM-01",
    "RIM-02",
    "RIM-03"
  ],
  "searchAliases": [
    "add another pattern",
    "small repeated records",
    "collection summary editor"
  ],
  "dominantTask": "Create and maintain a small collection of same-schema records before confirming that the collection is complete.",
  "regions": [
    "item-manager",
    "collection-context",
    "item-summary-list",
    "contextual-item-actions",
    "add-another-action",
    "collection-completeness",
    "continue"
  ],
  "regionRelationships": [
    "item-manager -> collection-context",
    "collection-context -> item-summary-list",
    "item-summary-list -> contextual-item-actions",
    "contextual-item-actions -> add-another-action",
    "add-another-action -> collection-completeness",
    "collection-completeness -> continue"
  ],
  "responsive": {
    "wide": "Summary rows expose key values and contextual actions without turning the collection into a dense data grid.",
    "intermediate": "Actions wrap inside each item boundary while long values expand vertically.",
    "compact": "Each item becomes identity, values, and actions; add or remove restores focus to a stable item or status.",
    "reflow": "Preserve one semantic DOM order and transform topology when a named relationship fails.",
    "readingOrder": "item-manager -> collection-context -> item-summary-list -> contextual-item-actions -> add-another-action -> collection-completeness -> continue",
    "navigationReplacement": "Replace lost adjacency with named in-flow or pane navigation to the same regions.",
    "stickyBehavior": "Persistence yields before it can obscure content, focus, validation, or short-height operation.",
    "overflowOwner": "The page owns vertical overflow unless the archetype names one bounded two-dimensional task region.",
    "interactionParity": "Preserve every action, state, recovery path, and focus return across topology changes."
  },
  "stateObligations": [
    "empty",
    "one item",
    "many items",
    "add pending",
    "edit pending",
    "duplicate warning",
    "incomplete item",
    "remove confirm",
    "remove undo",
    "external reorder",
    "permission unavailable",
    "collection complete"
  ],
  "boundaryVerdict": "needs-evidence",
  "grammarHandoff": [
    "product actors and nouns",
    "semantic owners",
    "domain rules and transitions"
  ],
  "principlesHandoff": [
    "exact geometry and thresholds",
    "measure and spacing",
    "sticky offsets and overflow values"
  ],
  "confidence": "high",
  "evidence": [
    "dominant-task",
    "region-relationship",
    "responsive-failure",
    "state-family",
    "official-research"
  ]
}
```

Do not return a class, token, component, source path, fixed breakpoint, or invented product fact.
