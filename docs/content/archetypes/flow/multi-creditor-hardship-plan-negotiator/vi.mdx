# Bộ thương lượng hardship plan đa creditor

## LOADS

None.

## Hồ sơ

### Danh tính

| Trường | Giá trị |
|---|---|
| Archetype ID | `multi-creditor-hardship-plan-negotiator` |
| Family | Flow |
| Dominant task | Thương lượng hardship plan bền vững với nhiều creditor bằng affordability envelope đã xác minh, so sánh offer/counter và bảo vệ combined commitment schedule. |
| Search aliases | `multi-creditor negotiation`, `affordability envelope`, `hardship counteroffer`, `combined commitment schedule` |
| Authority | Macro topology dùng chung, product-neutral. |

### Bất biến

- Dominant task luôn là: Thương lượng hardship plan bền vững với nhiều creditor bằng affordability envelope đã xác minh, so sánh offer/counter và bảo vệ combined commitment schedule.
- Required region graph luôn là `hardship-negotiator → verified-income-essential-cost-and-affordability → creditor-obligation-register → creditor-offers-and-counters ×n → global-commitment-envelope → scenario-and-priority-tradeoffs → selected-multi-creditor-plan → agreements-schedule-and-review`.
- Mọi state và action gắn với một selected context cùng provenance của nó.
- DOM order, reading order và meaningful focus order luôn trùng nhau.
- Grammar sở hữu product semantics; Principles sở hữu geometry chưa resolve; Direction sở hữu visual character.
- Wide, intermediate và compact giữ nguyên action, state, keyboard access, overflow ownership và recovery.

## Nhận diện

### Mã tình huống

| Mã | Tình huống | Phán quyết |
|---|---|---|
| `AR-HN-01` | Dominant task đã nêu là outcome chính của người dùng. | Candidate evidence. |
| `AR-HN-02` | Mọi required region và named relationship đều hiện diện. | Required evidence. |
| `AR-HN-03` | Wide, intermediate và compact dùng đúng topology transformation đã khai báo. | Required evidence. |
| `AR-HN-04` | Compact giữ mọi action, state, keyboard path, focus return và recovery. | Required evidence. |
| `AR-HN-05` | Template must model at least three creditors, counter one offer, reject a combined overcommitment, produce a sustainable schedule and retain evidence plus agreement provenance. | Required evidence. |
| `AR-HN-90` | budgeting | Từ chối. |
| `AR-HN-91` | debt list | Từ chối. |
| `AR-HN-92` | single settlement | Từ chối. |
| `AR-HN-93` | generic negotiation chat | Từ chối. |

### Quy tắc chọn

Chỉ chọn `multi-creditor-hardship-plan-negotiator` khi `AR-HN-01` đến `AR-HN-05` đều có evidence và không có mã `AR-HN-9*`. Trả `needs-evidence` khi owner hoặc relationship bắt buộc chưa rõ. Trả `reject` khi có rejection code. Khác biệt chỉ ở noun, density, color, card count, component hoặc state là `duplicate-or-variation`.

## Sơ đồ vùng

```text
hardship-negotiator
   `-- verified-income-essential-cost-and-affordability
      `-- creditor-obligation-register
         `-- creditor-offers-and-counters
            `-- global-commitment-envelope
               `-- scenario-and-priority-tradeoffs
                  `-- selected-multi-creditor-plan
                     `-- agreements-schedule-and-review
```

Biểu thức relationship đã khai báo: `hardship-negotiator → verified-income-essential-cost-and-affordability → creditor-obligation-register → creditor-offers-and-counters ×n → global-commitment-envelope → scenario-and-priority-tradeoffs → selected-multi-creditor-plan → agreements-schedule-and-review`.

### Nghĩa vụ vùng

| Vùng | Owner | Relationship |
|---|---|---|
| `hardship-negotiator` | Sở hữu dominant task hoàn chỉnh, version context và recovery của mọi descendant. | Là root của graph bắt buộc; không được thay bằng generic container. |
| `verified-income-essential-cost-and-affordability` | Sở hữu evidence, action, state và recovery của verified income essential cost and affordability. | Theo sau `hardship-negotiator` trong semantic order và dùng đúng selected context của vùng đó. |
| `creditor-obligation-register` | Sở hữu evidence, action, state và recovery của creditor obligation register. | Theo sau `verified-income-essential-cost-and-affordability` trong semantic order và dùng đúng selected context của vùng đó. |
| `creditor-offers-and-counters` | Sở hữu evidence, action, state và recovery của creditor offers and counters. | Lặp theo từng party tự trị và giữ authority cùng receipt riêng. |
| `global-commitment-envelope` | Sở hữu evidence, action, state và recovery của global commitment envelope. | Theo sau `creditor-offers-and-counters` trong semantic order và dùng đúng selected context của vùng đó. |
| `scenario-and-priority-tradeoffs` | Sở hữu evidence, action, state và recovery của scenario and priority tradeoffs. | Theo sau `global-commitment-envelope` trong semantic order và dùng đúng selected context của vùng đó. |
| `selected-multi-creditor-plan` | Sở hữu evidence, action, state và recovery của selected multi creditor plan. | Theo sau `scenario-and-priority-tradeoffs` trong semantic order và dùng đúng selected context của vùng đó. |
| `agreements-schedule-and-review` | Sở hữu evidence, action, state và recovery của agreements schedule and review. | Theo sau `selected-multi-creditor-plan` trong semantic order và dùng đúng selected context của vùng đó. |

## Hợp đồng responsive

### Wide

- **Failure trigger:** Wide kết thúc khi các vùng đồng thời không còn giữ label dễ đọc, association chính xác, action nhìn thấy và focus không bị che.
- **Topology response:** Giữ đồng thời đúng các owner được contract nêu: Affordability evidence, creditor register, offer/counter workspace and global commitment ledger remain visible.
- **Navigation replacement:** Không có khi mọi required owner vẫn dùng đồng thời được.
- **Sticky boundary:** Chỉ outcome hoặc action hiện tại được persist; nó reserve space và yield khi viewport thấp.
- **Overflow owner:** Chỉ `none` được sở hữu bounded horizontal overflow; nội dung thường không tạo page-level horizontal scroll.

### Intermediate

- **Failure trigger:** Intermediate bắt đầu khi persistent region ít ưu tiên nhất làm hỏng dominant cross-region relationship.
- **Topology response:** Chuyển đúng theo contract: Creditor offers and global envelope remain primary; detailed evidence and scenarios become drawers.
- **Navigation replacement:** Named synchronized drawer, disclosure hoặc pane thay mỗi persistent region bị dời và trigger hiển thị state hiện tại.
- **Sticky boundary:** Persistent action chỉ tồn tại khi exact target nhìn thấy và trở lại in-flow khi viewport thấp.
- **Overflow owner:** `none` giữ bounded overflow axis duy nhất và có text/list equivalent.

### Compact

- **Failure trigger:** Compact bắt đầu khi hai task owner đồng thời không thể giữ evidence dễ đọc và control 44-by-44 CSS pixel.
- **Topology response:** Tái cấu trúc theo primary sequence: Affordability summary → creditor → offer/counter → global impact → next creditor → combined schedule/review; each acceptance revalidates the envelope.
- **Navigation replacement:** Dùng một primary-pane sequence với Previous và Next rõ ràng, khôi phục selection, query, state và scroll context.
- **Sticky boundary:** Current action reserve content space, không che focus và trở lại normal flow khi viewport thấp.
- **Overflow owner:** Text hoặc relation ledger là primary; `none` chỉ là optional bounded region.

### Reflow

- Semantic và DOM order là `hardship-negotiator → verified-income-essential-cost-and-affordability → creditor-obligation-register → creditor-offers-and-counters → global-commitment-envelope → scenario-and-priority-tradeoffs → selected-multi-creditor-plan → agreements-schedule-and-review`.
- Text zoom, translation dài và control phóng to kích hoạt cùng named topology change.
- CSS không reorder visual content lệch khỏi keyboard hoặc assistive-technology order.
- Label và identifier dài được wrap; detail ẩn có accessible reveal rõ ràng.
- Nội dung thường không tạo page-level horizontal scroll.

### Ngang bằng tương tác

- Mọi selection, edit, action, explanation, retry và recovery ở wide vẫn reachable tại intermediate và compact.
- Topology change giữ selected entity, version, filter, pending state, validation result và recovery point.
- Dynamic update dùng một contextual status message mà không di chuyển focus.
- Modal nhận và giữ focus, hỗ trợ Escape hoặc Cancel, rồi trả focus về đúng trigger.
- Drag, drawing, fader, spatial hoặc point movement có parity bằng button, numeric hoặc list.
- Color, position, geometry và motion luôn có text hoặc structural equivalent.

## Nghĩa vụ trạng thái

| State | Vùng sở hữu | Nghĩa vụ |
|---|---|---|
| Khởi đầu / loading | `verified-income-essential-cost-and-affordability` | Xác định scope đang chờ và giữ semantic position. |
| Ready | `creditor-obligation-register` | Hiển thị dominant task hoàn chỉnh và version hiện tại. |
| Empty / không áp dụng | `creditor-offers-and-counters` | Phân biệt absence có nghĩa với evidence unavailable. |
| Lỗi / retry | `global-commitment-envelope` | Giữ context hợp lệ và cung cấp retry cục bộ mà không reset selection. |
| Permission / unavailable | `selected-multi-creditor-plan` | Không suy diễn evidence bị hạn chế là không tồn tại; cung cấp alternate route an toàn. |
| Pending | `agreements-schedule-and-review` | Ngăn action trùng và announce tiến độ mà không di chuyển focus. |
| Success | `agreements-schedule-and-review` | Hiển thị outcome, provenance và action hợp lệ kế tiếp. |
| Stale / conflict | `verified-income-essential-cost-and-affordability` | Giữ giá trị an toàn cuối cùng và yêu cầu reconciliation rõ ràng. |
| Chuyển focus | `agreements-schedule-and-review` | Chỉ đưa focus tới error summary bắt buộc hoặc modal vừa mở, rồi trả về đúng trigger. |
| Trình bày responsive | `hardship-negotiator` | Giữ selected entity, query, state và recovery khi topology đổi. |
| evidence missing/verified | `verified-income-essential-cost-and-affordability` | Hiển thị state đặc thù này cùng cause, consequence và recovery hợp lệ. |
| obligation disputed | `creditor-obligation-register` | Hiển thị state đặc thù này cùng cause, consequence và recovery hợp lệ. |
| creditor contacted/no-response | `creditor-offers-and-counters` | Hiển thị state đặc thù này cùng cause, consequence và recovery hợp lệ. |
| offer proposed/countered/accepted/expired | `global-commitment-envelope` | Hiển thị state đặc thù này cùng cause, consequence và recovery hợp lệ. |
| envelope safe/exceeded | `scenario-and-priority-tradeoffs` | Hiển thị state đặc thù này cùng cause, consequence và recovery hợp lệ. |
| priority conflict | `selected-multi-creditor-plan` | Hiển thị state đặc thù này cùng cause, consequence và recovery hợp lệ. |
| agreement pending | `agreements-schedule-and-review` | Hiển thị state đặc thù này cùng cause, consequence và recovery hợp lệ. |
| payment schedule active and review due. | `agreements-schedule-and-review` | Hiển thị state đặc thù này cùng cause, consequence và recovery hợp lệ. |

## Ranh giới

### Chấp nhận

- Chỉ accept khi dominant task chuyển required evidence thành outcome đã khai báo.
- Chỉ accept khi mỗi required region có owner độc lập và named relationship vẫn explicit.
- Chỉ accept khi template must model at least three creditors, counter one offer, reject a combined overcommitment, produce a sustainable schedule and retain evidence plus agreement provenance.

### Từ chối

- Từ chối budgeting; đây là evidence `AR-HN-90` và phải route sang archetype khác.
- Từ chối debt list; đây là evidence `AR-HN-91` và phải route sang archetype khác.
- Từ chối single settlement; đây là evidence `AR-HN-92` và phải route sang archetype khác.
- Từ chối generic negotiation chat; đây là evidence `AR-HN-93` và phải route sang archetype khác.
- Từ chối candidate chỉ thỏa task bằng cách đổi product noun hoặc visual treatment.

### Phán quyết ranh giới

Trả `accept` chỉ khi dominant task, complete required graph, transformation contract, state/recovery parity và `AR-HN-05` đều giữ. Trả `reject` cho rejection code. Trả `needs-evidence` khi owner hoặc relationship chưa resolve. Báo `duplicate-or-variation` cho khác biệt chỉ ở noun, density, color, card count, component hoặc state.

## Bàn giao

- **Grammar handoff:** Bind product-specific owner, label, permission, truthful state meaning và permitted action vào declared regions.
- **Principles handoff:** Resolve exact grid, measure, gap, alignment, sticky offset, bounded overflow realization và relationship-driven transition points.
- Hai handoff không được xóa required region, thay dominant task hoặc làm yếu keyboard, focus, responsive hay recovery parity.

## Bằng chứng nghiên cứu không ràng buộc

### Ranh giới bằng chứng

Research bên ngoài là advisory evidence, không phải product truth. Nó hỗ trợ synthesis của task relationship, responsive transformation, interaction và accessibility obligation. Nó không đặt tên StarCi owner, chọn exact geometry, tạo product fact hoặc cấp quyền copy source interface.

### Nguồn

| Nguồn | Hỗ trợ điều gì | Không chứng minh điều gì |
|---|---|---|
| [CFPB — Debt Collection](https://www.consumerfinance.gov/consumer-tools/debt-collection/) | Hỗ trợ debt verification, response, and dispute context. | Không chọn archetype này, không định nghĩa product truth và không cấp quyền copy geometry. |
| [MoneyHelper — Dealing with Debt](https://www.moneyhelper.org.uk/en/money-troubles/dealing-with-debt) | Hỗ trợ affordability and multi-debt planning. | Không chọn archetype này, không định nghĩa product truth và không cấp quyền copy geometry. |
| [W3C WAI — Focus Order](https://www.w3.org/WAI/WCAG22/Understanding/focus-order.html) | Hỗ trợ stable negotiation and error-summary focus. | Không chọn archetype này, không định nghĩa product truth và không cấp quyền copy geometry. |

Source set gồm tài liệu official hiện hành từ ít nhất ba tổ chức độc lập và có W3C accessibility evidence.

## Đầu ra

```json
{
  "archetypeId": "multi-creditor-hardship-plan-negotiator",
  "situationCodes": [
    "<matched AR-HN-* codes>"
  ],
  "searchAliases": [
    "multi-creditor negotiation",
    "affordability envelope",
    "hardship counteroffer",
    "combined commitment schedule"
  ],
  "dominantTask": "Negotiate a sustainable hardship plan across multiple creditors by proving an affordability envelope, comparing offers and counters, and guarding the combined commitment schedule.",
  "regions": [
    "hardship-negotiator",
    "verified-income-essential-cost-and-affordability",
    "creditor-obligation-register",
    "creditor-offers-and-counters",
    "global-commitment-envelope",
    "scenario-and-priority-tradeoffs",
    "selected-multi-creditor-plan",
    "agreements-schedule-and-review"
  ],
  "regionRelationships": [
    "hardship-negotiator → verified-income-essential-cost-and-affordability → creditor-obligation-register → creditor-offers-and-counters ×n → global-commitment-envelope → scenario-and-priority-tradeoffs → selected-multi-creditor-plan → agreements-schedule-and-review"
  ],
  "responsive": {
    "wide": "<simultaneous structure>",
    "intermediate": "<named synchronized panes or drawers>",
    "compact": "<primary-pane sequence>",
    "reflow": "<semantic order and text-pressure obligations>",
    "readingOrder": "hardship-negotiator → verified-income-essential-cost-and-affordability → creditor-obligation-register → creditor-offers-and-counters → global-commitment-envelope → scenario-and-priority-tradeoffs → selected-multi-creditor-plan → agreements-schedule-and-review",
    "navigationReplacement": "<none or explicit replacement>",
    "stickyBehavior": "<reserved-space behavior and yield condition>",
    "overflowOwner": "none",
    "interactionParity": "<preserved action, state, selection, and recovery>"
  },
  "stateObligations": [
    "evidence missing/verified",
    "obligation disputed",
    "creditor contacted/no-response",
    "offer proposed/countered/accepted/expired",
    "envelope safe/exceeded",
    "priority conflict",
    "agreement pending",
    "payment schedule active and review due."
  ],
  "boundaryVerdict": "<accept | reject | needs-evidence>",
  "grammarHandoff": [
    "<product meaning and semantic owners>"
  ],
  "principlesHandoff": [
    "<unresolved geometry only>"
  ],
  "confidence": "<high | medium | low>",
  "evidence": [
    "<business or current-source facts>",
    "<official task research>",
    "<accessibility research>"
  ]
}
```

