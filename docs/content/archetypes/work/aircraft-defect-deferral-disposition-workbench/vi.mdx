# Aircraft defect deferral disposition workbench

## LOADS

None.

## Record

### Identity

| Trường | Giá trị |
|---|---|
| Archetype ID | `aircraft-defect-deferral-disposition-workbench` |
| Family | Work |
| Dominant task | Xử lý một discrepancy của tàu bay bằng cách xác lập khả năng áp dụng MEL, đánh giá tương tác với các hỏng khác, gắn procedure bảo dưỡng/vận hành, khởi động đúng interval sửa chữa và trả verdict defer, repair hoặc no-dispatch có kiểm soát. |
| Search aliases | `aircraft defect deferral disposition`, `aircraft defect deferral disposition workspace`, `aircraft defect deferral disposition control` |
| Authority | Macro topology dùng chung và trung lập sản phẩm; Grammar sở hữu ngữ nghĩa sản phẩm, Principles sở hữu geometry chưa giải quyết và Direction sở hữu visual character. |

### Invariants

- Xử lý một discrepancy của tàu bay bằng cách xác lập khả năng áp dụng MEL, đánh giá tương tác với các hỏng khác, gắn procedure bảo dưỡng/vận hành, khởi động đúng interval sửa chữa và trả verdict defer, repair hoặc no-dispatch có kiểm soát.
- Mọi vùng bắt buộc tạo thành một chuỗi bằng chứng đến quyết định có thể truy vết; đầu ra trung gian không tự đóng task.
- Mỗi vùng bắt buộc giữ owner riêng và cùng selected context.
- Wide, intermediate và compact giữ DOM, reading/focus order có nghĩa, action parity và recovery xác định.

## Recognition

### Situation codes

| Mã | Tình huống | Phán quyết |
|---|---|---|
| `AR-ADDDW-01` | Dominant task là outcome quan sát được bắt buộc. | Bằng chứng bắt buộc. |
| `AR-ADDDW-02` | Toàn bộ region graph và quan hệ đã đặt tên là cần thiết. | Bằng chứng bắt buộc. |
| `AR-ADDDW-03` | Compact giữ action, state, recovery và nghĩa focus của wide. | Bằng chứng bắt buộc. |
| `AR-ADDDW-04` | State đặc thù task có thể đổi sau khi người dùng tạo work state. | Bằng chứng bắt buộc. |
| `AR-ADDDW-90` | Dominant task thực ra là `stage-gated-process-record`. | Reject. |
| `AR-ADDDW-91` | Dominant task thực ra là `evidence-led-case-resolution-dossier`. | Reject. |
| `AR-ADDDW-92` | Dominant task thực ra là `permit-to-work-isolation-control-room`. | Reject. |
| `AR-ADDDW-93` | Dominant task thực ra là `flight-dispatch-release-workbench`. | Reject. |

### Selection rule

Chọn `aircraft-defect-deferral-disposition-workbench` khi và chỉ khi `AR-ADDDW-01` đến `AR-ADDDW-04` có bằng chứng và không mã nào từ `AR-ADDDW-90` đến `AR-ADDDW-93` đúng. Trả `needs-evidence` khi owner hoặc relationship chưa được giải quyết; trả `reject` khi có rejection code.

## Region graph

```text
defect-disposition → exact-aircraft-configuration-operation-and-mel-revision → discrepancy-facts-and-system-location → exact-mel-item-branch-condition-and-exception-proof ↔ all-concurrent-defects-and-combination-prohibition-register → branch-owned-rectification-category-start-event-and-expiry-clock → named-maintenance-procedure ↔ named-operational-procedure → placard-route-operation-and-special-approval-restrictions → independent-maintenance-signoff ↔ operational-control-signoff → controlled-deferral-rectification-or-no-dispatch-lineage
```

### Region obligations

| Region | Nghĩa vụ owner và relationship |
|---|---|
| `defect-disposition` | Sở hữu dominant task, toàn bộ trạng thái hậu duệ và boundary recovery. |
| `exact-aircraft-configuration-operation-and-mel-revision` | Sở hữu bằng chứng hoặc hành động của Exact Aircraft Configuration Operation And Mel Revision và giữ quan hệ đã khai báo với selection hiện tại. |
| `discrepancy-facts-and-system-location` | Sở hữu bằng chứng hoặc hành động của Discrepancy Facts And System Location và giữ quan hệ đã khai báo với selection hiện tại. |
| `exact-mel-item-branch-condition-and-exception-proof` | Sở hữu bằng chứng hoặc hành động của Exact Mel Item Branch Condition And Exception Proof và giữ quan hệ đã khai báo với selection hiện tại. |
| `all-concurrent-defects-and-combination-prohibition-register` | Sở hữu bằng chứng hoặc hành động của All Concurrent Defects And Combination Prohibition Register và giữ quan hệ đã khai báo với selection hiện tại. |
| `branch-owned-rectification-category-start-event-and-expiry-clock` | Sở hữu bằng chứng hoặc hành động của Branch Owned Rectification Category Start Event And Expiry Clock và giữ quan hệ đã khai báo với selection hiện tại. |
| `named-maintenance-procedure` | Sở hữu bằng chứng hoặc hành động của Named Maintenance Procedure và giữ quan hệ đã khai báo với selection hiện tại. |
| `named-operational-procedure` | Sở hữu bằng chứng hoặc hành động của Named Operational Procedure và giữ quan hệ đã khai báo với selection hiện tại. |
| `placard-route-operation-and-special-approval-restrictions` | Sở hữu bằng chứng hoặc hành động của Placard Route Operation And Special Approval Restrictions và giữ quan hệ đã khai báo với selection hiện tại. |
| `independent-maintenance-signoff` | Sở hữu bằng chứng hoặc hành động của Independent Maintenance Signoff và giữ quan hệ đã khai báo với selection hiện tại. |
| `operational-control-signoff` | Sở hữu bằng chứng hoặc hành động của Operational Control Signoff và giữ quan hệ đã khai báo với selection hiện tại. |
| `controlled-deferral-rectification-or-no-dispatch-lineage` | Sở hữu bằng chứng hoặc hành động của Controlled Deferral Rectification Or No Dispatch Lineage và giữ quan hệ đã khai báo với selection hiện tại. |

## Responsive contract

### Wide

- **Failure trigger:** Wide kết thúc khi các vùng đồng thời không còn giữ được label dễ đọc, liên kết chính xác và action đầy đủ.
- **Topology response:** Discrepancy, MEL applicability, defect đồng thời, expiry clock, procedure bundle, restriction và hai signoff cùng hiện diện.
- **Navigation replacement:** Không có khi mọi vùng bắt buộc vẫn đồng thời dùng được.
- **Sticky boundary:** Chỉ trạng thái hoặc action của task hiện tại được persist; nó dành chỗ và yield khi chiều cao ngắn.
- **Overflow owner:** `exact-aircraft-configuration-operation-and-mel-revision` sở hữu overflow có giới hạn; page không sở hữu overflow ngang.

### Intermediate

- **Failure trigger:** Intermediate bắt đầu khi vùng hỗ trợ ưu tiên thấp nhất làm quan hệ chính không còn dùng được.
- **Topology response:** MEL branch đã chọn và verdict là chính; applicability/interaction cùng procedure/restriction luân phiên, còn expiry/signoff rail bền vững.
- **Navigation replacement:** Các evidence view có tên thay vùng bị dời và giữ đúng selection cùng trigger.
- **Sticky boundary:** Verdict hiện tại chỉ persist khi target vẫn thấy được và trở lại flow khi chiều cao ngắn.
- **Overflow owner:** Owner có giới hạn của wide vẫn cục bộ; view luân phiên không tạo nested page scroll.

### Compact

- **Failure trigger:** Compact bắt đầu khi hai vùng task không thể đồng thời giữ bằng chứng dễ đọc, target 44 px và focus không bị che.
- **Topology response:** Discrepancy → MEL item branch chính xác với điều kiện/ngoại lệ → mọi defect đồng thời → category/start/expiry → procedure bảo dưỡng → procedure vận hành và restriction/placard → hai signoff → defer, repair hoặc no-dispatch.
- **Navigation replacement:** Primary-pane sequence có Back khôi phục selection, state, query, scroll context và đúng trigger.
- **Sticky boundary:** Action bar dành sẵn chỗ, không che focus và trở về normal flow khi chiều cao ngắn.
- **Overflow owner:** Visual có giới hạn có bản tương đương dạng chữ theo thứ tự làm representation compact chính.

### Reflow

- Semantic, DOM và meaningful focus order giữ graph: `defect-disposition → exact-aircraft-configuration-operation-and-mel-revision → discrepancy-facts-and-system-location → exact-mel-item-branch-condition-and-exception-proof ↔ all-concurrent-defects-and-combination-prohibition-register → branch-owned-rectification-category-start-event-and-expiry-clock → named-maintenance-procedure ↔ named-operational-procedure → placard-route-operation-and-special-approval-restrictions → independent-maintenance-signoff ↔ operational-control-signoff → controlled-deferral-rectification-or-no-dispatch-lineage`.
- Label dài, bản dịch, zoom và control phóng lớn kích hoạt cùng topology change đã đặt tên.
- CSS không reorder semantic; content thường không tạo page-level horizontal scroll.
- Detail bị ẩn luôn có reveal path accessible rõ ràng.

### Interaction parity

- Mọi selection, edit, action, explanation, retry và recovery của wide đều tới được ở intermediate và compact.
- Đổi topology giữ đúng selected object, order, data state, pending result và error context.
- Pointer action có phương án keyboard và single-pointer không drag khi có movement.
- Dynamic update announce một status có ngữ cảnh mà không giật focus; màu không là tín hiệu duy nhất.
- Modal nhận và giữ focus, hỗ trợ Escape hoặc Cancel rồi trả focus về đúng trigger.

## State obligations

Các state đặc thù task: MEL loading/current/superseded, discrepancy open/clarified, item applicable/not listed/not applicable, concurrent interaction clear/blocking, category assigned/unknown, interval active/near-expiry/expired, procedures incomplete/complete, placard pending/applied, restriction compatible/blocking, signoff pending/signed/rejected, defer active/rectified/extended with authority and aircraft dispatchable/not dispatchable.

| State | Region sở hữu | Nghĩa vụ |
|---|---|---|
| Initial / loading | `exact-aircraft-configuration-operation-and-mel-revision` | Nêu scope đang tải, giữ chỗ primary region và chỉ block owner lỗi. |
| Ready | `discrepancy-facts-and-system-location` | Hiện object, owner relationship, selection và action hợp lệ bằng text lẫn semantics. |
| Empty / not applicable | `discrepancy-facts-and-system-location` | Phân biệt empty thật, no-match và not-applicable rồi cung cấp next action hợp lệ. |
| Error / retry | `operational-control-signoff` | Giữ context/input hợp lệ, nêu owner lỗi và cung cấp local retry. |
| Permission / unavailable | `controlled-deferral-rectification-or-no-dispatch-lineage` | Giải thích restriction mà không ngụ ý bằng chứng ẩn là không tồn tại và cung cấp safe exit. |
| Pending | `controlled-deferral-rectification-or-no-dispatch-lineage` | Ngăn duplicate action, giữ đúng target và announce progress mà không dời focus. |
| Success | `controlled-deferral-rectification-or-no-dispatch-lineage` | Xác nhận outcome chính xác, giữ selection và cung cấp next action hoặc recovery. |
| Stale / conflict | `exact-aircraft-configuration-operation-and-mel-revision` | Giữ last-safe value, nêu version/time conflict và yêu cầu recovery rõ. |
| Focus transition | `controlled-deferral-rectification-or-no-dispatch-lineage` | Chỉ dời focus vào modal hoặc error summary bắt buộc rồi trả về đúng trigger. |
| Responsive presentation | `defect-disposition` | Giữ state, selection, query, pending result và recovery khi topology đổi. |

## Boundaries

### Accept

- Accept khi dominant task là: Xử lý một discrepancy của tàu bay bằng cách xác lập khả năng áp dụng MEL, đánh giá tương tác với các hỏng khác, gắn procedure bảo dưỡng/vận hành, khởi động đúng interval sửa chữa và trả verdict defer, repair hoặc no-dispatch có kiểm soát.
- Accept khi mọi region và relationship bắt buộc trong graph đều cần để hoàn thành task.
- Accept khi compact giữ task, state và recovery bằng replacement topology thay vì stack box desktop.

### Reject

- Reject `stage-gated-process-record`; đây là bằng chứng `AR-ADDDW-90` và phải route sang archetype kề.
- Reject `evidence-led-case-resolution-dossier`; đây là bằng chứng `AR-ADDDW-91` và phải route sang archetype kề.
- Reject `permit-to-work-isolation-control-room`; đây là bằng chứng `AR-ADDDW-92` và phải route sang archetype kề.
- Reject `flight-dispatch-release-workbench`; đây là bằng chứng `AR-ADDDW-93` và phải route sang archetype kề.

### Boundary verdict

Chỉ trả `accept` khi dominant task, complete graph và compact parity cùng đúng. Khác biệt chỉ ở noun, density, color, component, card count hoặc state là `duplicate-or-variation`.

## Handoff

- **Grammar handoff:** Gắn owner, label, permitted action và nghĩa state trung thực của sản phẩm vào các region đã khai báo.
- **Principles handoff:** Giải quyết grid, measure, gap, alignment, sticky offset, bounded overflow và transition point theo relationship.
- Không handoff nào được bỏ region bắt buộc, đổi dominant task hoặc làm yếu interaction parity.

## Non-binding research evidence

### Evidence boundary

Research bên ngoài là bằng chứng tham khảo, không phải product truth. Nó hỗ trợ tổng hợp quan hệ task, adaptive behavior và accessibility obligation; nó không chọn StarCi owner, exact geometry hay quyền sao chép interface nguồn.

### Sources

| Nguồn | Hỗ trợ điều gì | Không chứng minh điều gì |
|---|---|---|
| [W3C WAI — Status Messages](https://www.w3.org/WAI/WCAG22/Understanding/status-messages) | Nghĩa vụ accessibility cho reflow, focus, trạng thái và tương tác tương đương. | Không chọn archetype, không định nghĩa product truth và không cho phép sao chép geometry. |
| [EASA Easy Access Rules for Air Operations — MEL](https://www.easa.europa.eu/en/document-library/easy-access-rules/online-publications/easy-access-rules-air-operations?erules-id=ERULES-1963177438-11920) | Quan hệ nghiệp vụ chính thức và boundary quyền hạn đặc thù cho dominant task. | Không chọn archetype, không định nghĩa product truth và không cho phép sao chép geometry. |
| [FAA AC 120-125 — MEL Management Program](https://www.faa.gov/documentLibrary/media/Advisory_Circular/AC_120-125.pdf) | Quan hệ nghiệp vụ chính thức và boundary quyền hạn đặc thù cho dominant task. | Không chọn archetype, không định nghĩa product truth và không cho phép sao chép geometry. |

Tập nguồn gồm ít nhất ba tổ chức chính thức độc lập và bằng chứng accessibility của W3C.

## Output

```json
{
  "archetypeId": "aircraft-defect-deferral-disposition-workbench",
  "situationCodes": [
    "<matched AR-ADDDW-* codes>"
  ],
  "searchAliases": [
    "aircraft defect deferral disposition",
    "aircraft defect deferral disposition workspace",
    "aircraft defect deferral disposition control"
  ],
  "dominantTask": "Disposition one aircraft discrepancy by establishing MEL applicability, evaluating interactions with other unserviceabilities, binding maintenance and operational procedures, starting the correct rectification interval and returning a controlled defer, repair or no-dispatch verdict.",
  "regions": [
    "defect-disposition",
    "exact-aircraft-configuration-operation-and-mel-revision",
    "discrepancy-facts-and-system-location",
    "exact-mel-item-branch-condition-and-exception-proof",
    "all-concurrent-defects-and-combination-prohibition-register",
    "branch-owned-rectification-category-start-event-and-expiry-clock",
    "named-maintenance-procedure",
    "named-operational-procedure",
    "placard-route-operation-and-special-approval-restrictions",
    "independent-maintenance-signoff",
    "operational-control-signoff",
    "controlled-deferral-rectification-or-no-dispatch-lineage"
  ],
  "regionRelationships": [
    "no generic defect category may replace the exact MEL branch, and neither procedure nor either signoff can stand in for its paired owner."
  ],
  "responsive": {
    "wide": "<simultaneous structure>",
    "intermediate": "<failure and named pane response>",
    "compact": "<primary-pane replacement sequence>",
    "reflow": "<semantic order and text-pressure obligations>",
    "readingOrder": "defect-disposition -> exact-aircraft-configuration-operation-and-mel-revision -> discrepancy-facts-and-system-location -> exact-mel-item-branch-condition-and-exception-proof -> all-concurrent-defects-and-combination-prohibition-register -> branch-owned-rectification-category-start-event-and-expiry-clock -> named-maintenance-procedure -> named-operational-procedure -> placard-route-operation-and-special-approval-restrictions -> independent-maintenance-signoff -> operational-control-signoff -> controlled-deferral-rectification-or-no-dispatch-lineage",
    "navigationReplacement": "<none or named evidence view or primary-pane sequence>",
    "stickyBehavior": "<reserved-space behavior and short-height yield>",
    "overflowOwner": "exact-aircraft-configuration-operation-and-mel-revision",
    "interactionParity": "<preserved action, state, selection, focus, and recovery>"
  },
  "stateObligations": [
    "MEL loading/current/superseded",
    "discrepancy open/clarified",
    "item applicable/not listed/not applicable",
    "concurrent interaction clear/blocking",
    "category assigned/unknown",
    "interval active/near-expiry/expired",
    "procedures incomplete/complete",
    "placard pending/applied",
    "restriction compatible/blocking",
    "signoff pending/signed/rejected",
    "defer active/rectified/extended with authority",
    "aircraft dispatchable/not dispatchable"
  ],
  "boundaryVerdict": "<accept | reject | needs-evidence>",
  "grammarHandoff": [
    "<product meaning and semantic owners>"
  ],
  "principlesHandoff": [
    "<unresolved geometry only>"
  ],
  "confidence": "<high | medium | low>",
  "evidence": [
    "<business or current-source facts>",
    "<official task research>",
    "<accessibility research>"
  ]
}
```

