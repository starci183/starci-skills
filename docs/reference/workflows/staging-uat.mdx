---
title: "staging-uat"
description: "A delivery that is already published must be verified on another stack before it is released: the same head is observed and walked there, and the chain ends with the two receipts in a person's hands rather than with a deployment."
---

import { Callout } from 'nextra/components'

# staging-uat
<Callout type="info">
Generated by `docs/scripts/generate-docs.mjs` from [`workflows/staging-uat.json`](https://github.com/starci183/starci-skills/blob/main/workflows/staging-uat.json). Edit the source, not this page.
</Callout>

## When

> A delivery that is already published must be verified on another stack before it is released: the same head is observed and walked there, and the chain ends with the two receipts in a person's hands rather than with a deployment.

## Chain

| Step | Branch | Operator | Presets | Fan-out | Max parallel |
| --- | --- | --- | --- | --- | --- |
| 1 | 1/1 | [`environment.preflight`](/reference/operators/environment-preflight) | `roles` = `["be","fe"]` | — | — |
| 2 | 2/1 | [`workspace.bind`](/reference/operators/workspace-bind) | `role` = `"be"` | — | — |
| 2 | 2/2 | [`workspace.bind`](/reference/operators/workspace-bind) | `role` = `"fe"`<br />`runtimeNeed` = `"consume"` | — | — |
| 3 | 3/1 | [`frontend.direction.decide`](/reference/operators/frontend-direction-decide) | `intent` = `"modify"`<br />`changeLevel` = `"refine"` | — | — |
| 4 | 4/1 | [`frontend.presentation.resolve`](/reference/operators/frontend-presentation-resolve) | — | — | — |
| 5 | 5/1 | [`frontend.source.apply`](/reference/operators/frontend-source-apply) | `mode` = `"dry"` | — | — |
| 6 | 6/1 | [`frontend.surface.audit`](/reference/operators/frontend-surface-audit) | `env` = `"staging"` | `matrix` | 3 |
| 7 | 7/1 | [`quality.verify`](/reference/operators/quality-verify) | — | — | — |
| 8 | 8/1 | [`uat.verify`](/reference/operators/uat-verify) | `env` = `"staging"` | — | — |

## Loops

This chain has no loop.

## Ends

`user`
