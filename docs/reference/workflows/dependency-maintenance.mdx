---
title: "dependency-maintenance"
description: "One verified package release must be consumed through exact frontend dependency metadata, without source or presentation changes. Package consumption and quality evidence precede the separately bound runtime surface audit and UAT; this chain alone never claims a user-facing delivery."
---

import { Callout } from 'nextra/components'

# dependency-maintenance
<Callout type="info">
Generated by `docs/scripts/generate-docs.mjs` from [`workflows/dependency-maintenance.json`](https://github.com/starci183/starci-skills/blob/main/workflows/dependency-maintenance.json). Edit the source, not this page.
</Callout>

## When

> One verified package release must be consumed through exact frontend dependency metadata, without source or presentation changes. Package consumption and quality evidence precede the separately bound runtime surface audit and UAT; this chain alone never claims a user-facing delivery.

## Chain

| Step | Branch | Operator | Presets | Fan-out | Max parallel |
| --- | --- | --- | --- | --- | --- |
| 1 | 1/1 | [`environment.preflight`](/reference/operators/environment-preflight) | `roles` = `["fe"]` | — | — |
| 2 | 2/1 | [`workspace.bind`](/reference/operators/workspace-bind) | `role` = `"fe"`<br />`runtimeNeed` = `"none"` | — | — |
| 3 | 3/1 | [`dependency.update`](/reference/operators/dependency-update) | — | — | — |
| 4 | 4/1 | [`quality.verify`](/reference/operators/quality-verify) | — | — | — |

## Loops

This chain has no loop.

## Ends

`user`
