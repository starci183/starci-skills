---
title: "library-maintenance"
description: "An explicitly authorized existing owner package needs a behavior repair and next patch version. Bind its owner checkout, reproduce the regression, repair through library.source.apply and verify the package. Product presentation changes still follow the frontend workflows. Consumer integration, surface audit and UAT follow after the verified package is made available under its separately bound publication authority; a package proof is never a consumer delivery claim."
---

import { Callout } from 'nextra/components'

# library-maintenance
<Callout type="info">
Generated by `docs/scripts/generate-docs.mjs` from [`workflows/library-maintenance.json`](https://github.com/starci183/starci-skills/blob/main/workflows/library-maintenance.json). Edit the source, not this page.
</Callout>

## When

> An explicitly authorized existing owner package needs a behavior repair and next patch version. Bind its owner checkout, reproduce the regression, repair through library.source.apply and verify the package. Product presentation changes still follow the frontend workflows. Consumer integration, surface audit and UAT follow after the verified package is made available under its separately bound publication authority; a package proof is never a consumer delivery claim.

## Chain

| Step | Branch | Operator | Presets | Fan-out | Max parallel |
| --- | --- | --- | --- | --- | --- |
| 1 | 1/1 | [`workspace.bind`](/reference/operators/workspace-bind) | `role` = `"fe"`<br />`runtimeNeed` = `"none"` | — | — |
| 2 | 2/1 | [`library.source.apply`](/reference/operators/library-source-apply) | — | — | — |
| 3 | 3/1 | [`quality.verify`](/reference/operators/quality-verify) | — | — | — |

## Loops

This chain has no loop.

## Ends

`user`
