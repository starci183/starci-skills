---
title: "backend-feature"
description: "Server behaviour, an API contract, persistence or a job changes for one business feature; the architecture is decided first and the promise is reconciled after the code lands. This chain writes no surface, so it carries no frontend.surface.audit and no uat.verify: uat.verify requires a frontend-surface-audit input and a bound fe route, and neither exists here. A feature whose promise reaches a person through a screen is not this workflow — it is full-feature, which walks the journey before it publishes."
---

import { Callout } from 'nextra/components'

# backend-feature
<Callout type="info">
Generated by `docs/scripts/generate-docs.mjs` from [`workflows/backend-feature.json`](https://github.com/starci183/starci-skills/blob/main/workflows/backend-feature.json). Edit the source, not this page.
</Callout>

## When

> Server behaviour, an API contract, persistence or a job changes for one business feature; the architecture is decided first and the promise is reconciled after the code lands. This chain writes no surface, so it carries no frontend.surface.audit and no uat.verify: uat.verify requires a frontend-surface-audit input and a bound fe route, and neither exists here. A feature whose promise reaches a person through a screen is not this workflow — it is full-feature, which walks the journey before it publishes.

## Chain

| Step | Branch | Operator | Presets | Fan-out | Max parallel |
| --- | --- | --- | --- | --- | --- |
| 1 | 1/1 | [`environment.preflight`](/reference/operators/environment-preflight) | `roles` = `["be"]` | — | — |
| 2 | 2/1 | [`workspace.bind`](/reference/operators/workspace-bind) | `role` = `"be"` | — | — |
| 3 | 3/1 | [`business.decide`](/reference/operators/business-decide) | `mode` = `"model"` | — | — |
| 4 | 4/1 | [`architecture.decide`](/reference/operators/architecture-decide) | `alternatives` = `1`<br />`selectionPolicy` = `"automatic"` | — | — |
| 5 | 5/1 | [`backend.source.apply`](/reference/operators/backend-source-apply) | — | — | — |
| 6 | 6/1 | [`quality.verify`](/reference/operators/quality-verify) | — | — | — |
| 7 | 7/1 | [`business.decide`](/reference/operators/business-decide) | `mode` = `"reconcile"` | — | — |
| 8 | 8/1 | [`git.publish`](/reference/operators/git-publish) | — | — | — |

## Loops

This chain has no loop.

## Ends

`git.publish`
