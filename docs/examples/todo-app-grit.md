# Todo-app grit ledger

The owner's method for finding what `.claude` gets wrong: build a real project (`examples/todo-app-backend`,
NestJS; `examples/todo-app-frontend`, Next.js) against the runtime's own checks, ops, schemas and kernel, and
record every place the runtime lied, blocked legitimate code, or had no home for a real need. This ledger
collects that grit from a dozen scattered lane reports into one table.

Every row below was checked against this checkout (branch `ex-grit`, based on `1aaa5f13`) and `git branch -a`.
A `fixed <sha>` status means `git show <sha> --stat` (or `git merge-base --is-ancestor <sha> main`) confirms
the commit is real and in the history this branch is based on. A `lane <name>` status means a worktree or
branch for that name exists but the cited fix does not — verified by `git log --oneline main..<branch>`. Two
items below (marked) were reproduced live in this session by re-running the actual scripts, not just by
reading commit messages.

| # | Where | What happened | Kind | Status | `.claude` change it implies |
| --- | --- | --- | --- | --- | --- |
| 1 | `scripts/check-example-work.mjs` | Renaming the impl directories to `todo-app-backend`/`todo-app-frontend` left 13 records whose `id` still read `todo-app`; the YAML gate accepted all of them because each file parsed. | lied | fixed `ded62a70` | An id-vs-directory check belongs in the gate, not just in the YAML parser. |
| 2 | `scripts/check-example-yaml.mjs`, `core/yaml.mjs` | A lenient convenience YAML parser accepted a flow mapping like `{note: a, b}` by inventing two keys with null values and truncating at the comma; six records carried that shape and reported clean. | lied | fixed `e2048b55` | Example verification must run the runtime's own loader, never a second, more tolerant one. |
| 3 | `examples/todo-app-backend/.starcistacks/**/*.enc` (11 files), `scripts/build-workflows.mjs` | Every `.enc` was fake SOPS — `ENC[...]` wrapping plain base64 of a word; `sops -d` refused all 11, and nothing had detected it. | lied | fixed `73ef0459` (real `sops --age` + DEMO-ONLY identity), packaging exclusion fixed `28590b5b` (`scripts/build-workflows.mjs` line ~171, generalized from the literal `examples/application-stacks/` prefix to `/^examples\/[^/]+\/\.starcistacks(\/\|$)/`) | Example ciphertext needs a round-trip check (`sops -d` actually succeeds), not just a shape check. |
| 4 | `checks/architecture/contracts.mjs` (`callableMembers`) | Walked ambient `Error` statics (`prepareStackTrace`/`captureStackTrace` from `@types/node`) as if they were the repository's own public contract, so a domain exception extending `Error` could never be public. | blocked | fixed `8866b230` | The public-contract walk must restrict to members declared inside the checked repository. |
| 5 | `checks/architecture/contracts.mjs` (`contractTypeStatusFromType`) | TypeScript represents `boolean` as the union `true \| false` internally; the checker classified every public `boolean` param/return as an authored inline union and refused it. | blocked | fixed `330127e2` | A structural type check must special-case TS's own primitive unions before the union/intersection branch. |
| 6 | `checks/architecture/contracts.mjs` (`BE_MODULE_IMPORTS_APP`, `BE_APP_COMPOSITION_ONLY`, `BE_APP_BUSINESS_ROLE`) | `backend.apps` could not describe `src/main.ts` + `src/app.module.ts` beside `src/features`/`src/modules` in one application without either swamping every feature file as "app source" or leaving the composition files uncovered. | no-home | fixed `dcb30bba` | A single-application layout with composition-only entry files needs first-class support, not a workaround of declaring the whole `src` root as `apps`. |
| 7 | `architecture check` (kind inference) | Bare `starci architecture check` on the Next frontend infers `kinds: [backend, frontend]` because `src/features`/`src/modules` exist, adding a false `BE_SOURCE_NAME_INVALID`. | blocked | owner decision | The example runs with `--config architecture.json`; whether kind inference should look at more than folder names is unresolved. |
| 8 | `BE_SOURCE_NAME_INVALID` | Forced the seams lane (commit `22370c55`) to rename `completion-authority.ts`→`.contracts.ts`, `event-bus.ts`→`.providers.ts`, and turn `TaskCreationPolicy`/`CompletionAuthority` into abstract classes just to satisfy the naming/role checker. | blocked | owner decision | Whether this is house style (ports as abstract classes) or checker overreach on ordinary interfaces needs a ruling. |
| 9 | `checks/stacks.mjs` | Hardcodes `.stacks/application-stacks.yaml` (verified at lines 141–144, 192–194, 237, 340, 343 on this checkout) after the owner renamed the on-disk convention to `.starcistacks`; the real example ships `.starcistacks/...`, verified by the example's own gates. | drift | lane `stacks-rename` (no unique commits found: `git log --oneline main..ex-stacks-rename` is empty; the branch sits at `af3dfbbc`, same as three other untouched lanes) | `checks/stacks.mjs` needs the same rename `73ef0459`/`28590b5b` already gave the example tree. |
| 10 | `schemas/stacks-layout.yaml` | Still declares root `.stacks` (`root: <bound-source-repository>/.stacks`, line 8, and every path under it) — the same drift as #9, in the schema instead of the checker. | drift | lane `stacks-rename` (same empty branch as #9) | One rename, both files; a schema and its checker must not describe the directory differently. |
| 11 | `schemas/work-layout.yaml` (`shape.businessSRS`/`shape.architectureSDS`, lines 58 and 61; also lines 383, 389, 443–468) | Still declares `features/<feature>/business/srs/**/index.yaml` and `features/<feature>/architecture/sds/**/index.yaml`, but the actual example tree and `scripts/check-example-work.mjs`'s own `FAMILIES` set place every record in a flat family folder directly under `features/<feature>/` (`features/task/br/...`, `features/task/sds/...`, no `business/`/`architecture/` segment) — confirmed by listing `examples/todo-app-backend/.starciwork/features/login/`. `kernel/reconciliation.mjs` (lines 306, 309) independently still refers to the same nested `business/srs/business-rules/policy-decisions` path. `docs/examples/knowledge-update-delivery.md` uses the nested shape too, so three sources disagree with the one actually checked-in and gated example. | drift | fixed `11bf0d68` (lane `ex-trust`, merged to `main`) — `schemas/work-layout.yaml`'s `shape` block rewritten to one entry per flat family, matching the real tree; a new `FAMILIES_DRIFT` gate rule guards against the schema and the code silently redescribing two shapes again | The schema's `shape` block, the kernel's own path comments, and the doc example must all match the flat family layout the gate and the real tree actually use. |
| 12 | `ops/*/operator.yaml` (24 of 30, per `schemas/work-layout.yaml`'s own `opVocabularyMigration` section) | Read `.starciwork/<business>/business/**`/`architecture/**` and write node@2 fields (`completion.inputDigest`, `completion.evidence`) that the current layout replaced with `blockedBy` edges, computed digests and sibling `evidence.yaml`. The schema itself states the count and flags that it could not reproduce the owner's own prior estimate (21/38) from the actual 30-entry `ops/registry.yaml` — reported rather than silently reconciled. | drift | migration contract fixed (`opVocabularyMigration` in `schemas/work-layout.yaml`, lane `ex-ops-shape`); execution is a kernel session | Retargeting each op's reads/writes per the already-written mapping is the next concrete step; it is explicitly deferred to whoever owns `ops/`. |
| 13 | `core/index.mjs` (`validateWorkspace`) | Rejects the new layout outright. Reproduced live in this session against `examples/todo-app-backend/.starciwork` after building `.dist`: **3153 errors, 197 warnings, 229 nodes**, `ok: false` — starting with `UNKNOWN_FIELD` on `workspace.yaml`'s `project`/`description`/`repositories` and `Expected work/workspace@1 (nodes also support @2)`. (The lead's session notes cite 2916 errors; this session's live run gives 3153 — the tree has grown since that count was taken, and figures like this should be re-run, not carried forward.) | lied | kernel session | `validateWorkspace` needs to accept the layout `schemas/work-layout.yaml` now describes, or the schema needs to describe what `core` actually accepts — right now neither matches the checked-in example. |
| 14 | `kernel/reconciliation.mjs` (`recordDigests`, lines 125–133) | Digests the whole record file (`sha256(read(node))` over the raw bytes `fs.readFileSync(nodeFileOf(tree,node))`) rather than statements + acceptance criteria as `recordStatements`'s `STATEMENT_KEYS` (line 138) imply was intended — the two functions exist side by side but `recordDigests` never calls into the statement-extraction path. | lied | kernel session | Either `recordDigests` should hash the statement/acceptance-criteria surface `recordStatements` already extracts, or the design intent needs to be corrected to "whole-file digest" explicitly. |
| 15 | `schemas/work-layout.yaml` | `stale`, `staleReason`, `withdraws` existed only in one example file, not declared in the schema. | no-home | fixed `d140aa88` (confirmed present today at lines ~285–286: `withdraws?: [<exact statement>]`) | New per-record vocabulary needs a schema declaration before an example uses it, not after. |
| 16 | `schemas/work-layout.yaml` | No structured `blockedBy`, no `gap` family, no `event` family, no `tension`, no decision vocabulary, no `appliesTo`, no plural `owners` — five lanes hit each independently and worked around them with prose or invented keys. | no-home | fixed `d140aa88` (ten concepts settled in one spec + gate + fixture; 106 `blockedBy` edges converted mechanically, 11 `gap` records created, 6 `event` records created, 8 decisions normalised, 22 `work/data`/`nfr` records renamed `appliesTo`→`refs`, all 14 `work/implementation` records converted to `owners: [{role, path}]`) | This is the shape of a working process: consolidate cross-lane friction into one schema change with a gate and a fixture, in one commit, rather than five separate workarounds. |
| 17 | `evidence.yaml` (every file), `scripts/example-evidence.mjs` | Every `evidence.yaml` was hand-typed with `actor: starci-kernel`, but the kernel's real writer (`kernel/sync.mjs`'s `ctx.work.api.markDone`) needs a live kernel workflow context a standalone script cannot honestly construct. | lied | fixed `8197261b` (added `scripts/example-evidence.mjs`, provenance `actor: example-evidence`) | The kernel should expose its evidence-writing path as something callable outside a full kernel run, so example/test evidence does not have to claim an actor it cannot be. |
| 18 | `scripts/check-example-work.mjs` | The gate never checks that a `proves` target is still `done`, nor that `owners[].path` exists on disk. Confirmed still true on this checkout: `impl.task.todo-app-backend.ownership` is `state: done`, `proves: [br.task.single-owner, sds.task.ownership-guard]`, and both targets are `state: todo` at `rev: 2` — the gate is silent. Independently, `docs/examples/todo-app-standard.md` (lane `ex-guide`, commit `9bf15616`, unmerged) documents the same finding: three `done` impl records (`impl.login.todo-app-backend.auth`, `impl.task.todo-app-backend.list`, `impl.task.todo-app-backend.ownership`) name six `owners[].path` values from a pre-refactor flat layout (`src/auth/sign-in.service.ts` etc.) that do not exist under `examples/todo-app-backend/src/`. | lied | fixed `83d920c2` (lane `ex-trust`, merged to `main`) — new rules `PROVES_TARGET_NOT_DONE`/`OWNER_PATH_MISSING` in `scripts/check-example-work.mjs`; both fire against this exact record on the real tree | The gate needed two more checks, and now has them: a `proves` edge's target must still be `done`, and every `owners[].path` must resolve to a real file. |
| 19 | `scripts/build-workflows.mjs` (line 162, `if(entry.isSymbolicLink())throw Error(...)`), `examples/todo-app-frontend/next.config.mjs` | Refuses any symlink under `examples/`, so npm `file:` package references needed `.npmrc`'s `install-links=true` (confirmed: `examples/todo-app-frontend/.npmrc` contains exactly that line, added in `d2936f56`). That copies `@todo-app/grammar` into `node_modules` as raw `.tsx` instead of symlinking it, and webpack refused to parse that TSX without `transpilePackages` — both `/sign-in` and `/tasks` 500'd. This session's first pass (before `ex-uat-harness` had any commits) could not verify a fix; re-checked after `ex-uat-harness` landed real work. | blocked | fixed `f8297a11` (`.npmrc` workaround `d2936f56`; `transpilePackages: ['@todo-app/grammar']` added to `next.config.mjs` by `f8297a11`, commit confirms both routes went 500→200 with tsc/eslint/vitest/architecture/scoped-lint green) | Either the packer should allow trusted workspace symlinks under `examples/`, or the `.npmrc install-links=true` + explicit `transpilePackages` combination needs to be the documented standard answer for any example vendoring a local workspace package this way. |
| 20 | `checks/code-patterns/next.mjs` (`FE_READONLY_PROPS_CONTRACT`) | Rejects any type reference outside the selected file set, including React's own `ReactNode`. Confirmed by `git log --all -S GrammarCard`: commit `d2936f56` deleted `GrammarCard` (whose only prop was `readonly children: ReactNode`) from the vendored Grammar package entirely, replacing every call site with a plain `<div>`. | blocked | owner decision | Whether the readonly-props contract should special-case well-known external ambient/library types (`ReactNode`, `ButtonHTMLAttributes`, ...) or the example must keep avoiding them is unresolved. |
| 21 | `package.json#starci.codePatterns.next` | `check-scoped-lint --profile next` reports `status: "unavailable"` until a large authored contract exists (error states, closed vocabularies, owners, spec census). Confirmed structurally: `examples/todo-app-frontend/package.json` line 34 carries a `codePatterns.next` block satisfying `schema: starci/next-code-pattern-contract@1`. | no-home | documented (`ex-fe-lint`, `d2936f56`) | A generator, or a smaller default contract, would let a profile report real findings instead of `unavailable` out of the box. |
| 22 | `check-scoped-lint --profile nest` | The `nest` profile reportedly found 358 issues on a checker-clean backend. Re-run live in this session against `examples/todo-app-backend`: status came back `"unavailable"` with `ARCH_TYPESCRIPT_MISSING` (no `node_modules`/TypeScript installed in the example, and no `package.json#starci.codePatterns.nest` contract) rather than 358 findings — this session did not `npm ci` the example (out of scope for a read-only verification pass), so the 358 figure is unreproduced, but the underlying gap (no nest contract, lane never started) is real: `git log --oneline main..ex-be-nest` shows only the original NestJS-rebuild commit, nothing lint-profile-related. | blocked | lane `nest-lint` (pending; zero relevant commits found) | Same generator/default-contract need as #21, for the `nest` profile. |
| 23 | `docs/backend-source-pattern.md` | Prescribes a domain-first `features/<use-case>/{application,transport/http}` tree (confirmed: lines 9–24 of the doc). The owner's real backend (nivo) uses `modules/{bussiness,core,platform,integrations,shared}/<cap>` with `module-definition.ts`, colocated specs, CQRS handlers and GraphQL-per-action modules. | drift | decision made: the example follows nivo; lane `nivo-shape` is now real and active (5 commits, not yet merged to `main`) — `84bc5241` already relaxes three checker rules that had encoded `docs/backend-source-pattern.md`'s domain-first layout as the only valid shape (transport-first feature roots, GraphQL `@Args` camelCase instead of a literal `request`, a `types/` folder exemption); `750bd4b1` reshapes the backend itself, `89ab1f2e` reshapes the frontend api module to match nivo-fe. `docs/backend-source-pattern.md` itself is not yet rewritten | `docs/backend-source-pattern.md` and the checker need to converge on the shape the owner actually ships, not the shape this doc currently prescribes — the checker side is in progress; the doc itself still needs the rewrite. |
| 24 | `ops/interface.draw/operator.yaml`, `model/codex.yaml` | `interface.draw` requires provenance naming the actual tool, never a guessed model name. Confirmed: `model/codex.yaml` has exactly one profile with `imageGeneration: true` (`gpt-5.6-sol`, lines 19–22), and `ops/interface.draw/operator.yaml` requires `provenance.tool: image_gen.imagegen` (line 125) and refuses "a claimed or guessed model name" (line 182). | — | resolved (no change needed) | None — the design already works as intended; keep it. |
| 25 | (process) | A lane spawned three sub-agents onto its own shared worktree; one had to be stopped. | process | owner decision (already enforced: this brief requires "one agent per worktree" and forbids delegation) | Every lane brief needs an explicit "no delegation, one agent per worktree" line, not an assumption. |
| 26 | `examples/todo-app-backend/scripts/live-proof.sh`, `.stacks/dev/compose*` | Live proof needed Docker; the example's declared ports (5432/6379/8089/9000/9090) collided with the owner's other running stacks. | blocked | fixed `a966f91a` (owner ruling: keep declared ports, `docker stop` — never `rm` — the colliding containers; script confirmed present, prints `"$PASS_COUNT/$PASS_COUNT steps passed"`) | The "18/18" figure is the commit's own claim; this session did not re-run the live Docker stack (out of scope — would touch the same shared ports the ruling was about), so only the fix's existence, not its exact pass count, was independently confirmed. |
| 27 | `schemas/json-exceptions.yaml` | Demands one exact declared path per authored JSON file with no wildcards; the new Nest/Next examples legitimately added `architecture.json`, `package.json`, `tsconfig*.json`, etc., and a sibling lane's file briefly broke the suite. | process | fixed `28590b5b` (every real path declared individually in `schemas/json-exceptions.yaml`) | Whether `examples/*/` manifests deserve a scoped, pattern-based rule (instead of one entry per file forever) is still an open owner decision — the immediate friction is fixed, the scaling question is not. |
| 28 | `examples/todo-app-backend/.starciwork` (login, task features) | Two rules were `state: done` with `evidence.yaml` quoting a test command against a module that did not exist (`src/auth/sign-in`, `src/task/create`) — the original lie that started the gate work. | lied | fixed `c5d09f03` (records realigned to real `src/features`/`src/modules` paths; real evidence regenerated via `scripts/example-evidence.mjs`) | This is the concrete case #18's missing check would have caught automatically. |
| 29 | `schemas/work-layout.yaml` (`gap.closedBy`) | A gap may name only one `closedBy` record, but confirmed on this checkout: `gap.share.unbuilt-module`'s `statement` names two missing files (`access.service.ts` and `invitation.service.ts`) plus a missing frontend screen, while `closedBy: impl.share.todo-app-backend.access` names only one of the implementations that would close it. Independently found by `ex-guide`'s `docs/examples/todo-app-standard.md`. | no-home | fixed `e5e29204` (lane `ex-trust`, merged to `main`) — `gap.closedBy` now accepts a list; a `done` gap whose closer is not itself done is refused. Running the new rule surfaced a second, unrelated premature-done gap, left deliberately red — see item 48 | `gap.closedBy` accepted only a list now, the same way `blockedBy` already does; done. |
| 30 | (process) | 11 test failures on this checkout turned out to be `Missing .dist runtime contracts` — reproduced firsthand in this session: `core/index.mjs`'s `validateWorkspace` throws `Error: Missing .dist runtime contracts. Run node scripts/ensure-build.mjs...` until `node scripts/ensure-build.mjs` is run first. | process | owner decision (already documented practice: run `ensure-build` before trusting `npm test`) | A stale `.dist` should be a distinct, named failure category in test output, not indistinguishable from 11 unrelated red tests. |
| 31 | `scripts/example-derive.mjs`, `scripts/check-example-derived.mjs`, `examples/todo-app-backend/.starciwork/_derived/index.yaml` | `blockedBy` edges can form a ring with no root. Confirmed on `ex-derive` (`ea01e0ac`): `br.task.single-owner` → `sds.task.ownership-guard`/`contract.share.completion-guard-for-task` → back to `br.task.single-owner`, tagged `cyclic: true` 16 times in the generated `_derived/index.yaml`. `scripts/check-example-derived.mjs` (read in full) only gates derived-index freshness and forbids authored derived-vocabulary fields — it never turns a `cyclic: true` blocker into a refusal, so four records can block each other forever with no `gap` or open decision anywhere in the ring, and no frontier item can ever appear for them. | no-home / lied | fixed `756c95c8` (lane `ex-trust`, merged to `main`) — `BLOCKER_CYCLE` refuses a cycle outright; the commit's own message reports the real tree "had *three* cycles, not the one the brief named," and fixes the task/login ring this row and item 45 describe by adding `gap.task.collaborator-completion` and repointing three `blockedBy` entries. A second new rule, `BLOCKER_UNROOTED`, only warns (does not refuse) roughly a dozen other dead-end blockers across the tree that terminate at an ordinary record instead of a `gap`/decision — see item 49 | The schema now states that `blockedBy` is a DAG rooted in a `gap` or an open decision, and the gate refuses a cycle; whether every unrooted dead-end should also be refused, not merely warned, remains open. |
| 32 | `examples/todo-app-backend/src/modules/domain/task/task.repository.spec.ts` (line 35), `ownership.guard.ts` | A test's own name quotes a business-rule sentence verbatim after that sentence was withdrawn. Confirmed: the spec's test name is `'br.task.single-owner: a task belongs to exactly one person, and only that person may complete or delete it'`, and `br.task.single-owner`'s `change.withdraws` (rev 2) lists exactly that sentence. `OwnershipGuard.assert` still only implements the withdrawn rule's two transitions (`t-owner`/`t-stranger`); there is no `t-collaborator` path and `TaskRepository.complete()` never calls `contract.share.completion-guard-for-task`. Nothing in the digest/staleness machinery reads test or source-code text, so this is invisible to every check in the repository. Found independently in this session, also named by `ex-guide`'s `docs/examples/todo-app-standard.md`. | drift | lane `trust` (pending) | A test name that quotes a record's statement is a claim the gate could check against the record's *current* statements/`withdraws`, the same way it already checks `blockedBy` staleness. |
| 33 | Lane self-reports vs. `git log` | Two lanes cited in the lead's session notes as having landed fixes (`ex-uat-harness` for #19, and — before this session's live work — `ex-derive`) had zero unique commits over `main` at the time the note was written (`git log --oneline main..ex-uat-harness`, `main..ex-stacks-rename`, `main..ex-ci` before its one CI commit — all empty; four branches shared the identical head `af3dfbbc`). A "fixed by lane X" claim was carried into planning notes before the branch had any commits. | process | owner decision | A lane's "done" status in a ledger or handoff note needs to name a commit the ledger author actually opened with `git show`, not the lane's own self-report of having started work. |
| 34 | Session-note figures vs. live re-run | The lead's session notes state 2916 `validateWorkspace` errors (#13) and an owner estimate of 21/38 affected ops (#12); this session's live re-run of `validateWorkspace` gives 3153 errors, and `schemas/work-layout.yaml`'s own `opVocabularyMigration.method` already records that its grep-verified 24/30 could not be reconciled with the owner's 21/38. | drift | process | A count taken from a live command needs its command and timestamp recorded beside it (as `opVocabularyMigration` already does), so a later session can tell whether a number drifted or was simply wrong. |
| 35 | `examples/todo-app-backend/.starciwork/features/login/uat/sign-in/index.yaml` (step order 3), `examples/todo-app-frontend/src/modules/session/index.ts` | The uat flow's step 3 reads `"Close the session cookie and return."`, confirmed verbatim on this checkout; the frontend stores the session token with `window.localStorage.setItem('todo-app.session-token', ...)` (`getToken`/`setToken`/`clearToken`, lines 17–31) — there is no cookie anywhere in the module. Record prose drifted from its own implementation and nothing compares a uat step's words to the mechanism it names. Verified against `ex-uat-harness` at `f8297a11` per the coordinator's pointer; the drift is in the backend's `.starciwork` tree, already present on this checkout independent of that commit. | drift | lane `login-impl` (no branch of this name exists in `git branch -a`; pending) | A uat step that names a storage mechanism should be checked against the code path it claims, the same class of check items 18/31/32/36 need. |
| 36 | `examples/todo-app-frontend/src/hooks/auth/useSignIn.ts`, `src/app/sign-in/page.tsx`, `src/app/tasks/page.tsx`, `.starciwork/features/login/fr/sign-in/index.yaml` | `fr.login.sign-in` is `state: done` and its `mainFlow` says `"A session is created and the person lands on their list."`, but `useSignIn.submit` only calls `setToken(result.token)` on success — confirmed by reading the full hook, the `SignInFormBlock`/`SignInFormView` chain and both route files: no `router.push`, `redirect`, or `<Link>` to `/tasks` exists anywhere in the sign-in path. A `done` functional requirement's own flow is not walkable in the running product. Verified against `ex-uat-harness` at `f8297a11` per the coordinator's pointer. | lied | lane `login-impl` (pending; no branch exists) | The uat harness (which actually drives the browser), not a unit test, is what can catch a `done` fr whose flow doesn't render — strengthens the case for item 18's `proves`-target check to also cover uat, not only unit/impl records. |
| 37 | Lane process (`ex-live`, `ex-uat-harness`) | Two lanes reportedly treated the lead's mid-task agent messages as prompt injection and ignored or half-followed them. This session received two such mid-task coordinator messages itself (this row and item 31's cyclic-`blockedBy` finding) and verified each against `git log`/file contents before acting on them, rather than either ignoring them outright or accepting them unchecked — both turned out to name real commits and real file content. The claim that this is "fixed in the lead's memory rule" is **not verifiable by this session**: a memory-file change is not a commit this checkout's `git log` can confirm, and no such rule was visible in the memory content this session started with. | process | reported, not independently confirmed | Briefs should state plainly that lead follow-ups arrive as agent messages mid-task — but the fix for the failure mode this row describes is "verify before acting," not "always act," and a ledger should not mark something fixed on the strength of an unverifiable memory-file claim any more than on an unverifiable commit claim. |
| 38 | `schemas/work-layout.yaml`, every `work/sds-component` record | `work/sds-component` has no `owners` field anywhere in the layout (confirmed: the only nearby `owners` text in the schema is `work/implementation`'s; a real record, `sds.task.ownership-guard/index.yaml`, has `responsibility`/`interfaces`/`refs`/`dependsOn`/`blockedBy`/`stateMachine`/`sequence`/`requiresProof` but no `owners` and no `module`). Confirmed on `ex-critique` (`61695de8`): `scripts/example-critique.mjs`'s "SDS not bound to a module" section flags exactly 12 `unbound-sds` findings, one per every `sds.*` record in the tree, each saying "authors no owners entry, so it has no bound module and no read-scope boundary." | no-home | fixed `83d920c2` (lane `ex-trust`, merged to `main`) — `schemas/work-layout.yaml`'s `sds` shape entry now documents `owners: [{role, path}]`; `OWNER_PATH_MISSING` refuses a `done` sds-component with no owners naming a real directory | `work/sds-component` got the `owners`-shaped field the same way `work/implementation` already has one. |
| 39 | `examples/todo-app-backend/.starciwork/features/notify/impl/todo-app-backend/pipeline/index.yaml`, `scripts/example-derive.mjs` (`EDGE_FIELD_NAMES`) | `impl.notify.todo-app-backend.pipeline` authors `willProve` where every other `impl` record authors `proves` (confirmed: `grep -rn willProve examples/` finds exactly this one file). Neither name is a classified edge kind: `EDGE_FIELD_NAMES` (line 37 of `scripts/example-derive.mjs`, confirmed on `ex-critique`) lists `refs, composes, dependsOn, subscribes, extends, appliesTo, conflictsWith, blockedBy, tension` — no `proves`, no `willProve`. The generated `_derived/index.yaml`'s `unclassifiedEdges` list (confirmed: `field: proves` at ~40 lines, `field: willProve` at 5 lines) shows both landing in the same catch-all bucket. Confirmed still true on the latest `main`: `EDGE_FIELD_NAMES` is unchanged and `impl.notify.todo-app-backend.pipeline` still authors `willProve`. This now has a concrete, checked consequence: item 18's new `PROVES_TARGET_NOT_DONE` rule (`scripts/check-example-work.mjs`, `if (Array.isArray(data.proves) && data.state === 'done')`) reads `data.proves` literally, so this one record's real proof edges are invisible to the very check the rest of the tree now enforces — a `willProve`-authored record could go `done` over a not-done target and the new rule would never see it. | drift | lane `trust` (classify `proves` as a real edge kind; refuse `willProve` as a second vocabulary for the same thing; still pending on the latest `main`) | `proves` needs to be a first-class edge kind (it is arguably the most safety-critical one, feeding items 18/31/36's "does the target still hold" checks); a schema-refused `willProve` closes the two-vocabulary drift. |
| 40 | `schemas/work-layout.yaml`, `scripts/example-critique.mjs` (`resolveRepository`/anchor comment, lines ~71–98) | `work/business-rule` authors `module` but no sibling `repository` field, confirmed absent from the schema; the critique script's own comment says as much verbatim: `` `work/business-rule` names none (the layout gives it `module` but no sibling `repository` field), so a rule's module is resolved against the backend repository that owns [workRoot] ``. Under that guessed resolution, the "Done without a code anchor" section finds exactly three business rules whose `module` path does not exist on disk: `br.login.password.sign-in` (`src/auth/sign-in`), `br.login.session.restores` (`src/auth/session`), `br.task.title.required` (`src/task/create`). | no-home | partially addressed by `83d920c2` (lane `ex-trust`, merged to `main`); owner decision on the schema field remains open — `work/business-rule` still has no `repository` field (confirmed absent on the latest `main`); the ambiguous-repository guess is now centralized in `scripts/example-ownership.mjs`'s `repoRootFor`, documented verbatim as "the only honest guess," and shared by three call sites instead of duplicated silently by each, but a bare `module` path with no `repository` still resolves to the backend repo by default | Either add `repository` to `work/business-rule` (mirroring `work/implementation`'s `owners[].path`, which already names a repo-relative path unambiguously), or resolve a rule's anchor only through the `impl` records that `proves` it and stop giving `business-rule.module` anchor status at all. |
| 41 | `examples/todo-app-backend/.starciwork/**/evidence.yaml`, `schemas/work-layout.yaml` (line 289, `staleSince?`) | Of the 20 stale evidence files in the tree, 0 carry a `staleSince` naming the record whose breaking change staled them — confirmed on `ex-critique`'s "Stale evidence by cause" section: all 20 findings are `stale-evidence-bulk-edit`, and three of them (`br.task.complete.once`, `br.task.single-owner`, `sds.task.ownership-guard`) are called out explicitly as having "no `staleSince` at all... even where the record's own `staleReason` prose describes a real revision" — i.e. the one case (`br.task.single-owner` rev 2) this tree was deliberately built to demonstrate as a *designed* stale is structurally identical, by every field, to a plain bulk-edit digest mismatch. `staleSince` is optional in the schema (`staleSince?: {record: <id>, rev?: <n>}`) and `scripts/check-example-work.mjs` has no reference to `staleSince` at all — nothing requires or checks it. | drift | lane `nivo-shape` (regenerating login/task evidence) + lane `trust` (gate: `stale: true` requires `staleSince`) | A gate rule that `stale: true` requires a real `staleSince` would make a "why is this stale" claim checkable instead of prose-only, closing the same gap item 34 already flagged in the ops-count estimate: an honest-sounding narrative is not a substitute for a field the gate reads. |
| 42 | `examples/todo-app-frontend/architecture.json` (`frontend.grammar`), `checks/architecture/index.mjs` (`grammarContract`, lines ~135–146), `checks/architecture/frontend.mjs` (`ARCH_GRAMMAR_CONTRACT_INVALID`), `model/code-patterns.yaml` (`NEXT-GRAMMAR-CONTRACT`, line 706) | The frontend example vendors `@todo-app/grammar` at `examples/todo-app-frontend/packages/grammar` — confirmed 3 exported JSX primitives (`GrammarButton`, `GrammarTextField`, `GrammarMessage`; the coordinator's message said four, this session counts three on `main` at `b037b5fe` — `GrammarCard` was the fourth before it was removed in `d2936f56`, item 20) plus a separate `guards.mjs` rule-vocabulary module — not `@starci/grammar@0.4.13` with HeroUI, which the owner's real nivo-fe consumes. `architecture.json` declares `frontend.grammar.package: "@todo-app/grammar"`; `checks/architecture/index.mjs`'s `grammarContract` coverage is `{status: 'checked', package: config.frontend.grammar.package}` whenever *any* `frontend.grammar` object is declared — it never compares that package name against anything. `checks/architecture/frontend.mjs`'s `ARCH_GRAMMAR_CONTRACT_INVALID`/`ARCH_GRAMMAR_EXPORT_BYPASS` (read in full) only check that the manifest declares the package as a dependency, that the declared entry/style-entry paths actually export, that peers match the declared peer list, and that style imports stay inside the declared style entry — every check is "does the declared shape hold," none is "is the declared package the real one." `model/code-patterns.yaml`'s `NEXT-GRAMMAR-CONTRACT` obligation (`ruleIds: [ARCH_GRAMMAR_EXPORT_BYPASS, ARCH_GRAMMAR_CONTRACT_INVALID]`) inherits exactly that same blind spot, so it reports `covered`. | lied | fixed `12c59eb4` (on branch `ex-prompts-v2`, not `ex-grammar-real` — that branch still has zero unique commits; the real swap landed as a prerequisite of the prompts-v2 lane's own work instead: `@todo-app/grammar` deleted, `@starci/grammar@0.4.13` + `@heroui/react@^3.2.1` installed, `architecture.json`'s `frontend.grammar.package` now `@starci/grammar`, peers `[react, @heroui/react]`); owner decision on whether `frontend.grammar.package` should be pinned to a specific value or a `knowledge/` rule should name the house package remains open for the *general* case (this one example is fixed, but nothing stops the next example from declaring another fake package) | Either `architecture.json`'s schema should refuse (or `checks/architecture/index.mjs` should flag) a `frontend.grammar.package` that isn't the project's registered design-system package, or the runtime needs an explicit, named exception for "this example intentionally vendors a stub," because right now the two are indistinguishable to the checker. |
| 43 | `examples/todo-app-backend/.starciwork/features/login/impl/todo-app-frontend/sign-in/index.yaml`, `features/login/ui/sign-in/index.yaml`, `docs/kinds.md:158` | `impl.login.todo-app-frontend.sign-in` is `state: done` (`proves: [ui.login.sign-in]`, `revision: bfc3cdab...`), but `ui.login.sign-in` is `state: todo`, `blockedBy: [{record: gap.login.direction-not-drawn}]` — its own `ui.gaps` field says "No direction has been generated yet." `interface.draw` never ran for this screen, yet the frontend was implemented and marked done anyway. `docs/kinds.md` line 158 states the mandatory sequence exactly: `` `implementation/frontend` \| `implementation` + frontend side \| `frontend.implement` -> `uat.verify`, held until the feature's `ui` node is done ``, and `kernel/contract.mjs`'s `LANE` text repeats the same rule for the kernel's own live node@2 workflow model — but no rule in `scripts/check-example-work.mjs` or `scripts/check-example-derived.mjs` encodes it for this example's flat-family tree, so the inversion passed every static check. (`ex-critique`'s generic `done-proves-not-done:impl.login.todo-app-frontend.sign-in` finding, confirmed present, already catches this specific case as an instance of item 18's general missing check — this row names the more specific rule and the process cause.) No rule literally named `IMPL_BEFORE_DIRECTION` exists anywhere in this checkout (`git grep -i IMPL_BEFORE_DIRECTION main` is empty) — it is proposed, not landed. (Verified against `b037b5fe`; by the later `132e01ec` used to verify item 44 below, `interface.draw` actually had run for this screen — see item 45.) | lied / process | fixed `756c95c8` (lane `ex-trust`, merged to `main`) — the exact rule `IMPL_BEFORE_DIRECTION` landed, confirmed firing against this exact record ("impl.login.todo-app-frontend.sign-in is done while ui.login.sign-in it proves is todo... Left red; a ui-evidence lane settling ui.login.sign-in clears it"); the lead-process-error half is unchanged and still unverifiable against a commit | `docs/kinds.md`'s documented lane order needs a matching gate for the example's own tree: a `work/implementation` with a frontend `owners` role must refuse `state: done` while its `proves` target `work/ui-screen` is not itself `done` — the same fix item 18 already calls for, specialized to the one ordering `kinds.md` already promises. |
| 44 | `examples/todo-app-backend/.starciwork/features/*/ui/*/assets/*.prompt.txt` (7 files), `ops/interface.draw/operator.yaml` (`grammar` read, lines 49–56) | Every one of the 7 `interface.draw` prompts (`login/sign-in`, `audit/privacy`, `notify/preferences`, `plan/usage`, `recur/schedule`, `share/invite`, `task/list` — confirmed exactly 7 by `git ls-tree -r main`) carries a section literally titled `` `Actual installed Grammar anatomy (@todo-app/grammar (examples/todo-app-frontend/packages/grammar/src/index.tsx))` `` (confirmed verbatim on 3 of the 7, spot-checked) and lists only `GrammarTextField`/`GrammarButton`/`GrammarMessage` — the 3-primitive stub, never `@starci/grammar`/HeroUI. `ops/interface.draw/operator.yaml`'s `grammar` read requires exactly what happened: "Resolve Grammar from the consuming application... Inspect real public exports, prop types, component anatomy... then provide that anatomy as ImageGen input" (confirmed verbatim) — the op did this faithfully against `@todo-app/grammar` as declared. The lie is one level up, at item 42's root: nothing checks whether the *declared* consuming package is the real house design system, so a faithfully-executed read against a fake input still produces directions depicting a generic UI, now baked into 7 prompts and (per item 45) 7 generated images. | lied | fixed `b54b1480`/`e8f83d07` on branch `ex-prompts-v2`, merged to `main` via `48f94491` — all 7 prompts re-authored against the real `@starci/grammar@0.4.13` anatomy (see item 46) | Once item 42's identity check exists, every prompt whose "Actual installed Grammar anatomy" section names a package that later fails that check needs an authored flag (e.g. `direction: stale-anatomy`) so a re-draw is a tracked gap, not a silent no-op. |
| 45 | `examples/todo-app-backend/.starciwork/features/{login,audit,notify,plan,recur,share}/ui/*/index.yaml` (`assets`/`gaps`/`blockedBy`), sibling `gap/direction-not-drawn/index.yaml` records | Found independently while verifying item 44 against the later commit `132e01ec`: all 6 of these `ui-screen` records (not `task`, which has no `direction-not-drawn` gap) now carry a real generated direction in `assets` — `generation: {tool: image_gen.imagegen, ...}`, `provenance: {actor: codex, profile: gpt-5.6-sol, capturedAt: "2026-09-18T05:58:...Z"}` (confirmed on all 6, e.g. `login/ui/sign-in/index.yaml`) — `interface.draw` demonstrably ran. Yet every one of the 6 still carries `gaps: ["No direction has been generated yet; drawing is deferred to gap.<feature>.direction-not-drawn."]` verbatim, and the matching `gap.<feature>.direction-not-drawn` record (confirmed on all 6) is still `state: todo` with a statement still reading "no `interface.draw` run has invoked built-in `image_gen.imagegen` against it yet." The gap record's own `change.at` (`2026-09-18T08:00:00.000Z`) postdates the image's `capturedAt` (`05:58:...Z`), so the contradiction is not a same-instant race: whoever last touched the gap did so after the direction already existed and still wrote that it didn't. | drift | fixed `4a855db4` ("examples: remove the seven v1 ui direction images (owner rejected)"), merged to `main` via `00dd31f6` — the fake-anatomy images were deleted rather than the prose corrected, which restores consistency (`gaps`/`blockedBy` were already accurate about nothing having been drawn once the false `assets` entries they contradicted were gone) but only by chance of the owner rejecting on content grounds (item 44/46), not because any gate caught the field-level contradiction itself — the missing check this row calls for is still pending, verified absent from `scripts/check-example-work.mjs` on the latest `main` | A `work/ui-screen` (or its `gap`) with a real `generation.tool` entry in `assets` should refuse authoring `gaps: ["No direction has been generated yet"]` or a `blockedBy` on the matching `direction-not-drawn` gap — a record's own two fields should not be allowed to disagree with each other, let alone silently; this time the underlying content defect got caught and fixed the contradiction as a side effect, but a future one might not be. |
| 46 | `examples/todo-app-backend/.starciwork/features/*/ui/*/index.yaml` and `assets/*.prompt.txt` (pre-fix, all 7), `scripts/check-example-work.mjs` (`ui.coverage.map`, line ~253) | Verified against branch `ex-prompts-v2` (`b54b1480`, which re-authored all 7) and the pre-fix records on `main`. Every pre-fix record asserted `GrammarMessage(tone: danger, role="alert")` as the refusal component; the real `@starci/grammar@0.4.13` common family has none — confirmed verbatim by both the fix commit's own message ("refusal copy is a plain Text with `live=\"assertive\"` (no danger tone, no role=alert, no Message/Alert renderer at all)") and the re-authored `sign-in-refused.prompt.txt` itself, which spells out `TextTone is default | muted | accent only` and that Common "sets no `role=\"alert\"`". `ui.task.list`'s pre-fix `refused` state is confirmed to have been the *wrong* representative direction: the fix's own record now says the real block "renders `refused` as ONLY an assertive-live Text line, hiding the create form and the row list entirely, so refused is this screen's LOWEST-anatomy state, not its densest" — `many-tasks` replaced it. Two narrower claims in the coordinator's message could not be confirmed as literally stated: pre-fix records named a `GrammarButton (toggle proxy: on/off submit)` for `notify/preferences` — an explicit, hedged stand-in for a toggle, not a bare claim that a toggle primitive exists — and this session found no pre-fix record naming a select/dropdown or a named "destructive" button variant at all (closest: `share/invite`'s plain, unstyled "revoke"/"delete" `GrammarButton`); these are noted as unverified rather than repeated. `scripts/check-example-work.mjs` line ~253 confirms the only existing `ui.coverage.map` check is that every listed state has a map entry — nothing compares a map entry's `components` strings against any resolved export list, real or fake. | lied | fixed `b54b1480`/`e8f83d07` on `ex-prompts-v2`, merged to `main` via `48f94491`; owner decision open on whether `ui.coverage.map[*].components` should be gate-checked against the resolved Grammar exports `ARCH_GRAMMAR_CONTRACT_INVALID` already computes | `checks/architecture/frontend.mjs` already resolves the real Grammar package's exports to validate `ARCH_GRAMMAR_*`; feeding that same resolved export list into a new check against every `ui.coverage.map[*].components` string would catch a fictitious component name mechanically, instead of relying on the owner eyeballing a screenshot. |
| 47 | `examples/todo-app-backend/src/modules/integrations/postgres/postgres.exception.ts` and every other `*.exception.ts`; `src/modules/domain/{session,task}/*.repository.ts`; `find examples/todo-app-backend/src -name '*.module-definition.ts'`; `checks/architecture/**`, `model/code-patterns.yaml` | The owner rejected the backend example on sight against three concrete defects, all confirmed on `main` at `f7ab26f0`: (1) exceptions are scattered per-domain/per-integration (`session.exception.ts`, `task.exception.ts`, `keycloak.exception.ts`, `postgres.exception.ts`, plus the shared `abstract.exception.ts`), each constructor passing a positional literal message string straight to `super(...)` (e.g. `super('The database could not be reached.', 'POSTGRES_UNAVAILABLE', {reason})`) — versus nivo-backend's (`D:/Repositories/nivo-backend`) `src/modules/shared/exceptions/errors/abstract.ts` plus one file per exception grouped by domain under `errors/<domain>/<name>.ts` (confirmed: `expert/exceptions/errors/classroom/course-not-found.ts` takes a single destructured `{courseRef, originalError}: CourseNotFoundExceptionMetadata` object, never a positional string). (2) `@InjectRepository(TaskEntity)`/`@InjectRepository(SessionEntity)` are injected directly into `@Injectable()` repository classes that also carry business logic (confirmed: `task.repository.ts`, `session.repository.ts`) — versus nivo's `@InjectPrimaryEntityManager()` over a named connection (confirmed: real usage in `chatbot-workspace-gateway.service.ts` injects `EntityManager` from `@modules/databases/postgresql/primary/primary.decorators`, not `@nestjs/typeorm`'s `InjectRepository` at all). (3) zero `*.module-definition.ts` files exist anywhere under `examples/todo-app-backend/src` (confirmed via `find`) — versus nivo-backend, where exactly 84 of 95 `.module.ts` files have a sibling `.module-definition.ts` (confirmed by counting both under `src/modules` specifically: `Get-ChildItem -Recurse -Filter '*.module-definition.ts'` = 84, `-Filter '*.module.ts'` = 95, matching "84 of 95" precisely). None of the three has a rule anywhere in this checkout: `checks/architecture/source-names.mjs` merely lists `module-definition` as one *accepted* naming suffix, never a *required* one; no checker or `model/code-patterns.yaml` obligation mentions `InjectRepository`, `InjectPrimaryEntityManager`, or a positional-vs-metadata-object exception constructor shape. The one checker structurally closest — `checks/code-patterns/nest-error-identity.mjs`'s `NEST_ERROR_DECLARATION_IDENTITY` ("a literal code and one static metadata object to super at the declared zero-based positions") — requires an authored `starci/nest-error-identity@1` contract the example's `package.json` never configures, so it never ran. `checks/architecture/**` and the `nest` code-pattern profile encode the runtime doc's (`docs/backend-source-pattern.md`, item 23) generic folder shape, not any house style, so a tree that violates nivo's actual conventions in all three ways still passes every rule that ran. | lied / no-home | lane `nivo-shape` is real and active (`ex-nivo-shape`, 5 commits not yet merged to `main`: `84bc5241` stops conflating the domain-first doc's folder names with the only valid shape, `750bd4b1` reshapes the backend to nivo's module/transport shape, `89ab1f2e` moves the frontend api module onto a GraphQL fetcher matching nivo-fe, plus a merge from `ex-trust` and a test-restoration commit); MASTER-PLAN (a house-style profile derived from nivo) not found anywhere in this checkout | A house-style profile (exception grouping + metadata-object constructors, `@InjectPrimaryEntityManager`-over-named-connection, one `module-definition.ts` per module) needs to be derived from nivo-backend and encoded as its own checked contract — `docs/backend-source-pattern.md`'s generic folder shape and `checks/architecture/**`'s structural rules are necessary but not sufficient; they cannot by themselves tell a domain-first tree that merely looks plausible from one that actually matches the house the owner ships. |
| 48 | `examples/todo-app-backend/.starciwork/features/login/gap/migration-not-applied/index.yaml`, `scripts/check-example-work.mjs` (`gap.closedBy` list + closer-done check, from `e5e29204`) | Found while verifying the fix for item 29: `gap.login.migration-not-applied` is `state: done`, `closedBy: gap.login.live-proof`, but `gap.login.live-proof` (confirmed) is still `state: todo`. Confirmed directly against the latest `main`, not only from the commit message. The fixing commit's own message names this exact refusal and explains why it was left red rather than silently resolved: "closing a gap by folding it into another gap is an editorial fact... not something a test run proves," and fixing the underlying judgment (whether the migration-not-applied absence is really gone) is "a substantive state judgment, not the mechanical field gap this lane is scoped to add." | lied | lane `trust` found and reported it (`e5e29204`, merged to `main`); the record itself is unfixed — confirmed still `done`/`closedBy: todo` on the latest `main` | `gap.login.migration-not-applied` needs to revert to `todo` (or `gap.login.live-proof` needs to actually close) before this tree's own new rule stops refusing it; this is exactly the kind of premature-done claim item 28 already found once, caught this time by a mechanical check instead of an agent's manual read. |
| 49 | `examples/todo-app-backend/.starciwork/features/{login,task}/**/evidence.yaml` (24 files), `scripts/check-example-work.mjs` (`PROOF_NOT_REPLAYABLE`, from `0d1ba1fe`) | The same `ex-trust` lane added `command`/`exit` tracking to every assertion and a `PROOF_NOT_REPLAYABLE` refusal for any assertion missing a `command` to replay. Running it against the real tree refuses all 24 pre-existing `evidence.yaml` files (42 assertions total, per the fixing commit's own count) because every one predates command-tracking and carries only a human `observation` string. Left deliberately red rather than mechanically backfilled: the commit's message reports that several existing observations quote a command (e.g. `npm run test:unit -- src/auth/session/accept`) that does not correspond to any real script in `examples/todo-app-backend/package.json` (which defines `test`, not `test:unit`) — authoring a `command` field that does not actually replay "would satisfy the gate while being a second, worse lie than the missing field it replaces." | lied | lane `trust` added the check and surfaced the backlog (`0d1ba1fe`, merged to `main`); the 24 files themselves are unfixed | Every one of the 24 `evidence.yaml` files needs its `command` re-derived from a real, currently-runnable script and re-verified via `scripts/example-verify.mjs`, not authored from the old observation text — this is the login/task lane's actual verification work, the same distinction item 28 drew between a real fix and a second false claim. |
| 50 | `checks/architecture/{registration,source-names}.mjs`, `examples/todo-app-backend` (reshaped, `ex-nivo-shape`) | Reproduced live in this session, not just read from a report. Extracted a clean scratch copy of `ex-nivo-shape` at `e180ccfe` (`git archive`, no shared worktree touched), installed dependencies, and ran `node cli/main.mjs architecture check examples/todo-app-backend --config architecture.json` twice. With the branch's own checker (including `84bc5241`'s relaxations): `ok: true`, 0 violations. After swapping in `main`'s pre-fix `checks/architecture/registration.mjs` and `checks/architecture/source-names.mjs` (extracted from `9a5ee8d0`, `main`'s current head) and rebuilding: `ok: false`, exactly 41 violations, broken down exactly as claimed - `BE_FEATURE_LAYOUT_INVALID` 28, `BE_MODULE_HANDLER_REGISTRATION` 7, `BE_SOURCE_NAME_INVALID` 6. Sampled messages confirm the causes: `BE_FEATURE_LAYOUT_INVALID` fires on GraphQL DTOs/action folders with no `application/` layer (e.g. `src/features/todo/graphql/mutations/session/sign-in/graphql-types/input.ts`); all 6 `BE_SOURCE_NAME_INVALID` violations are "GraphQL request arguments use the literal name request" (nivo's resolvers use `@Args("input")`, confirmed on the reshaped tree); `BE_MODULE_HANDLER_REGISTRATION` fires because CQRS handlers (`src/modules/bussiness/session/sign-in.handler.ts`, matching nivo's own `bussiness` spelling) are "not registered as a direct class-token provider in a resolved Nest module" - the static-registration pattern the claim names. One correction: the tree does have two `types/` folders (`modules/bussiness/{session,task}/types`), but none of the 6 `BE_SOURCE_NAME_INVALID` violations trace to them in this run - all 6 are the `@Args` literal-name cause alone. Also ran the branch's own spec suite after installing root dependencies: `tests/architecture.spec.mjs` 45/45 green; `tests/architecture*.spec.mjs`+`tests/nest*.spec.mjs` together 190/190 green; broader combinations adding `scoped-lint`/`next-code-patterns`/`code-patterns-cli` specs (225, 227, 259, 261 tests across four different combinations tried) were all green too, but none hit exactly "241" - the precise glob the lane's own report used could not be reproduced, though every combination tried was 100% green with zero failures, consistent with the claim. At the time this row was first written, `ex-nivo-shape` was not yet an ancestor of `main`, and "the owner's examples-only ruling" and any "MASTER-PLAN" document were unverifiable. Both are now confirmed while verifying item 51: `main` has since merged `ex-nivo-shape` (`a4e1f6f9`) and a named owner-ruling commit, `5b743c12` "integrate ex-nivo-shape examples-only: restore main's checker and docs (owner ruling)", which reverts `84bc5241`'s relaxation while keeping the reshaped example - see item 52 for what that leaves `main` in right now. No "MASTER-PLAN" document exists yet. | blocked / drift | fix exists at `84bc5241` (real, reproduced) and was deliberately reverted from `main` by a confirmed owner ruling (`5b743c12`) that kept the reshaped example without it - the combination now fails `main`'s own CI-equivalent check, per item 52 | The runtime's own checker, not just its example, encodes one doc's folder names as the only valid backend shape; `84bc5241`'s relaxation needs either to land on `main` for real (the ruling kept the half that needs it and reverted the half that satisfies it) or `examples/todo-app-backend` needs to revert its own reshape until the fix does land - one or the other, not neither, which is the state item 52 finds `main` in right now. |
| 51 | `examples/todo-app-frontend/src/modules/api/client.ts`, every `examples/todo-app-backend/src/features/*/transport/http/*.controller.ts`, `src/modules/domain/session/session.repository.ts#findActive` | Confirmed exactly, on branch `ex-uat-live` (`03e323a6`): `client.ts`'s `apiRequest` sets `headers.authorization = "Bearer ${options.token}"`; every one of the five task controllers (`create-task`, `complete-task`, `delete-task`, `list-tasks`, `reopen-task`) reads `@Headers('x-session-token')` instead, so `sessionToken` is `undefined` for every real browser call. `SessionRepository.findActive(token)` calls `this.rows.findOneBy({ token })` with no guard against an undefined `token`; TypeORM's `findOneBy` silently drops an `undefined` property from its `where` criteria rather than filtering on it, so the query matches an arbitrary session row instead of refusing. The result, confirmed live by the UAT run's own read-back curl: an unauthenticated `GET /tasks` returns 200 with another person's task list. The run's own `result.md` (`features/task/uat/create/runs/20260918T080854Z-f7ab26f0/result.md`) names this precisely: "session.repository.ts#findActive calls this.rows.findOneBy({ token }) with that undefined token - TypeORM does not filter on an undefined criterion, so it silently matches an arbitrary session row instead of refusing." Every other gate was green: `live-proof.sh` uses `x-session-token` throughout (confirmed, 11 call sites), matching the backend and never exercising the mismatch; the unit specs (`list-tasks.use-case.spec.ts` etc., confirmed) test the `application/*.use-case` layer directly against a faked repository, never touching `@Headers`, `SessionRepository`, or any header at all - so "unit tests used the right header" is not quite literal, they simply never reach the header layer that would be wrong; the architecture checker is a static TypeScript structural check with no header-name vocabulary at all; and `contract.login.identity-for-task` (`state: done`) names only an abstract `surface: actorOf: (request) -> {personId} or null`, no header, no transport detail whatsoever. Only a real-browser `uat.verify` run, reading back the actual response body against the actual signed-in person, caught it. | lied | lane `login-impl` is now a real branch (`ex-login-impl` exists in `git branch -a`) but has zero unique commits over `main` - the defect is unfixed | `contract.login.identity-for-task`'s `surface` needs to name the actual transport mechanism (header name) as data the gate can check both sides against, not just an abstract function shape; `SessionRepository.findActive` needs to refuse an undefined/empty `token` before querying, independent of the header-naming fix; and this is itself the strongest argument in the whole ledger for making `uat.verify` (not unit tests, not `live-proof.sh`, not the architecture checker) a required prover for every `fr` before `done` - exactly the theme items 36 and 43 already raised from the UI side, now confirmed from the security side. |
| 52 | `main`'s current head (verified live, not from a report), `checks/architecture/registration.mjs`/`source-names.mjs`, commit `5b743c12` | Found independently while verifying item 51's branch ancestry: `main` has since merged both `ex-nivo-shape` (`a4e1f6f9`, the reshaped backend from items 47/50) and a follow-up, `5b743c12` "integrate ex-nivo-shape examples-only: restore main's checker and docs (owner ruling)" - a one-line-message commit that reverts `84bc5241`'s checker relaxation (confirmed: `checks/architecture/registration.mjs` and `source-names.mjs` on the current `main` are byte-identical to the pre-fix `9a5ee8d0` versions used in item 50's comparison) while keeping the reshaped example content (confirmed: 39 files under `examples/todo-app-backend/src` still reference the `bussiness` module path). Reproduced live in this session: extracted a fresh scratch copy of the actual current `main` head via `git archive` (no shared worktree touched), installed dependencies, and ran the exact command `.github/workflows/todo-app-example.yml` runs in CI (confirmed at line 96, which fails the job on `ok: false`): `node cli/main.mjs architecture check examples/todo-app-backend --config architecture.json` returns `ok: false`, the identical 41 violations from item 50 (`BE_FEATURE_LAYOUT_INVALID` 28, `BE_MODULE_HANDLER_REGISTRATION` 7, `BE_SOURCE_NAME_INVALID` 6). This is not a held-off fix or a pending lane; it is the present state of `main`'s own checked-in example against `main`'s own checker, right now. | lied / blocked | owner ruling confirmed (`5b743c12`) reverted the checker fix but kept the reshaped example that needs it, and this session could not find any record of the resulting break being tracked or accepted anywhere in this checkout | Either the checker fix needs to go back in (the owner ruling was presumably about scope/timing, not about the fix being wrong - it passed 45/190+ specs clean in item 50's own reproduction), or `examples/todo-app-backend` needs to revert its own layout until the fix lands, because right now neither side of that pairing is true and `main`'s own CI-equivalent command fails on `main`'s own head. |
| 53 | `examples/todo-app-backend/.starciwork/features/login/ui/sign-in/assets/sign-in-refused.prompt.txt` (v2), `ops/interface.draw/operator.yaml` (`grammar` read) | Confirmed verbatim on `main`: the v2 prompt's Grammar-anatomy section says the anatomy was "verified against the real consuming block at examples/todo-app-frontend/src/components/blocks/sign-in-form/component.tsx". Confirmed by history: `component.tsx` was created in `46acf30e` and rewritten to consume real `@starci/grammar` in `12c59eb4`, both ancestors of `b54b1480` (the commit that authored this v2 prompt). The direction was therefore composed by reading the screen it is supposed to direct, after that screen was already built - the exact `IMPL_BEFORE_DIRECTION` inversion item 43 named, now happening a second time inside the very fix (`ex-prompts-v2`) that corrected item 44/46's first instance, and this time closed into a circle: draw was supposed to precede implement, implement happened first, and the redraw then cited the implementation as its own source of truth. The op's own `grammar` read (confirmed, same wording as item 44) asks only to "resolve Grammar from the consuming application... inspect real public exports" - it never asks to verify anatomy against a specific consuming *block*'s already-written JSX, so this cross-check was the prompt author's own addition, not something the op required. | process / lied | open - MASTER-PLAN candidate | A direction that can only match the implementation it was read from proves nothing; the draw prompt's anatomy must derive from the Grammar package's own exports and the `work/ui-screen` record alone, never from a specific consuming component's source, or `interface.draw` needs an explicit rule refusing a prompt whose own text names a consuming block as a verification source. |
| 54 | `examples/todo-app-backend/.starciwork/features/login/ui/sign-in/assets/sign-in-refused.prompt.txt`, `ops/interface.draw/operator.yaml` (`knowledge` read, line 44), `knowledge/ui/**` | Confirmed: the op's `knowledge` read path is literally "knowledge/INDEX.json + applicable UI composition, presentation, proof and family topics" - four named categories. The prompt's "Relevant UI knowledge" section cites exactly one file, `knowledge/ui/proof/brand.yaml` (BRAND-1, BRAND-2, BRAND-3) - proof only. Confirmed `knowledge/ui/composition/*` and `knowledge/ui/presentation/*` both exist with topics directly on point for this exact screen and state: `composition/feedback.yaml` (`ui.composition.feedback`, rule FEEDBACK-1 "Correct at the nearest owner" - literally "when something fails... who says so, and how small is that owner") and `presentation/tone.yaml` (`ui.presentation.tone` - "given a line of copy whose rank is already decided, which public tone prop expresses that rank") were never cited, despite the prompt's own refusal-`Text` line being exactly the case both files exist to govern. The prompt reads as a component inventory plus two hex codes and one contrast rule, with no aesthetic or hierarchy intent anywhere in it. | no-home / process | open | The prompt template needs a required composition/presentation section, sourced from the same `knowledge/ui/**` topics the op's own `knowledge` read already names; `interface.draw` needs to refuse (or at minimum warn on) a prompt whose knowledge citations are proof-only when composition/presentation topics with a matching `appliesTo`/`family` exist and were not cited. |
| 55 | `checks/render.mjs`, `checks/brand.mjs`, `ops/interface.implement/operator.yaml` (capture/render-compare requirements, confirmed at multiple lines including 405-409, 428, 459-479), `scripts/check-example-work.mjs` (concept 11, line ~364) | Confirmed both `checks/render.mjs` (reads an implementation capture's PNG bytes plus its kept markup and checks them against brand tokens pixel-for-pixel) and `checks/brand.mjs` exist in the runtime, but neither is referenced anywhere in `scripts/*.mjs` or `.github/workflows/todo-app-example.yml` - never run against this example. No file anywhere under `examples/` is named or shaped like an implementation capture (`git ls-tree | grep -i capture` is empty). `ops/interface.implement/operator.yaml` already designs for exactly this: "capture every required screen/state/viewport... Open the captures and compare each mapped draw with its corresponding rendered evidence... Complete only when required implementation and rendered proof pass" - the op's own text requires it, but nothing mechanical enforces it on this tree. `scripts/check-example-work.mjs`'s concept 11 (the only capture-adjacent gate rule that exists) checks only that an *existing* implementation asset does not carry ImageGen provenance - it never requires that a capture exist at all before a frontend impl record reaches `done`. (As of the latest `main`, `impl.login.todo-app-frontend.sign-in` itself is honestly `state: todo`, not `done` - the specific record item 43 found is no longer wrongly claiming done, but the missing capture-and-compare gate this row describes is unrelated to that record and still absent generally.) | lied | fixed on branch `starci183/ex-capture` - new `scripts/example-render-proof.mjs` composes the canonical `checks/render.mjs`/`checks/brand.mjs` functions (PNG palette from the capture bytes, primary-presence, card anatomy from the kept markup, mascot slots from the ui record surfaces), and `scripts/check-example-work.mjs` now refuses a `work/implementation` that is `state: done` and frontend (same predicate as `IMPL_BEFORE_DIRECTION`: proves a `work/ui-screen`, or its repository resolves to a `role: fe` workspace entry) unless real captures in the PNG-plus-markup shape exist and every check passes - a `skip` on a core check refuses as `RENDER_PROOF_INCOMPLETE`, an uncheckable claim is not a pass. First honest run against the real `impl.audit.todo-app-frontend.privacy` captures refused `done`: all 12 PNGs fail `palette-off-brand` (lossy-compression chroma smearing baked into the captures, 10-17 dominant off-token colours each) and `entity-list-in-card` cannot resolve card anatomy because the brand identity family `common` carries no DNA snapshot on this host - so the record regressed `done -> todo` behind new `gap.audit.render-proof`, and its `evidence.yaml` was re-bound by `scripts/example-evidence.mjs` with the render-proof command now on record as a failing assertion alongside the previously-recorded architecture failure | A `work/implementation` with a frontend `owners` role needs to require real captures (matching `checks/render.mjs`'s PNG-plus-markup shape) and a passing `render`/`brand` check against the direction and the brand record before `state: done` - the design in `ops/interface.implement/operator.yaml` already says this; it needs a mechanical gate, the same class of fix items 18/31/43/51 already established for other `proves`/`done` edges. |
| 56 | `.github/workflows/todo-app-example.yml` (the `live` job), `examples/todo-app-backend/scripts/live-proof.sh`, `examples/todo-app-backend/src/features/todo/http/`, `examples/todo-app-backend/package.json` | The workflow's `live` job still curls a REST contract that no longer exists: `POST $api/auth/sign-in`, `POST/GET/DELETE $api/tasks[...]`, `x-session-token` headers (confirmed verbatim in the checked-in workflow), and an API-readiness wait against `http://localhost:3001/tasks`. The app today exposes GraphQL at `/graphql` plus `/health` and the sepay webhook only (`src/features/todo/http/**`, `app.module.ts` composition), and `live-proof.sh`'s own header states it was "rewritten for GraphQL (ex-nivo-shape)" because the REST doors are gone. So the checked-in job that exists to "prove the product behavior with curl - not a mock" cannot pass against the app it builds, and the workflow header's claim that the runtime's gates "stay green for the example at all times" is false at three separate points (this job; the architecture check, items 50/52/57; the scoped-lint step guarded off with `if: false`). The task lane's live proofs (22/22) had to be produced by running the script by hand through Git Bash against a dedicated, this-tree-built API instance on a separate port, with the API_URL pin recorded into the evidence. | drift | open (example-CI lane; out of ops/backend.implement's write boundaries from here) | The `live` job's steps need porting to the GraphQL operations `live-proof.sh` already encodes (or the job needs to run that script itself, which is the real, maintained live contract), its readiness probe needs `/health`, and the header's "at all times" either becomes true or the workflow names its known-red jobs the way the scoped-lint step honestly does. |
| 57 | `checks/architecture/**` vs `examples/todo-app-backend` at `starci183/ex-be-task` (post six-lane merge, head `4e58c781` + this lane's work), item 52's "identical 41 violations" claim | Re-ran the exact CI command (`node cli/main.mjs architecture check examples/todo-app-backend --config architecture.json`) on this lane's checkout: `ok: false` with 155 violations before this lane touched any code - `BE_FEATURE_LAYOUT_INVALID` 105, `BE_SOURCE_NAME_INVALID` 20, `BE_MODULE_HANDLER_REGISTRATION` 30 - i.e. 3.8× item 52's number, every merged feature lane adding instances of the same three rule classes its files cannot avoid (each feature's GraphQL resolvers/DTOs; every CQRS handler in every capability module). This lane's own new files (`task-counts` query/handler/resolver/module/DTO) add exactly 4 more of the same classes (measured: 155 → 159; notably zero new `BE_SOURCE_NAME_INVALID`, because the new resolver takes no `@Args` at all). Item 34's own lesson applies to items 50/52: the 41 figure was true when written and is stale now; while the item 52 ruling is unresolved, every lane that merges inflates the backlog it will have to fix. | drift | open - same owner ruling item 52 names; count re-measured live on this lane's head, command above | No new fix beyond item 52's single decision - land the checker relaxation or revert the example's reshape - but the decision's cost is per-lane-merge compounding, and the ledger's numbers need re-running each pass, not inheriting. |
| 58 | `examples/todo-app-backend/.starciwork/features/task/fr/create/index.yaml`, `scripts/check-example-work.mjs`, `scripts/example-verify.mjs`, `examples/todo-app-backend/package.json` | The record is `state: done` with a header comment: "provenBy is written by starci-kernel from requiresProof and the provers' own proves edges. An agent-authored provenBy is refused: it would be this record speaking for another one." Confirmed against the full gate: `provenBy` appears nowhere in `scripts/check-example-work.mjs` (only in derive/critique as an edge *kind*), so nothing refuses it. And the block it carries does not survive its own record's requiresProof: `requiresProof.uat.required: true` while the named prover `uat.task.create` is `state: todo` (blocked by gap.task.live-proof, confirmed); `provenBy.e2e` records `command: npm run test:e2e -- tasks/create, exit: 0, at: 6f1e4b7d` but no `test:e2e` script exists in `package.json` today (only build/start/start:dev/test/test:watch), so nothing in the tooling can replay that claim; and `provenBy.unit` names `impl.task.todo-app-backend.ownership`, which br.task.single-owner's rev-2 withdrawal dragged back to `todo` after the unit claim was recorded - a done `fr` silently resting on a prover that was not done at read time. This is item 28's premature-done shape and item 51's "make uat.verify a required prover" point, now arriving from the `requiresProof`-satisfaction angle: the gate audits evidence digests but never checks a done record against its own `requiresProof`. | lied | open - MASTER-PLAN candidate | The gate needs a rule that a done record's `requiresProof` is satisfied as recorded (e2e commands resolve to real scripts, named uat provers are themselves done), and the "agent-authored provenBy is refused" claim either gets enforced or gets corrected - a comment naming a rule the gate doesn't have is its own small lie (same family as item 18's silent edges). |

| 59 | `examples/todo-app-backend/.starciwork/features/plan/integration/sepay/index.yaml` (`endpoints`, create-intent), `examples/todo-app-backend/src/modules/integrations/sepay/sepay.client.ts`, `scripts/check-example-work.mjs` | `gap.plan.sepay-not-reachable` read "no request has ever reached SePay's sandbox merchant account for real". Requests have now been made from this machine and are recorded as replayable assertions in that record's own new `evidence.yaml` (overall outcome `fail`, each raw exit kept): `my.sepay.vn` answers in 0.4-0.7s; `GET /userapi/transactions/details/1`, the record's own get-transaction path, answers **401** both with and without an `authorization` header, so it is a live route refusing a credential this checkout cannot hold (`.starcistacks/dev/runtime/env/KEYS.md`: every decrypted value is "a placeholder string, never a real credential"); and the record's create-intent path `POST /userapi/transactions/qr` answers **404 text/html**, byte-for-byte what `POST /userapi/transactions/definitely-not-a-route` returns, while a route that does exist answers a wrong-method 404 with a JSON body (`{"status":404,"error":404,"messages":{"error":"Resource Not Found"}}`). `developer.sepay.vn/en/sepay-api/v1/api-giao-dich` (fetched, 200) documents `/userapi/transactions` as list/details/count only. The accepted SDS therefore names an endpoint the provider does not serve: no credential, real or otherwise, completes that leg, and `requiresProof.live` is unachievable exactly as designed. The same probe surfaced a real client defect, now fixed — `response.json()` on the empty-body 404 threw `SyntaxError: Unexpected end of JSON input` before the status could be reported, and a gateway status outside the closed `pending`/`paid`/`failed` vocabulary was cast straight into `ConfirmPaymentHandler`, where anything not `failed` activates a subscription. | lied / no-home | open — measurement, client fix and record updates sit on branch `starci183/ex-be-plan` (unmerged; agents do not push); the SDS half is deliberately untouched, because `ops/backend.implement/operator.yaml` routes an observed SDS contradiction to `implementation.architecture` (`ops/architecture.decide`, separate child worktree), which no lane has run | Nothing anywhere compares an `integration` node's declared external paths against the provider's published API, so an invented endpoint survives authoring, implementation, unit tests and every gate indefinitely — the same "no mechanical check on this claim" shape as items 42/46, one level up and on the external-API side. Either the layout gains a place to record the provider's real surface with a check against it, or a run against the real provider must gate an integration node leaving `todo`. |

## Lied

Items 1, 2, 3, 13, 14, 17, 18, 28, 31, 36, 42, 43, 44, 46, 47, 48, 49, 51, 52, 53, 55, 58, 59. The common shape: a gate
reported clean, or a component silently computed something other than its own stated design, while the tree,
the running product, a generated design artifact, or `main`'s own current head underneath it was false. Eleven
of these (1, 2, 3, 17, 18, 28, 31, 42, 43, 44, 46) are fixed; the rest (13, 14, 36, 47, 48, 49, 51, 52, 53, 55, 58, 59) are open. Item 51 is the most serious finding in this ledger: a real, live authentication bypass, confirmed by

curl, that every gate except a real-browser `uat.verify` run missed. Item 52 is `main`'s own head failing its
own CI-equivalent architecture check right now, confirmed by reproducing the exact command CI runs. Item 53 is
a second instance of item 43's implement-before-direction inversion, this time closed into a circle: the v2
prompt that fixed items 44/46's fake anatomy was itself "verified against the real consuming block" - i.e.
composed by reading the already-implemented screen, so the direction could only ever agree with the code it
was supposedly there to direct. Item 55 is the standing capability gap that let item 53's predecessor (the
fake-anatomy mismatch) go undetected by every mechanical check and require a human watching a UAT video to
catch: `checks/render.mjs`/`checks/brand.mjs` exist and can compare an implementation capture against brand
tokens, but nothing runs them, and no gate requires a capture to exist before a frontend `work/implementation`
reaches `done`. Item 39's `proves`/`willProve` classification gap is still the mechanical reason a
`willProve`-authored record is invisible to `ex-trust`'s own `PROVES_TARGET_NOT_DONE` check. Items 48 and 49 are
`ex-trust`'s own new rules catching two fresh instances of item 28's pattern, left deliberately red.

## Blocked

Items 4, 5, 6, 7, 8, 19, 20, 22, 26, 50, 52. Five are fixed outright (4, 5, 6, 19, 26). Three are explicit
owner-decision questions about checker overreach (7, 8, 20). Items 50 and 52 are two views of the same fact at
two different points in this session: 50 found the nivo-shape checker fix (`84bc5241`) real, tested and held
off `main`; 52 found that `main` has since merged the reshaped example anyway while a follow-up owner-ruling
commit reverted the checker fix back out - so "held off on purpose" became "actively broken" between two
passes of the same ledger. One item (#22) has not been attempted at all.

## No-home

Items 6 (also blocked), 12, 15, 16, 21, 29, 38, 40, 47, 54, 56. Five of eleven (6, 15, 16 as a batch, 29, 38) now have
a landed fix. The remainder: 12's op-vocabulary migration is written but not executed; 21's `codePatterns`
contract is documented but has no generator or smaller default; 40's schema field is still missing; 47 is a
fresh, three-part instance of the same "the checker's rules don't match the house's real conventions" problem
23 already named; 54 is the same shape of gap one level into the draw op itself - `interface.draw`'s own
`knowledge` read asks for composition, presentation, proof and family topics, the prompt template has no
required section for the first two, and nothing refuses a prompt that cites proof rules alone; 56 is the same
shape again on the other side of the boundary - an `integration` node names concrete paths on someone else's
API, and no field, schema or check anywhere holds the provider's real surface to compare them against.

## Drift

Items 9, 10, 11, 23, 32, 34, 35, 39, 41, 45, 56, 57. Three of ten are fixed: 11, 9/10 (the `.stacks`/`.starcistacks`
rename, landed and merged), and 45 (fixed by circumstance - the owner rejected the fake-anatomy images on
content grounds, which incidentally restored the record's internal consistency). Item 23 got worse, not better,
across this session: `ex-nivo-shape` merged into `main`, then a follow-up owner-ruling commit reverted both the
checker relaxation and the doc rewrite while keeping the reshaped example, so the schema, the doc, the checker
and the real example now name three different shapes simultaneously (see item 52). The rest (32, 34, 35, 39,
41) are still open; 34's own point - that a count needs its command and timestamp recorded beside it - is what
let this session catch and correct eleven stale item statuses across five follow-up passes.

## Process

Items 25, 27 (partially), 30, 33, 34, 37, 43 (partially), 53 (partially), 54 (partially). These are about how
lanes and ops work rather than what the runtime enforces: delegation discipline (25), a build-freshness trap
this session hit directly (30), trusting a lane's own completion claim without checking `git log` (33), how a
mid-task message from the lead should be handled (37 - this ledger's own answer, applied seven times in this
session, was to verify each one against `git log`, file contents, and in two cases a live re-run of the actual
command, before recording it), a lane-sequencing error (43's process half, still unverifiable against a
commit), and two instances of an op skipping its own required inputs under time pressure (53: a prompt author
read the implementation instead of the Grammar package; 54: a prompt author cited one knowledge file instead of
searching all four required categories). None of these need a code fix in `.claude`'s checks alone; most need a
standing habit, though 53/54 also name concrete gate rules `interface.draw` itself could enforce.

## What the refactor of `.claude` must do first

1. **Gate the Work graph's structural truth against the actual product, and against itself.** `ex-trust` already landed the core of this - `PROVES_TARGET_NOT_DONE`, `OWNER_PATH_MISSING`, `BLOCKER_CYCLE`, `IMPL_BEFORE_DIRECTION`, `gap.closedBy` as a list. What's left: make `proves` a real, classified edge kind; turn `BLOCKER_UNROOTED` into a refusal; require `stale: true` to carry a real `staleSince`; require a record's own `assets` to agree with its own `gaps`/`blockedBy` prose; check a test name against a record's current `statements`/`withdraws`; check a uat step's own prose against the code it names; and make a *contract*'s own surface name the real transport mechanism, not just an abstract function shape - closes items 32, 35, 36, 39, 41, 45, 51 (contract-surface half), 56 (an `integration` node's declared external endpoints have no recorded provider surface anywhere to be checked against).
2. **Land the nivo-shape checker fix on `main`, for real, now.** `84bc5241` is real and fixture-tested; a follow-up commit kept the reshaped example that needs it while reverting the fix that lets it pass, so `main`'s own head fails its own CI-equivalent check today. Either land the fix or revert the example's reshape - closes items 23, 47, 50, 52, the single highest-value, best-verified fix in this ledger.
3. **Require captures and a passing render/brand check before a frontend implementation reaches `done`, and make `uat.verify` a required prover for every `fr`.** `ops/interface.implement/operator.yaml` already designs for exactly this; `checks/render.mjs`/`checks/brand.mjs` already exist to do the comparing; nothing wires them together or enforces it, and item 55's predecessor (items 44/46) needed a human watching a UAT video to catch what this gate would have caught mechanically - closes items 51 (required-prover half) and 55.
4. **Fix `interface.draw`'s own input discipline**: a direction's anatomy must derive from the Grammar package and the `work/ui-screen` record alone, never from a specific consuming component's already-written source (item 53 - a direction that can only agree with the implementation it was read from proves nothing); and the prompt template needs a required composition/presentation section sourced from the same `knowledge/ui/**` topics the op's own `knowledge` read already names, with the op refusing a proof-only citation when on-point composition/presentation topics exist (item 54) - closes items 53, 54.
5. **Make a real, code-level safety net for the TypeORM class of bug item 51 found**: refuse a `findOneBy`/`findOne` whose `where` object can carry an undefined property without an explicit guard - closes item 51 (code-safety half).
6. **Finish the ops vocabulary migration `schemas/work-layout.yaml`'s own `opVocabularyMigration` already specifies**: retarget the 24 affected `ops/*/operator.yaml`, then bring `core/index.mjs`'s `validateWorkspace` and `kernel/reconciliation.mjs`'s `recordDigests` in line with the layout - closes items 12, 13, 14.
7. **Ship defaults for the design-system and code-pattern tooling, and check identity where it already resolves one.** A generator or smaller default `codePatterns` contract so `next`/`nest` profiles report real findings instead of `unavailable`; `architecture.json`'s `frontend.grammar.package` checked against a registered house design system; every `ui.coverage.map[*].components` string checked against the real export list `ARCH_GRAMMAR_*` already computes - closes items 17, 21, 22, 42, 46.
8. **Rule on the checker-overreach questions together, backfill the two red backlogs `ex-trust` surfaced, and add the missing `repository` field.** Kind inference, `BE_SOURCE_NAME_INVALID`'s forced renames, and the readonly-props contract's rejection of ambient external types are one policy decision; `gap.login.migration-not-applied` needs to revert to `todo`; all 24 `evidence.yaml` files need real replayable commands; `work/business-rule` needs a `repository` field - closes items 7, 8, 20, 40, 48, 49.

Five items (42, 44, 45, 46) turned out already fixed on a branch or in a cleanup commit this ledger had not yet
looked at; six more (9, 10, 11, 18, 29, 31, 38, plus 43) were marked pending in earlier passes and turned out
fixed by `ex-trust`/`ex-stacks-rename`. Item 43's own record (`impl.login.todo-app-frontend.sign-in`) is now
honestly `state: todo` rather than falsely `done` - a genuine, verified improvement found while checking item
55, though the missing capture-and-compare gate item 55 describes is unrelated to that one record and still
absent generally. Item 23 went from "pending" to "in progress" to "actively broken on `main`'s own head" across
three passes of this same session - the same lane's fix landed, then was partially reverted, faster than this
ledger's own passes could keep up without re-checking `git log` each time. That is the lesson this ledger keeps
relearning: a "must do first" list, and every status above it, is only as current as the last `git log` it was
checked against - this document needs to be re-verified against `main`'s current head before anyone acts on
it, not re-read.
