# interface.generate — brief

Follow operator.md; write its declared paths and your branch response/; replace the running response.json before exit. ? optional; * fallback. Context is limited to what request.json names.

## Done when

Done when, under mode apply, the `frontend-source-application` and its `changes` account for one commit on the session branch whose `writes` carry every declared path with both hashes and only values the `inventory` frozen beside the `frontend-presentation-resolution` and `resolved-tree` publishes, with a clean sweep and the committed tree equal to the resolved one, that tree realizing the direction the `frontend-direction-decision` selected over `candidates` served on `host`, printed, falsified and scored, with the surface class in the `ui-coverage` and the selected rendered PNG recorded as `selected-candidate-capture` and `outcome.primary`; or, under mode dry, the same with a null commit and an untouched checkout; or, when the direction declared a presentation delta of none, the same selected render evidence beside an empty owner map, no chosen rule and an empty inventory.

Primary output: `frontend-source-application`

## Inputs

`frontend-art-direction`?, `business-promise-authority`?, `backend-source-application`?, `architecture-decision`?, `frontend-direction-decision`?, `frontend-surface-audit`?, `library-source-application`?, `units`?, `landing-composition`?, `findings`?, `knowledge-repair-receipt`?

## Outputs

Before write/resume, read ../../templates/kinds/<kind>: md .contract.json + .skeleton.md; data .schema.json; artifact follows operator.md. Current-status declared kinds; evidence must exist. Paths: response/.

`art-direction-adoption` `data/art-direction-adoption.json`? (data)
`frontend-direction-decision` `direction.md` (md)
`ui-coverage` `data/coverage.json` (data)
`candidates` `artifacts/<candidateId>.html`? (artifact)
`selected-candidate-capture` `artifacts/<candidateId>.<viewport>.png` (artifact)
`direction-image` `artifacts/images/<slot>.png`? (artifact)
`host` `artifacts/host.json`? (artifact)
`knowledge-question` `data/knowledge-question.json`? (data)
`knowledge-coverage` `data/knowledge-coverage.json`? (data)
`family-understanding` `data/family-understanding.json`? (data)
`frontend-presentation-resolution` `resolution.md` (md)
`inventory` `data/inventory.json` (data)
`resolved-tree` `artifacts/<target>.resolved.tsx` (artifact)
`frontend-source-application` `response.md` (md)
`changes` `changes.md` (md)
`writes` `data/writes.json` (data)

## Stops

`INVALID_INPUT`, `SESSION_MISSING`, `ROUTE_UNVERIFIED`, `SOURCE_DRIFT`, `SCOPE_UNFROZEN`, `CHANGE_LEVEL_AMBIGUOUS`, `OWNER_CEILING_INVALID`, `BUSINESS_REQUIRED`, `BACKEND_REQUIRED`, `ARCHITECTURE_REQUIRED`, `GRAMMAR_REQUIRED`, `EVIDENCE_MISSING`, `REFERENCE_EVIDENCE_EXHAUSTED`, `REFERENCE_MISSING`, `NO_VIABLE_DIRECTION`, `DIRECTION_CHOICE_REQUIRED`*, `KNOWLEDGE_UNBOUND`, `GRAMMAR_UNPUBLISHED`, `OWNER_CONFLICT`, `RULE_MISSING`, `UNKNOWN_RULE`, `RESOLUTION_STALE`, `WRITE_REJECTED`, `NO_PROGRESS`, `KNOWLEDGE_QUESTION`
