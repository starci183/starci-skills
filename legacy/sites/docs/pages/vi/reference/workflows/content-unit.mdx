---
title: "content-unit"
description: "Một đơn vị giáo trình cần brief, các ấn bản ngôn ngữ, track code và một lượt review độc lập; đưa lên MinIO vẫn là việc của người."
---

import { Callout } from 'nextra/components'

# content-unit
<Callout type="info">
Generated by `docs/scripts/generate-docs.mjs` from [`workflows/content-unit.json`](https://github.com/starci183/starci-skills/blob/main/workflows/content-unit.json). Edit the source, not this page.
</Callout>

## Khi nào

> Một đơn vị giáo trình cần brief, các ấn bản ngôn ngữ, track code và một lượt review độc lập; đưa lên MinIO vẫn là việc của người.

## Chuỗi

| Bước | Nhánh | Operator | Preset | Fan-out | Song song tối đa |
| --- | --- | --- | --- | --- | --- |
| 1 | 1/1 | [`content.generate`](/reference/operators/content-generate) | `maxReviewRounds` = `2` | — | — |

## Vòng lặp

Chuỗi này không có vòng lặp.

## Kết thúc ở

`user`
