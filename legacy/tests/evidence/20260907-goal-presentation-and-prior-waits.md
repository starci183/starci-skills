# Goal presentation and corrected-mission admission evidence

This candidate is based on skills commit 5f874f80bac7617c19de75173f951e5e1c80ec8a.

The existing goal preview now renders evidence-labelled architecture when supplied by discovery. Its scope hash includes that graph; absent topology remains unresolved. The isolated patch passed eleven focused checks including actual English and Vietnamese CLI previews, schema parity and preserved historical scope. Two actual Mermaid parser/browser renders passed, including hostile labels and Unicode. This is runtime presentation proof, not product UI UAT.

A real corrected Chatbot mission retained three earlier waiting review parents. Current re-entry validation ran before classifying their earlier mission and incorrectly returned PLAN_BUSY. The repair moves the existing exact sealed-prior-wait predicate into one shared home. Earlier waits must still pass request, expected, identity, coordinate, evidence, containment and time checks. They are retired history, never completed current work. Current running attempts, waiting obligations, worker slots and leases still block.

Nine focused wait/correction checks passed. A separate public lifecycle produced three returned parent reviews followed by a fresh accepted KEEP, corrected and confirmed the mission, then previewed and committed its new forecast. It passed with zero current goal credit from historical work, unchanged parent evidence and refusal of a tampered middle receipt. A read-only native probe classified the three old waits without changing seven checked files.

The combined release still requires its own full suite and package/install readback. These checks do not establish product API correctness, durable chat recovery, accounting correctness or desktop/mobile UAT.
