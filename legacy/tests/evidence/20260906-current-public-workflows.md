# Current public workflows — 2026-09-06

A public frontend journey could not plan browser UAT without inventing identity, seed and backend work. A separate frontend reconciliation could not consume its actual interface producer. The operator tables now declare conditional requirements; planner and admission gates read the same conditions. Public UAT retains source/runtime admissions, declarative browser evidence, every case and all three lanes. Authenticated and seeded defaults retain their prerequisites.

The user required fresh execution and UAT data instead of compatibility acceptance. Active execution refuses old revisions, upgrade metadata and retired/relocated roots. The migration execution adapter and legacy output-root/mission acceptance are removed; historical records are not rewritten. The current starci/v2.2 wire identifier remains.

Fixture teardown now restores only the exact host declarations it owns. Regression tests cover failure cleanup, unrelated declarations, conditional nested workspace roles, anonymous/no-fixture evidence, frontend producer ancestry, and fresh reset after refusal.

Validation evidence is retained outside the shipped tree with the final release manifest. The combined pre-merge candidate passed 392 script tests with zero skips and 76 generated documents. Final merged release validation and clean packed installation are recorded separately; these are runtime regressions, not completed product UAT.
