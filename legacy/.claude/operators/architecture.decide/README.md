# architecture.decide

Select one source-grounded architecture, close store ownership and operation contracts, verify retained components, and answer an independent critique. A done response contains `architecture-decision`, `current-state`, `stack-model`, and `independent-critique` artifacts. The operator changes no product source and publishes no decision until every feedback criterion passes.

Run the repository operator build, then `python tests/test_contract.py` from this directory.
