{
  "limits": { "feedbackRounds": 2 },
  "history": "Retain each missing journey, invalid entry, case defect, shared prerequisite or tier mismatch with the changed flow and reassessment. Do not rerun an unchanged plan.",
  "items": [
    { "id": "request-valid", "expect": "The request schema passes.", "check": { "kind": "machine", "ref": "request-shape" }, "onFail": { "action": "ask", "fromStep": "bind-journeys", "instruction": "Ask for the malformed request field." } },
    { "id": "files-valid", "expect": "Declared plan files exist and match digests.", "check": { "kind": "machine", "ref": "request-files" }, "onFail": { "action": "ask", "fromStep": "bind-journeys", "instruction": "Ask for the missing or changed input." } },
    { "id": "journeys-complete", "expect": "Every goal journey maps to exactly one flow and entry.", "check": { "kind": "review", "ref": "journey-coverage" }, "onFail": { "action": "retry", "fromStep": "compose-flows", "instruction": "Add the missing flow or correct the invalid entry, then reassess all journeys." } },
    { "id": "cases-executable", "expect": "Every case is bounded and observably executable.", "check": { "kind": "review", "ref": "case-executability" }, "onFail": { "action": "retry", "fromStep": "compose-flows", "instruction": "Repair the named action, assertion or step-budget defect." } },
    { "id": "prerequisites-isolated", "expect": "Account aliases and fixture namespaces are isolated and policy-consistent.", "check": { "kind": "review", "ref": "prerequisite-isolation" }, "onFail": { "action": "retry", "fromStep": "compose-flows", "instruction": "Repair only the colliding or policy-invalid prerequisite reference." } },
    { "id": "units-consistent", "expect": "Flow units and tiers exactly match the plan.", "check": { "kind": "review", "ref": "units-and-tiers" }, "onFail": { "action": "retry", "fromStep": "compose-flows", "instruction": "Correct the named unit, goal, tier or unchecked-coverage disposition." } },
    { "id": "response-valid", "expect": "The response schema passes.", "check": { "kind": "machine", "ref": "response-shape" }, "onFail": { "action": "retry", "fromStep": "emit-uat-plan", "instruction": "Repair only the response shape." } },
    { "id": "response-bound", "expect": "The response binds the request revision.", "check": { "kind": "machine", "ref": "request-binding" }, "onFail": { "action": "retry", "fromStep": "emit-uat-plan", "instruction": "Correct the request binding." } },
    { "id": "outputs-unique", "expect": "Output ids are unique.", "check": { "kind": "machine", "ref": "unique-outputs" }, "onFail": { "action": "retry", "fromStep": "emit-uat-plan", "instruction": "Remove the duplicate output id." } },
    { "id": "artifacts-valid", "expect": "Output artifacts are scoped and digest-bound.", "check": { "kind": "machine", "ref": "output-files" }, "onFail": { "action": "retry", "fromStep": "emit-uat-plan", "instruction": "Repair the invalid artifact record." } },
    { "id": "observations-valid", "expect": "Observations bind criteria and evidence.", "check": { "kind": "machine", "ref": "observation-binding" }, "onFail": { "action": "retry", "fromStep": "emit-uat-plan", "instruction": "Correct the invalid observation." } },
    { "id": "journey-input-present", "expect": "Goal, feature, environment and entry evidence are available.", "check": { "kind": "event", "ref": "essential-input-missing" }, "onFail": { "action": "ask", "fromStep": "bind-journeys", "instruction": "Ask for the exact missing journey or entry evidence." } },
    { "id": "capabilities-present", "expect": "Required file inspection capabilities are available.", "check": { "kind": "event", "ref": "capability-unavailable" }, "onFail": { "action": "block", "fromStep": "bind-journeys", "instruction": "Name the unavailable capability and planning check it prevents." } },
    { "id": "transient-complete", "expect": "Transient reads and writes complete.", "check": { "kind": "event", "ref": "transient-execution-failure" }, "onFail": { "action": "retry", "fromStep": "inspect-existing-flows", "instruction": "Retry only the interrupted read or artifact write once." } }
  ],
  "stopWhen": "Return done only when every criterion passes. After two failed feedback rounds, return mismatch with concrete unresolved journeys or case defects and no claim that a flow was walked."
}
