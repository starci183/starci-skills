# workspace.bind

Bind an exact declared route to its hydrated checkout, source head and derived mutation readiness. Similar names and runtime endpoints are never authority. This operator observes Git and installed-tree state but repairs nothing.

Run the repository operator build, then `python tests/test_contract.py` from this directory.
