{
  "schemaVersion": 1,
  "id": "data.plan",
  "purpose": "Plan one isolated seed unit per required flow or data family, with explicit stores, attribution, volume, expected read-back and rollback.",
  "scope": "Read-only planning over goals, UAT/surface inputs and backend data declarations. Produces a seed plan and units; it does not place or delete data, change schemas, access stores, provision accounts, or carry credentials.",
  "modules": { "request": "request.json", "response": "response.json", "validate": "validate.json", "step": "step.json", "criteria": "criteria.json" }
}
