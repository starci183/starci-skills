{
  "steps": [
    { "id": "bind-promise-identity", "instruction": "Read the request, business authority, surface map, canonical identity assets and exact family knowledge. Use business authority before prompt prose. Ask for a missing promise or canonical identity instead of inventing either.",
      "reads": [{ "alias": "@workspaces/requests", "path": "<requestId>.json", "when": "always" }, { "alias": "@workspaces/inputs", "path": "<declared-input-path>", "when": "for each bound authority, plan or identity asset" }, { "alias": "@workspaces/context", "path": "<declared-context-path>", "when": "for each supporting reference" }],
      "writes": [], "calls": [{ "alias": "@tools/fileread", "purpose": "Read the request, authority and exact identity evidence.", "when": "always" }, { "alias": "@tools/imageview", "purpose": "Inspect canonical visual assets and image references.", "when": "when image files are bound" }], "checks": ["request-shape", "request-files", "promise-and-identity"], "produces": ["Bound promise and canonical identity"] },
    { "id": "assign-composition", "instruction": "Read exact Grammar, family, composition and presentation references. Assign each planned element to one Grammar relationship or a custom boundary, then order sections so each creates a distinct visual event advancing the promise.",
      "reads": [{ "alias": "@workspaces/grammar", "path": "<declared-grammar-path>", "when": "for each bound Grammar relationship" }, { "alias": "@workspaces/knowledge", "path": "<declared-knowledge-path>", "when": "for each bound family, composition or presentation reference" }],
      "writes": [], "calls": [{ "alias": "@tools/fileread", "purpose": "Read the exact design authority used by the composition.", "when": "always" }], "checks": ["element-ownership"], "produces": ["Owned section storyboard"] },
    { "id": "specify-assets-motion", "instruction": "Choose one medium for every asset slot and write an identity-preserving brief. Specify motion level, static reduced-motion state, performance budget and audit evidence for every section. Use web search only to resolve one named public referent.",
      "reads": [{ "alias": "@workspaces/context", "path": "<declared-reference-path>", "when": "when a visual or product reference is declared" }, { "alias": "@workspaces/knowledge", "path": "ui/proof/<declared-reference-path>", "when": "when proof guidance is bound" }],
      "writes": [{ "alias": "@workspaces/artifacts", "path": "<requestId>/<attemptId>/landing-composition.md", "when": "after the contract is complete" }],
      "calls": [{ "alias": "@tools/websearch", "purpose": "Resolve one named public referent without broadening product claims.", "when": "only when supplied evidence cannot identify it" }, { "alias": "@tools/sourcewrite", "purpose": "Persist the complete landing composition.", "when": "after all sections are specified" }],
      "checks": ["story-and-assets", "motion-and-proof"], "produces": ["landing-composition"] },
    { "id": "emit-composition", "instruction": "Write and validate the response after all criteria pass. Hand source work only to interface.generate and evidence work only to interface.audit; claim no rendered result.",
      "reads": [{ "alias": "@workspaces/artifacts", "path": "<requestId>/<attemptId>/landing-composition.md", "when": "when the composition passes" }],
      "writes": [{ "alias": "@workspaces/responses", "path": "<responseId>.json", "when": "always" }],
      "calls": [{ "alias": "@tools/sourcewrite", "purpose": "Write the response record.", "when": "always" }, { "alias": "@cli/python", "purpose": "Run scripts/validate_operator.py for this bundle and records.", "when": "always" }, { "alias": "@tools/print", "purpose": "Present the accepted composition.", "when": "after validation passes" }],
      "checks": ["response-shape", "request-binding", "unique-outputs", "output-files", "observation-binding"], "produces": ["Response record"] }
  ]
}
