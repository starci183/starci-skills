{
  "limits": { "feedbackRounds": 2 },
  "history": "Retain the exact promise, identity, ownership, storyboard, medium, motion or evidence defect with the changed section and reassessment. Never regenerate an unchanged contract.",
  "items": [
    { "id": "request-valid", "expect": "The request schema passes.", "check": { "kind": "machine", "ref": "request-shape" }, "onFail": { "action": "ask", "fromStep": "bind-promise-identity", "instruction": "Ask for the malformed request field." } },
    { "id": "files-valid", "expect": "Declared identity and context files match digests.", "check": { "kind": "machine", "ref": "request-files" }, "onFail": { "action": "ask", "fromStep": "bind-promise-identity", "instruction": "Ask for the missing or changed evidence file." } },
    { "id": "authority-grounded", "expect": "The promise and canonical identity are evidence-grounded.", "check": { "kind": "review", "ref": "promise-and-identity" }, "onFail": { "action": "ask", "fromStep": "bind-promise-identity", "instruction": "Name the unsupported promise or identity gap and request authority." } },
    { "id": "ownership-complete", "expect": "Every element has one explicit Grammar or custom owner.", "check": { "kind": "review", "ref": "element-ownership" }, "onFail": { "action": "retry", "fromStep": "assign-composition", "instruction": "Repair the named ownership gap without redefining shared Grammar." } },
    { "id": "story-assets-complete", "expect": "Every section advances the promise and every asset slot has one feasible medium.", "check": { "kind": "review", "ref": "story-and-assets" }, "onFail": { "action": "retry", "fromStep": "specify-assets-motion", "instruction": "Repair the weak section or ambiguous asset slot and reassess the full sequence." } },
    { "id": "motion-proof-complete", "expect": "Motion, reduced-motion, performance and audit contracts are complete.", "check": { "kind": "review", "ref": "motion-and-proof" }, "onFail": { "action": "retry", "fromStep": "specify-assets-motion", "instruction": "Repair the named motion or evidence contract; do not render it here." } },
    { "id": "response-valid", "expect": "The response schema passes.", "check": { "kind": "machine", "ref": "response-shape" }, "onFail": { "action": "retry", "fromStep": "emit-composition", "instruction": "Repair only the response shape." } },
    { "id": "response-bound", "expect": "The response binds the request revision.", "check": { "kind": "machine", "ref": "request-binding" }, "onFail": { "action": "retry", "fromStep": "emit-composition", "instruction": "Correct the request binding." } },
    { "id": "outputs-unique", "expect": "Output ids are unique.", "check": { "kind": "machine", "ref": "unique-outputs" }, "onFail": { "action": "retry", "fromStep": "emit-composition", "instruction": "Remove the duplicate output id." } },
    { "id": "artifacts-valid", "expect": "Output artifacts are scoped and digest-bound.", "check": { "kind": "machine", "ref": "output-files" }, "onFail": { "action": "retry", "fromStep": "emit-composition", "instruction": "Repair the invalid artifact record." } },
    { "id": "observations-valid", "expect": "Observations bind criteria and evidence.", "check": { "kind": "machine", "ref": "observation-binding" }, "onFail": { "action": "retry", "fromStep": "emit-composition", "instruction": "Correct the invalid observation." } },
    { "id": "essentials-present", "expect": "Surface, promise and canonical identity are available.", "check": { "kind": "event", "ref": "essential-input-missing" }, "onFail": { "action": "ask", "fromStep": "bind-promise-identity", "instruction": "Ask for the exact missing composition authority." } },
    { "id": "capabilities-present", "expect": "Required file or image inspection capabilities are available.", "check": { "kind": "event", "ref": "capability-unavailable" }, "onFail": { "action": "block", "fromStep": "bind-promise-identity", "instruction": "Name the unavailable capability and blocked evidence check." } },
    { "id": "transient-complete", "expect": "Transient reads and writes complete.", "check": { "kind": "event", "ref": "transient-execution-failure" }, "onFail": { "action": "retry", "fromStep": "assign-composition", "instruction": "Retry only the interrupted read or artifact write once." } }
  ],
  "stopWhen": "Return done only when every criterion passes. After two failed feedback rounds, return mismatch or blocked with the remaining concrete composition defects; produce no source or media."
}
