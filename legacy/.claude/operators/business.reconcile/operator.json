{
  "schemaVersion": 1,
  "id": "business.reconcile",
  "purpose": "Compare delivered backend or frontend source with every row of a published business coverage matrix and republish the head only when the reconciliation supports the requested state.",
  "scope": "Reads one delivered source receipt, bound source, verification evidence and published authority; writes reconciliation artifacts and one content-addressed feature head. It does not change the promise or matrix, modify product source, deploy, or claim quality or UAT beyond supplied evidence.",
  "modules": { "request": "request.json", "response": "response.json", "validate": "validate.json", "step": "step.json", "criteria": "criteria.json" }
}
