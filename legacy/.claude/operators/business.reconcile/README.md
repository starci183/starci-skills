# business.reconcile

Compare delivered source with every row of the frozen business coverage matrix. A discrepancy returns the complete reconciliation and claims without publishing a model. A clean reconciliation may republish an existing head as `implemented` or `in-progress` through a legal, content-addressed transition.

Run the repository operator build, then `python tests/test_contract.py` from this directory.
