# interface.generate

Builds one frontend surface from supplied product authority and the bound UI system, then renders and
inspects the real result before one local commit. Existing surfaces require the selected visual
baseline. Rejected images are discarded and concrete feedback drives a bounded targeted repair.
