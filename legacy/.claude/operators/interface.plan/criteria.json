{
  "limits": { "feedbackRounds": 2 },
  "history": "Retain each omitted or duplicated surface, shell conflict, tier mismatch or data-contract defect with the changed rows and reassessment. Never rerun unchanged mapping.",
  "items": [
    { "id": "request-valid", "expect": "The request schema passes.", "check": { "kind": "machine", "ref": "request-shape" }, "onFail": { "action": "ask", "fromStep": "bind-surface-scope", "instruction": "Ask for the malformed request field." } },
    { "id": "files-valid", "expect": "Declared reference files exist and match digests.", "check": { "kind": "machine", "ref": "request-files" }, "onFail": { "action": "ask", "fromStep": "bind-surface-scope", "instruction": "Ask for the missing or changed reference." } },
    { "id": "all-surfaces-mapped", "expect": "Every referenced or observed surface appears exactly once.", "check": { "kind": "review", "ref": "surface-coverage" }, "onFail": { "action": "retry", "fromStep": "map-surfaces", "instruction": "Add the omitted surface or merge the named duplicate, then reassess the inventory." } },
    { "id": "one-shell", "expect": "One shared shell is chosen from bound design authority.", "check": { "kind": "review", "ref": "shell-once" }, "onFail": { "action": "retry", "fromStep": "map-surfaces", "instruction": "Resolve the named shell conflict without designing unit-level presentation." } },
    { "id": "units-consistent", "expect": "Map and unit list agree on ids, goals, tiers and dependencies.", "check": { "kind": "review", "ref": "units-match-map" }, "onFail": { "action": "retry", "fromStep": "map-surfaces", "instruction": "Correct the named row or unit and recheck the one-to-one mapping." } },
    { "id": "data-contracts-grounded", "expect": "Every surface has an evidence-grounded read/write contract.", "check": { "kind": "review", "ref": "data-contracts" }, "onFail": { "action": "retry", "fromStep": "map-surfaces", "instruction": "Repair the unsupported or missing surface data contract from bound authority." } },
    { "id": "response-valid", "expect": "The response schema passes.", "check": { "kind": "machine", "ref": "response-shape" }, "onFail": { "action": "retry", "fromStep": "emit-surface-map", "instruction": "Repair only the response shape." } },
    { "id": "response-bound", "expect": "The response binds the request revision.", "check": { "kind": "machine", "ref": "request-binding" }, "onFail": { "action": "retry", "fromStep": "emit-surface-map", "instruction": "Correct the request binding." } },
    { "id": "outputs-unique", "expect": "Output ids are unique.", "check": { "kind": "machine", "ref": "unique-outputs" }, "onFail": { "action": "retry", "fromStep": "emit-surface-map", "instruction": "Remove the duplicate output id." } },
    { "id": "artifacts-valid", "expect": "Output artifacts are scoped and digest-bound.", "check": { "kind": "machine", "ref": "output-files" }, "onFail": { "action": "retry", "fromStep": "emit-surface-map", "instruction": "Repair the invalid artifact record." } },
    { "id": "observations-valid", "expect": "Observations bind criteria and evidence.", "check": { "kind": "machine", "ref": "observation-binding" }, "onFail": { "action": "retry", "fromStep": "emit-surface-map", "instruction": "Correct the invalid observation." } },
    { "id": "scope-present", "expect": "Feature, surface reference and required design authority are available.", "check": { "kind": "event", "ref": "essential-input-missing" }, "onFail": { "action": "ask", "fromStep": "bind-surface-scope", "instruction": "Ask for the exact missing feature or reference input." } },
    { "id": "capabilities-present", "expect": "Required file or image inspection capabilities are available.", "check": { "kind": "event", "ref": "capability-unavailable" }, "onFail": { "action": "block", "fromStep": "bind-surface-scope", "instruction": "Name the unavailable capability and surface evidence it prevents." } },
    { "id": "transient-complete", "expect": "Transient reads and writes complete.", "check": { "kind": "event", "ref": "transient-execution-failure" }, "onFail": { "action": "retry", "fromStep": "observe-frontend", "instruction": "Retry only the interrupted read or artifact write once." } }
  ],
  "stopWhen": "Return done only when every criterion passes. After two failed feedback rounds, return mismatch with the concrete remaining map defects and no claim that omitted surfaces are covered."
}
