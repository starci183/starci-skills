# interface.plan

Map every page, modal and drawer once, choose a shared shell once, and emit matching surface units with goals, tiers, dependencies and data contracts. This operator plans boundaries; it does not draw or implement them.

Run the repository operator build, then `python tests/test_contract.py` from this directory.
