{
  "machine": [
    { "id": "request-shape", "check": "request-schema", "phase": "request" }, { "id": "request-files", "check": "input-files", "phase": "request" },
    { "id": "response-shape", "check": "response-schema", "phase": "response" }, { "id": "request-binding", "check": "request-binding", "phase": "response" },
    { "id": "unique-outputs", "check": "unique-output-ids", "phase": "response" }, { "id": "output-files", "check": "output-files", "phase": "response" },
    { "id": "observation-binding", "check": "observation-references", "phase": "response" }
  ],
  "review": [
    { "id": "surface-coverage", "instruction": "Verify every route, page, modal and drawer shown by the supplied reference or observed source appears exactly once. No required surface may be silently deferred." },
    { "id": "shell-once", "instruction": "Verify the shared shell and navigation are decided once from the bound Grammar and family evidence; individual units do not invent separate shells, composition, copy or presentation values." },
    { "id": "units-match-map", "instruction": "Verify every map row has exactly one unit with the same id, goal and tier; every dependency resolves; journey tiers follow mission paths; secondary tiers have a concrete reason." },
    { "id": "data-contracts", "instruction": "Verify every surface states what it reads and writes, grounded in the business promise when bound, without inventing business behavior." }
  ],
  "failurePolicy": "Record the exact omitted surface, duplicate mapping, shell conflict, tier mismatch or unsupported data contract. Revise only affected map rows and reassess full coverage; never design or implement an individual unit here."
}
