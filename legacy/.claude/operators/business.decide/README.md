# business.decide

Turn a confirmed promise into source-bound claims, a complete enforcement coverage matrix and a legal content-addressed business head. Mandatory dimensions and discovered consumers cannot disappear. `implemented` is reserved for reconciliation against delivered source.

Run the repository operator build, then `python tests/test_contract.py` from this directory.
