# s7 — migration: `_local` + old journal → ledger (§10)

## Files changed

- `scripts/ledger-migrate.mjs` (new) — `migrateLedger({repoRoot,journalFile,machineFile,dryRun,archive,now,pidAliveFn})`
  per §10, exported and runnable as `node scripts/ledger-migrate.mjs --repo <root> [--journal-file <f>]
  [--machine-file <f>] [--dry-run] [--archive true]`. Per workflow directory under
  `_local/workflows/<id>/`: folds `goal.md`+`goal.json` → `goals` rev, `events.g*.jsonl` then `events.jsonl`
  → `events` (digest-chained, original `seq` kept in `payload_json._seq`), `state.json` → the newest
  `state_snapshots` checkpoint (`state.engine.journalFile` rewritten to `ledgerFile`+`machineFile`),
  `reports/contracts/checks/inbox` → their tables, plus the retired journal's `jobs/leases/events/
  state_snapshots/incidents/budget_reservations` for that workflow id (`ai/*` leases mirrored to the machine
  DB, `machine_ref` set on the paired ledger lease). Idempotent via one `migrations` row per source path
  (`_local/workflows/<id>` and `<journalFile>#<id>`); a second run skips recorded sources. Refuses
  (writes nothing for that workflow) on a live `kernel.lock` pid or an old-journal unsettled job behind
  `state.json`'s generation (`kernel-reconcile-required`). `--archive true` moves the directory to
  `_local/workflows-archive/<id>.migrated`; nothing is ever deleted.
  **This session's addition**: picked up the §4 `inputs` table contract addendum (merged from
  `s0-ledger-db`, itself picked up from `main`) — `goal.mjs`'s `stageExternalInputs` copies owner files
  under `_local/inputs/<id>/<n>-<basename>` before this refactor, and those bytes now import into the
  ledger `inputs` table via `ledger.inputs.put`, read off the `ref`/`sourceRef` entries declared in
  `state.json`/`goal.json` (deduped across the two, since both carry the same array), keyed by
  `goal.revision`. A declared ref whose file the directory no longer holds is reported per-workflow as
  `inputsLost:[{key,reason:'input-file-missing'}]` — a loss, not a refusal, per docs/ledger-db.md:161-162.
  The import is recorded in `migrations` (`kind:'workflow-inputs'`) for idempotency/audit, and
  `workflows.updated_at` is restored after `inputs.put`'s internal `ensureWorkflow` call touches it (it
  writes its own `now()`, which would otherwise clobber the value computed from the workflow's real event
  history). `imported` summary counts gained an `inputs` field (count of bytes imported); this is additive,
  nothing else in the repo parses the summary JSON's shape strictly.
- `kernel/journal.mjs` — unchanged except a top-of-file comment marking it retired-but-kept as the legacy
  reader `ledger-migrate` and the historical specs depend on; no new writers were added, nothing deleted
  that the migrator or an old test needs.
- `tests/ledger-migrate.spec.mjs` (new, 8 tests) — fixture builder for a realistic `_local/workflows/<id>/`
  (goal, state with `engine.journalFile`, two event segments, one report/contract/check/inbox item) plus an
  old-schema journal built with `openJournal` (settled + leased jobs, a repo-fenced lease, one `ai/openai`
  lease, an old snapshot). Covers: full import counts and content (workflow row, events in order with the
  original seq preserved, `verifyChain` ok, the `ai/*` lease mirrored to the machine DB with its token, the
  imported state's `journalFile`→`ledgerFile`+`machineFile` rewrite); a second run is a no-op; a live kernel
  lock refuses and writes nothing; an unsettled journal job behind the file-state generation refuses as
  `kernel-reconcile-required`; a dead lock does not refuse and `--dry-run` writes nothing at all;
  `--archive` moves the directory. **This session's addition**: owner-input import (declared-but-missing
  file is `inputsLost` not a refusal; a file declared on both `state.json` and `goal.json` imports once;
  `origin`/`sha256`/`size`/`goal_revision` land correctly; importing inputs does not disturb
  `workflows.updated_at`); a second run of an inputs import is a no-op.
- `tests/journal-retention.spec.mjs` — untouched this stream (already passing; owned per the file-ownership
  list as `tests/journal*.spec.mjs`).

## Verified

- `node --test tests/ledger-migrate.spec.mjs` → 8 pass / 0 fail.
- `node --test tests/journal-retention.spec.mjs` → 9 pass / 0 fail.
- Full `npm test` (after merging the latest `starci183/s0-ledger-db`) → **2197 tests, 2182 pass, 9 fail, 6
  skip** (baseline 2166 pass / 0 fail / 6 skip + this branch's 31 new tests, all passing). All 9 failures are
  outside this stream's owned files — see "Failures believed to be another stream's" below.
- `grep -rn "_local" kernel/ cli/ bin/ hosts/` still returns many hits outside `ledger-migrate`/
  `workflow-export`/docs — expected until every stream lands; `kernel/journal.mjs` itself has none (it never
  read/wrote `_local`, only the old `journal.sqlite`).
- Real-data dry-run (`node scripts/ledger-migrate.mjs --repo D:/Repositories/nivo-backend --dry-run`, no
  writes):
  ```json
  {"ok":false,
   "workflows":[
     {"id":"_archive-r15","imported":{"events":0,"snapshots":0,"jobs":0,"leases":0,"reports":0,"contracts":0,"checks":0,"inbox":0,"goals":0,"inputs":0}},
     {"id":"agentos-admin-fe","imported":{"events":0,"snapshots":0,"jobs":0,"leases":0,"reports":0,"contracts":0,"checks":0,"inbox":0,"goals":0,"inputs":0}},
     {"id":"agentos-alpha-20260915-r4","imported":{"events":5383,"snapshots":2,"jobs":0,"leases":0,"reports":10,"contracts":3,"checks":2,"inbox":0,"goals":1,"inputs":0}},
     {"id":"nivo-stacks-reinstall-20260915-r6","imported":{"events":17561,"snapshots":3,"jobs":2,"leases":2,"reports":8,"contracts":1,"checks":1,"inbox":0,"goals":1,"inputs":0},
      "inputsLost":[{"key":"1-observer-inventory.json","reason":"input-file-missing"},{"key":"2-nivo-stacks-review-20260915.md","reason":"input-file-missing"}]}
   ],
   "skipped":[],
   "refused":[
     {"id":"agentos-business-design-20260915-r4","reason":"kernel-reconcile-required"},
     {"id":"nivo-be-architecture-refactor-20260916","reason":"kernel-lock-held"},
     {"id":"nivo-fe-architecture-refactor-20260916","reason":"kernel-reconcile-required"}
   ]}
  ```
  `ok:false` here is expected/correct (three of the seven workflow dirs fail closed exactly as designed —
  this is what a dry-run is for); the lead should re-run without `--dry-run` once those three are resolved
  (two look like genuinely in-flight kernel work, one has a live lock). The `inputsLost` pair on
  `nivo-stacks-reinstall-20260915-r6` is real: this repo's owner-input directory really is missing two files
  its `state.json`/`goal.json` declare, and the addendum's design (import what exists, report the rest as
  lost, never refuse the whole workflow for it) is exactly what caught it.

## Requests to other streams

- None outside the docs addendum s0 already picked up. My owned files needed no change from any other
  stream's files this session.

## Open questions

- The §10 prompt text (line 26 of the brief) enumerates the `imported` summary fields without `inputs`; the
  keystone doc (`docs/ledger-db.md:161-162`, a later addendum merged from `main` via `s0-ledger-db`) requires
  importing the `_local/inputs/<id>/` directory and recording losses. I added `inputs` (a count) and
  `inputsLost` (an array, only present when non-empty) to the workflow summary entry rather than inventing a
  different shape — additive, and nothing else in the repo parses this JSON strictly. If the lead wants a
  different field name/shape, it's isolated to the `imported`/`inputsLost` construction at the end of the
  per-workflow loop in `migrateLedger`.
- `ensureWorkflow` (kernel/ledger-db.mjs, not owned here) unconditionally bumps `workflows.updated_at` to its
  own `now()` on every call, including from `inputs.put`. `ledgerApply` now restores the correct
  `updated_at` after the inputs loop; flagging in case another stream also calls `ensureWorkflow`/`inputs.put`
  outside a full workflow-row rewrite and hits the same clobber.

## Failures believed to be another stream's (not touched by this stream)

Full `npm test` after merging the current `starci183/s0-ledger-db` tip: 9 failures, none in
`scripts/ledger-migrate.mjs`, `kernel/journal.mjs`, or `tests/ledger-migrate*.spec.mjs`/`tests/journal*.spec.mjs`.

- **6 failures trace to one config change**: `starci183/s0-ledger-db` merged `main`'s
  "model: codex implement/write on gpt-5.6-luna (refactor alignment), verify stays sol, pool cap 10"
  (`model/registry.yaml`/`model/runtimes.yaml`), which raised a pool cap from 8 to 10 and moved codex's
  implement/write pin. Tests still asserting the old numbers/pin:
  - `tests/runtime-allocator.spec.mjs`: "prefer-then-overflow fills the preferred runtime before it offers
    the next one" (`10 !== 8`), "a runtime another kernel is already on is not the first choice…", "a decide
    operation downgrades to the Codex and Claude pools…", "a hard implement operation prefers the heavy
    pools…", "a medium implement operation keeps Sol first…" (all `gpt-5.6-luna` vs expected `gpt-5.6-sol`).
  - `tests/goal-contract.spec.mjs`: "runtime pools are keyed by provider window with goal §3 caps"
    (`codex-agent cap`, `10 !== 8`).
  These look like the allocator/model stream's tests not yet updated for a config change that landed via
  `main`, not authored by s0 or this stream.
- **3 failures unrelated to model config, also outside owned files**:
  - `tests/execution-resolve-v3.spec.mjs`: "resolver selects deterministically and preserves unavailable
    observations" — `TypeError: Cannot read properties of null (reading 'target')`.
  - `tests/frontend-workflow.spec.mjs`: "compiled JSON is reproducible and remains valid after relocation" —
    `false !== true`.
  - `tests/public-package.spec.mjs`: "public starci CLI installs the host separately from project metadata
    and refuses overwrites" — `null !== 0` (looks like a spawned install process exit code, possibly
    environment/network-sensitive; this test does a real `npm pack`+install).
  Not investigated further — outside this stream's file ownership and unrelated to ledger/journal/migration
  code paths.
