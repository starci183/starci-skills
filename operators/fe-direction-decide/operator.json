{
  "schemaVersion": 8,
  "id": "fe.direction.decide",
  "domain": "fe",
  "job": "Decide one evidence-backed, implementation-ready frontend direction for one authorized target.",
  "resources": {
    "profile": "sol-fresh",
    "requires": [
      "webSearch",
      "browser"
    ],
    "policy": {
      "webSearch": "bounded",
      "grammarBound": true,
      "imageGeneration": "authority-only"
    }
  },
  "refs": [
    {
      "alias": "@receipt/business-promise-authority/<invocationId>",
      "required": true,
      "purpose": "Business authority for the requested outcome."
    },
    {
      "alias": "@receipt/backend-implementation/<invocationId>",
      "required": false,
      "purpose": "Technical authority when the direction touches a delivered contract."
    },
    {
      "alias": "@receipt/architecture-decision/<invocationId>",
      "required": false,
      "purpose": "Technical authority when boundaries are in question."
    },
    {
      "alias": "@grammar",
      "required": true,
      "purpose": "Published Grammar: the compositions a direction may bind."
    },
    {
      "alias": "@knowledge/grammars/starci",
      "required": false,
      "purpose": "Family realization rules: how the Core family is meant to realize Common. Law about the Grammar, never the Grammar itself."
    },
    {
      "alias": "@knowledge/ui/composition",
      "required": true,
      "purpose": "What a tree must contain before it exists."
    },
    {
      "alias": "@workspaces/fe",
      "required": true,
      "purpose": "Current implementation as evidence, never as the requested direction."
    },
    {
      "alias": "@worktrees/uat/<flow>/<case>",
      "required": false,
      "purpose": "Evidence and counterevidence; a prior PASS is not current authority."
    },
    {
      "alias": "@receipt/fe-direction-decision/<invocationId>",
      "required": false,
      "purpose": "A previous direction; evidence unless the exact identity and fingerprint match."
    },
    {
      "alias": "@artifacts",
      "required": true,
      "purpose": "Where candidate renders and the decision receipt are written."
    }
  ],
  "contextContract": "context.md",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "inputValidator": "validate-input.mjs",
  "outputValidator": "validate-output.mjs",
  "selfTest": "self-test.mjs",
  "sideEffects": [
    "write only the typed decision receipt and inspectable direction artifacts under input.project.artifactRootRef"
  ],
  "forbiddenSideEffects": [
    "modify product or authority source",
    "invent business, backend, architecture, authentication, persistence, or data behavior",
    "publish shared UI Grammar",
    "start, stop, or reconfigure runtime services",
    "claim implementation, visual quality, quality-gate, or UAT completion"
  ],
  "stopConditions": [
    "required authority is missing, stale, contradictory, or outside the frozen scope",
    "the frontend change level or owner ceiling is ambiguous",
    "a reusable Grammar contract is missing",
    "no candidate survives mandatory falsification",
    "several material candidates remain and product authority must choose",
    "a resume adds no effective authority, evidence, or exact selection"
  ]
}
