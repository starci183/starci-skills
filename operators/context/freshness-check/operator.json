{
  "schemaVersion": 7,
  "id": "context/freshness-check",
  "domain": "context",
  "job": "Compare one cached context receipt with one expected identity tuple.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [],
  "stopConditions": [
    "the expected identity tuple is malformed",
    "the cached receipt claims an invalid identity"
  ]
}
