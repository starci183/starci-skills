# architecture/decision-challenge input

Adversarially test the strongest architecture candidate.

Accept only the declared state and exact artifact references: `decisionFrameRef`, `currentStateRef`, `optionSetRef`. Validate before knowledge retrieval or side effects.
