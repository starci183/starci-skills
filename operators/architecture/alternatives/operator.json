{
  "schemaVersion": 7,
  "id": "architecture/alternatives",
  "domain": "architecture",
  "job": "Produce materially different architecture alternatives from one frozen architecture snapshot.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "read only the supplied exact references"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
