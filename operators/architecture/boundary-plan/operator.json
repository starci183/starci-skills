{
  "schemaVersion": 7,
  "id": "architecture/boundary-plan",
  "domain": "architecture",
  "job": "Define one bounded architecture analysis scope from exact objective and evidence references.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "read only the supplied exact references"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
