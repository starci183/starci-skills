# architecture/boundary-plan input

Compile one canonical exact source-and-test boundary.

Accept only the declared state and exact artifact references: `businessHeadRef`, `planningContextRef`, `sourceReceiptRef`, `patternBindingRef`. Validate before knowledge retrieval or side effects.
