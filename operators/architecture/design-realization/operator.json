{
  "schemaVersion": 7,
  "id": "architecture/design-realization",
  "domain": "architecture",
  "job": "Map one approved architecture to concrete modules, connections, resources, deployment units, migrations, credentials, and proof paths.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "read only the supplied exact references"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
