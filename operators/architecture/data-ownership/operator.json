{
  "schemaVersion": 7,
  "id": "architecture/data-ownership",
  "domain": "architecture",
  "job": "Model physical stores, resources, readers, writers, migrators, transactions, backup, and restore ownership.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "read only the supplied exact references"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
