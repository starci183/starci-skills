# architecture/source-discovery input

Discover live schema and complete sibling-family evidence.

Accept only the declared state and exact artifact references: `businessHeadRef`, `planningContextRef`, `routeReceiptRef`, `sourcePartitionRef`. Validate before knowledge retrieval or side effects.
