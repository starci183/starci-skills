{
  "schemaVersion": 7,
  "id": "architecture/boundary-challenge",
  "domain": "architecture",
  "job": "Challenge one proposed architecture boundary against ownership, consistency, deployment, and recovery evidence.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "read only the supplied exact references"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
