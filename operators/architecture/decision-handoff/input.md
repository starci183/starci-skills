# architecture/decision-handoff input

Freeze the selected decision for role-specific planning.

Accept only the declared state and exact artifact references: `decisionFrameRef`, `optionSetRef`, `challengeReceiptRef`, `selectedDecisionRef`. Validate before knowledge retrieval or side effects.
