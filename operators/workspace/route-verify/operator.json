{
  "schemaVersion": 7,
  "id": "workspace/route-verify",
  "domain": "workspace",
  "job": "Resolves one request to one project-role route and enforces its Git mutation policy.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "read only the supplied exact references"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the active checkout branch violates the routed Git mutation policy",
    "the requested work exceeds this operator job"
  ]
}
