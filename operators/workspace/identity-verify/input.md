# workspace/identity-verify input

Prove machine decrypt identity without exposing credential values.

Accept only the declared state and exact artifact references: `machineIdentityRef`, `encryptedCredentialRosterRef`. Validate before knowledge retrieval or side effects.
