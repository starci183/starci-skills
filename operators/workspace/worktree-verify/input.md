# workspace/worktree-verify input

Prove one durable write root and its ownership boundary.

Accept only the declared state and exact artifact references: `hydrationReceiptRef`, `worktreeDeclarationRef`. Validate before knowledge retrieval or side effects.
