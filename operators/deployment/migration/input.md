# deployment/migration input

Apply only declared migration or initialization jobs.

Accept only the declared state and exact artifact references: `executionPlanRef`, `artifactPublishReceiptRef`, `migrationPlanRef`, `rollbackPointRef`. Validate before knowledge retrieval or side effects.
