{
  "schemaVersion": 7,
  "id": "deployment/migration",
  "domain": "deployment",
  "job": "Produce one bounded migration result from exact supplied evidence.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "apply declared migration or initialization job"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
