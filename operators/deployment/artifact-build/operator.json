{
  "schemaVersion": 7,
  "id": "deployment/artifact-build",
  "domain": "deployment",
  "job": "Builds immutable release artifacts from pinned source revisions.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "write ignored build outputs and immutable local artifacts declared by the execution plan"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
