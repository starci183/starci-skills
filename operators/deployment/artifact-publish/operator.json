{
  "schemaVersion": 7,
  "id": "deployment/artifact-publish",
  "domain": "deployment",
  "job": "Publishes declared immutable artifacts to the approved registry.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "publish declared immutable artifacts"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
