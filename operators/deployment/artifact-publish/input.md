# deployment/artifact-publish input

Publish immutable artifacts to the declared registry.

Accept only the declared state and exact artifact references: `executionPlanRef`, `artifactBuildReceiptRef`, `registryRef`, `credentialReceiptRef`. Validate before knowledge retrieval or side effects.
