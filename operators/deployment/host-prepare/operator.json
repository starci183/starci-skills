{
  "schemaVersion": 7,
  "id": "deployment/host-prepare",
  "domain": "deployment",
  "job": "Prepares the declared host idempotently.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "apply only declared idempotent host setup"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
