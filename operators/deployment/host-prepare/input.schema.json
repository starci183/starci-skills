{
  "$id": "https://starci.dev/v6/operators/deployment/host-prepare/input.schema.json",
  "title": "deployment/host-prepare operator input",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "runId",
    "stage",
    "status",
    "facts",
    "payload"
  ],
  "allOf": [
    {
      "oneOf": [
        {
          "properties": {
            "stage": {
              "const": "deployment.host"
            },
            "status": {
              "const": "ready"
            }
          },
          "required": [
            "stage",
            "status"
          ]
        }
      ]
    }
  ],
  "properties": {
    "schemaVersion": {
      "const": 6
    },
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "stage": {
      "enum": [
        "deployment.host"
      ]
    },
    "status": {
      "enum": [
        "ready"
      ]
    },
    "facts": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "executionPlanRef",
        "hostRef",
        "credentialReceiptRef",
        "setupSourceRef"
      ],
      "properties": {
        "executionPlanRef": {
          "type": "string",
          "minLength": 1
        },
        "hostRef": {
          "type": "string",
          "minLength": 1
        },
        "credentialReceiptRef": {
          "type": "string",
          "minLength": 1
        },
        "setupSourceRef": {
          "type": "string",
          "minLength": 1
        }
      }
    }
  }
}
