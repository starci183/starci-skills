# deployment/host-prepare input

Prove and prepare only the declared host/runtime boundary.

Accept only the declared state and exact artifact references: `executionPlanRef`, `hostRef`, `credentialReceiptRef`, `setupSourceRef`. Validate before knowledge retrieval or side effects.
