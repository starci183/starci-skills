# deployment/execution-root-init input

Initialize disposable ignored execution state.

Accept only the declared state and exact artifact references: `executionPlanRef`, `manifestReceiptRef`, `ignoreProofRef`. Validate before knowledge retrieval or side effects.
