{
  "schemaVersion": 7,
  "id": "deployment/manifest-validate",
  "domain": "deployment",
  "job": "Validate one exact deployment manifest against pinned release intent.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "read only the supplied exact references"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
