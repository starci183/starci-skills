{
  "schemaVersion": 7,
  "id": "deployment/recover",
  "domain": "deployment",
  "job": "Produce one bounded recover result from exact supplied evidence.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "mutate only the declared smallest deployment-owned repair boundary"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
