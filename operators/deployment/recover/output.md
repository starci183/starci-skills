# deployment/recover output

Return `recoveryReceiptRef`, one typed decision (`retry`, `rollback`, `approval-required`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
