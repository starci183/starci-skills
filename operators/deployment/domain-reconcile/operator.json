{
  "schemaVersion": 7,
  "id": "deployment/domain-reconcile",
  "domain": "deployment",
  "job": "Reconciles declared domain and TLS state with the provider.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "reconcile declared domain and TLS provider state"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
