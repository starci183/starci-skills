# deployment/domain-reconcile input

Reconcile each declared hostname through exactly one owner and driver.

Accept only the declared state and exact artifact references: `executionPlanRef`, `domainPlanRef`, `credentialReceiptRef`. Validate before knowledge retrieval or side effects.
