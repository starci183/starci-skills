# deployment/intent-bind input

Resolve exactly one project, role, environment, topology, and declared release.

Accept only the declared state and exact artifact references: `routeReceiptRefs`, `imperativeRequestRef`, `deploymentIntentRef`. Validate before knowledge retrieval or side effects.
