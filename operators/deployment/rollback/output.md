# deployment/rollback output

Return `rollbackReceiptRef`, one typed decision (`rolled-back`, `blocked`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
