{
  "schemaVersion": 7,
  "id": "deployment/rollback",
  "domain": "deployment",
  "job": "Produce one bounded rollback result from exact supplied evidence.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "apply declared rollback to provider and runtime state"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
