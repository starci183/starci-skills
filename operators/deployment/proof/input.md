# deployment/proof input

Reconcile routes, immutable artifacts, provider changes, runtime identity, probes, credentials hygiene, and rollback identity.

Accept only the declared state and exact artifact references: `deploymentContractRef`, `executionPlanRef`, `artifactPublishReceiptRef`, `rolloutOrRollbackReceiptRef`, `monitorReceiptRef`. Validate before knowledge retrieval or side effects.
