# deployment/execution-plan input

Compute value-free setup, release, migration, traffic, monitoring, and rollback deltas.

Accept only the declared state and exact artifact references: `deploymentContractRef`, `manifestReceiptRef`, `observedStateRef`. Validate before knowledge retrieval or side effects.
