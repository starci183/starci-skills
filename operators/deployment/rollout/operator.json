{
  "schemaVersion": 7,
  "id": "deployment/rollout",
  "domain": "deployment",
  "job": "Produce one bounded rollout result from exact supplied evidence.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "mutate declared remote runtime and release workflow state"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
