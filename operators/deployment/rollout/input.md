# deployment/rollout input

Roll out immutable identities through the declared runtime manager.

Accept only the declared state and exact artifact references: `executionPlanRef`, `artifactPublishReceiptRef`, `hostReceiptRef`, `domainReceiptRef`. Validate before knowledge retrieval or side effects.
