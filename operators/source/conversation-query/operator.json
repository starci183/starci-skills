{
  "schemaVersion": 7,
  "id": "source/conversation-query",
  "domain": "source",
  "job": "Resolve one bounded provider-neutral conversation identity against current provenance-head metadata.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [],
  "stopConditions": [
    "the query contains transcript text, prompts, credentials, or secrets",
    "the candidate metadata is stale, ambiguous, or outside the authorized project and role",
    "a returned candidate cannot be rebound to its current durable head hash"
  ]
}
