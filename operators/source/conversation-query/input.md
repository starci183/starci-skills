# source/conversation-query input

Query conversation provenance without mutating durable heads.

Accept only the declared state and exact artifact references: `queryRef`, `projectRoleRef`, `provenanceIndexRef`. Validate before knowledge retrieval or side effects.
