{
  "schemaVersion": 7,
  "id": "source/conversation-record",
  "domain": "source",
  "job": "Compare-and-set one redacted provider-neutral conversation snapshot as an immutable provenance head.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "append one immutable provenance head when compare-and-set succeeds"
  ],
  "stopConditions": [
    "the snapshot is not explicitly redacted",
    "the redaction receipt does not bind the exact snapshot hash and active policy",
    "the write authority is absent",
    "the same identity already has a different snapshot hash"
  ]
}
