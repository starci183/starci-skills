{
  "schemaVersion": 8,
  "id": "fe.surface.audit",
  "domain": "fe",
  "job": "Observe one rendered surface across a declared viewport and state matrix, measure every presentation value it actually produces, compare each measurement against the contract claim on that node, and return findings drawn only from the bound rule inventory.",
  "resources": {
    "profile": "sol-reviewer",
    "requires": [
      "browser"
    ],
    "policy": {
      "webSearch": "never",
      "grammarBound": true,
      "imageGeneration": "never"
    }
  },
  "refs": [
    {
      "alias": "@knowledge/ui/proof/<topic>",
      "required": true,
      "purpose": "What only becomes true once rendered; the audit's rule inventory."
    },
    {
      "alias": "@receipt/fe-source-application/<invocationId>",
      "required": true,
      "purpose": "The stated intention this audit exists to contradict."
    },
    {
      "alias": "@source/starci-academy/fe",
      "required": true,
      "purpose": "Proves the observed surface is the applied surface."
    },
    {
      "alias": "@runtime",
      "required": true,
      "purpose": "The endpoint that serves the route under observation."
    },
    {
      "alias": "@artifacts",
      "required": true,
      "purpose": "Where captures and the audit receipt are written."
    }
  ],
  "contextContract": "context.md",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "inputValidator": "validate-input.mjs",
  "outputValidator": "validate-output.mjs",
  "selfTest": "self-test.mjs",
  "sideEffects": [
    "read the rendered surface at the bound runtime endpoint",
    "write only the typed audit receipt and its capture evidence under input.project.artifactRootRef"
  ],
  "forbiddenSideEffects": [
    "modify product source, the applied tree, knowledge, or Grammar",
    "repair, restyle, or work around anything it observes",
    "start, stop, deploy, or reconfigure runtime services",
    "cite a rule identifier absent from the bound knowledge inventory",
    "record a verdict outside the canonical verdict model",
    "accept a contract claim as evidence that a node passes",
    "report a verdict for a node it did not measure"
  ],
  "stopConditions": [
    "the observed source head differs from the applied source head",
    "the runtime endpoint does not serve the bound route",
    "a declared matrix entry produced no capture",
    "a finding would cite an identifier the bound knowledge does not publish",
    "a resume adds no effective knowledge, applied source, matrix, or runtime delta"
  ]
}
