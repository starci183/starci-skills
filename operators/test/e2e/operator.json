{
  "schemaVersion": 7,
  "id": "test/e2e",
  "domain": "test",
  "job": "Produce one bounded e2e result from exact supplied evidence.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "start declared test dependencies",
    "materialize and reset isolated test data",
    "run connected scenarios"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
