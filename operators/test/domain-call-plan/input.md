# `test/domain-call-plan` input

## Context

- `context.missionId`: Mission identity.
- `context.completedDomains`: Already returned domains.

## Input

- `input.domainOrder`: Ordered domain calls.
- `input.returnReceipts`: Typed RETURN receipts already consumed.
