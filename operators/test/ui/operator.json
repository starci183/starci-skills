{
  "schemaVersion": 7,
  "id": "test/ui",
  "domain": "test",
  "job": "Run one deterministic visual-fidelity test against an approved baseline.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "open and control the running app in a real browser",
    "authenticate with an approved test account"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
