{
  "schemaVersion": 7,
  "id": "test/unit",
  "domain": "test",
  "job": "Produce one bounded unit result from exact supplied evidence.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "run manifest-owned focused unit commands"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
