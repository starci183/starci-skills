{
  "schemaVersion": 7,
  "id": "test/ui-quality-audit",
  "domain": "test",
  "job": "Issue one UI-quality verdict from a frozen immutable evidence set.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "control only the declared running application through a real browser",
    "never mutate product source, business authority, external providers, or the audited application state outside declared reversible interaction"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
