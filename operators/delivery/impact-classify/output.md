# `delivery/impact-classify` output

- `output.outcome`: atomic impact classification: `frontend-only`, `backend-required`, or `blocked`.
- `output.evidenceRefs`: exact evidence references supporting the classification.
