# quality/lint input

Run canonical lint with zero errors and zero warnings.

Accept only the declared state and exact artifact references: `changeReceiptRef`, `lintCommandRef`. Validate before knowledge retrieval or side effects.
