# quality/unit-coverage input

Run real unit tests and produce the sole LCOV/four-metric coverage receipt.

Accept only the declared state and exact artifact references: `changeReceiptRef`, `unitCommandRef`, `coveragePolicyRef`. Validate before knowledge retrieval or side effects.
