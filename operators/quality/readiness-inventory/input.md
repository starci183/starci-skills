# quality/readiness-inventory input

Execute check-only readiness across routed roles.

Accept only the declared state and exact artifact references: `routeSetRef`, `declaredGateSetRef`, `staleRegistryRef`. Validate before knowledge retrieval or side effects.
