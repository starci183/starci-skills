# quality/delivery-proof input

Reconcile the complete source-only delivery fence.

Accept only the declared state and exact artifact references: `changeReceiptRef`, `gateReceiptSetRef`, `businessHeadRef`. Validate before knowledge retrieval or side effects.
