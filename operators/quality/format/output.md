# quality/format output

Return `formatReceiptRef`, one typed decision (`pass`, `in-boundary`, `boundary-drift`, `external-blocker`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
