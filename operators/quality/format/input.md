# quality/format input

Run the repository check-only formatter.

Accept only the declared state and exact artifact references: `changeReceiptRef`, `formatCommandRef`. Validate before knowledge retrieval or side effects.
