{
  "schemaVersion": 7,
  "id": "quality/coding-preflight",
  "domain": "quality",
  "job": "Bind one implementation boundary to its exact templates, rules, and static-gate plan.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "read only the exact frozen targets, nearest declared implementation references, applicable lint configuration and TypeScript configuration"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
