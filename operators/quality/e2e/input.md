# quality/e2e input

Run the real E2E entrypoint with nonzero tests and independent behavioral evidence.

Accept only the declared state and exact artifact references: `changeReceiptRef`, `e2eCommandRef`, `seedReceiptRef`. Validate before knowledge retrieval or side effects.
