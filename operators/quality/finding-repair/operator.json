{
  "schemaVersion": 7,
  "id": "quality/finding-repair",
  "domain": "quality",
  "job": "Repairs one approved readiness finding.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "mutate only one exact approved finding boundary and emit a typed decision proof"
  ],
  "stopConditions": [
    "an exact reference is missing or its fingerprint differs",
    "the requested work exceeds this operator job"
  ]
}
