# quality/rule-binding-check input

Prove pattern situations, gates, and published machine identities are accountable.

Accept only the declared state and exact artifact references: `ruleRegistryRef`, `gateCatalogRef`, `publishedMachineRef`. Validate before knowledge retrieval or side effects.
