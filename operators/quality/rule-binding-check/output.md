# quality/rule-binding-check output

Return `ruleBindingReceiptRef`, one typed decision (`pass`, `fail`), evidence references, and findings. The skill state machine—not this operator—routes the emitted state.
