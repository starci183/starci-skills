# Execute `fe/return-consume`

Consume one typed child RETURN receipt and correlate its mission, parent-child identity, authority heads, source heads, and exact FE resume state.

Perform only this job. Validate exact mission, parent identity, resume state, authority/source-head,
and progress identity where present.
