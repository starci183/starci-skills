# Execute `fe/direction-rank`

Rank one direction set against frozen authority and recommend exactly one.

Open and inspect the supplied visualize HTML comparison. Do not rank a prose-only set or a set whose
wide, constrained, or material-state panels are missing.

Rank product value and composition trade-offs without preselecting Grammar packages or visual DNA.
The recommendation is provisional until mandatory `knowledge/ui` law validation and subsequent routed
Grammar Core compilation. Follow `AI-first -> Rules-first -> Grammar-last`. Return only this atomic result; never route internally.
