# Execute `fe/direction-rank`

Rank one direction set against frozen authority and recommend exactly one.

Open and inspect the supplied visualize HTML comparison. Do not rank a prose-only set or a set whose
wide, constrained, or material-state panels are missing.

Use StarCi-native frontend authority from `knowledge/ui.md` and the routed project Grammar. Follow `AI-first -> Rules-first -> Grammar-last`. Return only this atomic result; never route internally. 
