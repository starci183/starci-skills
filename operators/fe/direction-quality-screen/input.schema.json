{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://starci.dev/v7.2.1/operators/fe/direction-quality-screen/input.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "operatorId", "context", "input"],
  "properties": {
    "schemaVersion": { "const": 7 },
    "operatorId": { "const": "fe/direction-quality-screen" },
    "context": {
      "type": "object",
      "additionalProperties": false,
      "required": ["authorityRefs", "evidenceRefs", "uiKnowledgeId", "sourceFingerprint", "reviewerModel", "reviewerCount", "contextIsolation", "forkTurns", "reviewerExecutionRef", "debug"],
      "properties": {
        "authorityRefs": { "description": "Exact approved user-outcome and Business authority preserved by this screen.", "$ref": "#/$defs/nonEmptyRefs" },
        "evidenceRefs": { "description": "Content-addressed raster and benchmark evidence available to this job.", "$ref": "#/$defs/nonEmptyRefs" },
        "uiKnowledgeId": { "description": "Mandatory UI-law root used after the pixel-first observation.", "const": "fe.ui" },
        "sourceFingerprint": { "description": "Latest product-source fingerprint represented by the raster.", "$ref": "#/$defs/sha256" },
        "reviewerModel": { "description": "Exact fresh visual reviewer model.", "const": "gpt-5.6-sol" },
        "reviewerCount": { "description": "Exactly one reviewer owns this atomic screen.", "const": 1 },
        "contextIsolation": { "description": "Fresh-context isolation prevents producer self-review.", "const": "fresh" },
        "forkTurns": { "description": "No inherited turns may bias the first-glance judgement.", "const": "none" },
        "reviewerExecutionRef": { "description": "Content-addressed receipt for the reviewer execution.", "type": "string", "pattern": "^execution://[0-9a-f]{64}$" },
        "debug": { "description": "Requires observable call, return, scores, and transition trace.", "const": true }
      }
    },
    "input": {
      "type": "object",
      "additionalProperties": false,
      "required": ["targetRef", "directionRef", "representativeRasterRef", "benchmarkRasterRefs", "attempt", "minimumScore"],
      "properties": {
        "targetRef": { "description": "Exact browser-visible surface being screened.", "type": "string", "minLength": 1 },
        "directionRef": { "description": "Current direction invalidated when its score is below nine.", "type": "string", "minLength": 1 },
        "representativeRasterRef": { "description": "One fresh latest-source raster representing the direction.", "type": "string", "pattern": "^raster://sha256:[0-9a-f]{64}$" },
        "benchmarkRasterRefs": { "description": "Approved product-family rasters used to judge native quality.", "type": "array", "minItems": 1, "maxItems": 4, "uniqueItems": true, "items": { "type": "string", "pattern": "^raster://sha256:[0-9a-f]{64}$" } },
        "attempt": { "description": "Fresh direction attempt number from one through three.", "type": "integer", "minimum": 1, "maximum": 3 },
        "minimumScore": { "description": "Fixed production continuation threshold.", "const": 9 }
      }
    }
  },
  "$defs": {
    "sha256": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "nonEmptyRefs": { "type": "array", "minItems": 1, "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } }
  }
}
