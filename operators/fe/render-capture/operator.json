{
  "schemaVersion": 7,
  "id": "fe/render-capture",
  "domain": "fe",
  "job": "Capture one deterministic rendered evidence set for one frozen contract.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "contextRefs": [
    "fe.ui"
  ],
  "sourceReferenceRefs": [],
  "sideEffects": [
    "write content-addressed raster artifacts and one retained raster-only blind-review packet"
  ],
  "stopConditions": [
    "required authority is missing or stale",
    "the target boundary is ambiguous",
    "source or render-state identity drifts after preflight",
    "completion would require source mutation, visual judgment, review, or another operator job"
  ]
}
