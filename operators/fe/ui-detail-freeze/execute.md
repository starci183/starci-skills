# Execute `fe/ui-detail-freeze`

ui detail freeze one closed frontend target.

Use only the closed `context` and `input`. Return one typed `output.outcome` plus this job's result, gaps, and evidence. Do not route, persist a session, or choose another operator.

Frontend creation follows `AI-first -> Rules-first -> Grammar-last`. UX synthesis, UI-direction synthesis, principle/law compilation, semantic-detail freeze, layout compilation, Grammar application, implementation, and audit are separate jobs. This job runs after mandatory-law compilation and emits one immutable detail binding before layout and Grammar compilation. Audit jobs observe and report; they never repair source.

Freeze each approved destination with native link semantics and a real non-null href; freeze every contract-declared progress datum as a progress presentation; and rank every compact numeric fact below its owning section title. A static or button-only destination, missing declared progress, or fact-over-title rank is a gap, not a valid detail.
