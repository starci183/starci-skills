# Execute `fe/contract-freeze`

Freeze one selected frontend direction into one executable contract.

Reconcile every interaction found by runtime observation. Each one must be preserved, or explicitly
removed/replaced with authority and rationale. Freeze visible surface owners, the mandatory UI-law
binding, the law-governed semantic-detail binding, the pre-source Grammar Core binding, its packaged component/object contracts, visual DNA,
product-family evidence, and one state for wide, intermediate, and compact. A visual direction never authorizes silent behavior
deletion. Missing reconciliation is `blocked`, not an implicit design choice.

Make the frozen `uxUiChangeLevel` and its negative mutation boundary explicit in the contract.
`Refine` must preserve region/order/flow; `reconstruct` must preserve the approved product and
behavior boundary; `new` must name the approved absent target.

Freeze exactly one `mediaDecision`. Choose `none` when no media slot has a user purpose; choose `reuse`
when a suitable approved asset serves the frozen role; otherwise choose `generate` whenever a sound
composition intentionally reserves media space for an authorized purpose. Media requires a frozen
user purpose, owner/placement, approved asset authority or generation brief, responsive crop/contain
treatment, alt intent, and no-image fallback.
`layoutIntegrity=sound` and `contentCompleteness=complete` are mandatory: image generation cannot hide
a layout defect, missing content, weak hierarchy, or an otherwise unexplained dead zone.

Use mandatory laws from `knowledge/ui/INDEX.md` plus applicable law records and packaged UI/DNA from the routed Grammar Core as distinct
owners. Follow `AI-first -> Rules-first -> Grammar-last`. Return only this atomic result; never route internally.
