# Execute `fe/contract-freeze`

Freeze one selected frontend direction into one executable contract.

Reconcile every interaction found by runtime observation. Each one must be preserved, or explicitly
removed/replaced with authority and rationale. Freeze visible surface owners, routed Grammar bindings,
and one state for wide, intermediate, and compact. A visual direction never authorizes silent behavior
deletion. Missing reconciliation is `blocked`, not an implicit design choice.

Make the frozen `uxUiChangeLevel` and its negative mutation boundary explicit in the contract.
`Refine` must preserve region/order/flow; `reconstruct` must preserve the approved product and
behavior boundary; `new` must name the approved absent target.

Freeze exactly one `mediaDecision`. Choose `none` when edited content and interaction structure already
communicate the outcome, `reuse` when an approved existing asset owns the role, and `generate` only
when a purpose-built bitmap materially improves comprehension or orientation and no approved reusable
asset serves it. A generated-media decision must freeze the exact user purpose, placement, generation
brief, responsive treatment, and alternative intent. Image generation remains downstream asset
production and cannot manufacture product meaning.

Use StarCi-native frontend authority from `knowledge/ui.md` and the routed project Grammar. Follow `AI-first -> Rules-first -> Grammar-last`. Return only this atomic result; never route internally. 
