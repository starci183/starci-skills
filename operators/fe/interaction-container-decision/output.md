# `fe/interaction-container-decision` output

## JSON architecture

The output state is pending review or blocked. For every interaction, the artifact records all five considered containers, one selected container, four explicit rejection reasons, behavioral semantics, and desktop/mobile transformation. It does not approve implementation or mutate source. All intermediate comparisons purge at skill-terminal.
