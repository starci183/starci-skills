# Execute `fe/iconography-resolve`

Resolve icon meaning from the frozen business/UI role, never from visual novelty or a nearby glyph name.
Search every approved catalog first. Reuse an upstream Heroicon when its established meaning matches
the role. Do not redraw books, homes, microphones, terminals, trophies, chat, stacks, or other exact
catalog concepts. A custom SVG is eligible only when no searched glyph communicates the authorized
meaning without distortion; freeze an `icon-brief://` with viewBox, outline/compact cuts, stroke,
corners, optical weight and test fixtures matching the one `visualFamilyRef`.

Return only the immutable decision manifest. Do not mutate source, generate unrelated artwork, route,
or infer a business meaning from incumbent iconography.
