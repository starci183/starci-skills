# Execute `fe/mutation-boundary-freeze`

Freeze one authorized frontend mutation boundary with verified route, exact files, before hashes, approved contract fingerprint, and the exact owner ceiling. Every file names its visible owner. For feature audit, accept the target plus every feature-owned or directly nested page/modal/drawer owner, mutable nested layouts, and explicitly authorized mutable ancestors. Reject shared branches, immutable parent shells, headers, navigation, and other ancestors even when they affect the rendered target. An excluded shared-owner finding is recorded while the remaining in-scope audit continues. If it is required for feature closure, report an authority request to the Control Panel; never silently widen mutation authority or duplicate shared behavior locally to evade the ceiling.

Perform only this job. Validate exact mission, parent-child, authority/source-head, and progress identity where present.
