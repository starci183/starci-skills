# Execute `fe/runtime-observe`

Observe one frontend runtime without mutating shared processes.

Inventory every visible surface owner and every existing interaction before judging or redesigning the
target. Search, filter, navigation, disclosure, selection, mutation, progress, recovery, scrolling,
dragging, overlays, and keyboard behavior may not disappear into prose. Observe wide, intermediate,
and compact states. `observed` is invalid when any responsive state or direct evidence is missing.

Use StarCi-native frontend authority from `knowledge/ui.md` and the routed project Grammar. Follow `AI-first -> Rules-first -> Grammar-last`. Return only this atomic result; never route internally. 
