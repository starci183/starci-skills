# Execute `fe/runtime-observe`

Observe one frontend runtime without mutating shared processes. Bind and apply
`knowledge/runtime-capture-readiness.md` before starting or reusing any listener. Prove the routed
worktree, runtime working directory, contained dependency strategy, manifest/lock status,
runtime origin, production build/exact target load, and effective viewport controls. Reject an escaping `node_modules`
symlink/junction, a missing local-package export target, an unproven port reuse, or a viewport control
whose requested and observed dimensions differ. Record manifest/lock drift as a repository finding;
when contained dependencies still pass the production build and exact target load, drift alone does not
block observation. Do not mutate manifests or lockfiles to heal the observation harness.

Inventory every visible surface owner and every existing interaction before judging or redesigning the
target. Search, filter, navigation, disclosure, selection, mutation, progress, recovery, scrolling,
dragging, overlays, and keyboard behavior may not disappear into prose. Observe wide, intermediate,
and compact states. Each responsive state records requested and observed dimensions plus a fresh raster
fingerprint; duplicate rasters or unchanged observed dimensions invalidate `observed`. `observed` is
invalid when runtime readiness, any responsive state, or direct evidence is missing.

Use StarCi-native frontend authority from `knowledge/ui/INDEX.md`, its applicable law records, and the routed project Grammar. Follow `AI-first -> Rules-first -> Grammar-last`. Return only this atomic result; never route internally.
