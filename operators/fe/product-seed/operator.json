{
  "schemaVersion": 6,
  "id": "fe/product-seed",
  "domain": "fe",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "knowledgeRefs": [
    "fe.product-seeding"
  ],
  "sourceReferenceRefs": [],
  "accepts": [
    {
      "stage": "seed.materialize",
      "status": "ready",
      "allFacts": [
        "source-written"
      ]
    }
  ],
  "emits": [
    {
      "when": "all required business states are reproducibly materialized",
      "stage": "test.unit",
      "status": "ready",
      "factsAdd": [
        "seed-evidence"
      ]
    },
    {
      "when": "any required state cannot be safely materialized",
      "stage": "seed.result",
      "status": "blocked",
      "factsAdd": [
        "seed-blocked"
      ]
    }
  ],
  "sideEffects": [
    "coordinator performs only declared isolated external effects for fe/product-seed",
    "keep input, output, loaded context, worker observations, drafts, evidence, and receipts in task-session storage only",
    "purge every intermediate session object at every parent-skill terminal state"
  ],
  "stopConditions": [
    "missing state owner or evidence",
    "unknown sensitive state",
    "non-deterministic materialization",
    "missing reset operation",
    "production credential or data requirement",
    "state outside the approved business-state contract",
    "Stop on unknown sensitive state, production data or credential dependency, nondeterminism, missing reset, or state outside the approved contract.",
    "runtime binding revision or session ownership is invalid",
    "the operation would broaden its approved authority, source, or external boundary"
  ]
}
