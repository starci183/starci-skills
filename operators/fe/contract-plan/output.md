# `fe/contract-plan` output

## JSON architecture

The output state is contract-ready, a typed gap, or blocked. The artifact maps every surface to reuse, extension, or creation; freezes exact source scope; and forbids implementation-time composition invention. Session intermediates purge at skill-terminal.
