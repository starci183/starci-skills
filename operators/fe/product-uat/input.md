# `fe/product-uat` input

## JSON architecture

Provided refs freeze the approved UX flow, its approved interaction-container plan, passed fidelity receipt, business outcome, and either `null` or one prior UX/UI resolution with its exact request and repair receipts. Runtime loads the running app, deterministic test identity, exact proof knowledge, and one declared orchestration profile. The identity must cover entitled, unentitled, empty, populated, failure, retry, resume, container behavior, and responsive variants required by the approved journey. UAT session data purges at skill-terminal.
