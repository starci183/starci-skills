# Execute `fe/authority-reconcile`

Repair one compile-time frontend gap owned by routed Grammar. Do
not mutate product source, redesign the surface, review pixels, or choose another operator.

Use only the typed compile gap, its direct evidence, the exact Grammar owner, and the
closed reuse boundary with before hashes and revision. Change and publish the smallest reusable
contract that is demonstrably wrong or missing.
Never generalize a page-specific preference, copy the user's suggested patch as a universal rule, or
edit authority merely to make one implementation pass. Never compensate with page-local CSS or a
private component rule. Preserve unrelated consumers; record exact before/after revisions, boundary
effects, package/export identity, content hash, changed authority, and consumer impact refs; and emit
`resumeState=request-compile`. Return only the typed authority-repair receipt;
the parent Skill owns resumed compilation.
