# Execute `fe/authority-reconcile`

Reconcile one proven frontend authority defect owned by routed Grammar or shared UI knowledge.

Use only the classified finding, its direct counterevidence, the exact affected authority, and the
closed reuse boundary. Change the smallest reusable contract that is demonstrably wrong or missing.
Never generalize a page-specific preference, copy the user's suggested patch as a universal rule, or
edit authority merely to make one implementation pass. Preserve unrelated consumers and record the
affected owner references. Return one typed result; do not repair product implementation or select
another operator. After reconciliation the Skill repairs the implementation, captures fresh rasters,
and restarts AI visual review.
