# Execute fe/authority-reconcile

1. Validate input and freeze its artifact identities.
2. Retrieve `fe.design-maintenance` from Qdrant; do not load unrelated knowledge.
3. Execute only this maintenance responsibility; preserve source-first proof, rejected attempts, exact authority ownership, and closed consumer scope.
4. Write only declared receipts/side effects, validate output, and return control to the skill state machine.

## LOADS

| Alias | Target | Kind | Why |
| --- | --- | --- | --- |
| `@fe-design-maintenance` | `fe.design-maintenance` | qdrant | retrieve only the law needed by this operator |

Stop when required evidence is missing or stale; the result would cross the declared boundary.
