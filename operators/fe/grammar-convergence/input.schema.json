{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://starci.dev/v7.2/operators/fe/grammar-convergence/input.schema.json",
  "type": "object", "additionalProperties": false,
  "required": ["schemaVersion", "operatorId", "context", "input"],
  "properties": {
    "schemaVersion": { "const": 7 },
    "operatorId": { "const": "fe/grammar-convergence" },
    "context": {
      "type": "object", "additionalProperties": false,
      "required": ["evidenceRefs", "authorityRevision", "routedGrammarRevision"],
      "properties": {
        "evidenceRefs": { "description": "Exact layout, law, Grammar, DNA, and benchmark evidence used for convergence.", "type": "array", "minItems": 1, "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
        "authorityRevision": { "description": "Frozen product authority revision that the packaged Grammar realization must preserve.", "type": "string", "minLength": 1 },
        "routedGrammarRevision": { "description": "Frozen routed Grammar revision that supplies product-family packages and visual DNA.", "type": "string", "minLength": 1 }
      }
    },
    "input": {
      "type": "object", "additionalProperties": false,
      "required": ["targetRef", "layoutCompilationRef", "uiLawBindingRef", "uiDetailBindingRef", "grammarCoreRef", "packagedContractRefs", "visualDnaRef", "productFamilyRef", "benchmarkRasterRefs", "constraints"],
      "properties": {
        "targetRef": { "description": "The one frontend target being bound to Grammar Core before source mutation.", "type": "string", "minLength": 1 },
        "layoutCompilationRef": { "description": "Exact law-approved responsive layout compilation to realize.", "type": "string", "pattern": "^layout-compile://" },
        "uiLawBindingRef": { "description": "Exact mandatory UI-law binding that Grammar cannot waive.", "type": "string", "pattern": "^ui-law-binding://" },
        "uiDetailBindingRef": { "description": "Exact law-governed semantic-detail binding that packaged Grammar must preserve.", "type": "string", "pattern": "^ui-detail-binding://" },
        "grammarCoreRef": { "description": "Exact Grammar Core authority supplying packaged UI object and component contracts.", "type": "string", "pattern": "^grammar-core://" },
        "packagedContractRefs": { "description": "Nonempty packaged object and component contracts selected for the layout.", "type": "array", "minItems": 1, "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "pattern": "^grammar-package://" } },
        "visualDnaRef": { "description": "Exact visual DNA identity governing product-family appearance.", "type": "string", "pattern": "^visual-dna://" },
        "productFamilyRef": { "description": "Opaque product-family identity used for convergence and blind visual review.", "type": "string", "pattern": "^product-family://" },
        "benchmarkRasterRefs": { "description": "Content-addressed family benchmark rasters used without source rationale.", "type": "array", "minItems": 1, "maxItems": 16, "uniqueItems": true, "items": { "type": "string", "pattern": "sha256[-/][0-9a-f]{64}\\.(png|jpg|jpeg|webp)$" } },
        "constraints": { "description": "Closed constraints for this atomic Grammar Core compilation.", "type": "array", "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } }
      }
    }
  }
}
