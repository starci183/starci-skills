# Execute `fe/grammar-convergence`

Compile the routed Grammar Core into one already law-valid, semantic-detail-frozen responsive layout before any product-source
mutation. Grammar Core is the packaged UI foundation: reusable object/component contracts plus visual
DNA and product-family identity. Tokens may participate inside those packages, but token-only binding
is incomplete and must be refused.

Bind the exact `uiLawBindingRef` and `uiDetailBindingRef` without redefining either. Select at least one routed packaged contract,
the exact visual-DNA identity, the product-family identity, and content-addressed benchmark rasters.
Issue a `grammarBindingRef` only at `before-source-mutation`; never treat Grammar as cosmetic cleanup
after layout or after source apply.

Use only the closed `context` and `input`. Return one typed `output.outcome` plus this job's result, gaps, and evidence. Do not route, persist a session, or choose another operator.

Frontend creation follows `AI-first -> Rules-first -> Grammar-last`. Direction synthesis, mandatory
UI-law validation, semantic-detail freeze, layout compilation, Grammar Core compilation, implementation, and audit are
separate jobs. UI laws remain compulsory constraints; Grammar Core owns packaged UI and visual DNA.
