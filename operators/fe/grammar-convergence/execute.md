# Execute Grammar convergence

## LOADS

| Alias | Target | Kind | Why |
|---|---|---|---|
| `@grammar-complex-cases` | `fe.grammar-complex-cases` | qdrant | converge exact business-free package owners, neutral treatments and complex cases |

1. Run `node validate-input.mjs <input.json>` and verify the two exact package locks. Reject ranges, tags, missing integrity values and implicit Grammar dependencies.
2. Run the business-free gate over fact IDs, presentation-state IDs, rule predicates, complex-case predicates and fixtures. Stop if any external semantic token participates in selection.
3. Resolve each decision slot by closed fact predicate, priority, specificity and stable rule ID. A tie is ambiguous and stops that slot.
4. Bind the winning rule to one exact generic export, template hash, invariants, variable axes, state map, complex-case row and proof references.
5. Preserve neutral-state ownership: an opaque Product Block maps domain state to neutral presentation state; Grammar maps only that neutral state to a generic treatment.
6. Record a `grammar-gap` instead of approximating when no truthful rule, owner, state map, complex-case row, allowed axis or locked export exists.
7. Run `node validate-output.mjs <output.json>`. Emit nothing unless it passes. A passing business-free gate advances to source fit; a failed gate returns `blocked` with refusal codes.

Do not write application code, create business-bearing components, publish npm packages or use Principles to replace a missing Grammar owner.
