# Execute `fe/grammar-convergence`

Apply Grammar 7.4 to one already compiled semantic layout. Produce the complete validated decision
manifest: exact package bindings, semantic compositions, required decisions, responsive bindings,
sticky/scroll ownership, and an empty gap set. The manifest is the authority passed to rendering,
contract freeze, implementation, repair, and audit; a prose summary is never sufficient.

Use only the closed `context` and `input`. Return one typed `output.outcome` plus this job's result, gaps, and evidence. Do not route, persist a session, or choose another operator.

Frontend creation follows `AI-first -> Rules-first -> Grammar-last`. UX synthesis, UI-direction synthesis, principle/law compilation, layout compilation, Grammar application, implementation, and audit are separate jobs. Audit jobs observe and report; they never repair source.

Return `grammar-gap` when any requested pattern is absent from the selected package or any visible
role lacks one decision. Never translate the gap into raw utilities, pixels, palette names, or an
application-owned imitation.
