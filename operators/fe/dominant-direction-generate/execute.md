# Execute `fe/dominant-direction-generate`

Use one fresh `gpt-5.6-sol` execution with no inherited turns. Generate the strongest reversible
replacement composition inside the frozen owner ceiling. Render one inspectable HTML artifact through
`visualize` with realistic wide, compact, and consequential interaction or recovery states. Bind the
nearest product-family signatures and Grammar decision manifest, then return a construction contract
that can proceed immediately to freeze and apply. Do not generate alternatives, rank directions, or
ask the user to approve the dominant direction.
