{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://starci.dev/v7.2/operators/fe/layout/input.schema.json",
  "type": "object", "additionalProperties": false,
  "required": ["schemaVersion", "operatorId", "context", "input"],
  "properties": {
    "schemaVersion": { "const": 7 },
    "operatorId": { "const": "fe/layout" },
    "context": {
      "type": "object", "additionalProperties": false,
      "required": ["evidenceRefs", "authorityRevision"],
      "properties": {
        "evidenceRefs": { "description": "Exact direction and law evidence used to compile the layout.", "type": "array", "minItems": 1, "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
        "authorityRevision": { "description": "Frozen authority revision whose task hierarchy and content constraints the layout preserves.", "type": "string", "minLength": 1 }
      }
    },
    "input": {
      "type": "object", "additionalProperties": false,
      "required": ["targetRef", "directionRef", "uiLawBindingRef", "uiDetailBindingRef", "constraints"],
      "properties": {
        "targetRef": { "description": "The one frontend target whose responsive geometry is being compiled.", "type": "string", "minLength": 1 },
        "directionRef": { "description": "The law-approved UI direction that constrains the layout.", "type": "string", "minLength": 1 },
        "uiLawBindingRef": { "description": "Exact mandatory UI-law compilation that must precede layout work.", "type": "string", "pattern": "^ui-law-binding://" },
        "uiDetailBindingRef": { "description": "Exact law-governed semantic-detail binding that layout geometry must preserve.", "type": "string", "pattern": "^ui-detail-binding://" },
        "constraints": { "description": "Closed constraints for this atomic layout-compilation job.", "type": "array", "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } }
      }
    }
  }
}
