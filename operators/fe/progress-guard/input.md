# `fe/progress-guard` input

## Context

- `context.history`: Prior progress fingerprints.

## Input

- `input.candidateFingerprint`: Candidate receipt progress fingerprint.
- `input.cycleKind`: Guarded cycle kind.
