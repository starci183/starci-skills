# fe/learning-request input

Record one durable design-learning request after source proof.

Accept only the declared state and exact artifact references: `feedbackProofRef`, `authorityGapRef`. Validate before knowledge retrieval or side effects.
