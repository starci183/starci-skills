# Execute `fe/source-apply`

Apply one frozen frontend contract to one exact source boundary.

Bind the mutation to the frozen behavior-contract fingerprint. Preserve every interaction marked
`preserve`; implement every authorized replacement; never infer removal from a visual direction.

Fail closed unless the invocation also binds the exact mandatory UI-law result, frozen semantic-detail binding, semantic iconography manifest, and the pre-source
Grammar Core compilation containing packaged object/component contracts plus visual DNA. Apply any
generated image only from the immutable media manifest produced from the frozen decision and brief; absence of the bitmap uses the frozen
fallback without changing layout ownership or content meaning.

Use mandatory laws from `knowledge/ui/INDEX.md` plus applicable law records and packaged UI/DNA from the routed Grammar Core as distinct
owners. Follow `AI-first -> Rules-first -> Grammar-last`. Return only this atomic result; never route internally.
