{
  "schemaVersion": 7,
  "id": "fe/source-apply",
  "domain": "fe",
  "job": "Apply or repair one frozen frontend contract inside one exact source boundary.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "contextRefs": [
    "fe.ui"
  ],
  "sourceReferenceRefs": [],
  "sideEffects": [
    "mutate only the exact frozen frontend source boundary"
  ],
  "stopConditions": [
    "required authority is missing or stale",
    "the target boundary is ambiguous",
    "a tracked or untracked effect falls outside the frozen source boundary",
    "completion would require compilation, design, capture, review, or another operator job"
  ]
}
