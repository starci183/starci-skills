# `fe/ux-flow` output

## JSON architecture

The output state is pending review or blocked. The artifact records navigable nodes, guarded transitions, recovery, and state behavior. `potentialSignals` marks unapproved persistence or backend needs for typed routing; it never silently changes business or data storage. All session intermediates purge at skill-terminal.
