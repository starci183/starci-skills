# `fe/ux-flow` input

## JSON architecture

`payload.provided` carries one approved UI direction and exact business capabilities. `payload.loads` resolves only journey/state knowledge and orchestration. `payload.session` holds ephemeral models until the skill-terminal cleanup.
