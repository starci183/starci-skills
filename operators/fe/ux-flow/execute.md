# Execute `fe/ux-flow`

Synthesize exactly one UX flow graph for the declared actor and target outcome.

Read only the verified backend route, approved `.worktrees/businesses` head, declared journey evidence, and exact UX laws in `context`. Use AI to connect the approved meaning into a complete decision graph; laws constrain validation, failure feedback, recovery, refresh, resume, async continuity, and safe completion. Source code is evidence only and cannot invent policy.

Return one `uxFlowGraph` covering the closed surface set, with observable nodes, transitions, side effects, recovery paths, and all six continuity dimensions. Return `blocked` only with exact missing authority. Do not choose visual style, layout, Grammar objects, interaction containers, source changes, or the next operator.
