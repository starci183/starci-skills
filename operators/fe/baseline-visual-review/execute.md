# Execute `fe/baseline-visual-review`

Use exactly one fresh `gpt-5.6-sol` with no inherited turns. Inspect every supplied raster from pixels
only and return one complete finding batch, five evidenced axes, and a noncanonical score. This
operator routes discovery; it can never emit `PASS`. No findings returns `ready-for-closure` with
`SUSPENSE`, which requires the complete closure matrix next. Findings return `failed` for owner
classification and `fe/audit-route`.
