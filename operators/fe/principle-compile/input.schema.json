{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://starci.dev/v7.2/operators/fe/principle-compile/input.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "operatorId", "context", "input"],
  "properties": {
    "schemaVersion": { "const": 7 },
    "operatorId": { "const": "fe/principle-compile" },
    "context": {
      "type": "object",
      "additionalProperties": false,
      "required": ["evidenceRefs", "authorityRevision", "uiLawAuthorityRef"],
      "properties": {
        "evidenceRefs": { "description": "Exact evidence used to evaluate the selected direction against mandatory UI laws.", "type": "array", "minItems": 1, "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
        "authorityRevision": { "description": "Frozen revision of the product and UX authority being evaluated.", "type": "string", "minLength": 1 },
        "uiLawAuthorityRef": { "description": "Exact mandatory knowledge/ui law authority, excluding packaged Grammar ownership.", "type": "string", "pattern": "^knowledge://ui-laws/" }
      }
    },
    "input": {
      "type": "object",
      "additionalProperties": false,
      "required": ["targetRef", "selectedDirectionRef", "requiredLawRefs", "constraints"],
      "properties": {
        "targetRef": { "description": "The one frontend target whose direction is being law-compiled.", "type": "string", "minLength": 1 },
        "selectedDirectionRef": { "description": "The selected provisional UI direction that must satisfy the mandatory laws.", "type": "string", "minLength": 1 },
        "requiredLawRefs": { "description": "Every applicable mandatory UI law that must be satisfied before layout compilation.", "type": "array", "minItems": 1, "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "pattern": "^ui-law://" } },
        "constraints": { "description": "Closed constraints for this atomic law-compilation job.", "type": "array", "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } }
      }
    }
  }
}
