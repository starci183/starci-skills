# Execute `fe/principle-compile`

Validate one selected UI direction against every applicable mandatory law routed by `knowledge/ui/INDEX.md`. Emit an
exact `uiLawBindingRef` only when every required law has a satisfied evidence cell. Missing laws,
unchecked laws, or design-token/component/DNA substitutions are `blocked`.

Use only the closed `context` and `input`. Return one typed `output.outcome` plus this job's result, gaps, and evidence. Do not route, persist a session, or choose another operator.

This operator owns mandatory laws, not StarCi appearance. Packaged objects/components, visual DNA,
tokens, and product-family identity belong to Grammar Core and must not be smuggled into UI law refs.
Direction synthesis, law validation, layout, Grammar Core compilation, implementation, and audit are separate jobs.
