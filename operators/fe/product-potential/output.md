# `fe/product-potential` output

## JSON architecture

The output state records assessment completion or blockage. Each hypothesis is classified without granting authority. `nextCandidates` carries typed necessity, risk, approval, input, and resume routing. Session intermediates purge at skill-terminal.
