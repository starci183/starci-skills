# Execute `fe/ux-audit`

Audit one target for UX continuity and recovery without repairing it.

Return one evidence-bound check for every observed entry, action, transition, interruption, recovery,
navigation, and exit. Verify that every existing interaction is preserved or has explicit authority to
change. A summary is never a verdict; `passed` requires every structured check to pass.

Use StarCi-native frontend authority from `knowledge/ui.md` and the routed project Grammar. Follow `AI-first -> Rules-first -> Grammar-last`. Return only this atomic result; never route internally. 
