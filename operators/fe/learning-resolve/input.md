# fe/learning-resolve input

Audit the correction and learn it into the smallest Grammar or Principle authority.

Accept only the declared state and exact artifact references: `learningRequestRef`, `sourceProofRef`, `authorityContextRef`. Validate before knowledge retrieval or side effects.
