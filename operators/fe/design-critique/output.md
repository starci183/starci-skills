# `fe/design-critique` output

## JSON architecture

The output state is accepted, revise, or blocked. The critique classifies every claim, records how source evidence is treated, and includes an independent counterproposal when revision is meaningful. Session artifacts purge at the skill-terminal boundary.
