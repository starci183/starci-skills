{
  "schemaVersion": 7,
  "id": "fe/direction-generate",
  "domain": "fe",
  "job": "Render one dominant or three-to-four evidence-authorized frontend directions into one inspectable HTML artifact.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "contextRefs": [
    "fe.ui"
  ],
  "sourceReferenceRefs": [],
  "sideEffects": [
    "write one local inspectable visualize HTML comparison artifact"
  ],
  "stopConditions": [
    "required authority is missing or stale",
    "the target boundary is ambiguous",
    "the requested mode is not authorized by the compiled request",
    "completion would require source mutation, choice, or another operator job"
  ]
}
