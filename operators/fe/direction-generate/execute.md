# Execute `fe/direction-generate`

Render the requested frontend direction set as one actual, inspectable local `visualize` HTML
artifact. Do not mutate product source, rank a choice, or choose another operator.

`mode=dominant` renders exactly one Grammar-valid direction and emits `requiresChoice=false`.
`mode=alternatives` is legal when evidence shows no materially dominant direction after Grammar
validation or the user explicitly requests comparison; it renders three or four materially
different Grammar-valid directions and emits `requiresChoice=true`. Every
rendered direction must include realistic content, wide and
constrained composition, and a material interaction or recovery state. Prose-only output is invalid.

Consume the compiled unfamiliar-domain research without reopening it. Reject generation when the
compiled evidence omits required bounded external research, hides an exhausted-search gap, or leaves
a material business or recovery decision to taste. When research was exhausted but the compiled
contract proves every material decision from approved business facts, product-family signatures,
Grammar, and product-neutral UX principles, render the result as an explicit reversible hypothesis
and expose the consequential assumptions in the artifact. Never copy external page anatomy or treat
an optional design skill or dataset as StarCi authority.

Run exactly one fresh `gpt-5.6-sol` generation when execution reaches ideation, and report that
execution even if later filtering or artifact creation blocks completion. Before rendering,
normalize every candidate through `knowledge/grammar/common/semantic-composition.md` and validate its
Grammar decision manifest. Record accepted and rejected candidates once; rejected candidates never
enter the artifact. A missing accepted count or missing HTML effect returns `blocked`.

Use `knowledge/direction-visualization.md`, `knowledge/ui.md`, and the routed project Grammar in
`AI-first -> Rules-first -> Grammar-last` order. Return only this typed generation receipt; the parent
Skill owns any choice and the next job.
