# Execute `fe/direction-generate`

Generate three or four materially different frontend directions only when no dominant action exists.
Apply `knowledge/direction-visualization.md`. Produce an inspectable `visualize` HTML comparison that
renders every direction with realistic content, wide and constrained composition, and material
interaction/recovery states. A prose-only direction set is incomplete.

Before rendering, normalize every visible role through the semantic-composition catalog and validate
one Grammar decision manifest per candidate. Reject candidates that rely on an unpublished proposal,
a Grammar gap, local reconstruction of generic anatomy, or business meaning inside Grammar. Rejected
candidates are never rendered or ranked. Business-derived creativity may remain in application
blocks/composites only when every rendered constituent binds published Grammar authority.

These are product-direction hypotheses, not law or Grammar bindings. Do not choose packaged components,
tokens, recipes, or visual DNA here. The selected direction remains provisional until mandatory
`knowledge/ui` law validation and subsequent routed Grammar Core compilation. Follow
`AI-first -> Rules-first -> Grammar-last`. Return only this atomic result; never route internally.
