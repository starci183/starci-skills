{
  "schemaVersion": 7,
  "id": "core/handoff-ack",
  "domain": "core",
  "job": "Acknowledge one immutable handoff after verifying every accepted artifact identity.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": ["write one immutable acknowledgement object"],
  "stopConditions": ["objective or artifact hash mismatch", "the consumer accepts only a subset of the handoff"]
}
