{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://starci.dev/v7/operators/core/handoff-ack/output.schema.json",
  "type": "object", "additionalProperties": false,
  "required": ["schemaVersion", "operatorId", "output"],
  "properties": {
    "schemaVersion": { "const": 7 },
    "operatorId": { "const": "core/handoff-ack" },
    "output": {
      "type": "object", "additionalProperties": false,
      "required": ["outcome", "ackRef", "purgeRefs", "evidenceRefs", "reason"],
      "properties": {
        "outcome": { "description": "Whether the consumer accepted, rejected, or could not verify the handoff.", "enum": ["accepted", "rejected", "blocked"] },
        "ackRef": { "description": "Immutable acknowledgement reference, or null when not accepted.", "type": ["string", "null"], "minLength": 1, "maxLength": 1024 },
        "purgeRefs": { "description": "Exact retained refs the runtime may purge after accepted acknowledgement.", "$ref": "#/$defs/refs" },
        "evidenceRefs": { "description": "Evidence comparing the handoff and consumer artifact identities.", "$ref": "#/$defs/refs" },
        "reason": { "description": "Rejection or blocker explanation, otherwise null.", "type": ["string", "null"], "minLength": 1, "maxLength": 1024 }
      }
    }
  },
  "$defs": { "refs": { "type": "array", "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "minLength": 1, "maxLength": 1024 } } }
}
