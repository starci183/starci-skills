{
  "schemaVersion": 7,
  "id": "core/handoff-emit",
  "domain": "core",
  "job": "Emit one immutable handoff for one completed capability and one objective.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": ["write one immutable task-session handoff object"],
  "stopConditions": ["artifact identity mismatch", "a mutation resume candidate lacks approval"]
}
