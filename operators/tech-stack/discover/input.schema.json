{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://starci.dev/v7/operators/tech-stack/discover/input.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "operatorId", "context", "input"],
  "properties": {
    "schemaVersion": { "const": 7 },
    "operatorId": { "const": "tech-stack/discover" },
    "context": {
      "type": "object",
      "additionalProperties": false,
      "required": ["manifestRefs", "configurationRefs", "deploymentRefs"],
      "properties": {
        "manifestRefs": { "description": "Exact manifest files resolved by default search at the frozen source fingerprint.", "$ref": "#/$defs/refs" },
        "configurationRefs": { "description": "Exact configuration files resolved by default search at the frozen source fingerprint.", "$ref": "#/$defs/refs" },
        "deploymentRefs": { "description": "Exact deployment files resolved by default search at the frozen source fingerprint.", "$ref": "#/$defs/refs" }
      }
    },
    "input": {
      "type": "object",
      "additionalProperties": false,
      "required": ["project", "objectiveRef", "sourceFingerprint"],
      "properties": {
        "project": { "description": "Project whose observed stack is inventoried.", "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" },
        "objectiveRef": { "description": "Exact objective that requires the inventory.", "type": "string", "minLength": 1, "maxLength": 512 },
        "sourceFingerprint": { "description": "Frozen fingerprint shared by every context reference.", "$ref": "#/$defs/sha256" }
      }
    }
  },
  "$defs": {
    "sha256": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "refs": { "type": "array", "maxItems": 64, "uniqueItems": true, "items": { "type": "string", "minLength": 1, "maxLength": 1024 } }
  }
}
