# interface.draw — brief

Follow operator.md; write its declared paths and your branch response/; replace the running response.json before exit. ? optional; * fallback. Context is limited to what request.json names.

## Done when

Done when the `frontend-art-direction` binds the frozen brief, actual PNG/prompt provenance, Grammar adoption and the selected direction ready to be displayed before frontend implementation.

Primary output: `frontend-art-direction`

## Inputs

`business-promise-authority`?, `backend-source-application`?, `architecture-decision`?, `landing-composition`?, `surface-map`?, `frontend-art-direction`?

## Outputs

Before write/resume, read ../../templates/kinds/<kind>: md .contract.json + .skeleton.md; data .schema.json; artifact follows operator.md. Current-status declared kinds; evidence must exist. Paths: response/.

`frontend-art-direction` `data/art-direction.json` (data)
`art-direction-png` `artifacts/<candidate>.png` (artifact)
`art-direction-prompt` `artifacts/<candidate>.prompt.txt` (artifact)
`knowledge-coverage` `data/knowledge-coverage.json`? (data)
`family-understanding` `data/family-understanding.json`? (data)

## Stops

`INVALID_INPUT`, `SOURCE_DRIFT`, `EVIDENCE_MISSING`, `NO_PROGRESS`, `DIRECTION_CHOICE_REQUIRED`*
