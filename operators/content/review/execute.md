# Execute `content/review`

Use exactly one fresh `gpt-5.6-sol` execution with no inherited turns after the complete required bundle exists. Independently challenge factual accuracy, nuance, prerequisite fit, observable learning outcomes, beginner clarity, Vietnamese or other language quality, interview depth, example coverage, visual claim fidelity, idiomatic cross-track code parity, and immutable E2E proof. Inspect supplied images rather than reviewing their prompt alone and read actual command evidence rather than trusting pass prose.

Approve only when every applicable score is at least 85 and no error remains. Otherwise assign each error to exactly one owner: concept to brainstorm, prose/pedagogy to write, visual to image, implementation to code, or executable contract/harness to E2E. Return the highest-upstream error as the typed revision outcome so invalidated downstream artifacts are rebuilt. Do not repair artifacts inside this operator.
