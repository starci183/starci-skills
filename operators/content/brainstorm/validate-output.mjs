import { validatorFor, runValidatorCli } from '../../validation.mjs';
export const validateOutput=validatorFor(new URL('./output.schema.json',import.meta.url),(v)=>{const e=[];const o=v.output;const dirs=o.dispositions.map(x=>x.direction).sort().join(',');if(dirs!=='add,change,remove')e.push('dispositions must contain add, change, and remove exactly once');if(o.outcome==='ready'&&(!o.briefRef||!o.evidenceRefs.includes(o.briefRef)||!o.learnerInputRefs.length||!o.learnerOutputRefs.length||!o.exampleRefs.length||!o.interviewOutcomeRefs.length||o.reason!==null))e.push('ready brainstorm requires a registered complete brief and no reason');if(o.outcome==='blocked'&&(o.briefRef!==null||!o.reason))e.push('blocked brainstorm requires null brief and reason');return e;});
if(process.argv[1]?.endsWith('validate-output.mjs'))await runValidatorCli(validateOutput,'node validate-output.mjs <artifact.json>');
