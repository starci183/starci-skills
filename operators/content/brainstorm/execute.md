# Execute `content/brainstorm`

Use exactly one fresh `gpt-5.6-sol` execution with no inherited turns. Act as the teacher and instructional designer, not the bulk writer. From exact evidence, define prerequisites, observable learning outcomes, conceptual boundaries, misconceptions, example matrix, interview question/follow-up expectations, behavioral contract, and explicit add/change/remove dispositions. Prefer framework-agnostic mental models unless framework behavior is itself the subject. Write one inspectable brief; do not write the article, generate media, implement code, run tests, or review a completed bundle.

Return only the declared output contract. Block when learner authority or source evidence is insufficient.
