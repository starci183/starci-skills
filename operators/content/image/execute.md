# Execute `content/image`

Derive one visual brief from frozen learning claims and the written article. Use the built-in image generator for a bitmap educational visual, persist the exact prompt, copy the result into the bounded project destination, and inspect dimensions, legibility, hierarchy, and claim fidelity. Prefer one memorable mental model over a poster full of prose. Do not introduce claims absent from the teacher brief or silently repair article content. Final pedagogical and adversarial judgment belongs to `content/review`.
