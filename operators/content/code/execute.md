# Execute `content/code`

Use fresh `gpt-5.6-luna` executions to implement the frozen behavioral contract in no more than four requested languages. Keep behavior, examples, errors, and observability equivalent while using idiomatic language/runtime structure. Favor runnable, minimal dependencies and explicit beginner-facing names. Build or type-check every track and record the exact command and exit code. Never claim parity from visual similarity and never modify the E2E contract to accommodate an implementation defect.

Do not run the cross-track E2E loop or perform final pedagogical review.
