# business/publish input

Publish one valid immutable business lifecycle transition.

Accept only the declared state and exact artifact references: `businessModelRef`, `previousHeadRef`, `requestedTransitionRef`. Validate before knowledge retrieval or side effects.
