# workspace.bind — brief

Follow operator.md; write its declared paths and your branch response/; replace the running response.json before exit. ? optional; * fallback.

## Done when

Done when the `workspace-route-binding` and its `route` name one checkout resolved from the declaration and its hydrated projection alone, with the policy and cleanliness checks required by its write scope, at its observed source head with its derived mutation readiness, and with no runtime bound.

Primary output: `workspace-route-binding`

## Inputs

none

## Outputs

`workspace-route-binding` `response/response.md`
`route` `response/data/route.json`

## Stops

`INVALID_INPUT`, `SOURCE_DRIFT`, `NO_PROGRESS`, `IDENTITY_UNVERIFIED`, `ROUTE_UNDECLARED`, `ROUTE_UNHYDRATED`, `ROUTE_MISMATCH`, `BRANCH_POLICY_VIOLATION`, `CHECKOUT_DIRTY`
