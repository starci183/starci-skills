{
  "schemaVersion": 9,
  "id": "service.operate",
  "domain": "service",
  "job": "Bring one auxiliary service the environment declares — an observability stack, a code-quality service, a tunnel beside a served runtime — to the state that declaration asks for, prove from its own probe that it is in that state, and record the holder that answers, under the lease that orders sessions.",
  "package": "operator.md",
  "errors": "errors.json",
  "validator": "validate.mjs",
  "selfTest": "self-test.mjs",
  "resources": {
    "profile": "sol-fresh",
    "grammarBound": false,
    "mode": "inline",
    "tools": {
      "@tools/fileread": "context-aliases",
      "@tools/shell": "declared-commands",
      "@tools/container": "operate",
      "@tools/http": "probe",
      "@tools/secrets": "resolve-by-name"
    }
  },
  "primaryOutput": "service-receipt"
}
