{
  "schemaVersion": 6,
  "id": "platform/mcp-config",
  "domain": "platform",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "knowledgeRefs": [
    "platform.operations"
  ],
  "sourceReferenceRefs": [],
  "accepts": [
    {
      "stage": "platform.mcp.config",
      "status": "ready",
      "allFacts": [],
      "noneFacts": []
    }
  ],
  "emits": [
    {
      "stage": "platform.mcp.index",
      "status": "ready",
      "factsAdd": [
        "platform-mcp-config-ready"
      ],
      "factsRemove": []
    }
  ],
  "sideEffects": [
    "write only the approved ignored generated MCP execution config",
    "keep input, output, context, observations, and receipts in task-session memory only",
    "purge all intermediate session objects at every parent-skill terminal state"
  ],
  "stopConditions": [
    "required evidence is missing or stale",
    "the result would cross the declared boundary"
  ]
}
