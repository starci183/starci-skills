{
  "schemaVersion": 6,
  "id": "platform/reference-reindex",
  "domain": "platform",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "knowledgeRefs": [],
  "sourceReferenceRefs": [],
  "accepts": [
    { "stage": "platform.mcp.index", "status": "ready", "allFacts": ["platform-mcp-config-ready"], "noneFacts": [] }
  ],
  "emits": [
    { "stage": "platform.mcp.publish", "status": "ready", "factsAdd": ["platform-reference-index-ready"], "factsRemove": [] },
    { "stage": "platform.mcp.publish", "status": "blocked", "factsAdd": [], "factsRemove": [] }
  ],
  "sideEffects": [
    "bootstrap the pinned Python Qdrant Edge and MCP runtime plus loopback Caddy when machine-local readiness is absent",
    "merge only the named Codex user MCP entry without replacing unrelated settings",
    "apply noop, incremental, or full indexing only to the declared local reference partition after complete proof",
    "write generated runtime state only below ignored machine-local state roots",
    "never mutate a reference checkout or any Git index",
    "purge task-session inventories, catalogs, candidate points, and receipts at the parent-skill terminal"
  ],
  "stopConditions": [
    "a selected reference is outside .worktrees/references",
    "a reference checkout is dirty or its revision changes during the run",
    "the adaptive decision cannot be reproduced from complete eligible blob, byte, record, delete, compatibility, and cost evidence",
    "a candidate generation cannot be proved before activation"
  ]
}
