{
  "schemaVersion": 7,
  "id": "platform/observability-reconcile",
  "domain": "platform",
  "job": "Converge one approved metrics stack to its exact scrape, dashboard, filtering, and remote-write boundary.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "apply only the approved observability configuration, service, dashboard, and remote-write delta"
  ],
  "stopConditions": [
    "the approval, configuration fingerprint, or credential capability is stale or mismatched",
    "an undeclared service, scrape target, dashboard, label, or destination would be accessed",
    "a concurrent revision would be overwritten",
    "health, delivery, ordering, retry, or sensitive-data proof fails"
  ]
}
