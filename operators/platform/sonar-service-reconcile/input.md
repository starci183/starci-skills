# platform/sonar-service-reconcile input

Reconcile shared Sonar service and strict project quality gates.

Accept only the declared state and exact artifact references: `projectSetRef`, `qualityProfileRef`, `credentialReceiptRef`. Validate before knowledge retrieval or side effects.
