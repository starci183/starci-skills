{
  "schemaVersion": 7,
  "id": "platform/sonar-service-reconcile",
  "domain": "platform",
  "job": "Converge one approved Sonar project set to its exact profile, gate, and enforcement bindings.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [
    "apply only the approved Sonar project, profile, gate, and enforcement delta"
  ],
  "stopConditions": [
    "the approval, provider revision, or credential capability is stale or mismatched",
    "a project, profile, gate, or setting outside the desired set would be accessed",
    "a concurrent provider revision would be overwritten",
    "post-apply associations cannot be reread and proved"
  ]
}
