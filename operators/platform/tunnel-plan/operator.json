{
  "schemaVersion": 6,
  "id": "platform/tunnel-plan",
  "domain": "platform",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "knowledgeRefs": [
    "platform.tunnel"
  ],
  "sourceReferenceRefs": [],
  "accepts": [
    {
      "stage": "platform.tunnel.plan",
      "status": "ready",
      "allFacts": [],
      "noneFacts": []
    }
  ],
  "emits": [
    {
      "stage": "platform.tunnel.apply",
      "status": "ready",
      "factsAdd": [
        "platform-tunnel-plan-ready"
      ],
      "factsRemove": []
    }
  ],
  "sideEffects": [
    "keep input, output, loads, observations, plans, and evidence in task-session memory only",
    "purge every session object at each parent-skill terminal state"
  ],
  "stopConditions": [
    "required evidence is missing or stale",
    "the result would cross the declared boundary",
    "a source, provider, runtime, DNS, tunnel, or record mutation would occur",
    "an intermediate session object would be persisted"
  ]
}
