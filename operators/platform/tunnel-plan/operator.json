{
  "schemaVersion": 7,
  "id": "platform/tunnel-plan",
  "domain": "platform",
  "job": "Derive one value-free Cloudflare HTTP(S) ingress plan from exact authority and observed resource state.",
  "inputSchema": "input.schema.json",
  "outputSchema": "output.schema.json",
  "sourceReferenceRefs": [],
  "sideEffects": [],
  "stopConditions": [
    "the authority or observed resource revisions are missing or stale",
    "the hostname or tunnel is owned by another declared principal",
    "the requested ingress is not one bounded HTTP or HTTPS route",
    "a credential value or zone-wide discovery would be required"
  ]
}
