{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://starci.dev/v7.0/operators/platform/tunnel-plan/output.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "operatorId", "output"],
  "properties": {
    "schemaVersion": { "const": 7 },
    "operatorId": { "const": "platform/tunnel-plan" },
    "output": {
      "type": "object",
      "additionalProperties": false,
      "required": ["outcome", "planRef", "planSha256", "effects", "conflicts", "evidenceRefs"],
      "properties": {
        "outcome": { "enum": ["ready", "blocked"], "description": "Whether one closed value-free plan was derived; only the Skill machine routes this result." },
        "planRef": { "type": ["string", "null"], "pattern": "^session://tasks/[A-Za-z0-9._-]+/.+$", "description": "Ephemeral exact plan reference when ready; otherwise null." },
        "planSha256": { "anyOf": [{ "$ref": "#/$defs/sha256" }, { "type": "null" }], "description": "Hash binding the exact requested and observed resource tuple when ready; otherwise null." },
        "effects": { "type": "array", "maxItems": 3, "uniqueItems": true, "items": { "enum": ["create-tunnel", "update-tunnel-route", "upsert-proxied-dns"] }, "description": "Minimal Cloudflare effects required for convergence; empty when already converged or blocked." },
        "conflicts": { "type": "array", "maxItems": 8, "uniqueItems": true, "items": { "type": "string", "minLength": 1 }, "description": "Value-safe ownership or revision conflicts; empty when ready." },
        "evidenceRefs": { "$ref": "#/$defs/evidenceRefs", "description": "Authority and exact resource evidence used to derive the plan." }
      }
    }
  },
  "$defs": {
    "sha256": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "evidenceRefs": { "type": "array", "minItems": 1, "maxItems": 16, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } }
  }
}
