# platform/tunnel-plan input

Plan one value-free Cloudflare HTTP tunnel and DNS route.

Accept only the declared state and exact artifact references: `routeReceiptRef`, `hostnameRef`, `originRef`, `ownershipRef`. Validate before knowledge retrieval or side effects.
