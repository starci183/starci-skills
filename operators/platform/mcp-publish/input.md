# platform/mcp-publish input

Publish the read-only MCP boundary and prove routed search.

Accept only the declared state and exact artifact references: `mcpConfigReceiptRef`, `sourceIndexReceiptRef`, `tunnelReceiptRef`. Validate before knowledge retrieval or side effects.
