import {runValidatorCli,validatorFor} from '../../validation.mjs';
const schemaUrl=new URL('./output.schema.json',import.meta.url);
function semantic(v){const e=[];if(v.payload.decision!=='ready'||v.stage!=='platform.mcp.publish'||v.status!=='ready')e.push('/stage: expected ready publish route');if(v.payload.state.status!=='completed'||v.payload.state.code!=='platform-source-index-ready')e.push('/payload/state: mismatch');if(v.payload.state.emits.stage!==v.stage||v.payload.state.emits.status!==v.status)e.push('/payload/state/emits: mismatch');if(!v.facts.includes('platform-source-index-ready')||!v.payload.state.emits.factsAdd.includes('platform-source-index-ready'))e.push('/facts: missing platform-source-index-ready');if(v.payload.produced.indexAction==='unchanged'&&v.payload.produced.durableWrites.length)e.push('/payload/produced/durableWrites: unchanged must not write');if(v.payload.produced.indexAction!=='unchanged'&&!v.payload.produced.durableWrites.length)e.push('/payload/produced/durableWrites: changed index must name write');if(v.payload.cleanup.retention!=='until-skill-terminal'||v.payload.cleanup.purgeAt!=='skill-terminal')e.push('/payload/cleanup: invalid');return e}
export const validateOutput=validatorFor(schemaUrl,semantic);if(process.argv[1]&&import.meta.url===new URL(`file:///${process.argv[1].replaceAll('\\\\','/')}`).href)await runValidatorCli(validateOutput,'usage: node validate-output.mjs <output.json>');
