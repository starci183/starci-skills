# platform/tunnel-apply input

Apply one approved tunnel ingress and proxied DNS mapping.

Accept only the declared state and exact artifact references: `tunnelPlanRef`, `credentialReceiptRef`. Validate before knowledge retrieval or side effects.
