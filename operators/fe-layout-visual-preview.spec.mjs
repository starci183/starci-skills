import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import test from 'node:test';
import { validateOutput } from './fe/layout/validate-output.mjs';
const output = () => ({ schemaVersion: 7, operatorId: 'fe/layout', output: { outcome: 'compiled', result: { summary: 'Responsive geometry compiled for wide, intermediate, and compact viewports.', artifactRefs: ['artifact://layout-geometry', 'artifact://responsive-proof'], directionRef: 'direction://approved', uiLawBindingRef: 'ui-law-binding://profile-v1', uiDetailBindingRef: 'ui-detail-binding://profile-v1', layoutCompilationRef: 'layout-compile://profile-v1', responsiveStates: [{viewport:'wide',stateRef:'state://wide'},{viewport:'intermediate',stateRef:'state://intermediate'},{viewport:'compact',stateRef:'state://compact'}] }, gaps: [], evidenceRefs: ['direction://approved', 'ui-law-binding://profile-v1', 'ui-detail-binding://profile-v1'] } });
test('accepts an evidence-bound layout compilation and rejects legacy review routing', () => { assert.deepEqual(validateOutput(output()), { valid: true, errors: [] }); const routed = output(); routed.output.reviewPreview = { renderer: 'visualize' }; assert.equal(validateOutput(routed).valid, false); });
test('layout owns geometry compilation after mandatory laws and before Grammar Core', () => { const execute = readFileSync(new URL('./fe/layout/execute.md', import.meta.url), 'utf8'); assert.match(execute, /responsive layout geometry/i); assert.match(execute, /AI-first -> Rules-first -> Grammar-last/); assert.match(execute, /separate jobs/); assert.match(execute, /cannot issue or replace a UI-law binding/i); });
