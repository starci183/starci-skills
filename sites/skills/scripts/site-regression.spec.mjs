import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const read=p=>JSON.parse(fs.readFileSync(new URL(p,import.meta.url),'utf8'));
test('site exposes current contracts and all matrix definitions',()=>{const site=read('../src/catalog.generated.json');const source=read('../../../ops/catalog.json');assert.deepEqual(site.operators.map(o=>o.id),source.ops.map(o=>o.id));assert.equal(site.workflows.length,8);assert.ok(site.workflows.every(w=>w.definition));assert.ok(site.operators.find(o=>o.id==='task.execute'));assert.ok(!site.operators.find(o=>o.id==='interface.generate'));});
