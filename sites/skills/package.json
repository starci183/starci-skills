{
  "name": "@starci/skills-site",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:data": "node scripts/generate-data.mjs",
    "predev": "npm run generate:data",
    "dev": "vite",
    "prebuild": "npm run generate:data && npm test",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "node --test scripts/site-regression.spec.mjs"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@starci/grammar": "0.4.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^5.0.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  },
  "description": "The StarCi Skills landing page, generated from the runtime tree."
}
