# Gate runtime router

## LOADS

None.

## Routes

| Evidence | Runtime target |
|---|---|
| A frontend lint rule fired | `fe/lints/context.md` |
| A backend lint rule fired | `be/lints/context.md` |

Run the repository gate first. Route each emitted rule name to its child context; a clean run loads no
child gate law.
