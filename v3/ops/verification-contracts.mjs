import { add, read, step, proof, stop, targetRead, businessRead, repoRead, environmentRead, identityRead, fixtureRead, nodeWrite, evidenceWrite } from './contracts.mjs';

add('uat.plan','Define executable acceptance cases for the selected journeys without running them.','Viết case nghiệm thu chạy được cho journey đã chọn, chưa thực thi.','architecture',[],
 [targetRead(),businessRead(),repoRead(),environmentRead(),read('resources','selected identity and fixture resources, when required','Read existing aliases, isolation policy and fixture definitions without provisioning anything. Anonymous/no-fixture modes are valid when explicitly applicable.','Đọc alias, chính sách cách ly và fixture hiện có, không provision. Anonymous/no-fixture hợp lệ khi đúng ngữ cảnh.')],
 [nodeWrite('.work/<business>/uat/<flow>/node.md + ui/<screen>/node.md + ux/<piece>/node.md',['body: Flow / Actor / Preconditions / Case steps / Expected / Proof / Cleanup / Isolation','refs','dependsOn','assertions','required','state'],'Flow parent states purpose and common preconditions. UI leaves identify screen/state, viewport/theme and appearance criteria. UX leaves identify case/AC, actor, fixture, ordered action, expected output/state, persistence/denial proof and cleanup ownership. Planned leaves remain todo.','Cha flow ghi mục đích/precondition chung. Lá UI ghi screen/state, viewport/theme, tiêu chí appearance. Lá UX ghi case/AC, actor/fixture, action có thứ tự, expected output/state, persistence/denial proof, cleanup owner. Lá dự kiến vẫn todo.'),evidenceWrite('E/case-coverage.md','Record acceptance coverage and safety/isolation review; no run result is emitted for unexecuted cases.','Ghi review AC coverage, an toàn/cách ly; không tạo run result cho case chưa chạy.')],
 [step('business target repo','node','Enumerate selected actor journeys from acceptance, including happy, invalid/denied, interrupted and reload/resume paths where applicable. Distinguish expected assertions from proposed locator strategies.','Liệt kê journey actor từ AC, gồm happy/invalid/denied/gián đoạn/reload khi áp dụng. Tách expected assertion với cách tìm locator dự kiến.'),step('repo environment resources','node','Resolve actual entry routes and documented API/read-back instruments. Record exact environment/actor/fixture IDs, preconditions and allowed effects; do not invent missing accounts or route controls.','Resolve route entry thật, API/read-back có tài liệu; ghi environment/actor/fixture IDs, precondition, effect được phép. Không bịa account/control.'),step('business resources target','node','Write cases before execution: input, action, expected feedback, persistent postcondition, negative outcome and evidence instrument. Keep expected results independent of what the current implementation happens to do.','Viết case trước khi chạy: input/action/feedback mong đợi/postcondition lưu/negative/evidence instrument. Expected độc lập hành vi code hiện tại.'),step('resources environment target','node','Specify namespace and session isolation, cleanup owner and permitted rollback set. Mark independent flows parallel-ready only after checking shared writes; planning does not spawn agents.','Ghi namespace/session isolation, cleanup owner và tập rollback được phép. Chỉ đánh dấu flow song song sau kiểm shared write; plan không spawn.'),step('business target repo resources environment','evidence','Review every scoped AC has a case or explicit accepted deferral and that each case is reproducible from declared prerequisites. Stop after plan.','Review AC trong scope có case hoặc defer thật đã chấp nhận, mỗi case reproduce được từ prerequisite. Dừng sau plan.')],
 [proof('cases','Each case has stable assertion IDs and a concrete expected observation/proof source; no unrun case is marked passed.','Mỗi case có ID assertion ổn định, expected quan sát cụ thể và nguồn proof; không pass case chưa chạy.')],
 [stop('FLOW_UNDEFINED','An in-scope journey lacks a grounded entry, actor, expected result or prerequisite ownership.','Journey trong scope thiếu entry/actor/expected hoặc owner prerequisite có nguồn.')]);

add('interface.audit','Inspect actual rendered selected screens and record truthful visual acceptance independently of implementation.','Xem screen render thật đã chọn và ghi nghiệm thu visual trung thực độc lập implementation.','uat.ui',['authorized navigation/sign-in and non-destructive UI state setup'],
 [targetRead(),businessRead(),repoRead(),environmentRead(),identityRead(),read('design','.work/_resources/design/<resource>/resource.yaml + accepted visual evidence','Read actual accepted design/state/viewport expectations and relevant installed UI proof rules; do not require a nonexistent universal design grammar.','Đọc expected design/state/viewport thật đã chấp nhận và UI proof rule áp dụng; không ép grammar toàn cục không tồn tại.')],
 [nodeWrite('N/node.md',['refs','assertions','completion.inputDigest','completion.evidence','body: Current findings / Unchecked scope'],'Bind environment, actor or anonymous and all served repositories in refs. Record current findings without restyling source or altering acceptance.','Refs gắn environment, actor hoặc anonymous, mọi repo đang serve. Ghi finding hiện tại, không restyle source/đổi acceptance.'),evidenceWrite('E/<screen>.png + E/measurements.json + E/runtime.json + E/review.md','Capture real image bytes, viewport/theme/route/state and actual DOM/computed measurements. Provenance includes environment, actor or anonymous, servedVersions, tool, capturedAt, servedVersionEvidence assertion. Images must be opened and inspected.','Chụp bytes ảnh thật, viewport/theme/route/state, DOM/computed measurement thật. Provenance gồm environment, actor hoặc anonymous, servedVersions, tool, capturedAt, servedVersionEvidence assertion. Phải mở xem ảnh.')],
 [step('target business repo design','node','Select only the requested screen/state matrix and acceptance assertions. Read actual implementation ownership and accepted design; do not substitute a generated concept for the target.','Chọn matrix screen/state và assertion đúng yêu cầu. Đọc owner implementation/design được chấp nhận; không thay target bằng concept AI.'),step('environment identity repo target','evidence','Apply common browser provenance and isolation protocol. Verify actual rendered product login/state and served build, not just URL/HTTP 200. Anonymous paths explicitly omit authenticated identity.','Theo protocol browser provenance/isolation; kiểm login/state product thật và build serve, không chỉ URL/HTTP 200. Anonymous ghi rõ không có actor đăng nhập.'),step('design target environment','evidence','Reach each selected state using observed controls, capture actual viewport/theme and retain local hashed images. Inspect the images for hierarchy, density, text/content, overflow, responsive arrangement and reference fidelity.','Vào từng state bằng control quan sát được, chụp viewport/theme thật, giữ ảnh local có hash. Xem hierarchy/density/text/content/overflow/responsive/fidelity.'),step('design target business','evidence','Measure applicable contrast, computed layout, focus/keyboard behavior and motion/reduced-motion with actual tool observations. Label subjective design judgement separately from numeric measurements.','Đo contrast/layout/focus-keyboard/motion-reduced-motion khi áp dụng bằng tool thật. Tách đánh giá design chủ quan với số đo.'),step('target design','node evidence','Write per-assertion pass/fail/not-run/inconclusive and owner-routed defects. Missing runtime/capture is a blocker; known visual failure remains fail. Validate and stop without source fixes or UAT behavioral certification.','Ghi pass/fail/not-run/inconclusive từng assertion và defect theo owner. Thiếu runtime/ảnh là blocker; lỗi visual thật vẫn fail. Validate rồi dừng, không sửa source hoặc chứng nhận behavior UAT.')],
 [proof('visual','Required UI assertions pass with real inspected local PNG/JPEG/WebP assets and passing served-version observation. Hash integrity alone is insufficient.','Assertion UI bắt buộc pass bằng ảnh PNG/JPEG/WebP local thật đã xem và observation served-version pass. Chỉ hash không đủ.')],
 [stop('CAPTURE_UNAVAILABLE','Selected state cannot be reached/captured on the verified product runtime.','Không vào/chụp được state đã chọn trên runtime product đã xác minh.'),stop('IDENTITY_UNPROVEN','Guarded state lacks authorized actor/custody or real authenticated product content.','State bảo vệ thiếu actor/custody được phép hoặc nội dung product đăng nhập thật.')]);

add('uat.verify','Execute one selected real-product journey and prove its promised interaction and persistence.','Chạy một journey product thật và chứng minh tương tác/persistence đã hứa.','uat.ux',['declared product UI actions within isolated fixture namespace','authorized sign-in; no provisioning or automatic cleanup'],
 [targetRead(),businessRead(),repoRead(),environmentRead(),identityRead(),fixtureRead(),read('cases','.work/<business>/uat/<flow>/**/node.md','Read the prewritten case steps/ACs, UI versus UX scopes and required quality admissions if declared. Do not invent expected results during the run.','Đọc case steps/AC viết trước, scope UI/UX và quality admission nếu khai. Không bịa expected trong lúc chạy.')],
 [nodeWrite('N/node.md',['refs','assertions','completion.inputDigest','completion.evidence','body: Current defects / Unreached assertions / Cleanup ownership'],'Keep requirement and expected case unchanged. Bind environment, actor and served repo resources; retain actual failures and cleanup still owed.','Giữ nguyên requirement/expected case. Gắn resource environment/actor/repo serve; giữ fail thật và cleanup còn nợ.'),evidenceWrite('E/walk.json + E/result.json + E/screens/*.png + E/readback.json + E/runtime.json','Record original planned assertion IDs, actual actions and observed locators, input redactions, screenshots, feedback, persisted read-back/reload observations, exit/result status and provenance. UX includes actual passing kind:behavior assertion only when observed.','Ghi assertion IDs kế hoạch, action/locator quan sát thật, input che bí mật, ảnh, feedback, read-back/reload lưu trữ, result status, provenance. UX chỉ có assertion kind:behavior pass khi thật sự quan sát.')],
 [step('target business cases repo','','Freeze expected cases and input digest before product actions. Confirm scope/allowed mutations and required admissions at applicable source versions; absence is reported, not bypassed.','Chốt expected case/input digest trước action product. Kiểm scope/effect được phép/admission bắt buộc tại source version áp dụng; thiếu thì báo, không bypass.'),step('environment identity fixture repo','evidence','Verify physical runtime/origin, served versions, configuration and actor login into real product content. Check fixture preconditions read-only; do not create accounts, seed missing outcomes or switch to another environment.','Kiểm target runtime/origin/version/config và actor login vào nội dung product thật. Check precondition fixture chỉ đọc; không tạo account/seed outcome/chuyển môi trường.'),step('cases environment identity fixture','evidence','Use actual browser skill/tools or permitted installed Playwright with isolated session and namespace. Discover controls from current DOM/accessibility state and perform ordered UI actions; retain failures where they occur.','Dùng browser skill/tool thật hoặc Playwright cài thật được phép, tách session/namespace. Tìm control từ DOM/accessibility hiện tại, thực hiện UI action có thứ tự; giữ lỗi tại nơi xảy ra.'),step('cases business fixture','evidence','For each expected assertion observe actual response, UI feedback and persistent postcondition; reload/resume or use authorized read-only API/DB corroboration as specified. Direct API writes cannot stand in for UI actions.','Mỗi assertion quan sát response/feedback/postcondition lưu thật; reload/resume hoặc API/DB chỉ đọc được phép theo case. API write không thay UI action.'),step('cases target','evidence','Judge appearance, interaction/experience and persistence separately. Screenshot-only is no behavior pass; unreached steps are not-run, uncertain read-back inconclusive and mismatches fail. Keep actual tool output and inspect captured images.','Chấm appearance/interaction-experience/persistence riêng. Chỉ ảnh không pass behavior; bước chưa tới not-run, read-back chưa rõ inconclusive, sai expected fail. Giữ output tool và xem ảnh.'),step('target fixture','node evidence','Record defects, evidence and exact remaining fixture cleanup responsibility. Perform no unselected cleanup or repair. Validate required assertions; only a fully proven selected leaf can be done. Stop after the result.','Ghi defect/evidence/trách nhiệm cleanup còn lại chính xác. Không tự cleanup/sửa ngoài chọn. Validate assertions; chỉ lá chứng minh đủ mới done. Dừng sau kết quả.')],
 [proof('journey','Actual controls performed the promised action; required AC observations include one passing behavior assertion, served-version proof and persistence/negative checks when required.','Control thật thực hiện action đã hứa; AC bắt buộc có behavior assertion pass, served-version proof và persistence/negative khi yêu cầu.')],
 [stop('PREREQUISITE_UNVERIFIED','Runtime version, actor membership, fixture state or expected case is not grounded.','Runtime version/quyền actor/fixture/expected case thiếu nguồn.'),stop('ACTION_OUTSIDE_SCOPE','Continuing requires broader account/data/service mutation than this journey permits.','Cần mutation account/data/service rộng hơn journey cho phép.')]);

add('quality.verify','Run the selected repository quality gates and report their actual bounded verdict.','Chạy quality gate repo đã chọn và báo verdict thực tế đúng phạm vi.','operations',['selected local verification commands; no source repairs'],
 [targetRead(),repoRead(),read('gates','actual repository scripts/config + selected gate criteria','Read actual commands, thresholds, test scopes and applicable product-change proof. Explicit task scope may authorize normal tests; do not impose a new blanket E2E approval. Shared destructive tests still need scoped authority.','Đọc command/ngưỡng/phạm vi test thật và proof thay đổi product. Scope task có thể cho phép test bình thường, không ép duyệt E2E mới mọi lần; test shared phá dữ liệu vẫn cần quyền đúng scope.')],
 [nodeWrite('N/node.md',['refs','assertions','body: Gate scope / Current findings / Untested limits','completion.inputDigest','completion.evidence'],'List required gates and exact source/config coverage, never project-wide claims from diff-only gates. Accepted debt is separate, never relabels raw failure pass.','Ghi gate bắt buộc, coverage source/config chính xác; gate chỉ diff không claim cả project. Debt được chấp nhận là riêng, không biến raw fail thành pass.'),evidenceWrite('E/gates/<gate>.json + E/logs/<gate>.txt + E/coverage.json','Per gate retain command, cwd/repo, actual commit/tree, configuration/hash, exit, test counts, thresholds and observed metrics, raw redacted output and failure classification.','Mỗi gate giữ command/cwd/repo/commit-tree/config-hash/exit/số test/ngưỡng/số đo/output che bí mật/phân loại fail.')],
 [step('target repo gates','node','Inspect baseline and selected source revisions/configuration before execution. Verify commands exist and distinguish no-test, unavailable runner and a real failing test.','Inspect baseline/revision/config trước chạy. Xác minh command tồn tại; tách không có test, runner không có và test fail thật.'),step('repo gates','evidence','Run required checks on the intended bytes; retain exact command/exit/output. Record all required failures possible without unsafe continuation. No source, threshold or command weakening.','Chạy check bắt buộc trên bytes đích; giữ command/exit/output thật. Ghi mọi fail bắt buộc có thể thu an toàn. Không sửa source/giảm ngưỡng/đổi lệnh dễ hơn.'),step('gates repo','evidence','Compare each metric to its configured/requested threshold and applicable scope. Preserve absent thresholds as unconfigured, not zero; no-test success is not test coverage.','So metric với ngưỡng cấu hình/yêu cầu và scope áp dụng. Ngưỡng thiếu là unconfigured, không phải 0; lệnh không có test thành công không chứng minh coverage.'),step('target repo gates','node evidence','Classify source regression, baseline debt, environment block and suspected flakiness using diagnostics. Report raw outcomes and any approved exception separately; validate and stop, no repair chain.','Phân loại regression/debt baseline/block môi trường/nghi flaky từ diagnostic. Báo raw outcome và ngoại lệ được phép riêng; validate và dừng, không chain sửa.')],
 [proof('gates','Each required check has actual observed output and a passing criterion; failed/unavailable checks cannot complete a required acceptance leaf.','Mỗi check bắt buộc có output quan sát thật và đạt tiêu chí; check fail/unavailable không hoàn tất lá acceptance bắt buộc.')],
 [stop('GATE_UNAVAILABLE','Required command/config/runtime cannot run as declared.','Command/config/runtime bắt buộc không chạy như khai.'),stop('SOURCE_CHANGED','Source or configuration changed during measurements, making results incoherent.','Source/config đổi trong lúc đo làm result không đồng nhất.')],{placeholders:{gate:'Declared repository gate ID / ID gate repo đã khai'}});

add('api.verify','Verify a selected API contract against the real served target with isolated authorized effects.','Kiểm API contract đã chọn trên target thật với effect được phép và cách ly.','uat.ux',['declared API test writes within owned namespace; no provisioning, deploy or broad cleanup'],
 [targetRead(),businessRead(),repoRead(),environmentRead(),identityRead(),fixtureRead(),read('suite','actual repository API test suite + selected architecture contract','Read real suite command/cases, API schema/auth/error/transaction expectations and selected required assertions. Missing suite is a source-owner gap, not fabricated passing cases.','Đọc suite command/case thật, schema/auth/error/transaction và assertion bắt buộc. Thiếu suite là gap owner source, không bịa case pass.')],
 [nodeWrite('N/node.md',['refs','assertions','completion.inputDigest','completion.evidence','body: Contract/data/lifecycle findings'],'Bind actor/environment/repositories, preserve selected API expectations and name untested or failed cases.','Gắn actor/environment/repo, giữ expected API đã chọn, ghi case fail/chưa test.'),evidenceWrite('E/api-result.json + E/api-output.txt + E/readback.json + E/runtime.json','Store actual suite cases and result output, status/error/schema checks, read-back and lifecycle observations, namespace effects and runtime provenance. API proof is not UI proof.','Lưu case/result suite thật, status/error/schema check, read-back/lifecycle, effect namespace, runtime provenance. Proof API không phải proof UI.')],
 [step('target business suite repo','','Bind required assertions to actual suite cases and verify test/runner revision. Identify undeclared effects before running, without rewriting product cases here.','Gắn assertion bắt buộc với case suite thật, kiểm revision test/runner. Nhận diện effect chưa khai trước chạy; không sửa case product tại đây.'),step('environment identity fixture repo','evidence','Verify target physical/runtime version, authorization and owned fixture preconditions; distinguish anonymous cases from authenticated actors. Do not seed tested outcomes.','Kiểm target vật lý/runtime version/quyền/precondition fixture sở hữu; phân biệt case anonymous và actor đăng nhập. Không seed outcome đang test.'),step('suite environment identity fixture','evidence','Execute the actual declared API client suite with isolated inputs and preserve redacted request/result diagnostics. Copy only cases the runner reported; missing output is not-run or inconclusive.','Chạy API client suite khai thật với input cách ly, giữ diagnostic request/result đã che. Chỉ ghi case runner báo; output thiếu là not-run/inconclusive.'),step('business suite fixture','evidence','Check response contract, persistent read-back and lifecycle/authorization independently, including replay/idempotency or denial where promised. Observe owned effects and cleanup status; do not delete outside an explicitly selected safe set.','Kiểm response contract/read-back lưu/lifecycle-quyền riêng, gồm replay/idempotency/denial khi hứa. Quan sát effect sở hữu và cleanup; không xóa ngoài tập an toàn được chọn rõ.'),step('target suite','node evidence','Record per-assertion verdict and actual served-version evidence, validate bindings and finish without UI claims or source repair.','Ghi verdict từng assertion và served-version evidence thật, validate binding, kết thúc không claim UI/sửa source.')],
 [proof('api','Required contract/data/lifecycle observations pass with actual behavior assertion and verified served build; no guessed API case or unconditional HTTP-200 success.','Observation contract/data/lifecycle bắt buộc pass, có behavior assertion thật và build serve xác minh; không đoán case hoặc coi HTTP 200 luôn thành công.')],
 [stop('SUITE_MISSING','Required API behavior has no executable declared case or cannot be reached safely.','Behavior API bắt buộc thiếu case thực thi đã khai hoặc không vào an toàn.'),stop('NAMESPACE_CONFLICT','Test data overlaps shared/foreign state or effect authorization is insufficient.','Dữ liệu test chồng shared/foreign state hoặc thiếu quyền effect.')]);

add('business.reconcile','Compare the accepted business tree with delivered source and real acceptance evidence without rewriting requirements.','Đối chiếu cây nghiệp vụ đã chấp nhận với source giao và proof nghiệm thu thật, không sửa yêu cầu.','operations',[],
 [targetRead(),businessRead(),repoRead(),read('proofs','selected implementation/UAT/release nodes and original evidence bundles','Read original assets, assertion observations, completion digests, commit lineage and test/runtime revisions; chat summaries are navigation hints, not acceptance proof.','Đọc asset gốc, observation assertion, completion digest, lineage commit và revision test/runtime; tóm tắt chat chỉ gợi ý nơi tìm, không phải proof.')],
 [nodeWrite('N/node.md',['body: Requirement-to-delivery matrix / Discrepancies / Deferred scope / Current decision','refs','assertions','completion.inputDigest','completion.evidence'],'For each AC list expected, enforcing source, proving evidence, actual, freshness and gap owner; no business done/implemented flag is copied over an incomplete tree.','Mỗi AC ghi expected/source enforce/evidence/actual/freshness/gap owner; không chép nhãn business done/implemented lên cây thiếu.'),evidenceWrite('E/reconciliation.json','Record requirement IDs, source/evidence refs, actual verification and discrepancy list; preserve deferred and untested work explicitly.','Ghi requirement ID, source/evidence ref, kiểm chứng thật, danh sách chênh lệch; giữ scope defer/chưa test rõ.')],
 [step('target business proofs repo','','Resolve original requirement/evidence versions and delivered repository commits; reject stale/missing refs before claiming enforcement.','Resolve version requirement/evidence gốc, commit repo giao; loại ref stale/thiếu trước khi claim enforce.'),step('business repo proofs','evidence','Compare each required business dimension to delivered code and independent observations, including denial and persistence where required. Existing tests proving another behavior do not close this AC.','So từng dimension bắt buộc với code giao và observation độc lập, gồm denial/persistence khi yêu cầu. Test hành vi khác không đóng AC này.'),step('business proofs target','node evidence','Record pass, discrepancy, stale and unproven rows plus named owners; keep implementation state and UAT result separate. Never edit expected to match actual or waive missing scope without a real decision.','Ghi pass/chênh/stale/chưa chứng minh kèm owner; tách trạng thái implementation và UAT. Không đổi expected theo actual hoặc waive scope thiếu tùy ý.'),step('target proofs','node evidence','Validate all refs and report the derived business completion and remaining leaves. Stop; proposing a next repair does not execute it.','Validate ref, báo completion business suy ra và lá còn lại. Dừng; đề xuất sửa tiếp không có nghĩa thực thi.')],
 [proof('reconciliation','Every selected required AC is genuinely supported by current source/evidence; unresolved rows remain visible and cannot certify the parent complete.','AC bắt buộc đã chọn có source/evidence hiện tại thật; dòng chưa xử lý vẫn hiện, không chứng nhận cha xong.')],
 [stop('DELIVERY_UNPROVEN','A required claim has no coherent code/evidence revision or contradicts observed behavior.','Claim bắt buộc thiếu revision code/evidence nhất quán hoặc mâu thuẫn quan sát.')]);

add('workflow.verify','Inspect selected peer/piece outcomes against one task purpose without running or changing their work.','Kiểm kết quả peer/piece đã chọn so với một mục đích task, không chạy/sửa việc của peer.','operations',[],
 [targetRead(),read('assignments','actual selected task/piece assignments + original node IDs','Read the explicitly selected peer set, exact goals, owned write scopes and delivered result locations. Do not infer another task identity from a title or send a self-message as peer delivery.','Đọc peer set được chọn rõ, goal chính xác, write scope và nơi result giao. Không suy task identity từ title hoặc coi tự gửi mình là đã gửi peer.'),repoRead(),read('proofs','selected peers original .work nodes and evidence','Read original current requirement/code/evidence, not a summarized pass. Task messages may locate proof but cannot replace it.','Đọc requirement/code/evidence gốc hiện tại, không tóm tắt pass. Message task chỉ chỉ nơi proof, không thay proof.')],
 [nodeWrite('N/node.md',['body: Purpose coverage / Peer outcomes / Integration / Remaining gaps','refs','assertions','completion.inputDigest','completion.evidence'],'Map task done-when to selected peer node IDs and actual repository delivery. Record missing peer proof, conflicting ownership and integration gaps without modifying their states.','Map done-when task sang peer node IDs và repo giao thật. Ghi proof peer thiếu, ownership xung đột, gap integration; không sửa state peer.'),evidenceWrite('E/portfolio-review.json','Retain source/evidence identity checks and task-to-piece coverage; no invented delivery receipt or automatic follow-up message.','Giữ check identity source/evidence và coverage task–piece; không bịa biên nhận giao hoặc tự nhắn follow-up.')],
 [step('assignments target','node','Freeze the selected purpose/peer set for this review and resolve exact node and repository identities. A missing peer locator is unknown, not permission to search unrelated tasks.','Chốt purpose/peer set đã chọn cho review, resolve node/repo chính xác. Thiếu locator peer là unknown, không tự tìm task không liên quan.'),step('proofs repo assignments','evidence','Read each original outcome and actual delivered bytes/commit; compare every assigned goal line and required assertion with its proof. Verify integration lineage and current versions separately.','Đọc outcome gốc và bytes/commit giao thật; so goal/assertion bắt buộc với proof. Kiểm lineage integration và version hiện tại riêng.'),step('target assignments proofs','node evidence','Report aggregate scope coverage, absent/failed/stale evidence and cross-piece conflicts. Preserve peer states, source, runtimes and messages; finish without dispatching another op.','Báo coverage scope tổng, proof thiếu/fail/stale và xung đột cross-piece. Giữ state peer/source/runtime/message; xong không dispatch op khác.')],
 [proof('portfolio','Every claimed peer contribution has direct inspectable proof and purpose mapping; a green summary alone is rejected.','Mỗi đóng góp peer được claim có proof trực tiếp xem được và mapping purpose; chỉ tóm tắt xanh bị loại.')],
 [stop('PEER_PROOF_MISSING','Selected peer identity, original evidence or actual delivery revision cannot be resolved.','Identity peer/evidence gốc/revision giao của peer được chọn không resolve được.')]);
