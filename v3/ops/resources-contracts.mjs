import { add, read, step, proof, stop, targetRead, businessRead, architectureRead, repoRead, environmentRead, identityRead, fixtureRead, nodeWrite, evidenceWrite, resourceWrite } from './contracts.mjs';

add('workspace.bind','Bind the selected repository and canonical .work location to verified local identities.','Gắn repo và nơi .work canonical đã chọn với identity local xác minh được.','operations',[],
 [targetRead(),read('declaration','current explicit repository scope + .work/_resources/repositories/<resource>/resource.yaml','Read explicit task location/repository selection and existing portable identity. A directory name, cwd or similar remote is only a hint when identity is unresolved.','Đọc lựa chọn repo/vị trí rõ trong task và identity portable hiện có. Tên thư mục/cwd/remote gần giống chỉ là gợi ý khi chưa resolve identity.')],
 [nodeWrite('N/node.md',['body: Repository binding / Work root / Source state / Readiness / Limits','refs','assertions'],'Record stable repo identity and observed checkout/head separately, permitted source write ceiling and canonical work-root location; do not mistake Git worktree for durable evidence storage.','Ghi identity repo ổn định, checkout/head quan sát riêng, ceiling ghi source được phép và work-root canonical; không nhầm Git worktree với nơi evidence bền.'),resourceWrite('repositories',['identity','remote','defaultRef','sourceRoots','workRootRef'],'Create/repair only the explicitly selected repository declaration from verified Git metadata; machine-specific checkout paths stay local or observations, not portable identity.','Chỉ tạo/sửa declaration repo đã chọn từ Git metadata xác minh; path checkout riêng máy là local/observation, không phải identity portable.'),evidenceWrite('E/repository-observation.json','Record actual Git root/common-dir, branch/full HEAD, worktree registration, dirty paths and link targets; secrets in remote URLs must be removed.','Ghi Git root/common-dir, branch/HEAD đủ, worktree registration, dirty paths, link targets thật; che bí mật trong remote URL.')],
 [step('declaration target','evidence','Resolve exact Git root/common directory and remote identity using read-only Git inspection. Inspect existing .work ownership and avoid creating a second canonical tree.','Resolve Git root/common-dir/remote identity bằng Git chỉ đọc; inspect owner .work hiện có, tránh tạo cây canonical thứ hai.'),step('declaration','evidence','Inspect actual branch/HEAD, registered worktree and unrelated dirty paths; resolve source links/junctions before suggesting a write ceiling. Do not stash/reset/clean/switch/remove.','Inspect branch/HEAD/worktree đã register/dirty ngoài scope; resolve link/junction trước ceiling ghi. Không stash/reset/clean/switch/remove.'),step('target declaration','node resource','Bind only verified identity and chosen portable refs, preserving user scope and existing IDs. Record missing identity or conflicting roots together.','Chỉ gắn identity đã xác minh, ref portable đã chọn; giữ scope người dùng và ID. Ghi cùng lúc identity thiếu/root xung đột.'),step('target declaration','evidence','Validate references and report checkout readiness separately from runtime readiness; no server, account, Git worktree or platform task is created. Stop.','Validate refs, báo readiness checkout tách runtime; không tạo server/account/Git worktree/task nền tảng. Dừng.')],
 [proof('identity','Observed Git identity matches selected repo scope and canonical .work binding; unresolved identity cannot become an invented resource.','Identity Git quan sát khớp scope repo và binding .work canonical; identity chưa rõ không thành resource bịa.')],
 [stop('REPOSITORY_AMBIGUOUS','Explicit selection and observed root/remote disagree or multiple canonical work roots remain.','Lựa chọn rõ và root/remote quan sát khác nhau hoặc còn nhiều work-root canonical.')]);

add('environment.preflight','Collect readiness and blockers for exactly the selected development/test effects, repairing nothing.','Thu readiness/blocker cho đúng effect phát triển/test đã chọn, không tự sửa.','operations',['read-only probes; no infrastructure/account changes'],
 [targetRead(),repoRead(),environmentRead(),read('capabilities','available tools/runtime + selected identity/fixture/service declarations','Inspect actual browser/Playwright availability when needed, declared commands, custody handles by name and target service probes. Do not open unrelated secrets.','Inspect browser/Playwright có thật khi cần, command khai, custody handle bằng tên, probe dịch vụ đích. Không mở bí mật không liên quan.')],
 [nodeWrite('N/node.md',['body: Required capabilities / Readiness / Blockers / Untested','refs','assertions'],'One row per scoped prerequisite: needed capability, declaration, actual observation, ready/blocked/not-needed, owner and exact remedy. Readiness is time-bound evidence, not lasting global green.','Một dòng mỗi prerequisite trong scope: capability cần, declaration, quan sát thật, ready/blocked/not-needed, owner, cách gỡ cụ thể. Readiness là evidence có thời điểm, không xanh toàn cục mãi.'),evidenceWrite('E/readiness.json','Collect all safely inspectable checks, with observed timestamp and source/config/environment revision; dependent checks unavailable due to earlier blocker remain not-run.','Thu mọi check inspect an toàn, có thời điểm và revision source/config/environment; check phụ thuộc bị chặn vẫn not-run.')],
 [step('target repo environment capabilities','node','Derive prerequisite list from the selected op/effect set, not the whole development cycle. Inspect target identity and read permissions first.','Suy prerequisite từ op/effect đã chọn, không cả vòng đời. Kiểm identity target/quyền đọc trước.'),step('repo capabilities','evidence','Inspect actual checkout, dependencies and command/browser capability without installing or repairing. Report unrelated dirt without deleting it.','Inspect checkout/dependency/command/browser thật, không install/sửa. Báo dirty ngoài scope, không xóa.'),step('environment capabilities','evidence','Probe declared endpoints/build identity, credential custody availability by name, fixture ownership and permitted effect scope. Sign-in mutation is not assumed if task only authorizes passive diagnostics.','Probe endpoint/build identity khai, custody sẵn qua tên, owner fixture và scope effect được phép. Không mặc định sign-in mutation nếu task chỉ passive diagnostic.'),step('target repo environment capabilities','node evidence','Record the complete blocker set and skipped dependent checks without stopping at the first issue. Validate and finish read-only; no automatic runtime/account/fixture op.','Ghi đủ blocker/check phụ thuộc chưa chạy, không chỉ lỗi đầu. Validate rồi kết thúc chỉ đọc; không tự gọi runtime/account/fixture op.')],
 [proof('readiness','Every needed prerequisite has a recent actual observation; absent capability stays blocked/not-run, not guessed ready.','Mỗi prerequisite cần có quan sát thật gần đây; capability thiếu vẫn blocked/not-run, không đoán ready.')],
 [stop('ENVIRONMENT_NOT_READY','A required scoped capability or target cannot be verified.','Capability/target bắt buộc đúng scope không xác minh được.')]);

add('identity.provision','Inspect or apply the explicitly selected identity/account change and prove the resulting actor.','Inspect hoặc áp dụng thay đổi identity/account được chọn rõ, chứng minh actor kết quả.','operations',['only explicitly authorized account create/repair/rotation; existing account use is not creation authority'],
 [targetRead(),businessRead(),environmentRead(),identityRead(),read('authority','current explicit account action + provider/admin custody declaration','Read exact create/repair/rotate/inspect scope, provider realm/tenant, expected role/membership and existing authoritative account lookup. UAT access alone does not permit creating/resetting users.','Đọc scope create/repair/rotate/inspect chính xác, provider realm/tenant, role/membership mong đợi, lookup account thẩm quyền. Quyền UAT không tự cho tạo/reset user.')],
 [nodeWrite('N/node.md',['body: Requested identity action / Desired actor / Checks / Limits','refs','assertions'],'Separate provider account creation, role assignment, backend membership and real product login; mark each unknown/failure honestly.','Tách tạo account provider, gán role, membership backend, login product thật; ghi unknown/fail từng phần trung thực.'),resourceWrite('identities',['environment','alias','provider','subject','roles','memberships','credentialRef','custodian','lifecycle'],'Store only verified non-secret identity/custody metadata. Keep environment-specific subjects distinct; never duplicate aliases to hide an uncertain existing account.','Chỉ lưu metadata identity/custody không bí mật đã xác minh. Tách subject theo environment; không tạo alias trùng để che account hiện có chưa rõ.'),evidenceWrite('E/identity-checks.json + E/login.png when safe + E/effect.json','Record actual before/after account IDs, role/membership checks, real product-login verification and custody resolution by name. Omit credentials/tokens/cookies and secret-bearing screenshots.','Ghi account ID trước/sau thật, check role/membership, login product thật, resolve custody qua tên. Không credential/token/cookie/ảnh chứa bí mật.')],
 [step('target authority environment identity business','','Confirm exact authorized action and expected actor permissions. Inspect existing account by provider subject/alias; uncertainty triggers read-only reconciliation, not another create.','Xác nhận action được phép và quyền actor mong đợi. Inspect account theo subject/alias; chưa rõ thì đối soát chỉ đọc, không create thêm.'),step('authority identity environment','evidence','Resolve sealed custody using available permitted secret mechanism without printing values; inspect realm/tenant, membership source and credential lifecycle ownership.','Resolve sealed custody qua cơ chế được phép có thật, không in giá trị; inspect realm/tenant, nguồn membership, owner vòng đời credential.'),step('authority identity business environment','resource evidence','Apply only requested missing/invalid owned identity changes. Reuse valid accounts; do not add admin privileges, reset passwords or edit another tenant as a workaround. Inspect-only mode performs no provider writes.','Chỉ sửa identity thuộc quyền đúng phần thiếu/sai được yêu cầu. Dùng account hợp lệ; không thêm admin/reset password/sửa tenant khác để workaround. Inspect-only không ghi provider.'),step('identity environment business','evidence','Verify provider subject/roles and normal backend membership independently, then actual product login if requested. A token or redirected URL alone does not prove the actor reached intended product content.','Kiểm subject/role provider và membership backend bình thường độc lập; login product thật nếu yêu cầu. Chỉ token/URL redirect không chứng minh actor vào đúng nội dung.'),step('target authority identity','node resource evidence','Publish names/refs and actual observed action outcome; rotation additionally proves new custody works and obsolete credential rejection only when rotation is selected. Stop without fixture creation.','Ghi tên/ref và outcome action quan sát thật; rotation kiểm custody mới hoạt động/credential cũ bị từ chối khi được chọn. Dừng, không tạo fixture.')],
 [proof('actor','Selected desired identity state and required real login/membership checks pass; provider existence alone cannot certify complete UAT actor readiness.','State identity mong đợi và login/membership bắt buộc pass; chỉ account provider tồn tại không đủ chứng nhận actor UAT.')],
 [stop('ACCOUNT_AUTHORITY_MISSING','The next identity mutation exceeds the explicit selected account action.','Mutation identity tiếp theo vượt action account đã chọn rõ.'),stop('CUSTODY_UNAVAILABLE','Approved sealed credential mechanism or account ownership cannot be verified.','Không xác minh được cơ chế sealed credential được phép hoặc owner account.')]);

add('data.plan','Define attributable fixtures, expected preconditions and exact safe cleanup for selected tests.','Định nghĩa fixture quy thuộc được, precondition và cleanup an toàn chính xác cho test đã chọn.','architecture',[],
 [targetRead(),businessRead(),architectureRead(),repoRead(),environmentRead(),read('cases','.work/<business>/uat/<flow>/**/node.md','Read selected expected outcomes and initial state to distinguish prerequisites from outcomes that the test itself must produce.','Đọc expected outcome và state đầu của test đã chọn để tách prerequisite với outcome test phải tự tạo.')],
 [nodeWrite('N/node.md',['body: Fixture purpose / Preconditions / Isolation / Placement / Verification / Cleanup','refs','dependsOn','assertions'],'State requested representative volume/content classes, ownership, exact namespace rules, expected initial state and safety limits; plan alone places no records.','Ghi volume/lớp content đại diện được yêu cầu, owner, luật namespace chính xác, initial state, giới hạn an toàn; plan không đặt record.'),resourceWrite('fixtures',['environment','namespace','actor','inputs','targets','expected','placement','cleanup','owner'],'Define resource refs to fixture bytes/scripts with revision, target entities/API paths, natural keys/IDs, ownership predicate, allowed update/delete set and cleanup owner. Use only schema-grounded fields.','Khai ref tới bytes/script fixture có revision, entity/API đích, natural key/ID, ownership predicate, tập update/delete cho phép, cleanup owner. Chỉ field có nguồn schema.'),evidenceWrite('E/fixture-plan-review.json','Retain schema/namespace/AC comparison proving fixture does not pre-create tested outcomes and deletion scope is attributable.','Giữ đối chiếu schema/namespace/AC chứng minh không seed outcome đang test và tập xóa quy thuộc được.')],
 [step('cases business repo architecture','node','Identify exactly which initial records and volume classes the selected assertions need; inspect actual schema and normal product creation APIs.','Xác định record ban đầu/lớp volume assertion cần, inspect schema thật và API tạo product bình thường.'),step('repo environment cases','resource','Assign disjoint namespaces, stable ownership predicates and creation/update methods. No schema change solely to make cleanup convenient; missing attribution is a design blocker.','Giao namespace tách biệt, ownership predicate ổn định, cách create/update. Không đổi schema chỉ để dễ cleanup; thiếu attribution là blocker thiết kế.'),step('cases business architecture','node resource','Specify fixture bytes, order/dependencies, expected read-back and rollback/deletion set. Existing shared rows are read-only unless separately authorized.','Viết bytes fixture, thứ tự/dependency, read-back mong đợi, tập rollback/xóa. Row shared hiện có chỉ đọc nếu chưa được cho phép riêng.'),step('target repo cases environment','evidence','Review schema validity, expected-versus-fixture separation, parallel collisions and cleanup subset safety. Finish the plan without seeding or provisioning.','Review schema, tách expected/fixture, collision song song và an toàn cleanup subset. Kết thúc plan, không seed/provision.')],
 [proof('fixture-plan','Inputs satisfy preconditions without synthesizing the tested result; exact data ownership and cleanup set are demonstrable.','Input thỏa precondition nhưng không dựng kết quả đang test; owner dữ liệu và tập cleanup chứng minh được.')],
 [stop('FIXTURE_UNATTRIBUTABLE','Required fixture rows cannot be isolated or cleanup cannot be restricted safely.','Không cách ly được row fixture hoặc không giới hạn cleanup an toàn.')]);

add('data.seed','Apply or clean only the selected owned fixture set and verify its exact observed state.','Apply hoặc dọn đúng fixture sở hữu đã chọn và kiểm state quan sát chính xác.','operations',['explicit fixture placement/update or exact authorized cleanup subset'],
 [targetRead(),fixtureRead(),environmentRead(),identityRead(),repoRead(),read('authority','current fixture operation scope + actual source-owned fixture script','Read selected inspect/apply/cleanup action and exact script/input revision; account provisioning, schema migration and broad database restore are outside this scope.','Đọc action inspect/apply/cleanup được chọn, revision script/input thật; provision account/migration schema/restore DB rộng ngoài scope.')],
 [nodeWrite('N/node.md',['body: Selected fixture action / Actual records / Checks / Remaining cleanup','refs','assertions'],'Record actual placed/reused/removed rows and remaining cleanup owner. A planned row list is not actual created data.','Ghi row thật đã đặt/dùng lại/xóa, cleanup owner còn lại. Danh sách row dự kiến không phải dữ liệu đã tạo.'),resourceWrite('fixtures',['placementEvidence','cleanupEvidence'],'Link actual placement/cleanup evidence to the existing fixture resource without modifying its intended outcomes.','Gắn evidence placement/cleanup thật vào resource fixture hiện có, không đổi outcome dự kiến.'),evidenceWrite('E/records.json + E/before-after.json + E/fixture-output.txt','For each row record store/key, namespace/owner, operation/outcome and read-back; cleanup receipt records only proven owned removed IDs. Redact sensitive fixture content.','Mỗi row ghi store/key/namespace-owner/operation-outcome/read-back; cleanup chỉ ghi ID đã chứng minh sở hữu bị xóa. Che content fixture nhạy cảm.')],
 [step('fixture authority environment identity repo','','Validate input/script revision, exact target, actor and mutation scope. Read-only inspect may proceed without assuming apply/cleanup authority.','Validate revision input/script, target/actor/scope mutation chính xác. Inspect chỉ đọc không tự coi có quyền apply/cleanup.'),step('fixture environment authority','evidence','Inspect every target key and classify matching/missing/invalid/foreign/uncertain records before changing any. Foreign or uncertain ownership is never overwritten/deleted.','Inspect key đích, phân loại đúng/thiếu/sai/foreign/chưa chắc trước sửa. Không overwrite/xóa row foreign hoặc owner chưa chắc.'),step('fixture authority repo environment','evidence','Apply only approved missing/invalid owned inputs through declared normal API or reviewed fixture runner; cleanup only selected exact owned set after fresh validation. Never seed the tested outcome or reapply uncertain mutations.','Chỉ apply input sở hữu thiếu/sai được cho phép qua API bình thường/runner fixture đã xem; cleanup đúng tập sở hữu sau validate mới. Không seed outcome đang test/reapply mutation chưa rõ.'),step('fixture environment target','node resource evidence','Read back actual records or prove selected removal, preserve shared rows, record partial effects and remaining cleanup. Validate and stop without executing UAT.','Read-back record thật hoặc chứng minh xóa đã chọn, giữ row shared, ghi effect một phần và cleanup còn lại. Validate rồi dừng, không chạy UAT.')],
 [proof('fixture-state','Actual read-back equals planned preconditions, or exact owned deletion is verified; partial/uncertain effects cannot be declared clean.','Read-back thật khớp precondition, hoặc xóa đúng tập sở hữu được kiểm; effect một phần/chưa chắc không được gọi sạch.')],
 [stop('FIXTURE_AUTHORITY_MISSING','Selected operation lacks permission for its exact data target/effect.','Operation chọn thiếu quyền cho data target/effect chính xác.'),stop('FIXTURE_SHARED_ROW','Mutation would affect foreign/shared or unattributable records.','Mutation chạm row foreign/shared hoặc không quy thuộc được.')]);

add('runtime.serve','Bring one selected development runtime to the authorized state and prove the actual served build.','Đưa một runtime phát triển đã chọn tới state được phép và chứng minh build serve thật.','operations',['only authorized start/stop/restart/build or integration action on owned runtime'],
 [targetRead(),repoRead(),environmentRead(),read('runtime','.work/_resources/runtimes/<resource>/resource.yaml + selected source command/config','Read exact runtime owner, command, checkout/build, endpoints, process/container identity, declared desired action and health/version probes. Integration merge is not implied by serving.','Đọc owner runtime, command, checkout/build, endpoint, process/container identity, action mong đợi, health/version probe. Serve không mặc định merge integration.')],
 [nodeWrite('N/node.md',['body: Desired runtime / Allowed effects / Source mapping / Actual health / Limits','refs','assertions'],'Keep code/build/served identity distinct and no UAT/release readiness claim from a healthy process.','Tách identity code/build/serve, process khỏe không chứng nhận UAT/release.'),resourceWrite('runtimes',['environment','commandRef','sourceRef','endpoints','probeRefs','custodyRefs','observationRef'],'Update only declared owned runtime refs and observed-state pointer. Record local PID/session handles in ignored local state or bounded evidence, not durable identity.','Chỉ cập nhật ref runtime sở hữu và con trỏ observation. PID/session local ở local ignore hoặc evidence hữu hạn, không là identity bền.'),evidenceWrite('E/runtime.json + E/probes.json + E/effects.json + E/build-output.txt when built','Record before/after generation, target/process ownership, actual build command/output, full source refs, immutable served artifact, endpoint probes and effect status.','Ghi generation trước/sau, owner target/process, build command/output thật, source refs đủ, artifact serve bất biến, probe endpoint, effect status.')],
 [step('target runtime environment repo','evidence','Inspect current process/container holder, source/build revision, ports and health. Reuse an already matching healthy runtime without restart.','Inspect process/container holder, revision source/build, port/health. Runtime đã khớp khỏe thì dùng lại không restart.'),step('runtime environment target','','Check selected desired action and allowed effects. A foreign process/port is not permission to kill it or silently choose another port/origin.','Kiểm action mong đợi/effect được phép. Process/port foreign không cho phép kill hoặc tự chọn port/origin khác.'),step('runtime repo environment','resource evidence','Perform only the exact authorized runtime change and bounded recovery. If integration is explicitly included, inspect ownership and conflict scope, preserve actual resulting commit and rerun affected gates; otherwise do not merge.','Chỉ thay đổi runtime được cho phép và recovery hữu hạn. Nếu có integration rõ thì inspect owner/conflict scope, giữ commit kết quả thật và rerun gate; nếu không, không merge.'),step('runtime environment repo','evidence','Probe actual served source/build identity, endpoints and stability using declared checks. A started PID or build success alone does not prove the target is serving the selected code.','Probe identity source/build đang serve, endpoint/stability theo check khai. Chỉ PID start hoặc build thành công không chứng minh target serve code đã chọn.'),step('target runtime','node resource evidence','Record actual state, partial effects and source-versus-served mapping, then stop. No account/fixture/deploy action is automatically performed.','Ghi state/effect một phần/mapping source–serve thật rồi dừng. Không tự account/fixture/deploy.')],
 [proof('runtime','Declared health and served-version evidence are observed on the actual target with correct owned generation.','Health/served-version khai được quan sát trên target thật, generation sở hữu đúng.')],
 [stop('RUNTIME_OWNER_CONFLICT','Target/process/port is foreign, ambiguous or changed concurrently.','Target/process/port foreign, mơ hồ hoặc đổi đồng thời.'),stop('RUNTIME_EFFECT_UNAUTHORIZED','Required start/reset/merge/configuration change exceeds selected action.','Start/reset/merge/config cần vượt action chọn.')]);

add('service.operate','Inspect or change one declared auxiliary service and verify its requested state.','Inspect hoặc đổi một dịch vụ phụ trợ đã khai và kiểm state yêu cầu.','operations',['explicit owned auxiliary-service lifecycle action only'],
 [targetRead(),environmentRead(),read('service','.work/_resources/services/<resource>/resource.yaml','Read exact declared service, owner, desired state, command/config refs, target identity, probes and allowed recovery; never infer service ownership from a listening port.','Đọc service/owner/desired state/command-config refs/target/probe/recovery được phép; không suy owner từ port đang nghe.')],
 [nodeWrite('N/node.md',['body: Service action / Observations / Limits','refs','assertions'],'Record one service outcome and partial effects; no product runtime or quality acceptance inferred.','Ghi outcome một dịch vụ và effect một phần; không suy runtime product/quality acceptance.'),resourceWrite('services',['environment','commandRef','probeRefs','observationRef'],'Update only service-owned observation/declaration fields included in scope; do not grant new authority by editing allowed effects.','Chỉ sửa observation/declaration field dịch vụ thuộc scope; không cấp quyền mới bằng sửa allowed effects.'),evidenceWrite('E/service-probes.json + E/effects.json','Retain actual holder and before/after health/stop evidence with selected command outcome and bounded recovery.','Giữ holder thật và health/stop trước/sau, outcome command chọn và recovery hữu hạn.')],
 [step('service environment target','evidence','Inspect target ownership, process/container identity and probe before action; reuse already matching state without restart.','Inspect owner target/process-container/probe trước action; state đã khớp thì dùng lại không restart.'),step('service environment','','Confirm selected inspect/up/down/restart action and authority. Do not touch product runtime servers, foreign holders or unlisted dependencies.','Xác nhận action inspect/up/down/restart và quyền; không chạm server product/holder foreign/dependency chưa liệt kê.'),step('service environment','resource evidence','Run only the declared authorized command if needed, preserve uncertain effects and avoid blind retries; no port stealing or undeclared install.','Chỉ chạy command khai được phép khi cần, giữ effect chưa rõ, không retry mù; không chiếm port/install ngoài khai.'),step('service target','node resource evidence','Verify requested state using actual service probe and holder identity, report limits and stop without gates or product operations.','Kiểm state yêu cầu qua probe/holder thật, báo giới hạn rồi dừng không chạy gate/op product.')],
 [proof('service','Up is supported by the declared useful health probe; down is supported by owned process/container and endpoint observations, not an ungrounded absence.','Up có probe health hữu ích đã khai; down có observation process/container sở hữu và endpoint, không dựa vắng mặt suy đoán.')],
 [stop('SERVICE_OWNERSHIP_UNKNOWN','Service target or effect is outside verified ownership/scope.','Target/effect dịch vụ ngoài ownership/scope đã xác minh.')]);
