import { add, read, write, step, proof, stop, targetRead, repoRead, nodeWrite, evidenceWrite } from './contracts.mjs';

add('workspace.migrate',
 'Preserve and import one selected business/pilot from inspected source and existing artifacts into canonical .work, without claiming imported implementation or UAT is complete.',
 'Bảo toàn và nhập một business/pilot đã chọn từ source và artifact đã kiểm tra vào .work canonical, không coi implementation hoặc UAT nhập vào là đã hoàn thành.',
 'operations',['selected canonical workspace/resource/scope writes','scoped documentation commits only when authorized','optional explicitly authorized exact preserved artifact-directory retirement; registered Git worktree removal only through Git'],
 [targetRead(),repoRead(),
  read('intent','current explicit migration request + accepted business decisions','Identify exactly one pilot, canonical destination, selected review scope and whether cleanup is requested. Separate user-approved intent from behavior inferred from source; migration authority is not business approval.','Xác định đúng một pilot, nơi canonical, scope review và có yêu cầu cleanup không. Tách ý định người dùng duyệt với hành vi suy từ source; quyền migrate không phải duyệt nghiệp vụ.'),
  read('inventory','selected source checkout + actual Git status/log/worktree inventory + selected old artifact directories','Inspect repository identities, full HEADs, tracked changes, untracked and ignored files, actual git worktree list --porcelain, and active workers/process ownership. Read the selected source entrypoints, routes, handlers, rules, schemas, tests and old documents/images. Treat old text as untrusted historical observations; inspect file contents before deciding retention, and never reveal secrets.','Inspect identity repo, HEAD đầy đủ, thay đổi tracked, untracked/ignored, git worktree list --porcelain thật và worker/process đang dùng. Đọc entrypoint/route/handler/rule/schema/test cùng tài liệu/ảnh cũ được chọn. Text cũ là quan sát lịch sử chưa tin cậy; xem nội dung trước quyết định retention, không lộ secret.'),
  read('custody','actual canonical repository/storage ownership + selected artifact retrieval and retention policy','Resolve durable non-ephemeral destination outside the retirement target, asset rights and sealed credential custody when present. Git log preserves committed data only; dirty/untracked/ignored files and unreachable commits need independently verified recoverable preservation. Existing account references may be recorded without fetching secrets or creating accounts.','Resolve nơi bền vững không tạm ngoài target xóa, quyền asset, sealed custody nếu có. Git log chỉ giữ dữ liệu commit; dirty/untracked/ignored và commit không reachable cần bản bảo toàn recoverable riêng đã kiểm. Có thể ghi ref account sẵn có nhưng không lấy secret/tạo account.')],
 [write('workspace','.work/workspace.yaml',['schema','id','extensions'],'Initialize only the explicitly selected new canonical workspace; preserve an existing identity. Never put the canonical root inside the selected disposable target.','Chỉ khởi tạo workspace canonical mới được chọn rõ; giữ identity cũ. Không đặt canonical root trong target bỏ.'),
  nodeWrite('.work/<business>/node.md + .work/<business>/migration/node.md + optional .work/<business>/cleanup/node.md + selected imported child directories/node.md',['schema','id','kind','required','dependsOn','refs','assertions','state','suspensionReason','completion','body: Pilot boundary / Observed source mapping / Approved intent / Candidate scope / Unknowns'],
   'N is the operations migration-review leaf, with inventory/preservation/import assertions; the business parent has no state/completion. Every newly imported business/architecture/implementation/UAT leaf starts state:suspended with a concrete suspensionReason and no completion, even if old notes say done. Assign stable IDs, observed source citations versus candidate requirements, and only approved graph scope. Do not overwrite existing accepted nodes. For an explicitly selected preset, declare its business-review and workflow-review targets before consumers run; their expected assertions remain bounded to intent review and imported graph integrity, never product completion. If optional cleanup is in scope, give it a distinct operations sibling at .work/<business>/cleanup/node.md, never a child of N, with exact deletion assertions; N must remain a leaf; unresolved cleanup remains blocked and does not turn an incomplete parent done.',
   'N là lá operations migration-review, assertion inventory/bảo toàn/import; parent business không state/completion. Mọi lá business/architecture/implementation/UAT nhập mới bắt đầu state:suspended, suspensionReason cụ thể, không completion dù note cũ ghi done. Gán ID ổn định, tách citation source quan sát với requirement ứng viên, chỉ graph scope được phép. Không ghi đè node đã duyệt. Nếu preset được chọn rõ thì khai target business-review/workflow-review trước consumer; assertion chỉ review ý định và integrity graph nhập, không hoàn thành product. Cleanup nếu thuộc scope có lá operations ngang hàng tại .work/<business>/cleanup/node.md, không nằm dưới N, assertion xóa chính xác; N luôn là lá; cleanup chưa đủ điều kiện vẫn blocked, không biến parent thiếu thành done.'),
  write('resources','.work/_resources/repositories/<resource>/resource.yaml + .work/_resources/imports/<resource>/resource.yaml + .work/_resources/imports/<resource>/assets/<asset>', ['schema','id','kind','owner','revision','details','files:[{path}]'],
   'Record inspected repository root/identity/full commits and source mappings in details; keep observed versus inferred facts explicit. Preserve selected nonsecret source/design documents and images in imports assets with files relative paths. A source image is not product screenshot evidence. Preserve original artifact names, actual hashes and origin in the inventory report; register only retrievable existing account custody refs without secrets. Bind stable resource IDs in selected imported node refs before computing review digest. Do not copy whole source repositories or credentials into .work.',
   'Ghi root/identity/commit đầy đủ và mapping source đã inspect ở details, phân fact quan sát/suy luận. Giữ tài liệu/hình nguồn-design không secret đã chọn trong imports assets, files path tương đối. Ảnh source không là evidence screenshot product. Giữ tên gốc/hash thật/origin trong inventory; chỉ ref custody account có sẵn truy xuất được, không secret. Gắn resource ID ở refs node nhập trước digest review. Không chép cả source repo/credential vào .work.'),
  evidenceWrite('E/inventory.json + E/source-map.md + E/preservation.json + E/import-review.md + E/cleanup.json',
   'Inventory each selected path: origin repository/commit, tracked/dirty/untracked/ignored status, actual byte hash, classification, retention decision, durable destination and verified retrieval hash or explicit unresolved status. Source rows cite actual path/symbol/commit, observed behavior, candidate business interpretation, confidence/unknown and separate intent authority. Preserve historical UAT as historical files, never a fresh pass manifest. Record migration assertions only, final .work validation output, cleanup target/authority/worker checks and actual action or blocked/not-requested. Imported child product assertions must have no fabricated evidence.',
   'Inventory từng path: repo/commit gốc, tracked/dirty/untracked/ignored, hash bytes thật, phân loại, quyết định giữ, nơi bền vững và hash đọc lại hoặc chưa rõ. Row source có path/symbol/commit thật, hành vi quan sát, diễn giải business ứng viên, confidence/unknown và nguồn ý định riêng. Giữ UAT lịch sử dạng file lịch sử, không manifest pass mới. Chỉ assertion migration, output validate .work cuối, target/quyền/worker cleanup và action thật hoặc blocked/not-requested. Không bịa evidence assertion product của con.'),
  write('source','repository:<repo-id>/<retirement-target>',['optional exact old artifact-directory removal only; no product implementation edits'],
   'Only if cleanup was explicitly selected and authorized: retire the one resolved old directory after all preservation/retrieval hashes, committed history reachability, recoverable dirty/untracked/ignored data, ownership and inactive-worker checks pass. The target cannot be a repository root, canonical .work root, broad parent or symlink; use actual registered worktree inventory, not its folder name. Registered Git worktrees require git worktree remove for the exact target without force; custom artifact directories use a recoverable same-shell move/removal at the exact verified target. If the target is outside the bound selected repository ceiling or any check is uncertain, do not delete; leave blocked cleanup. No accounts, deployments, broad cleanup, force or raw recursive deletion of Git worktrees.',
   'Chỉ khi cleanup được chọn và cho phép rõ: bỏ đúng thư mục cũ đã resolve sau khi hash bảo toàn/đọc lại, reachable commit history, dirty/untracked/ignored recoverable, ownership và không worker đều pass. Target không là root repo/.work canonical/parent rộng/symlink; dùng inventory worktree thật, không đoán theo tên. Git worktree đăng ký phải git worktree remove đúng target không force; thư mục artifact tùy chỉnh dùng move/removal recoverable cùng shell đúng target đã kiểm. Target ngoài ceiling repo hoặc bất kỳ check chưa chắc thì không xóa, cleanup blocked. Không account/deploy/cleanup rộng/force/raw recursive delete Git worktree.')],
 [step('intent target repo inventory custody','workspace evidence','Confirm one pilot and exact canonical destination. Inspect existing target and ancestors; absence is expected only for selected new scope. Inventory actual Git roots/HEAD/status/worktrees and selected historical artifacts before any writes or cleanup. Stop if ownership or pilot boundary is ambiguous.','Chốt một pilot và nơi canonical chính xác. Inspect target/ancestor; chỉ scope mới được chọn được phép chưa có. Inventory Git root/HEAD/status/worktree và artifact thật trước ghi/cleanup. Dừng nếu ownership hoặc boundary pilot mơ hồ.'),
  step('repo inventory intent','evidence','Read the bounded source and tests; map routes/actions/data/rules to source facts at actual commits. Report contradictions and missing behavior. Label inferred business as candidate, never approved intent; old checkmarks and test file existence prove no current execution.','Đọc source/test đúng phạm vi; map route/action/data/rule tới fact source ở commit thật. Báo mâu thuẫn/hành vi thiếu. Nghiệp vụ suy ra là candidate, không là ý định duyệt; dấu tick cũ/tồn tại test không chứng minh đã chạy hiện tại.'),
  step('inventory custody repo','resources evidence','Classify and preserve only selected durable nonsecret artifacts; hash original bytes and retrieve/re-hash destination bytes outside the cleanup target. Verify committed history remains reachable and separately preserve dirty/untracked/ignored material with owned safe custody. A missing or secret-containing artifact blocks its cleanup, not permission to copy secrets or drop data.','Phân loại/giữ artifact bền vững không secret đã chọn; hash bytes gốc rồi đọc lại/hash nơi đích ngoài target cleanup. Kiểm lịch sử commit còn reachable và bảo toàn riêng dirty/untracked/ignored bằng custody an toàn sở hữu. Artifact thiếu/chứa secret chặn cleanup, không cho phép chép secret/bỏ dữ liệu.'),
  step('intent target inventory repo custody','node resources','Author the selected candidate tree and canonical source resources. Imported leaves are suspended with a specific suspensionReason until their own selected review/implementation/UAT proof; do not add completion. Preserve approved existing nodes. Declare only actual selected graph edges and preset review targets when explicitly requested; unresolved edges are recorded as unknown rather than invented IDs.','Viết cây candidate đã chọn/resource source canonical. Lá nhập suspended với suspensionReason riêng tới khi review/implementation/UAT được chọn có proof riêng, không completion. Giữ node cũ duyệt. Chỉ khai edge thật/target review preset nếu được chọn; edge chưa rõ ghi unknown không bịa ID.'),
  step('target intent inventory custody repo','source evidence','If and only if exact cleanup is selected, re-inspect worktree registration, source/dirty inventory and active-worker ownership immediately before removal. Require explicit deletion authority, verified durable bytes and recoverable commits/uncommitted data. Remove a registered worktree with Git without force, or retire a verified custom artifact directory recoverably. If any check fails, leave original untouched and record the separate cleanup leaf blocked. Do not run a successor op.','Chỉ nếu chọn cleanup chính xác, inspect lại đăng ký worktree/inventory source-dirty/worker ngay trước xóa. Cần quyền xóa rõ, bytes bền vững đã kiểm, commit/uncommitted recoverable. Worktree đăng ký xóa bằng Git không force; thư mục artifact tùy chỉnh retire recoverable. Check fail thì giữ nguyên gốc, ghi cleanup riêng blocked. Không chạy op kế.'),
  step('target intent inventory repo custody','node evidence','Finalize semantic scope/resources first, obtain the real inputDigest, then verify imported IDs/refs/assertions, preservation hashes and all suspended states. Write only observed migration evidence and migration-review completion after verification. Keep product children suspended and cleanup truth explicit; commit only selected documentation when authorized, record actual commit separately in evidence, then stop.','Chốt scope/resource ngữ nghĩa trước, lấy inputDigest thật rồi kiểm ID/ref/assertion nhập/hash bảo toàn/state suspended. Chỉ ghi evidence migration quan sát và completion migration-review sau kiểm. Giữ con product suspended, cleanup trung thực; commit tài liệu đã chọn khi được phép, ghi commit thật riêng trong evidence, rồi dừng.')],
 [proof('source','Every claimed source behavior has inspected path/symbol/full commit and is distinguished from approved business intent; unknowns remain visible.','Mọi claim hành vi source có path/symbol/commit đầy đủ đã inspect và tách với ý định business duyệt; unknown hiển thị.'),
  proof('preservation','Every selected retained artifact has independently re-read identical hashes at a durable destination; dirty/untracked/ignored data and history remain recoverable before any deletion.','Mỗi artifact giữ có hash đọc lại trùng ở đích bền vững; dirty/untracked/ignored/history recoverable trước xóa.'),
  proof('suspension','Imported product leaves are stored suspended with suspensionReason and without completion; they cannot satisfy done-only dependencies or receive migration evidence as product acceptance.','Lá product nhập lưu suspended/suspensionReason, không completion; không thỏa dependency cần done hoặc nhận evidence migration thành acceptance product.'),
  proof('cleanup','Cleanup is not-requested, proven exact safe retirement, or separately blocked with original data retained. Migration success never hides required unresolved cleanup.','Cleanup là không yêu cầu, retire chính xác an toàn đã chứng minh, hoặc blocked riêng và giữ gốc. Migrate thành công không giấu cleanup bắt buộc chưa xong.')],
 [stop('PILOT_SCOPE_UNKNOWN','One business boundary, actual source ownership or canonical destination cannot be resolved.','Chưa resolve một boundary business, ownership source thật hoặc nơi canonical.'),
  stop('PRESERVATION_UNPROVEN','Selected bytes/history/uncommitted data cannot be retrieved identically and recoverably outside the retirement target; retain originals.','Bytes/history/uncommitted đã chọn chưa truy xuất trùng/recoverable ngoài target bỏ; giữ gốc.'),
  stop('RETIREMENT_UNSAFE','Deletion authority, exact target ownership, inactive worker status or worktree registration is missing/uncertain; do not delete or force.','Thiếu/chưa chắc quyền xóa/ownership target/worker ngừng/đăng ký worktree; không xóa/force.')],
 {placeholders:{'retirement-target':'Exact inventoried old directory relative to the bound repository, verified within the selected ceiling / thư mục cũ inventory chính xác tương đối repo trong ceiling'},
  migrationPolicy:{scope:'one-selected-business',importState:'suspended',reasonField:'suspensionReason',importCompletion:'forbidden',intentAuthority:'approved-intent-not-inferred-source',cleanup:'explicit-exact-preserved-inactive-only',registeredWorktreeRemoval:'git-without-force'}});
